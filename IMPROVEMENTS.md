# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I667** [speed] whether a warm-on-return background realization pays for itself is unmeasured: I659 made partial realization visible, and the next scan can count how many slow warm `search_symbols` calls now carry `(K more of T projects …)` | `WorkspaceRegistry` · `ToolContext` | if the new notes account for most slow warm calls, then on the first semantic call after a drop start realizing the remaining projects in the background (never on the idle sweep itself), exposed as an awaitable task so a test can observe it deterministically | the share of the ~1 h/week I642 estimate that the new notes attribute to partial realization | re-warming on the idle sweep once pressure clears (I659: it re-inflates the heap the drop just released, and there is no deterministic pressure-cleared signal) |
| **I668** [failures] a solution-wide `run_tests tests=[…]` answered `0 failures  build=ok errors=0 warnings=0 … exitCode=1` with `the filter matched tests in 1 of 2 test projects` while the E2E project had failed to build on two `xUnit1051` analyzer errors, and a single-project run printed `FAILED with no error-severity diagnostic` directly above the `error xUnit1051:` lines | `run_tests` · `DotnetRunner` build-output parser | treat any `: error <ID>:` line as error-severity whatever the id prefix, and when a project of a solution run produced no results with a non-zero exit, lead with `FAILED` naming it instead of `0 failures build=ok` | one misread green plus one extra run per analyzer-error build (2 in this run) | — |
| **I669** [failures] `write_text`'s `CompileRegression` remedy said `write_text declares no usings= and no retryWith=`, yet `write_text retryWith=r1 usings=["System.Globalization"] force=true` is what landed the file | `write_text` · rejection remedy | render `write_text`'s remedy from the same held-token wording `replace_symbol` uses, naming `retryWith=` and `usings=` | one misleading remedy per `CS0246` on a new file | — |
| **I670** [accuracy] after two `edit_text rows=[…] toPath=` moves the steer answered `pass edits=[...] with the next 2+ in ONE call`, but `edits=` cannot carry `rows=`/`toPath=` - the plural for a row move is `rows=` | `RepeatSteer` (`Unbatchable` / plural table) | treat an `edit_text` call carrying `toPath=`, `row=`, `rows=` or `section=` as unbatchable into `edits=`, or name `rows=` for a row move | one wrong steer per consecutive backlog move (~6 in this run) | — |
| **I671** [payload] an overwriting `write_text` whose edit raised policy warnings put its `overwrote existing  ` prefix on the first `WARNING policy` line (`overwrote existing  WARNING policy  TERSE107 …`) instead of on the file line | `write_text` · `FileService` response assembly | prefix the file's own line, after the warnings | one misparsed line per overwrite with warnings | — |
| **I672** [accuracy] `replace_symbol`/`delete_symbol` addressed at a primary-constructor id (`Type.#ctor`) probably target the whole `TypeDeclarationSyntax`, because that is the constructor's `DeclaringSyntaxReference` - the same cause I657 fixed for `get_symbol_source`; unverified | `replace_symbol` · `delete_symbol` | verify with `dryRun` on fixture `Scattered.#ctor`; then edit only the parameter list or refuse naming the type header | a whole type replaced or deleted by a constructor id | — |
| **I673** [failures] `analyze ids=<list>` answered `0 diagnostics` twice while `error xUnit1051` diagnostics in the same files were outside the list, and the next test run failed to build | `analyze` | when `ids=` filtered away N error-severity findings in scope, end with `N error(s) outside ids= not shown - drop ids= to see them` | one failed build plus a re-run per occurrence | — |
| **I674** [round trips] this run cost ~215 main-thread calls for 20 rows (~10.8 per row, above the ~6 target) plus ~420 calls across 16 planners; the excess went to planner premises that were predicted instead of run (I659 assumed FixtureSolution.slnx holds 2 projects - it holds 1, 5 calls to discover; several OBSERVE-RED steps were "predicted from source") and to one whole-suite fallout round from the E2E harness joining blocks with `\n` | `/ship-improvements` P1b planner contract | require each planner to RUN its OBSERVE-RED with read-only means (`workspace_status guard=`, `dryRun`, installed-binary calls) and to cite the call behind every fixture fact it relies on | ~3 main-thread calls per row | — |
| **I675** [accuracy] `analyze minSeverity=warning` still listed `info`-severity `TERSE101`/`TERSE107` policy records, so a warning-floor sweep over this run's files answered 7 info rows | `analyze` · policy findings | apply `minSeverity=` to policy records the way it applies to analyzer diagnostics | the info rows a warning-floor sweep returns (7 in one call here) | — |
| **I676** [fallback] on Windows the guard judges a Git-Bash POSIX path as inside the tree: `cat /tmp/x.log` is DENIED on 0.69.1 and 0.70.0, and SKILL.md's own `tail -5 /tmp/scan.out` example is false there (R2a review, executed on both binaries) | `ToolGuard.Inside` / `Expanded` | on Windows map `/<drive>/…` to `<drive>:\…` and `/tmp` to `Path.GetTempPath()` before the out-of-tree test, or treat a drive-less rooted POSIX path as outside a drive-letter tree | one refused call plus a scratch-file detour per Git-Bash temp-file read on Windows | — |
| **I677** [fallback] the guard misses a backgrounded or looped `.cs` read: `true & cat "src/TerseSharp.Core/Result.cs"` and `for f in src/TerseSharp.Core/*.cs; do cat "$f"; done` are ALLOWED on 0.69.1 and 0.70.0 (R2a review, executed) | `ToolGuard` (`Compound` / fencing) | judge each `&`-separated segment and a loop body's commands, denying whole when one names .NET source | a silent built-in `.cs` read per occurrence | — |
| **I678** [failures] on the macOS CI leg (run 36243627298) a `TerseTempSolution` server started with `--workspace <tmp>/FixtureSolution.slnx` answered `ERROR AmbiguousWorkspace: several workspaces are loaded` to an un-hinted `search_symbols` right after `load_workspace reload=true` - the reload of the same solution registered a second workspace, most likely because `/var/folders/...` and `/private/var/folders/...` name one directory; green on ubuntu and windows | `load_workspace` · `WorkspaceRegistry` | canonicalize the solution path (resolve symlinks / `realpath`) before keying a workspace, so a reload of the same file replaces it instead of adding one | one red macOS leg and a CI round per un-hinted test after a reload | — |
