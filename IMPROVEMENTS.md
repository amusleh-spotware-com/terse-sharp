# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I540** [round trips] `cleanup verify=true fix=style` and `cleanup verify=true fix=analyzers` are the CI pair this project's own `CLAUDE.md` mandates back-to-back, so they are ALWAYS two calls and two model gaps; this run made them 4 times (2 rounds x 2 modes) and the server's own repeat steer fired on them | `cleanup` | a `fix=ci` mode - or `fix=["style","analyzers"]` - that applies both CI rule sets in one pass and answers one verdict, naming which mode each named file belongs to | 2 calls and ~12 s of gap per end-of-task gate, on every task in this repo | — |
| **I541** [fallbacks] the guard correctly denied `cat > fixtures/.../RegionTail.cs <<'EOF'` but its `Call this instead:` named `get_file_outline path="…"` - a READ tool - for what was plainly a WRITE of a new `.cs` file; the usable answer was `write_text path=… force=true`, which the hook never named | `ToolGuard` | route by direction as well as file kind: a redirect (`>`, `>>`), a heredoc, or a `tee`/`cp`/`mv` target makes the replacement `write_text` (with `force=true` for a `.cs`), and only a bare read routes to the outline/`read_text` family | one wrong-tool suggestion per denied shell write; the hook's whole value is that its suggestion is runnable verbatim | — |
| **I542** [failures] a whole-solution `run_tests` was launched, then code was edited, so the in-flight verdict became stale and had to be `TaskStop`ped - twice in this run, ~19 min of wall clock abandoned; the second time it also held the build output and cost an `MSB3027` round trip to diagnose | `run_tests` | record the loaded workspaces' generation counters when a run starts and, when a document changes before it finishes, end the verdict with `STALE n document(s) changed after this run started` - or abandon it outright; the memo already tracks "nothing was written in between" for the opposite case | ~9 min per abandoned whole-solution run, plus the `build` lock round trip it causes | — |
| **I543** [unanswerable] a `TokenBudgetE2ETests` failure names the WHOLE 88-tool surface cost, but nothing answers that number from a working-tree build: the `terse call workspace_status` probe reports only the narrowed advertised profile (78 tools here), so re-measuring a 4-test budget failure cost 3 E2E runs (~90 s) instead of one 3 s probe | `workspace_status` | print the whole-surface cost beside the advertised one under `verbose=true` - `advertised=78 tools 28727 tokens  surface=88 tools 30565 tokens` - so the number the budget census asserts is readable without running the census | ~90 s and 3 test runs per budget re-baseline; the existing `advertised=` line already measures the wrong set for that question | — |
| **I544** [payload] `add_member` formats the whole DOCUMENT, not the inserted node: on a 960-line test file in a repo with no root `.editorconfig` whose style is declared in a committed `xTrader.sln.DotSettings` (`SPACE_AFTER_TYPECAST_PARENTHESES=True`), a 2-line insertion answered `changedLines=7` and rewrote 5 cast lines across 4 unrelated methods (`(ChartId?) null` to `(ChartId?)null`), because Roslyn's `csharp_space_after_cast` default disagrees with the team style on every cast in the file; reported on terse 0.60.0 | `add_member` | format only the annotated inserted node's span and leave every other line's trivia untouched; if whole-document normalization is deliberate, put it behind `format="node"` / `format="document"` defaulting to `node`, and prefer preserving existing trivia when no `.editorconfig` governs the file | the collateral hunks on every call, plus the `dryRun` inspection every call needs today - the reason a caller routes to an unchecked insert tool instead of the compile-gated one | — |
| **I545** [failures] a project-file write round-trips the `.csproj` through `XDocument` instead of editing it: removing ONE line re-indented the file from 4 spaces to 2, respaced every `<Tag/>` to `<Tag />`, deleted the blank lines between `ItemGroup`s and dropped the trailing newline - a 69-line diff for a 1-line change (terse 0.60.0, xTrader, 148 projects) | `write_text` | edit the matching element's line range in place and write the caller's text verbatim on a `.csproj`, rather than saving a reparsed `XDocument` from `ProjectFile`; assert in a test that every line the change does not target is byte-identical after the write | 68 collateral lines of review noise per project-file edit, plus the manual revert step that makes the tool unusable under a no-unrelated-edits commit rule | — |
| **I546** [failures] `write_text path=<file> ref=HEAD` - the documented undo - adds a UTF-8 BOM to a file that had none, so the restore is not byte-identical and leaves a 1-line diff behind; `AtomicWrite.EncodingOf` sniffs the preamble off the existing file, so the BOM is being added on the Roslyn apply path, where a `SourceText` carrying no encoding persists as UTF-8-with-BOM | `write_text` | carry the target file's existing preamble through the workspace apply path as well as the `AtomicWrite` one, and cover it with a test that restores a BOM-less file with `ref=HEAD` and asserts `changed_files` comes back empty | one phantom 1-line diff per undo; a restore that is not byte-identical is not a restore | — |
| **I547** [failures] a `symbol=` name is saturation-checked against the FUZZY candidate set before any exact-name or qualifier filter runs, so qualification never narrows anything and a popular leaf name is unresolvable: `SymbolLookup.ByNameAsync` asks `SymbolSearch.FindAsync` for `query.Member` (subsequence-ranked, so `ISymbolModel` also matches `ISymbolViewModel` and `IVwapSymbolViewModel` - 138 hits, exactly 1 tagged `EXACT`), refuses at `found.Count > NameCap` on line 51, and the exact-name filter on line 57 is never reached; `cTrader.Domain.Symbols.ISymbolModel` and bare `ISymbolModel` both answer the identical count while `symbolId="T:cTrader.Domain.Symbols.ISymbolModel"` resolves 38 members. The saturated path falls to `ByContainerAsync`, which reads the qualifier as a CONTAINING TYPE and looks for a member named `ISymbolModel` inside a type named `Symbols`, so a namespace-qualified top-level type can never match. 3 refusals in one session on the task's three central types, each worked around with `find_files` plus `get_file_outline`; terse 0.60.0 | `get_symbol` | filter to `symbol.Name == query.Member` BEFORE the `NameCap` test so one exact match resolves however many fuzzy candidates exist, then read a dotted qualifier as a namespace when the leaf is itself a type (the `SymbolReference.Matches` namespace-suffix rule already exists but sits after the cap), accept a bare fully-qualified name as its own documentation id, and replace the `Errors.SaturatedName` remedy - "qualify the name with its containing type" is inapplicable to a top-level type - with `symbolId=T:<fqn>` | 2-3 calls per refusal on every common type name, and the fallback is `find_files` plus `get_file_outline` - the exact built-in pair the name path exists to remove | — |

