# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I566** [round trips] every write invalidates the whole solution's compilations, so the FIRST semantic query after an edit pays a full realization - measured by a 0.61.0 user on a 149-project solution at **58 842 ms** after a 23-file resx sweep, **23 985 ms** after one `replace_symbol_body`, then 19 428, 11 122, 9 267 and 8 717 ms. A one-method body replacement invalidating enough to cost 24 s is where an incremental path would pay; the answers are worth it, so this is a cost row and not a defect | `replace_symbol_body` · `find_usages` · `analyze` | keep the compilations of the projects an edit did NOT touch, so an edit-then-verify hop re-realizes only the changed project and its dependents; measure the `compilations=realized in Nms` line after a one-member edit on a large solution before and after | 9-59 s per edit-then-verify hop on a large solution, which is the dominant wall-clock cost of the loop once the payload is small | — |
| **I568** [failures] `ShadowCopyAnalyzerLoaderTests.ReboundSolution_AfterRunningTheAnalyzer_LeavesTheOriginalAssemblyUnmapped` went red on the **windows leg only** of CI run 35242129796 with `Assert.Contains() Failure … Collection: []`, and green on a rerun of the same job; ubuntu and macos passed both times, and a 32-core local run of the whole suite passed. The empty collection means `Fixture.Generated`'s design-time build produced no syntax tree - the analyzer loaded (the non-empty assert above it passed) but had no symbol to flag - which is an MSBuild load failing under contention on a two-core runner, where `dotnet test` runs UnitTests and E2ETests as two concurrent processes and several E2E classes spawn fixture builds of their own | `run_tests` | make the load failure visible where it happens rather than three asserts later: have `WorkspaceRegistry.LoadAsync` surface a project that loaded with zero documents as a load failure rather than a silent empty project, so a contended design-time build is reported instead of being read as "the analyzer found nothing" | one whole CI cycle per occurrence, plus the triage that cannot distinguish a flake from a regression without it | re-running until green: it is not a diagnosis, and the same shape would hide a real regression in the shadow-copy loader. The test's own oracle was hardened in the same task - it now asserts the compilation has a syntax tree and names the project - which turns the next occurrence into a diagnosis but does not remove the cause |
| **I570** [fallbacks] `write_text delete=true` refuses a directory that is not empty, and nothing else here removes one, so cleaning up a fixture folder whose `bin`/`obj` a workspace load had just created fell back to `Bash rm -rf` - the one built-in fallback this task took that a tool could have served | `write_text` | accept `delete=true recursive=true` on a directory, bounded to the workspace root by `PathBoundary.Contains` and refusing anything a `.gitignore` does not cover, or at least name `bin, obj` as removable build output in the refusal's remedy rather than only listing them | one shell-out per fixture or scratch-directory cleanup, which is every task that creates one; the refusal already knows exactly what it is refusing over | removing more than it was pointed at: the current refusal is deliberate, so the widening has to be explicit and bounded, never a bare recursive delete |
| **I571** [round trips] an anchor that repeats N times in one file costs N sequential `edit_text` calls: `edits=` refuses two entries addressing occurrence 1 and 2 of the same anchor, so three identical `[Description]` strings in `BuildTools.cs` were three calls, each re-sending the whole before-and-after pair | `edit_text` | take `occurrence="all"` (or `replaceAll=true`) for a single `oldText`/`newText` pair, applied by DESCENDING offset so the ordinals cannot move under each other - the refusal's own reason is that entries apply against the text the previous one produced, which one pass does not have | N-1 calls and N-1 re-sent payloads per repeated anchor; the three-call run here re-sent ~1 400 characters twice for nothing | letting `edits=` carry several occurrences of one anchor: that is the shape already refused, and for a good reason |
| **I573** [failures] a scoped `run_tests` issued while THIS process already has a whole-solution `run_tests` in flight answered `MSB3027 ... locked by .NET Host` after 14.2 s of building, then listed five candidate holder processes - all of them its own run. The server knows it started that run; the caller did not have to be told to go process-hunting | `run_tests` · `build` | when a build- or test-spawning call is already in flight for the same solution in this process, answer immediately naming that run, its arguments and its age, instead of building into its file locks; the holder scan is the right answer only for a holder this process did not start | one 14 s failed build plus a five-line holder report per collision, which is every time a scoped run follows a backgrounded suite | refusing concurrent runs outright: two runs over DIFFERENT solutions are legitimate and already work |
| **I577** [payload] the batch steer is now once per run, but a session that opens ten separate runs of the same tool still reads the same advice ten times; the reporter asked for once per session per tool, or a suppression switch | `RepeatSteer` · `ExploreSteer` | suppress a steer for the rest of the session once the agent has complied with it - a call of that tool carrying its plural parameter, or a call of `explore_symbol` - which is behaviour-conditioned rather than an arbitrary cap | one steer line per superfluous firing, on the most-called tools in every session | once per session per tool as a bare counter: the steer state is static in a server process that E2E tests share, so any session-scoped suppression makes `ASecondConsecutiveCallOfTheSameTool_CarriesTheImperativeSteerThatNamesItsPluralParameter` and `ASteeredResponse_StaysWithinItsBudgetWithTheSteerCounted` order-dependent across the whole E2E leg - the exact shared-fixture trap this repo has already been bitten by. It needs a reset the tests can reach first |
| **I578** [accuracy] `format` and `cleanup fix=all` fall back to Roslyn's own whitespace defaults where no `.editorconfig` sets `indent_style`, and say so - but a repository whose convention lives in a ReSharper `*.sln.DotSettings` gets a rewrite against the wrong convention, and the note only tells the caller after the diff exists. On a file with mixed indentation that is large, avoidable style drift | `format` · `cleanup` | read `indent_style`, `indent_size` and `end_of_line` out of a `*.sln.DotSettings` at or above the file when no `.editorconfig` governs it, and use them as the formatting options rather than Roslyn's defaults | the whole whitespace hunk of every `format`/`cleanup` run in a DotSettings-only repository, plus the review cost of separating it from the real change | refusing to reformat when nothing governs the file: the note plus `dryRun=true` already make the fallback visible before anything is written, and refusing would break every repository that is happy with the defaults |
| **I579** [fallbacks] a UTF-16 file with NO byte order mark is still refused as binary by `read_text`, because nothing in its bytes distinguishes it from binary content. 0.65.0 fixed only the BOM-carrying case, which is what the field report carried; a hand-authored UTF-16 file without one still has no tool to read it and still forces a shell round trip | `read_text` · `search_text` | when the zero bytes of a probe fall at a consistent odd or even offset over the whole probe and the surviving bytes are printable, treat it as UTF-16 of that endianness and decode it - `HEURISTIC`, announced in the response, and never applied to a write | one shell-out per legacy-encoded template or resource file in a Windows solution, which is the class the report came from | decoding every file that merely contains zero bytes: a real binary would then be served as mojibake, which is worse than the refusal |
| **I581** [speed] parallelism is the corpus's worst metric - **1.212 tool calls per assistant message, only 16.2% of 14 022 messages carrying two or more**, against 17 032 calls and **46.9 h of model gap** at p50 5 918 ms - and the server gives clients nothing to dispatch on: `search_regex` over `src/**/*.cs` finds `Destructive = true` on 9 tools and **`ReadOnlyHint` on none of the ~50 read tools** | every `[McpServerTool]`, `ToolProfile` | set `ReadOnlyHint = true` on every non-mutating tool and `false` on every mutator, census-gated against the mutating set, so a client can overlap reads without overlapping writes | (b) parallelise - fan-out already deleted 2 970 round trips = 4.9 h this window; a shipping client measured 59% wall clock off a 3-read/1-write turn from this annotation alone (Codex CLI v0.134.0) | a blanket "be parallel" instruction: parallel batches of MUTATING calls were measured at a 21% error rate against 3.5% sequential, 78% of those errors collateral cancellations wiping ~3.6 siblings each (claude-code#64237), so the flag must be false on every writer; MCP annotations are hints a client may ignore (modelcontextprotocol.io, 2026-03-16), so this is a floor, not a guarantee |
| **I582** [accuracy] **77 of 79 `write_text` rejections and 47 of 47 `edit_text` rejections offered no `retryWith` token**, against 58 of 59 for `replace_symbol` and 27 of 28 for `add_member`; `write_text` re-paid **465 401 characters (~116 350 tokens)** of rejected payload in one week, at a mean **7 382 characters** per `CompileRegression` over 39 of them - the largest single re-paid class in the corpus | `write_text`, `edit_text`, `RejectedEdits` | hold the rejected content under a `retryWith` token for both tools, and accept `usings=` on the `write_text` retry, so a `CS0246` rollback costs a token instead of the whole file | ~116 350 tokens and 79 re-sent payloads a week; structured recovery payloads measured +36.7-40.0 pp task completion and 1.8-2.2x token efficiency per success on Anthropic models (arXiv:2606.05037) | the current remedy - telling the caller to put the directive in the content it re-sends - which is what the 39 `CompileRegression` re-sends of a mean 7 382-character file already are |
| **I584** [speed] `UnchangedRun.Replay` has **exactly one call site** (`BuildTools.cs:665`), so skip-and-replay covers only the build and test family: `cleanup` 50, `analyze` 42, `gate` 37, `load_workspace` 22, `get_diagnostics` 17 and `format` 13 byte-identical duplicates all re-ran in full - **181 calls, ~1.34 h of tool time plus 0.30 h of gap** - and `load_workspace` was re-issued with identical arguments **17 times in ONE session** at a p50 of 48 236 ms | `UnchangedRun`, `analyze`, `cleanup`, `format`, `gate`, `get_diagnostics`, `load_workspace` | widen the `UnchangedRun` gate past `BuildTools` to the analysis family and `load_workspace`, on the same "nothing written and no watcher event since" predicate it already computes | (d) never make the call - ~1.64 h a week, and it moves the redundant-call share off **11.9%** (2 027 of 17 032) toward MCP-Bench's <10% bar | `IdenticalCall`'s note, which `RepeatSteer` already emits on every tool and which did not prevent any of the 2 027; redundancy is the error class agents are measurably least able to detect on their own, because a redundant call succeeds (ParaRecover, arXiv:2609.12345) |
| **I585** [speed] **`explore_symbol` 0 calls and `impact_of` 0 calls in 10 118 terse calls across 115 sessions** - the third consecutive zero, and the exact re-measurement **I259** ("a rate still at zero deletes them") and **I323** ("reopen as the first item of the next release") each made their closing condition; 52 of 88 tools were called at all. Anthropic's tool-search documentation states verbatim that "Claude's ability to pick the right tool degrades once you exceed 30-50 available tools", which this run verified as a primary source after two scans recorded it UNVERIFIED | `explore_symbol`, `impact_of`, `ToolProfile` | delete both, folding them into `get_symbol usages=true` and `find_usages impact=true` as **I323** specified, in a release whose only change is the removal | ~640 tokens of the 27 933-token advertised surface on every request of every session, and two fewer candidates in a catalogue already ~2x over the vendor's stated threshold | a third description rewrite: **I259** shipped one built around the chain each tool replaces and the rate did not move off zero |
| **I586** [cost] `read_text verbose=true` on **872 of 1 511 calls (57.7%)** and `get_symbol_source verbose=true` on **371 of 923 (40.2%)** - a 3.8 pp move off **I261**'s 61.5% after its `symbolIds=` steer shipped - while `read_text` remains the largest payload tool at 7 441 577 characters, 4 931 per call; `verbose=true` is the documented opt-out of both the `.cs` outline default (**I133**) and the dedent | `read_text`, `get_symbol_source` | price the choice at the call: a verbose whole-file `.cs` read ends with what the outline would have cost against what this read cost, using the `tokens=` machinery that already exists | the outline is 32% of a file's tokens (**I133**); a tenth of the 872 verbose reads reverting is ~290 000 characters a week | re-steering these calls to the outline, refused in **I261** because they are precisely the calls that already overrode it; and a second `SKILL.md` "do not pass verbose defensively" line, which is the wording the 57.7% was measured against |
| **I588** [cost] framing residue below the individual floor, aggregated and measured with `tiktoken` on **360 014 characters of the 15 widest real terse responses**: multi-space column padding **2.49% (o200k) / 2.50% (cl100k)**, against **I326**'s 1.98%; plus constants repeated on the success path - `HEURISTIC  text match` 1 000x at 6 tokens, `0 diagnostics, one record per id and message` on 109 of 218 `analyze` calls at 9 tokens, `tools=razor_* hidden ...` 71x at ~37 tokens, the `.cs` outline NOTE 60x at ~69 tokens | `ResponseBuilder`, `analyze`, `workspace_status`, `read_text` | collapse the column separator to one space in `ResponseBuilder`, and drop those four constant clauses from the success path | ~2.5% of ~4.8 M terse result tokens = **~120 000 tokens a week**, plus ~14 000 from the constants | blank lines (measured **0.00%** this run), thousands separators (**0.00%**), absolute-to-relative path text (**0.00%** - already relativised) and trailing whitespace (0.45%) are placebo and must not be re-proposed; abbreviation stays refused; and **I326**'s own blast-radius argument still holds - this is a format change to every record of every tool at once and belongs in a release of its own, paired with **I585** |
| **I590** [accuracy] the truncation contract assumes a second call that does not happen: **968 terse truncation steers, and the agent's next call was a DIFFERENT tool 820 times (84.7%)** against 131 narrowed re-queries (13.5%) - and it is worst exactly where the record is a fact rather than a sample: `find_usages` 19 moved on / **0** re-queried, `resx_find` 17/**0**, `get_file_outline` 75/3, `history` 29/1, `changed_files` 118/6. So a clipped list is read as the whole answer, which is the confident wrong answer this repo's own rule bans | `ResponseBuilder`, `changed_files`, `get_file_outline`, `find_usages`, `resx_find`, `history` | raise the completeness rule per tool from the flat "within 10% of the cap" to a threshold derived from each tool's measured total distribution, so these listings come back whole; where they genuinely cannot, state the omission as a count of what is MISSING rather than as a steer to a call the agent makes 13.5% of the time | (accuracy, not tokens) 820 answers a week currently readable as complete when they are not; a production corpus of 4 175 file-search events measured the agent-initiated second-chunk rate at **exactly zero**, and found first-chunk INCLUSION binds while rank inside the chunk does not (arXiv:2608.26130) | a louder or better-worded steer - this one already fires 968 times and 84.7% of its audience never acts on it; and a cursor or "fetch the remainder" tool, measured as machinery models rarely use that yields no accuracy gain (arXiv:2609.20804). Raising every cap is refused too: it re-spends the payload the 10% rule exists to bound |
| **I591** [payload] `analyze changed=true severity=info` answered **43 records of which 42 were pre-existing `.terse.json` policy findings** on files this task only appended to, and `gate changed=true` answered `remaining=226` the same way - every TERSE10x row was a type or method this task did not author. The one actionable record (an `IDE0007` on a line just written) was 1 of 43, so two ~2 500-token responses were paid to find one 40-token finding, and a `FAILED` verdict meant nothing | `analyze` · `gate` | take `baseRef=` (default `HEAD`) and report only findings whose position falls inside a line the working tree ADDED or CHANGED against that ref - the hunk map `diff_symbols` already builds - with the rest folded to one `N pre-existing on these files` count | ~95% of an end-of-task analyze payload, on the call every task makes last; and it makes `gate`'s verdict mean "this task is clean" rather than "this repository is clean" | `sinceLast=true`, which compares against the previous run of the SAME scope in the same process - it cannot see what a task introduced, only what moved since it last looked, and it answers nothing on the first call of a session |
| **I592** [round trips] learning the shape of one 660-line file (`EditGate.cs`) cost **5 calls and ~6 000 tokens**: `get_file_outline` answered `51 members - contains= or all=true`, which is a refusal without a suggestion, so the file was read as three line ranges plus two `get_symbol_source` batches. A wide outline names `contains=` but the caller does not yet know what to contain-match on - that is the question they came to ask | `get_file_outline` | past the member cap, answer the **grouped** outline instead of refusing: member names only, no signatures, no positions, one line per accessibility or per `#region`, which is ~15% of the full outline and enough to choose `contains=` or a `symbolIds=` batch in ONE more call | 3 of 5 calls and ~4 500 tokens per wide file, on the navigation tool that opens almost every task | printing the full outline past the cap, which is what the cap exists to stop; and raising the cap, which re-spends the payload on every wide file rather than on the one the caller asked about |
| **I593** [failures] `write_text files=[{path, content}]` carrying two `.cs` files was refused `'...EditPulse.cs' is a C# file` because `force` is a **top-level** flag and no entry can carry its own, so the whole batch was re-sent with `force=true` - **~2 400 characters of file bodies paid twice** for a flag. `edit_text edits=[...]` entries DO carry their own `force`, so the two batch writers disagree | `write_text` | accept `force` on a `files=` entry the way `edit_text` accepts it on an `edits=` entry, and make the refusal name every offending path rather than the first | one re-sent batch per mixed write, at the payload size of the files themselves; it also removes the asymmetry between the two batch writers, which is what made the call wrong in the first place | dropping the `.cs` guard on `files=`: the guard is what routes a caller to the compile-gated tools, and a batch that silently wrote C# unguarded is the defect it exists to stop |
| **I594** [fallbacks] settling whether the `PreToolUse` guard still denies a piped `grep` took **two `Bash` calls that actually ran the command**, because nothing answers "what would the guard do with this?" without executing it - `ToolGuard.Inspect` is reachable only from a unit test, and `doctor` probes a fixed six-command set. Running a command to find out whether it is allowed is the one experiment that cannot be run on a command with side effects | `doctor` · `ToolGuard` | `doctor guard="<command>"` (or a `guard_check` tool) answering the verdict, the reason and the routing for an arbitrary command string, executing nothing | two shell-outs per guard triage, and it makes the guard's behaviour testable on destructive commands, which today can only be reasoned about | widening the `doctor` probe set, which is fixed by design and cannot cover a command the caller is asking about right now |
