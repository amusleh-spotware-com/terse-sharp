# Improvements backlog

One line per finding from the end-of-task tool-usage review: observed cost, the tool, the proposed
change, the expected saving. Shipped entries keep their measurement so a regression is visible.

## Shipped

| Finding | Tool | Change | Measured |
|---|---|---|---|
| The documentation id was 205 of the 336 characters on a member line, all of it derivable from the signature beside it | `get_file_outline`, `get_type_outline` | print `OrderService.Submit(Order)`; keep the id only where a name cannot address the member | outline budget tightened from ⅔ to ½ of the file it replaces; `OrderBook.cs` 261 → 167 tokens |
| Reproducing a 205-character id byte-exactly cost a round trip per typo | every `symbolId` parameter | accept names, qualified names and parameter lists; list candidates instead of guessing | one failed call removed per typo |
| `find_usages` gave the file but not the member, so an agent opened the file anyway | `find_usages` | add `src`/`test` and optional `in <Type>.<Member>` | container is opt-in because it measured 3× on a 46-usage symbol |
| `analyze` after every edit re-derived what `EditGate` had just computed | every mutating tool | report `errors=N (+D) warnings=N (+D)` on apply and `dryRun` | one `analyze` call removed per edit |
| `truncated=true` said nothing about what to do, so the retry widened `maxResults` and paid for the whole list | every listing tool | append `- narrow with <parameter>` | one wide re-query removed per truncation |
| Orienting on a symbol cost `get_symbol` + `find_usages` + `find_implementations` | `explore_symbol` | one call returns signature, doc, reach, implementations, XAML sites | 3 calls → 1 |
| Judging a rename's blast radius cost `find_usages` + `find_implementations` + manual project reasoning | `impact_of` | one call adds the projects that would recompile | 3+ calls → 1 |
| "Where is `IFoo` registered?" was unanswerable — open generics, factories and `Add*` extensions defeat grep | `find_registrations` | syntactic scan over the loaded solution, and an explicit "declared nowhere" answer | replaces a multi-file hunt that often failed outright |
| "What endpoints exist?" meant grepping `Program.cs` and every extension it calls | `list_endpoints` | every `Map*` registration with its member | replaces a multi-file hunt |
| Resolving one `{StaticResource}` meant reading `App.xaml` and every merged dictionary in order | `xaml_resolve` | workspace-wide key index with scope | ~8-15k tokens → ~100 |
| A `{Binding}` typo had no static answer in WPF at all | `xaml_bindings validate=true` | resolve the data context and walk the path against Roslyn | a class of runtime-only bug becomes a static one |
| Editing XAML meant line-based `Edit` on the file shape agents are measured worst at | `xaml_set_property`, `xaml_add_element`, `xaml_remove_element` | element-addressed edits, formatting preserved, malformed results refused | removes the last routine reason to `Read`+`Edit` a `.xaml` |
| A red→green loop re-printed every unchanged diagnostic on each iteration | `analyze sinceLast=true` | report only what appeared, plus what was fixed | the loop pays for the delta, not the set |
| An agent with the server installed still reached for `Read`/`Grep` out of habit | `terse guard`, `terse install --guard` | a `PreToolUse` hook that denies the built-in and names the replacement | closes the only failure that scales with every session |
| A `.resx` had no tool at all: `read_text` on a 500-key file is ~12.2k tokens and a neutral+fr+de family ~36.6k, and "which keys are untranslated" meant reading both and diffing 1 000 entries in-context | `resx_files`, `resx_get`, `resx_find`, `resx_usages`, `resx_set`, `resx_remove`, `resx_rename`, `resx_validate` | family-aware index; keys-only listing; cross-culture rows with `MISSING`; surgical `<data>` writes; a `RESX001`–`RESX009` lint | budgeted in `TokenBudgetE2ETests`: keys-only `resx_get` under ½ the widest fixture file, `resx_validate` over the whole workspace ≤ 800 tokens, `resx_files` ≤ 400, `resx_usages` ≤ 250 |
| `x:Uid` was reported but not linked to its `.resx` entry, and the join lived in a second private resx reader | `xaml_localization` | share `ResxIndex` with the resx tools | one parser instead of two that drift; closes the 0.7.0 open row |
| `dotnet format style`/`analyzers` had no equivalent, so every analyzer violation was fixed by hand through `replace_symbol_body` | `cleanup fix=style\|analyzers\|all` | apply the referenced analyzers' code fixes in-process, compile-gated, `UNFIXED <id>` for what no fixer covers | 1 call replaces `analyze` + N hand edits; asserted in `CodeFixServiceTests` |
| `--verify-no-changes` had to be answered with a full diff (`dryRun`), so the green case paid for the whole file | `format verify=true`, `cleanup verify=true` | one verdict line plus the changed-file list | `format verify` budgeted at ≤ 60 tokens, `cleanup verify` at ≤ 150, in `TokenBudgetE2ETests` |
| Formatting N touched files cost N calls, because `path=` took one file or the whole solution | `format`, `cleanup` | `path=` accepts a glob and a directory | N calls → 1 |
| A whole-solution `cleanup` silently rewrote `obj/…GlobalUsings.g.cs` | `format`, `cleanup` | skip generated code unless the file is named explicitly | removes a silent side effect from every sweep; `FormatServiceTests` asserts it |
| `dotnet clean` had no equivalent, and the guard had to allow it | `clean` | delete `bin`/`obj` with freed-byte counters, lock recovery and `dryRun` | the last routine `dotnet` shell-out on this repo's path is closed |
| A Razor project's answers pointed at `obj/…_razor.g.cs`: the mapped span was never read, so `get_diagnostics` reported `Home_razor.g.cs:117` for an error the build reports at `Home.razor:13` | `get_diagnostics`, `analyze`, `find_usages` | report `Location.GetMappedLineSpan()` and drop generated paths | a wrong answer removed: following it meant editing a file the next build overwrites |
| `search_symbols` returned 0 for every Blazor component, because Roslyn's source-declaration search skips generated documents | `search_symbols` | list components from the generator output at their `.razor` path | replaces a `search_text` fallback on every Blazor session |
| Reading a `.razor` meant `read_text` on the whole file, then re-deriving which `<Tag>` is a component | `razor_outline`, `razor_component` | resolve every tag through the generated document | `Weather.razor` 1 922 B → a 12-line outline; the generated C# it replaces is 10 655 B |
| A workspace never re-read the disk, so `write_text` on a new `.cs` then `replace_symbol` returned `SymbolNotFound`, and an external edit was answered from the load-time snapshot tagged `EXACT` | every semantic tool, `workspace_status`, `load_workspace(reload)` | watcher + lazy content-compared sync + targeted stamp check + per-kind generations | removes the `unload_workspace`+`load_workspace` round trip: this task alone paid it 4 times at ~2.5 s and 2 calls each |
| `Write`/`Edit` on a `.cs` that does not exist was denied with a remedy naming only tools that cannot create a file | `terse guard`, `SourceFile.Reject` | name `write_text(path, content, force=true)` when the path is missing, and never recommend it for a path that cannot be resolved | closes the dead end that produced a silent `edit_text force=true` fallback in 0.8.0 |
| `undo_last_change` never reverted anything: it replayed a stale-version `Solution` through `TryApplyChanges`, which refuses one, and only the empty-history path was ever tested | `undo_last_change` | replay the previous document texts onto the current solution | a tool that always answered "the workspace refused the revert" now works; regression test asserts the text is restored |
| **I4** Thirteen call sites rebuilt a whole-tree index per call — 5× `XamlResourceGraph.Build`, 2 raw `XamlFiles.Enumerate`, 5× `ResxIndex.Build`, 1 full-solution syntax walk; `xaml_localization` did two recursive walks + reparses in one call, and `xaml_validate path=X` parsed every XAML in the repo to answer about one file | `xaml_resolve`, `xaml_validate`, `xaml_styles`, `xaml_localization`, `xaml_find`, `resx_*`, `find_registrations`, `list_endpoints` | one `WorkspaceIndexes` per `LoadedWorkspace`, memoized per (kind, generation), single-flight, incremental by `(mtime, length)`, stamp-swept when the watcher is not `Active` | measured on a generated tree: 1 000 repeat `xaml_resolve` over 500 files = **1 build, 500 parses, 0 further reads** (was 500 000 parses); a one-file edit in a 200-file tree = **1 parse, was 200** (asserted both ways by falsifying the reuse) |
| **I6** `ResxIndex.Cache` was a `static ConcurrentDictionary` with no bound, no eviction and process-wide scope, pruned only by `Forget` on an edit | `resx_*` | per-workspace `ParsedDocumentCache`, LRU-bounded at 128 documents / 32 MB of estimated document bytes, disposed with its workspace | unbounded → bounded; a two-index test asserts one workspace's `Forget` leaves another's cache intact |
| **A green run and a clean build printed 60-150 tokens of counters, warnings and timing blocks no agent acts on.** `run_tests` was the second most-called build tool in this repo's own sessions (44 calls) and `build` the third (41) | `build`, `run_tests`, `rerun_failed` | emit one line when — and only when — the result has nothing else to say; `verbose=true` restores the full report, `includePassed`/`slowest` imply it | `RunTests_GreenRun_StaysTiny` tightened from ≤ 120 tokens to **≤ 40** and now asserts a single line; `Build_WhenClean_AnswersInOneLineUnlessVerboseIsAsked` asserts < 120 characters |
| **I1** No heading-level outline for a non-`.cs` file: locating two sections of `CLAUDE.md` required pulling the whole file — 216 lines (~2.6k tokens) read to change 3 spots | `read_text` | `headings: true` returns the heading map with line ranges; `section: "## Commands"` returns one section | `read_text` was the second most-called tool in this repo's sessions (344 calls), most of them markdown; the map is ~40 lines against 216 |
| **I2** `edit_text` needed a byte-exact unique `oldText`, forcing a prior full read of the region even when the target was addressable — 1 extra read per edit on every doc change | `edit_text` | `section: "## Commands"` replaces the whole section with no `oldText` at all | one read removed per documentation edit |
| **I3** `write_text` refused a path whose directory did not exist (`DirectoryNotFoundException` on the temp file), so creating a fixture tree cost four failed calls and a `Bash mkdir` | `write_text` | `AtomicWrite` creates the missing directories under the already-containment-checked path | 4 failed calls + 1 `Bash` fallback removed per new directory; `AtomicWriteTests` asserts it |
| **I7** `edit_text` matched `oldText` byte-exactly, so every multi-line edit against a CRLF file failed on the first try; the remedy ("include more surrounding text") made the retry *less* likely to match | `edit_text` | match, then fall back to a line-ending-normalized match mapped back to the file's real offsets; on a miss, name the file's closest lines instead | **130 of 577 `edit_text` calls (22.5%) in this repo's session log failed with `oldText matched 0 times`**; `FileServiceTests` asserts an `\n` `oldText` edits a CRLF file and that the remedy names `L1:` |
| **I11** `add_member` inserted the declaration with no separating blank line and glued it to the type's closing brace (`);}`) | `add_member` | one blank line before the member, `}` kept on its own line | **9 `add_member` calls cost ~12 corrective `edit_text` calls** in the watcher task and **6 cost 8** in the index task, every one a `force=true` line edit on C# |
| **I12** `write_text` wrote `\n` into a repository whose files are CRLF, so the next `format`/`cleanup` rewrote the whole file | `write_text` | keep the target file's endings; a new file takes the solution file's dominant ending | removes a whole-file diff per new file, and the `edit_text` mismatch it caused with **I7** |
| **I5** / **I13** `workspace=TerseSharp` — the worktree name `workspace_status` prints — was rejected `AmbiguousWorkspace` while a second workspace was loaded, and loading a solution nested inside another made every call ambiguous even with `workspace=` passed | `WorkspaceRegistry.Resolve` | rank hints (full path → solution file name → solution name → worktree name → root directory name → substring) and only treat ties *within the best tier* as ambiguous; resolve a path hint to the innermost workspace containing it | **88 `AmbiguousWorkspace` errors** in this repo's session log, the second most common error after `InvalidArgument` |
| The request path did file IO synchronously, and `search_text`/`search_regex` allocated one `string` per line of every file scanned | every file tool, `search_text`, `search_regex`, `AtomicWrite` | async IO throughout; span-based vectorized scanning with `Parallel.ForEachAsync`; sizes from the directory enumeration; `stackalloc` path normalization in `FileGlob` | a matching line allocates; a non-matching one no longer does |

## Open

| Finding | Tool | Proposal | Why not yet |
|---|---|---|---|
| **I21** (2026-08-01) `RazorIndex` has the same shape the index feature just removed from XAML and resx: a `static ConcurrentDictionary` parse cache with no bound and process-wide scope, plus a full `RazorFiles.Enumerate` + stat sweep per call at 5 sites (`razor_find`, `razor_validate`, `razor_usages`, `list_endpoints`, and **`workspace_status`**, which pays it on every status call) | `razor_*`, `list_endpoints`, `workspace_status` | route it through `WorkspaceIndexes` like the other two | **Deferred, not dropped.** It is not the same routing swap: `WorkspaceSync.Classify` returns `null` for `.razor`/`.cshtml`, so there is no Razor generation to key on and no watcher coverage at all. Closing it means adding `ChangeKind.Razor` and a fifth field to `WorkspaceGenerations` — a change to the watcher's invalidation contract, not to the index. Cost today is a walk + N stats per call (the per-file mtime check keeps it correct), i.e. the same order as the index's own degraded mode, not the 1-3 s reparse that XAML paid. |
| **I22** (2026-08-01) `resx_files` on a solution with more than 128 resource files now re-parses the overflow on every call, where the old unbounded `static` cache kept them forever | `resx_files`, `resx_validate` | cache the per-family key sets in the index the way the XAML index caches per-file records, so the listing tools answer from records rather than from parsed documents | Open, and a deliberate trade: the unbounded cache it replaces was **I6**, an unbounded leak. Invisible below 128 resource files. The record-level fix is the same shape as `XamlFileRecord` and is the natural companion to P3. |
| **I25** (2026-08-01) `xaml_find`, the XAML sweep inside `find_usages`/`rename_symbol`/`explore_symbol`, and `xaml_validate includeUnused=true` answer from parsed documents rather than from index records, so beyond the 128-document LRU they re-parse the whole tree on every call — the XAML twin of **I22** | `xaml_find`, `find_usages`, `rename_symbol`, `explore_symbol`, `xaml_validate includeUnused=true` | push the element facts they need (type name, path, attribute values, handlers, binding sites, `x:Class`) into `XamlFileRecord`, the way the resource, style and uid facts already are | Open, and the reason the SKILL and README now qualify "the second call reads no file at all" rather than claiming it for all five tools. Not a regression — those sites parsed the whole tree before this change too — but it is the half of the index that did not land, and a skill that overstated it would be worse than no skill |
| **I23** (2026-08-01) `cleanup`/`format` with no `path=` operate on every document in the solution, including `obj/**` generated files and files the task never touched: one call here edited 5 unrelated source files (pre-existing unused usings) and 12 generated files, and the out-of-scope edits had to be reverted with `git checkout` | `cleanup`, `format` | skip `obj`/`bin` documents, and take a `changed=true` scope that limits the pass to files modified since the workspace loaded | Open. Cost: 1 `git checkout` of 5 files, plus the review time to tell a drive-by from the change |
| **I26** (2026-08-01) `clean`, `format` and `cleanup` still print a per-project or per-file table on a success an agent does not act on, where `build`, `run_tests` and `rerun_failed` now answer in one line | `clean`, `format`, `cleanup` | the same `verbose=false` default: `clean ok freed=412MB`, `format applied 3 files changedLines=42`, and the diff behind `verbose=true` | Open. Deliberately not shipped in the same release as the `build`/`run_tests` change: `format`'s diff is sometimes the answer (an agent checks *what* was reformatted), so the quiet form needs its own judgement about what counts as "nothing to say", and `clean`'s counters are its whole product. |
| **I8** (2026-08-01) `replace_symbol` on a field declaration threw `InvalidCastException: FieldDeclarationSyntax → VariableDeclaratorSyntax` rather than editing or refusing cleanly | `replace_symbol` | handle `F:` ids, or refuse with a remedy naming `edit_text` | Open, and hit again in the index task (`ToolGuard.TextCommands`). One unusable call per field-level change; the workaround is the built-in-shaped `edit_text force=true`. The `remedy:` line also names unrelated symbols, so it reads as `SymbolNotFound` rather than "this tool cannot edit a field" |
| **I9** (2026-08-01) `replace_symbol` reported `applied` with `0 files changed` when the new declaration differed only in leading trivia — a silent no-op that reads as success | `replace_symbol` | say `0 files changed - the declaration is identical` explicitly | Open. Cost one undetected no-op in this task before the file was re-read |
| **I10** (2026-08-01) A `format`/`replace_symbol` call rewrites the whole document from the **loaded** Roslyn copy, so an `edit_text`/`write_text` change made since the workspace was loaded is silently reverted | `format`, `cleanup`, every symbol writer | re-read the file from disk before applying (or invalidate the document) when its timestamp is newer than the loaded snapshot | **Partly closed by the watcher**, but the sharpest form is still open and cost two silent losses in the index task: **interleaving `edit_text` and a symbol edit on the same file reverts the `edit_text`**, with `applied` reported both times. It surfaced only as a build error two steps later. `analyze` and `get_diagnostics` have the same blind spot — they reported two phantom `CS1501` errors for an `edit_text` the snapshot had not seen while `build` was green, so the *cheap* gate contradicts the arbiter. The rule an agent has to learn by hand today is "do every symbol edit on a file before any text edit on it", which no remedy states. |
| **I14** (2026-08-01) `cleanup fix=…` re-runs the project's whole analyzer set once per diagnostic id, up to 25 times per project, and `ids=` narrows only the filter, not the run | `cleanup` | run the analyzers once per pass over the *remaining* ids, or drive `CompilationWithAnalyzers` with the requested id set | Open. Invisible on the fixture; dominant on a large solution |
| **I15** (2026-08-01) `FixerCatalog.Catalogs` is a process-wide `static ConcurrentDictionary` with no bound and no invalidation on `unload_workspace`, so a rebuilt analyzer at an unchanged path keeps serving stale `CodeFixProvider` instances | `cleanup fix=…` | move it into the per-workspace cache and load fixer assemblies into a collectible context | Open. Same shape as **I6**, and it is why `clean` can meet a lock the server itself holds |
| **I16** (2026-08-01) `SymbolNotFound`'s `remedy: nearest:` line offered the exact string that had just been rejected — `EditGate.ApplyAsync(LoadedWorkspace, Solution, IReadOnlyList<DocumentId>, EditOptions, CancellationToken)` was suggested verbatim after failing, twice. Resolving it needed a `get_file_outline ids=full` round trip | `get_symbol`, `replace_symbol`, every `symbolId` parameter | accept the short form for generic parameter lists, or never suggest a candidate the resolver would also reject | Open. 3 wasted calls per occurrence; a remedy that repeats the failing input is worse than none, because it looks actionable |
| **I17** (2026-08-01) A failing assertion's *actual* value is truncated at ~50 characters, so `Assert.Equal(expected, wholeResponse)` — the only way to see a tool's real output from a test — is unusable as a diagnostic | `run_tests` | — | **Partly closed, and the diagnosis was wrong.** The 50-character truncation is xUnit's own assertion formatter, not TerseSharp: the server prints what the TRX contains. What TerseSharp *did* truncate was the failure message at 12 lines, which cut multi-line diffs; that cap is now 30. Making the full value visible needs an xUnit-side change (`Assert.Equal` with a custom comparer, or asserting on a shorter projection), so the remaining cost is a test-authoring rule, not a tool defect |
| **I18** (2026-08-01) `get_file_outline` on a file of top-level statements (`Program.cs`) returns `0 types` and nothing else, so the only way to see the file is `read_text` | `get_file_outline` | list top-level statements as a synthetic entry point, or say `top-level statements, N lines - use read_text` | Open. 1 dead call plus a full-file read per occurrence; the empty answer reads as "the file is empty", which is a claim the tool cannot support |
| **I19** (2026-08-01) `write_text`/`edit_text` are the only producers that tell the workspace what they wrote. `xaml_set_property`, `xaml_add_element`, `project_set_property`, `package_add`, `resx_set` and `solution_add_project` write through `AtomicWrite` without notifying | `XamlEditService`, `ProjectFile`, `SolutionFile`, `ResxEditService` | thread the workspace through and call `Sync.Notice` after each write | Deferred deliberately: the watcher covers all of them in the default configuration, and threading `LoadedWorkspace` into six services is a wider diff than the watcher feature justifies. Only `--no-watch` is affected. |
| **I20** (2026-08-01) The registry keys workspaces by `Path.GetFullPath`, so two spellings that normalise differently but name the same file — a symlinked worktree, an 8.3 short name, a substituted drive — produce two entries for one solution, after which every `Resolve` answers `AmbiguousWorkspace` | `load_workspace`, `WorkspaceRegistry` | key on file identity (`BY_HANDLE_FILE_INFORMATION`, `stat` inode) rather than on a normalised string | Mitigated further, not closed: `load_workspace(reload: true)` matches an existing entry with `PathBoundary.SameFile`, and hint ranking (**I5**/**I13**) now resolves the *second* entry rather than refusing — but two entries for one solution still exist and still cost a second load |
| **I24** (2026-08-01) There is no compile-gated way to rewrite a whole `.cs` file: `write_text force=true` is the only route and it bypasses `EditGate` entirely, so a 400-line service rewrite lands unchecked and the first signal is the next `build`. The index task did 9 of them, this one did 6 | `write_text`, a missing `replace_type` | run a whole-file `.cs` write through `EditGate` (diff + error-count comparison + rollback) instead of exempting it, or add `replace_type(typeSymbolId, declaration)` for the "rewrite this class" case | Open. Every whole-file rewrite is a `force=true` gap in the compile gate the server advertises, and it is the only remaining reason to pass `force` on a `.cs` |
| **I27** (2026-08-01) `replace_symbol_body` and `replace_symbol` emit the replacement's opening brace at column 0 (`{` flush left) when the body is passed with its own braces, so every use needs a `format` afterwards or leaves the file mis-indented | `replace_symbol_body`, `replace_symbol` | re-indent the supplied body to the member's own indentation, the way `add_member` now spaces the member it inserts | Observed 20+ times in this task. Cosmetic per call, but it is why a symbol edit is still followed by a `format` sweep |
| A generic `detail=` verbosity knob was proposed for every tool | all listings | one parameter instead of `signatures`, `ids`, `containers`, `filter` | **Rejected for now.** Those four already give per-tool control with names that say what they do; a generic `detail` would duplicate them and make each tool's cheapest form less discoverable. The repo rule is that improving an existing parameter beats adding a surface. `verbose` on `build`/`run_tests` is not this: it is a per-tool escape hatch from a *cheaper default*, not a knob that has to be found before the tool is useful. |
| Implicit styles (`Style` with a `TargetType` and no key) and `BasedOn` chains are not resolved | `xaml_resolve` | follow `BasedOn` and index `TargetType` so "why does this button look like that" is one call | Needs a resolution order model per dialect; a wrong winner would be a confident wrong answer, which the response contract forbids. Keyed styles already resolve. |
| Every subagent spawns its own server process and its own Roslyn workspace | `terse serve --shared` | one warm daemon over a local socket, N clients | Architectural: needs a transport, a lifetime owner and a lock story. Measured elsewhere at ~100 MB and ~10 s per extra process; worth doing, too large to land safely inside a feature release. |
| `xaml_add_element` inserts as the last child only | `xaml_add_element` | `position=first\|before\|after` | The common case is append; anything else is a `dryRun` away from `edit_text` today. |
| `find_registrations` matches container calls syntactically, so a registration wrapped in a helper method is reported at the helper, not the call site | `find_registrations` | follow one level of `Add*` extension methods | Following the chain arbitrarily becomes whole-program analysis; the current answer is honest about what it matched. |
