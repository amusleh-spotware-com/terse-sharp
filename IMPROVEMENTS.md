# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I432** [failure] an `edit_text rows=[...] toPath=` batch of 22 rows was refused whole on ONE ambiguous identifier - `'I410' matches the first cell of 2 table rows`, because a later row's Finding cell cites the earlier row's id - and the refusal is correct (a partial move must not happen) but names neither the two rows it matched nor the identifier that would disambiguate, so the entire ~14 000-character payload was re-sent verbatim. Measured this run: 1 refused call plus ~3 500 tokens | `edit_text rows=`, `edit_text row=` | name the matching rows' line numbers and the longer identifier that resolves (here `**I410**`) in the refusal, the way the `oldText` multi-match refusal already lists its candidates with `occurrence=` | one full batch re-send per ambiguous identifier, on the one call shape whose payload is the largest thing `edit_text` ever carries | dropping the all-or-nothing rule is refused: a half-moved backlog is worse than a refused one, and the current refusal already gets that half right |
| **I433** [failure] `add_member` takes several declarations in ONE `declaration` string while `replace_symbol` takes `declarations=[...]`, and the asymmetry cost a rejected call this run - `ERROR InvalidArgument: add_member rejected the call: unrecognized declarations`. The remedy listed the accepted set, so it was recoverable in one retry, but the schema is the defect: two sibling edit tools spell the same idea differently | `add_member`, `replace_symbol` | accept `declarations=[...]` on `add_member` as an alias that is joined with blank lines into the single `declaration` it already applies as one compile-gated edit - the same alias treatment `symbol=`/`symbolId=` and `pattern=`/`query=` already get | one rejected call per agent that reasons from `replace_symbol` to `add_member`, which is the natural direction because the batch form is the documented one | renaming `declaration` is refused: it is a shipped parameter name and removing it is a MAJOR change |
| **I434** [failure] `gate changed=true` is refused with `unrecognized changed`, yet `gate` runs `analyze`, `format` and `cleanup fix=all` - all three of which declare `changed` - and gate's own scope is already the files changed since the workspace loaded. Measured this run: one rejected call at the start of the end-of-task sweep, the exact moment the SKILL tells you to reach for `gate` | `gate` | accept `changed=true` as a no-op alias that documents what `gate` already does, or refuse it naming that behaviour rather than only listing accepted parameters, so the rejection teaches the default instead of just denying | one rejected call per end-of-task sweep by an agent arriving from `analyze changed=true`, which is the call the SKILL's ladder puts immediately before it | adding a real `changed=false` whole-solution mode is refused: `gate` exists to be the scoped end-of-task sweep, and the unscoped form is `analyze` plus `cleanup` |
| **I435** [payload] `SnippetSearch.Adopted` materialises `new string(line[..pad])` at every candidate line start, so the re-indent fallback allocates up to one small string per line of the file - about 5 000 on a large file - before the structural check that usually rejects the candidate. The path is a cold fallback (it runs only when the exact AND line-ending-relaxed searches have both missed), which is why it shipped, but the allocation gate calls a per-line allocation nobody measured the thing it exists to stop | `SnippetSearch` | carry the indent as a `(start, length)` pair into the file's own text rather than a `string`, compare later lines against `text.Slice(start, length)`, and materialise the indent ONCE for the winning region - the shape the rest of the file already uses | up to ~5 000 small string allocations per failed-anchor `edit_text` on a large file, at no behavioural cost | changing `SnippetMatch.Indent` from `string?` to a span is refused: it is a record-struct field and a `ref struct` cannot be one, so the materialisation has to happen at the boundary either way |
| **I436** [failure] the new read-only census is ONE-directional: `SchemaCensusE2ETests.EveryAdvertisedToolThatDeclaresDryRun_IsProvenRefusedUnderReadOnly` proves `Writers` covers every advertised tool declaring `dryRun`, but nothing proves `Writers` covers every MUTATING tool. Exactly one current entry escapes it - `undo_last_change`, whose method declares no `dryRun` - so a future mutating tool without `dryRun` would ship unproven under `--read-only`, which is the same silent-exemption shape that let the three `xaml_*` writers sit untested until this release | `SchemaCensusE2ETests`, `ToolContext.RejectWrite` | close the other direction: discover the writers from the server instead of a literal - either every `[McpServerTool]` method reachable from a `RejectWrite` call site, or by calling every advertised tool against a `--read-only` server and asserting the `ERROR ReadOnly` set equals `Writers` - with a reasoned, ratcheted exclusion set | the whole class of mutating tool that ships unproven under `--read-only`, measured once already at three tools | deriving it from `dryRun` alone is refused: `undo_last_change` is a real writer that declares none, so that discriminator is known-incomplete |



