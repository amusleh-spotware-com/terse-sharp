# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I392** [round trip] a `search_text`/`search_regex` hit is a `path:line`, so "which declaration is that line in?" costs an outline plus a ranged read. Measured this run: locating the method containing `SymbolEditService.cs:450` cost `get_file_outline contains="Many"` + `read_text startLine=438 endLine=455` (~900 tokens), and the same pair ran again to find the caller of `Errors.Misnamed` | `search_text`, `search_regex` | take `containers=true` and name the declaration each hit sits in, exactly as `diff_symbols` maps a hunk and `analyze` names the declaration of each position - the record becomes a symbol id you feed straight to `get_symbol_source` | 2 calls and ~900 tokens per "where does this line live", twice in one release task; it also turns a text hit into an addressable id rather than a coordinate | — |
| **I393** [payload] `search_text` has no whole-word mode, so a short literal drags in every longer identifier containing it. Measured this run: `queries=[..., "Bytes", ...]` returned 48 matches of which ~40 were `BytesPerMegabyte`, `ReadAllBytes`, `MaxNoteBytes` and `PressureBytes` (~1 400 tokens), and the useful 8 needed a second, narrower call | `search_text` | a `word=true` flag applying word-boundary matching to every entry of `query=`/`queries=`, so the cheap literal tool covers the case that today forces a rewrite into `search_regex` with `\b` | ~1 400 tokens and one call per short-literal search; `search_regex` already answers it, so this is discoverability, which the ranking rule calls the real veto | rewriting the query as a regex is the current answer and stays available - the row is that nothing steers you there, not that the capability is missing |
| **I394** [payload] changing a tool's `[Description]` costs re-sending its whole declaration. Measured this run: adding one `tokens` parameter to `read_text` meant a `replace_symbol` carrying all of `FileTools.ReadText` - ~4 700 characters, ~1 175 tokens - for a two-line change, and the second trim round was done with `edit_text force=true` precisely to avoid ~8 000 more | `replace_symbol`, `replace_symbol_body`, `edit_text` | let `replace_symbol` take the attribute list alone (`attributes=`), or an `edit_text force=true` that is documented as the sanctioned way to edit a declaration's attributes, so a description edit is addressed rather than retyped | ~1 100 tokens per description edit, and this repo edits descriptions on nearly every release that touches a parameter | — |
| **I395** [failure] a `CompileRegression` whose only new error is `CS0104` caused by a `usings=` entry does not name that entry. Measured this run: `usings=["ModelContextProtocol.Protocol"]` made `Result` ambiguous against `TerseSharp.Core.Result`; the remedy said "fix the edit, send the members that broke with it or pass allowErrors=true", none of which is the fix, and the recovery was to re-send the whole declaration without the using because a `retryWith` token holds the using it must drop | `replace_symbol`, `add_member`, `replace_symbol_body` | when every new error is `CS0104` naming a type the added namespace declares, say which `usings=` entry introduced it and that the retry should drop it - and let `usings=[]` on a `retryWith` mean "drop the held usings" instead of "keep them" | one whole re-sent declaration per collision (~1 000 tokens here) and a rollback that currently reads as unrelated to the parameter that caused it | — |
| **I396** [failure] a `get_symbol_source symbolIds=` batch of exactly ONE unresolvable id answers `ERROR SymbolNotFound` instead of the batch's own `NOT_RESOLVED` line, so the batch contract is not uniform at size 1. Measured this run: a new E2E test written against the documented batch shape failed on its first run and had to be rewritten to pass two ids | `get_symbol_source`, `SourceService` | route `symbolIds=` through the batch renderer whatever its length, so one entry answers `1 symbols` plus `NOT_RESOLVED <id>` rather than failing the call - the plural parameter should never change contract with its count | one red test and one fix round per occurrence; the wider cost is that a batch caller must handle two answer shapes | — |
| **I397** [failure] `read_text bytes=true` is silently dropped on the `ref=` shape, so the description's "on every shape read_text returns and once per paths= entry" is false there. Found by the 0.51.0 review: `RefRead.TextAsync` renders through `FileService.Rendered` directly, and enters `FileService.Stamps` with `Bytes=false` (`RefRead.Stamped`); `tokens=` was given a `ref=` stamp in the same release, `bytes=` was not | `read_text`, `RefRead` | answer the blob's real byte length at that ref - `git cat-file -s` through the existing `GitRunner`, not `Encoding.UTF8.GetByteCount` of the decoded text, which a CRLF checkout makes wrong - or narrow the `bytes` description to exclude `ref=` | one silently wrong claim per `ref=` read; the size of a historical file is otherwise unanswerable without a shell-out | counting the decoded string's UTF-8 bytes was rejected in the 0.51.0 review round: `git show` hands back decoded text, so the count would disagree with the blob on any checkout whose line endings were translated |
| **I398** [failure] six concurrent `write_text` calls creating new `.cs` files under one SDK-globbing project can answer a THIRD shape on Windows - `ERROR InvalidArgument: UnauthorizedAccessException: Access to the path is denied.` - where the contract is success or `EditConflict`. Observed on the windows leg of CI run 33064305439 (`WorkspaceSyncE2ETests.ConcurrentNewCSharpWrites_IntoOneProject_AnswerOnlyASuccessOrAnEditConflict`, 1 of 6 items), with ubuntu and macos green; the neighbouring `WriteText_WithTwoInterdependentCSharpFiles_LandsThemUnderOneCompileGate` failed one run earlier with `ERROR Transient: ... the process cannot access the file 'Fixture.Trading.csproj'` - the same contention on the same project file | `write_text`, `LoadedWorkspace.TryApplyAsync`, `ProjectFileGuard`, `AtomicWrite` | serialize the project-file snapshot and restore per project path, so two adds under one `.csproj` cannot both be replacing it, and map a losing race onto `EditConflict` rather than letting the raw `UnauthorizedAccessException` reach `ToolBoundary` as `InvalidArgument` | removes a windows-only red that costs a whole ~16 min CI leg per occurrence, and closes a response shape no caller can handle - the remedy it prints, "use search_symbols or find_files to get a valid id or path", is a confidently wrong answer for a write race | treating it as a flake and re-running is rejected as a diagnosis: the mechanism is a concurrent replace of one `.csproj` on a filesystem that refuses a second one, not scheduling noise |
| **I399** [failure] a rollback's `remedy:` names parameters the rejecting tool does not declare, so it cannot be acted on. Measured this run: `write_text` creating `PolicyServiceTests.cs` was rolled back on `CS0103 CultureInfo`, and the remedy read "retry with usings=[\"System.Globalization\"] and the retryWith token below" - `write_text` declares neither `usings` nor `retryWith`, so the whole ~6 200-character file was re-sent (~1 550 tokens) instead | `write_text`, `EditGate.ImportHintAsync` | either declare `usings=` and `retryWith=` on `write_text` as the three symbol edit tools do, or have `ImportHintAsync` render a remedy scoped to what the calling tool actually declares - a remedy naming an argument the schema refuses is a confidently wrong answer | ~1 550 tokens and one call per rejected new-file write; removes the one remedy in the server that cannot be followed | — |
| **I400** [failure] `build` lists its lock holders but not what they ARE, so classifying one costs a shell-out the server could have spared. Measured this run: a locked `build` named `holder pid=35524 dotnet ... exe=...\terse.dll`, and telling "another session's live E2E run" from "a stranded fixture host" took two `Get-CimInstance` calls (~700 tokens); the holder's PARENT was `TerseSharp.E2ETests.dll`, which the classifier could have read and reported | `build`, `run_tests`, `LockHolders` | walk one level up from each holder and classify a holder whose parent is a test host as `another test run - do not stop it`, printing the parent's command line; `LockHolders` already opens each holder to read its exe and start time | two `Bash` calls and ~700 tokens per locked build, and it removes the only built-in fallback this session needed | — |
| **I399** [failure] `ChangedTestSelectionE2ETests.RunTests_WhenABatchsOwnBuildCannotFinish_NamesTheProjectAndNeverOffersNoBuild` decides which of two failure shapes it gets by how FAST the runner is, not by the code. It drives a two-project batch with `timeoutSeconds=1` and asserts the BUILD phase answers `timed out, so no project ran`; on the macos leg of CI run 33065740734 the tiny Selection fixtures built inside the second and the RUN timed out instead, answering `FAILED timed out after 2963 ms, no test results were produced`. The same test was green on the previous run of the same code (`d4345ad`), whose only diff is one markdown line, so it is the runner and not the change set | `run_tests`, `ChangedTestSelectionE2ETests`, `fixtures/SelectionSolution` | make the precondition real instead of racing it - give the selection fixture a project whose build cannot finish inside the budget (a `Exec` target that sleeps, or a deliberately huge generated source), so the build-timeout branch is the only reachable one | removes a macos-only red that costs a ~9 min leg per occurrence, and makes the test able to fail for its own reason rather than for the runner's speed | widening `timeoutSeconds` is rejected: the test exists to exercise the build-timeout branch, and a larger budget removes the very branch it asserts |
| **I400** [failure] the windows E2E leg reds on a DIFFERENT pre-existing test almost every run, so a release pays a ~17 min leg per attempt for something the change set did not touch. Measured across four runs of the 0.51.0 content, whose diffs between them were one markdown line and one test constant: `775dee6` `WriteText_WithTwoInterdependentCSharpFiles_LandsThemUnderOneCompileGate` (`ERROR Transient` on `Fixture.Trading.csproj`), `d4345ad` `ConcurrentNewCSharpWrites_IntoOneProject_...` (`UnauthorizedAccessException`, see I398), `7166401` `ChildProcessTests.RunAsync_WhenADetachedGrandchildKeepsThePipeOpen_...` (18 346 ms against a 10 s ceiling), `dad599e` `UpdateE2ETests.ANewerRelease_IsAnnouncedOnceOnTheNextToolResponse` (1 343 calls over a 240 s budget against a LOCAL stub feed, so not a network wait). ubuntu and macos were green on all four | E2E suite, `TerseServerFixture`, CI | measure what the windows leg actually costs per test and cap the parallelism the E2E collection runs at on that OS - three of the four are contention on one shared fixture or one shared output, and the fourth is a background task starved by it; `run_tests runSettings=["xUnit.MaxParallelThreads=..."]` already exists as the lever | one ~17 min leg per release attempt, and it removes the class of red that makes a genuine regression indistinguishable from noise | re-running until green is rejected as a fix: it is what this row exists to stop being the answer, and each occurrence here was named on its own evidence before it was re-run |
| **I401** [failure] the code policy gates every edit that reaches `EditGate`, but only five tools expose `allowPolicy`, so six mutating tools can be refused with no way to force the edit. Found by the 0.51.0 review: `write_text` (`FileService.cs:454`, `:812`) - which `CLAUDE.md` names as the sanctioned bulk-edit path - plus `extract_interface`, `move_type_to_file`, `move_type_to_namespace` and `change_signature` (`RefactorTools.Options`) build `EditOptions` with `AllowPolicy` defaulted false. `move_type_to_file` on a legacy type is therefore unbypassable | `write_text`, `extract_interface`, `move_type_to_file`, `move_type_to_namespace`, `change_signature` | thread `allowPolicy` through `FileService.WriteTextAsync`/`GateManyAsync` and `RefactorTools.Options`, or exempt the refactors the way `format`/`cleanup`/`doctor` are exempted - a refactor relocates code rather than authoring it, so exemption is arguable, but silence is not | closes the FR-5 gap on 6 of 11 gated tools; removes the one class of edit that can be refused with no recourse | — |
| **I402** [failure] `PolicyE2ETests.ToolsList_ForEveryEditToolThePolicyGates_AdvertisesAllowPolicy` enumerates its five tool names **by hand**, which is exactly the "gate that checks only what somebody remembered to enrol" the census rule forbids - it cannot see the six tools of I401, and a tool added later is not covered | `ToolCensusE2ETests`, `PolicyE2ETests` | discover the subject: every advertised tool whose edit path reaches `EditGate.ApplyAsync` must declare `allowPolicy`, or sit in a reasoned, ratcheted exclusion set beside the other `ToolCensus` sets | makes the policy parameter self-enrolling, so the I401 gap would have failed the build instead of needing a reviewer to find it | — |
| **I403** [payload] a `retryWith` token is rendered with an explanatory note on the SAME line, so the obvious extraction - read to end of line - captures the note as part of the token. Measured this run: an E2E test extracted `retryWith=r1  the declaration is held...` and the replay answered `ERROR InvalidArgument`, costing one failed run plus two calls to diagnose; `SKILL.md` documents the token as `retryWith=r3`, which is what invites the naive read | `replace_symbol`, `add_member`, `replace_symbol_body`, `EditTools.Rejected` | put the note on its own line, or render the token last on its line - the token is the one machine-read value in the whole response and it should not need delimiting rules to parse | one failed retry per rejection an agent tries to automate, and it removes a parse ambiguity from the only value in a response meant to be fed back verbatim | — |


