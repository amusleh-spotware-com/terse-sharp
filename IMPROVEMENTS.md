# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I381** [cost] `replace_symbol symbolIds=/declarations=` does not check that each declaration's NAME matches its paired symbol id, so a mis-paired batch is decided only by the compiler. Measured twice in one run: one `CS1503`, then six `CS0102`/`CS0229` from a batch that re-declared members it was not replacing - 2 calls and ~1 200 characters of payload for a fault that is visible in the syntax | `replace_symbol`, `SymbolEditService` | refuse from syntax before compiling, the way `add_member` already answers `NameTaken`, naming the entry index and both names - `declarations[3] declares 'PerEntryOnly', but symbolIds[3] addresses 'Threshold'` | 2 compile round trips and the whole re-sent payload per mis-paired batch; the rollback path already costs the call plus every declaration | resolving it by matching declarations to ids automatically - the pairing is positional by contract, and reordering silently would be a confidently wrong edit |
| **I382** [payload] `get_file_outline` on a 100-member type returns the whole member list and only then says `100 members - narrow with contains=`. Measured this run on `ToolGuard.cs`: ~2 900 tokens returned, 9 members used - the steer arrives after the payload is paid, which is the same retrospective shape `I376` rejected for the repeat nudge | `get_file_outline`, `get_type_outline` | cap an unfiltered outline at ~40 members by default, listing the rest as a `N of M members - contains= or all=true` line, so the wide case costs a steer instead of a payload | ~2 000 tokens per wide outline; `ToolGuard.cs`, `SymbolEditService.cs` and `WorkspaceTools.cs` are all past the cap in this repo alone | truncating silently - an outline that never truncated is why the steer was added in the first place, so the omission must stay counted |
| **I383** [failure] converging two token budgets cost three `build` + `run_tests` cycles (~90 s each) because the measured number is printed only by the failing assertion. `terse call workspace_status --workspace <sln>` answers `advertised=<n> tools <t> tokens` from the freshly built binary in ~3 s, and `verbose=true` splits it into descriptions, parameters, schema frame and names - but nothing near the budget tests names that probe | `TokenBudgetE2ETests`, `DocsCoverageE2ETests`, `workspace_status` | put the probe command in the budget assertions' failure message, so a blown budget names the 3 s way to measure the next iteration instead of the 90 s one | ~3 minutes per budget change, on every release that touches a description | raising the budgets silently - they are the only guard against a payload that grows on every request |
| **I384** [speed] `run_tests` builds internally but reports only the test verdict, so the mandated "read the build before the test result" forces a separate `build` call first. Measured 9 times in this run at ~10-17 s each, purely to see a verdict `run_tests` already produced | `run_tests`, `rerun_failed` | print the build's own verdict line - `build ok errors=0 warnings=N` - above the test verdict whenever the run built, so the ordering the definition-of-done requires costs no second call | ~2 minutes per task cycle, and it removes the one `build`+`run_tests` pair the ladder cannot collapse | dropping the separate `build` from the gate - a test result read before its build result is the previous binary's, which is the failure the rule exists to stop |
| **I385** [accuracy] an `edit_text` whose `oldText` BEGINS with a newline rewrites EVERY line ending in the file. Reproduced deterministically this run on an LF `SKILL.md` in a CRLF checkout: anchoring on `"\n### The advertised surface..."` turned a 9-line insert into `+970 -943`, and re-anchoring on the same heading without the leading newline made it `+9 -0`. It survived a `write_text ref=HEAD` restore and reappeared on the next edit, so it is the edit path, not the checkout | `edit_text`, `FileService`, `LineEndings` | find the mechanism (a leading-newline anchor appears to route through a split/rejoin that adopts the WORKSPACE dominant ending instead of the file's own) and preserve the file's own endings on every path; add a unit test that edits an LF file inside a CRLF workspace and asserts the other lines are byte-identical | a whole-file diff on any `.md` whose endings differ from the solution's - it buried a 44-line change in a 943-line rewrite of a SHIPPED embedded resource, and cost 14 calls to find | anchoring around it - the caller cannot know a file's endings differ from the workspace's, so the workaround is undiscoverable |


