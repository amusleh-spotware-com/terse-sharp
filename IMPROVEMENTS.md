# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I372** [cost] a `replace_symbol symbolIds=[...] declarations=[...]` batch rejected for ONE unresolvable id costs the WHOLE payload. Measured twice this run: `ToolArgumentFilter.Unrecognized` answered `AmbiguousSymbol` (two overloads) and `T:TerseSharp.Server.FileTools.TextQuery` answered `SymbolNotFound` (one wrong namespace segment), and each time the other three declarations - **~2 400 and ~1 500 characters** - had to be re-sent verbatim. A `CompileRegression` holds the declarations behind a `retryWith` token; a RESOLUTION failure holds nothing | `replace_symbol`, `add_member`, `SymbolLookup` | issue a `retryWith` token for a resolution failure too, holding the declarations and pairing them by index, so the retry names the token plus the one corrected id - the mechanism and the 8-slot store already exist | ~2 000 characters of re-sent payload per mis-typed id, on the batch shape the repeat steer actively pushes callers towards | resolving the ambiguity by picking the first candidate - `AmbiguousSymbol` exists precisely because a wrong pick is undetectable |
| **I373** [accuracy] a `ToolGuard` denial of ONE segment of a compound `Bash` command rejects the WHOLE command, and the denial says so (`NO part of the command ran - re-issue the segments that are allowed`). Measured this run: `git fetch && git rev-parse && git pull && git log --oneline -1 && gh auth status` was denied for the `git log` segment alone, costing a full turn and a re-issue of four allowed segments | `ToolGuard` | when every denied segment has a replacement, put the rewritten command in `hookSpecificOutput.additionalContext` - `... && history maxResults=1` is not shell, so instead name the allowed prefix to re-issue and the tool call to make for the denied segment, rather than leaving the caller to split it | one turn per compound command containing a single denied segment; `cd X && <tool>` is the single most common `Bash` shape in the corpus (I368: `cd+grep` 213, `cd+tail` 144, `cd+cat` 101, `cd+ls` 85) | denying only the offending segment and running the rest: a `PreToolUse` hook can allow or deny, it cannot rewrite the command |
| **I374** [accuracy] `replace_symbol_body` answers `the body did not parse` with the remedy `pass a block starting with '{' or an expression body` for at least two causes it does not distinguish. Measured twice this run: (a) a block that DID start with `{` but was truncated mid-method, where the real fault was an unbalanced brace, and (b) a call targeting `ToolExamples.Worked`, a FIELD - `replace_symbol_body` has no body to replace there at all. Both got the same line, and the first read as a contradiction of what had just been sent | `replace_symbol_body`, `MemberDeclaration` | say which: report the unbalanced-brace position the way the array-parameter `InvalidArgument` now reports its offset (`I336`), and refuse a non-member target by KIND, naming `replace_symbol` or `edit_text force=true` for a field initializer | two misdirected retries measured this run, each re-sending the whole body - the `McpHost.RunAsync` body alone was ~1 900 characters | — |
| **I375** [unanswerable] no tool answers one MSBuild property across every project of a solution. `project_properties` answers one project's evaluated properties, so `IsTestProject` over an N-project solution is N calls - which is why `I347` could not be fixed by honouring the evaluated value and had to stay open. The same gap blocks any "which projects set X" question (`TargetFramework`, `IsPackable`, `EnableDefaultItems`) | `project_properties`, `list_projects` | let `list_projects` take `properties="IsTestProject,TargetFramework"` and print each project's evaluated value beside its name, from the evaluation the workspace already holds | turns an N-call sweep into one, and unblocks `I347`; on this solution N=4, on a 148-project tree N=148 | reading the `.csproj` text: the value routinely comes from `Directory.Build.props`, which is the whole reason `project_properties` reads MSBuild's evaluated set |
