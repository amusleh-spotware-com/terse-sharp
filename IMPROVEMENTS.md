# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I410** [failure] `get_symbol_source` DEDENTS its payload, so pasting it back as an `edit_text` anchor fails - measured this run: 6 `edit_text` entries in 2 batches answered `oldText matched 0 times` on text copied straight from a `get_symbol_source` read, each costing a `read_text verbose=true` re-read plus a re-issued batch (~1 500 tokens and 4 extra calls in total). The remedy names the cause exactly, which is why it was recoverable, but the round trip is paid every time | `get_symbol_source`, `edit_text` | let `edit_text` match a dedented anchor when the dedented form is unique in the file - the same normalization it already does for line endings - or give `get_symbol_source` an `indent=true` that returns the text as it sits | 2 calls and ~250 tokens per anchor edit taken from a symbol read, which is the normal way to find the text to anchor on | rendering every symbol read verbatim is refused by I-series measurement: the dedent is worth 18-31% at member scope |
| **I411** [fallback] nothing removes a DIRECTORY, so an empty folder a `write_text delete=true` leaves behind needs `Bash: rmdir` - measured this run once, after a probe file was written to the wrong fixture path and deleted again | `write_text`, `find_files` | `write_text delete=true` could remove a directory it just emptied, or take `directory=true` for an explicitly empty one; a non-empty directory stays refused | one `Bash` fallback per stray directory, and it closes the last file-system action with no tool - every other create, write and delete is served | deleting a non-empty directory is refused outright: no tool in this server removes more than it was pointed at |
| **I412** [round trip] `analyze paths=` caps at 10, but a release change set is 20+ `.cs` files, so the end-of-task sweep costs 3 calls - measured this run: 21 touched files answered in 3 calls of 10, 10 and 7 | `analyze` | `analyze changed=true` already exists and answers the same question in ONE call; the defect is that nothing steers you there when a `paths=` batch saturates - a saturated batch should end with `next: analyze changed=true` | 2 calls and ~120 tokens per end-of-task sweep, and it makes the cheaper form discoverable at the moment the expensive one is being used | raising the 10-path cap is refused: the cap bounds the response, and `changed=true` already answers the unbounded case |
| **I413** [unanswerable] no tool answers a question ABOUT the advertised surface - "which tools declare `dryRun` but not `allowPolicy`?" cost a full E2E test run to enumerate (I402), and "does this tool take `staged`?" is answered only by calling it or reading the source | `workspace_status`, a new schema query | `workspace_status` could take `tool=<name>` or `declares=<parameter>` and answer from the same advertised list it already counts - the surface is in memory, and this is the question every census test asks in code | one test run (~30 s) per surface question, and it turns a census a human writes into a query the agent can make | reading `tools/list` in the client is not an answer: the agent cannot query it, and the server already holds the list it prints the token count for |
