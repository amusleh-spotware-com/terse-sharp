# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I294** `find_files` is the only listing tool with no `root=`: `search_text`, `search_regex`, `changed_files`, `diff_text` and `history` all take one. Inspecting `%LOCALAPPDATA%/terse-analyzers` and `%TEMP%` while diagnosing a red suite cost **3 `Bash: ls`/`find` fallbacks** in this run | `find_files` | accept `root=<absolute directory>` exactly as `search_text` does, tagged `outside-workspace`, so a cache or log directory needs no shell | 3 shell fallbacks per diagnosis that leaves the workspace, and it is the one gap that forces `ls` on an agent already inside the gate | putting it behind `search_text` - that answers "which lines match", and the question here is "which files exist and how big are they" |
| **I295** an `add_member` or `replace_symbol add=` whose new member name is already declared in the type is applied, compiled, rejected `CompileRegression` and rolled back - **3 times in this run** (`Listing`, `Listed`, plus one more), each costing the whole declaration payload, ~4 000 characters re-sent, plus a full compile round trip | `add_member`, `replace_symbol`, `SymbolEditService` | check the target type's existing member names before applying and answer `ERROR NameTaken` naming the declaration that already has it, with its line - a duplicate name is decidable from syntax and needs no compile | one compile round trip and one re-sent declaration per collision; 3 in this run | refusing every same-name member - an overload is a legitimate same-name addition, so the check must compare parameter lists too |
| **I296** `write_text files=[...]` declares no `force`, so a batch mixing a new `.cs` file with `.props`/`.slnx`/`.csproj` entries is refused outright and has to be split into two calls - one rejected call plus one extra call, observed writing the `HangSolution` fixture | `write_text`, `FileService` | let `files=` carry the top-level `force`, or accept `force` per entry, so one batch writes a fixture whose files are of mixed kinds | 1 rejected call and 1 extra call per mixed batch; the compile gate already handles `.cs` entries inside a batch | dropping the `.cs` guard - `force` exists so a `.cs` write is deliberate, and a batch must not weaken that |
| **I297** `edit_text row=` moves exactly one row per call, so closing a backlog is one call per row - **8 calls in this run**, where the rows were known up front | `edit_text`, `FileService` | `rows=[{row, newText}, ...]` against one `toPath=`, applied as one write per file the way `edits=` already is | 7 of 8 calls per backlog closure, and the same shape every release cuts | reusing `edits=` - those are anchored on `oldText`, and the whole point of `row=` is that the old text never crosses the wire |
| **I300** an unscoped `changed_files` in a tree carrying 42 untracked `.research/**` files clipped at "52 files" and spent the whole budget on untracked entries, dropping a tracked, modified `.cs` file from the listing; the review subagent read that as the change set and had to be corrected by a second scoped call - 1 wrong conclusion plus 2 extra calls in this run | `changed_files` | order tracked, modified files ahead of untracked ones before clipping, or report the two counts separately (`9 tracked, 43 untracked - narrow with exclude=`), so a clipped listing never hides a file the diff actually changed | one call instead of three, and it removes the failure mode where a reviewer reviews a change set that is missing a file | dropping untracked files entirely - `changed_files` with an empty `baseRef` is documented to include them, and a new file is exactly what a review must see |
| **I298** five tests assert behaviour "outside a .NET tree" from a sandbox under `%TEMP%`, so a stray `.csproj` left anywhere above it by another session turns them red with no hint of the cause; diagnosing that cost roughly **40 minutes** of this run and the litter could not be removed | `ToolGuardTests`, `UpdateE2ETests` | give the sandbox a root the walk cannot escape, or have the failure name the marker file `IsDotNetTree` actually found, so the message says `%TEMP%/automate-head.csproj` instead of only "denied" | the whole diagnosis, and it recurs on any developer machine that has ever written a scratch project to the temp directory | asserting the tests are flaky and re-running - the failure is deterministic on that machine and the tests are simply not hermetic |

