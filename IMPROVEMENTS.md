# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I286** a `run_tests` that times out answers `no test results were produced` and nothing else, because the process tree is killed before VSTest writes its TRX - so one hanging E2E test produced **zero** information about which test hung, and identifying it cost 6 `run_tests` calls and ~122 minutes of wall clock (2400 s + 3000 s + 900 s + 420 s + 420 s + 240 s) of manual bisection | `run_tests`, `DotnetRunner` | on a timeout, name the tests that started and did not finish - pass `--blame-hang-timeout` so VSTest itself dumps the hung test, or parse the console progress the runner already receives | the whole bisection: 6 calls and ~2 hours here, and it recurs on every hang | reporting only the timeout - a timeout that cannot say what timed out is the confidently empty answer this server exists to remove |
| **I287** `replace_symbol` on `GitTools.ChangedFiles` was sent without its `[McpServerTool]`/`[Description]` attributes, so it silently **un-advertised a tool**: `build` stayed green, `analyze` stayed clean, `get_diagnostics` stayed empty, and only an E2E call answering `Unknown tool` caught it | `replace_symbol`, `SymbolEditService` | when the replaced declaration carried attributes and the replacement carries none, answer `WARNING attributes dropped: McpServerTool, Description` - it cannot be proven wrong, but it can be named | one silently changed public surface per forgotten attribute; here 2 failed E2E tests and ~15 minutes, and on a release it would ship | refusing the edit - dropping an attribute is sometimes exactly the intent, so the honest answer is a warning, not a rejection |
| **I288** nothing answers "which tags exist": the release flow needs it, `ChangelogReferenceTests` shells out to `git tag --list v*` for it, and this run made 2 `Bash: git tag` calls | `history` | `history tags=true` - one line per tag, newest first, with the commit it names; `contains=` already proves the pickaxe plumbing is there | 2 shell fallbacks per release cut, plus the census gate that currently spawns its own git | listing tags inside `changed_files` - it answers about the working tree, and a tag is history |
| **I289** moving one row between the `## Open` and `## Closed` tables is two `edit_text` entries that both carry the row's **full text** - a delete anchored on it and an append containing it - so a 1 200-character row crosses the wire twice; over the 16 rows closed in this run that is ~38 400 characters, ~9 600 tokens, of text the server already held | `edit_text`, `FileService` | address the row instead of its text: `edit_text toPath=… row="I269"` matching a markdown table row by its first cell, cutting it from the source table and appending it to the target's | the whole row payload per closure, twice - measured at ~9 600 tokens over one run | `section=` plus `toPath=` from **I285** - it moves a whole section, and a closure moves one row out of a table that must stay |
| **I291** `run_tests` has no way to reach VSTest's RunSettings passthrough, so the whole **per-test** parallelism layer is unreachable: `xUnit.MaxParallelThreads`, `xUnit.ParallelizeTestCollections`, `MSTest.Parallelize.Workers` and `NUnit.NumberOfTestWorkers` all live behind `dotnet test -- Name=Value`, which `DotnetRunner.Arguments` never emits. `parallel` added the per-**project** layer only, so an agent that wants to bound in-assembly concurrency has to shell out | `run_tests`, `DotnetRunner.Arguments` | a `runSettings` array mapped to one trailing `-- Name=Value` block, validated as `Name=Value` the way `properties` already is | the shell fallback on every suite that needs to bound or disable in-assembly parallelism - and it is the layer that actually governs a single-project run, which `parallel` deliberately does not touch | reusing `properties=` - those are `-p:` MSBuild properties evaluated at build time, and a RunSettings override is neither |
| **I292** a `projects=` batch of the same project twice ran 6 of 14 tests red purely from concurrency, and nothing in the response says the two invocations shared an assembly - the merged verdict reports the failures as if they were real | `run_tests`, `DotnetRunner` | when two entries of `projects=` resolve to the same `.csproj`, answer `ERROR InvalidArgument` naming the duplicate, or run it once - a batch cannot mean "run this assembly twice at once" | one full false-red batch per duplicate, and the debugging behind it - measured here at 6 phantom failures | serialising duplicates instead of refusing - it hides a caller mistake behind a silent behaviour change |
| **I293** neither `format` nor `cleanup fix=all` collapses a run of consecutive blank lines between members, both answering `no change - the result is identical to what is already there`, so the only way to fix the double blanks `add_member` leaves behind was `Bash: perl -0777 -pi` across three `.cs` files - the precise bulk-edit-through-the-shell breach this repo logs as recurring, committed on the one path that had no tool | `format`, `FormatService` | fold blank-line normalisation into `format` - it is whitespace, which is exactly what that tool owns, and `add_member`/`replace_symbol` are what produce the runs | one shell fallback per multi-member `add_member`, and it is a **write** fallback that bypasses `EditGate` entirely | doing it in `cleanup fix=all` - that is the analyzer-fix path, and IDE2000 is off by default for a reason; the whitespace formatter is the honest home |
| **I290** the `holder pid=13724 TerseSharp.E2ETests` line a locked build prints says "read its command line before stopping it", and reading it needs `Bash: powershell Get-CimInstance` - 2 shell fallbacks in this run, on the exact path the note exists to keep out of the shell | `build`, `run_tests`, `DotnetRunner` | include the holder's command line, truncated, on the holder line - the server already has the pid and already decided the process is worth naming | 2 shell fallbacks per locked build, and the judgement call about whether a pid is safe to stop | stopping the process for the caller - terse must never kill a process it did not start |
