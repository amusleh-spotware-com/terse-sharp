# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I333** [accuracy] `ShadowCopyAnalyzerLoader` falls back to `Map(original)` whenever `Copy` cannot produce a shadow directory, and says nothing. Measured while closing I332: the Roslyn analyzer package at `microsoft.codeanalysis.analyzers/3.3.4` (620 672 and 27 264 bytes) has **no** copy under `%LOCALAPPDATA%/terse-analyzers` - four other versions do - and an open-for-write probe reports both originals **LOCKED**. Harmless there, because nothing writes into the NuGet cache; the same silent fallback on an analyzer built from source is exactly the I52 defect coming back with every gate green | `ShadowCopyAnalyzerLoader`, `workspace_status` | find why `Copy` returns the source directory for that one package (`Files` swallowing an `IOException`/`UnauthorizedAccessException`, or `Publish` failing after a partial `Directory.Move`), then make the fallback observable - a counter on `workspace_status verbose=true`, or a one-line `WARNING` naming the directory that could not be shadowed | the whole point of I52: today an in-place map inside the workspace root is only visible after the user's build has already failed `MSB3027` | scoping the warning away, which is what I332 shipped - it fixes the false alarm, not the silent fallback; and deleting the shadow cache to force a re-copy, which was not tried because it can pull a directory out from under a live server |
| **I334** [cost] `read_text headings=true` **silently ignores `maxLines=`**. This run asked for `maxLines: 30` on `CHANGELOG.md` and got all **179** sections - about 2 400 tokens - because `FileService.Outline` never reads `request.MaxLines`. That is the confidently-wrong-answer shape this repository already refuses elsewhere: an argument the server does not honour is never silently dropped | `read_text`, `FileService.Outline` | honour `maxLines=` on the heading map with the standard `N/T sections truncated - narrow with ...` summary, and add `maxLevel=` so `## ` headings can be listed without their `### ` children - the two ways a heading map is actually narrowed | ~2 000 tokens per heading map of a long changelog or backlog, which is the first call of every release | refusing `maxLines=` instead of honouring it - the parameter is already declared on the tool, so refusing it would break callers that pass it harmlessly on other shapes |
| **I336** [speed] a malformed `edit_text edits=[...]` entry answers `JsonException ... BytePositionInLine: 2796` and the accepted parameter list, but does **not** name which entry is malformed. One 4-entry batch in this run had a missing `newText` key and the whole ~2 800-character payload had to be re-sent to find it | `edit_text`, `ToolArgumentFilter` | name the entry the way `declarations[1]:` already does for `replace_symbol`, and quote the ~80 characters around the offending byte - the same treatment the array-parameter `InvalidArgument` path already gives | one re-sent batch payload per malformed entry; batches are the shape the repeat steer actively pushes callers towards | inferring the missing key - a batch entry with no `newText` may be a genuine mistake about `section=`, so it must be named, not guessed |
| **I337** [accuracy] `get_symbol_source` on a type id concatenates the steer onto the outline with **no separator**, so the answer reads `SymbolEditService.AddRoute  struct private  :835-835steer: get_symbol_source symbolId=...`. Hit twice in one session; the line range and the steer run together, which is exactly the shape an agent parses positions out of | `get_symbol_source`, `SourceService.OutlinedAsync` | put the steer on its own line, the way every other steer in the surface already is | no tokens, but a position an agent can no longer misread; one character | dropping the steer, which is the whole point on a wide type |
| **I338** [accuracy] `AddReplacesItsOwnType`'s guard compares `edit.Target.Node.Span == container.Span` - **`TextSpan`s across documents**. Pre-existing, but multi-container `addTo=` routing (I329) makes a coincidental cross-file span match materially more reachable, and the failure mode is a spurious refusal of a valid edit | `replace_symbol`, `SymbolEditService` | compare the document id alongside the span, as every other cross-document check in that file already does | one wrongly refused batch, which costs the whole declaration payload plus a retry | widening the guard to a name comparison - two types can share a name, which is what `AmbiguousContainer` already exists to settle |
| **I339** [cost] `search_text matchesOnly=true` over a query with **no variable part** returns N identical lines. This run asked for `McpServerTool(Name = "` across `src/TerseSharp.Server/Tools/*.cs` to enumerate the 88 advertised tool names and got 88 records each reading `McpServerTool(Name = "` - **~1 100 tokens carrying zero information**, because the answer to "which names are declared" is exactly the part `matchesOnly` clips off | `search_text`, `TextSearchService` | when `matchesOnly=true` and the query holds no regex capture and no wildcard, either refuse it naming `matchesOnly=false`, or extend the printed span to the end of the syntactic token the match opens - the case this flag exists for is `search_regex`, where the pattern varies | ~1 100 tokens per enumeration call, and one wasted round trip when the agent then has to ask again | — |
| **I340** [cost] `search_text` has no per-file count mode, so "is `--tools` documented in the READMEs?" cost a 4-query pass that came back `40/88 matches truncated` - **~2 500 tokens** of CHANGELOG prose - to answer a question whose whole content is "0 in README.md, 0 in NUGET_README.md, 2 in SKILL.md" | `search_text` | add `countOnly=true`, answering one line per file with its match count and no matched text, bounded by the same `maxResults`; it composes with `queries=` by tagging each count `q1..qN` | ~2 400 tokens on a presence-check over a large tree, which is what a docs-gate sweep does per document | — |
| **I341** [accuracy] a mixed-outcome `edit_text edits=[...]` batch **opens with `ERROR`** even when most entries landed. This run sent three edits, two to `SKILL.md` and one to a `.cs` file; the `.cs` entry was correctly refused and the two markdown edits were applied, but the response leads `ERROR InvalidArgument: '...' is a C# file` and only then `SKILL.md changedLines=9 edits=2/2`, which reads as a failed call and invites a needless re-send of the whole payload | `edit_text`, `ToolBoundary` | lead a partial batch with the applied files, the way the all-applied shape does, and render the refusals underneath as `REFUSED <path>: <code> - <remedy>`; keep the bare `ERROR` first line for a batch where nothing landed | one re-sent batch payload (~2 800 characters was the measured size of the last such payload, I336) | — |
| **I342** [unanswerable] nothing reports what the **whole** advertised surface costs. `workspace_status` prints `advertised=N tools T tokens` only for the last `tools/list` the client actually made, so a server narrowed by a profile, by the markup families or by `.terse.json` can only report the narrowed number - and this task had to take the full-surface figure from a test constant (`TokenBudgetE2ETests.AdvertisedPayloadBudget`) to state a saving in the README | `workspace_status`, `AdvertisedCost` | under `verbose=true` report both, `advertised=64 tools 22097 tokens of 88 tools 25857` - the unfiltered payload is already in hand at the point the filter runs, so it costs one extra measurement and no extra listing | makes the one number that justifies every narrowing self-serving instead of derived from a test constant; ~1 test-file read per README claim | — |
| **I343** [accuracy] when a grandchild inherits the pipe, `ChildProcess` can answer with an **empty** capture while reporting `Drained=false` - "the capture is incomplete" over nothing at all, which is the confidently-wrong shape this repo refuses everywhere else. `SettledAsync` waits `DrainGrace=2s` on the reader *tasks*, then disposes the streams and reports whatever `CopyAsync`'s continuation happened to append (`ChildProcess.cs:179-195`); a continuation not scheduled inside that window loses text that the OS pipe already held. Measured on the v0.45.0 release run: `ChildProcessTests.RunAsync_WhenADetachedGrandchildKeepsThePipeOpen_AnswersAndSaysTheCaptureIsIncomplete` failed on the **windows** leg of two consecutive CI runs with `run.Output` empty, then passed on a re-run of the **same commit** - so the test asserts a guarantee the product does not make. Not reproducible locally at `xUnit.MaxParallelThreads=64` over 32 cores | `ChildProcess`, `build`, `run_tests` | on the grace timeout, attempt one bounded read of what is already buffered before `Release` disposes the streams, and only then report `Drained=false`; if that cannot be done without owning the reader task, make the emptiness explicit - `captured=0 bytes before the holder kept the stream open` - so an empty partial snapshot is never mistaken for a quiet command | removes a silent-data-loss path from every `build` and `run_tests` answer whose child leaks a pipe holder, and makes the test assert a real guarantee instead of scheduling luck | tuning `DrainGrace` upward - a timeout raise hides the race instead of removing it, and P1.5 of the debugging gate bans it |
| **I344** [accuracy] the probe path this repo's own HARD GATE names as evidence (b) - `dotnet src/TerseSharp.Server/bin/<Configuration>/net10.0/terse.dll call run_tests --workspace <this solution>` - **cannot work on this repository**: the probe process is running the very file its build target writes, so MSBuild retries `MSB3026` ten times and the call answers `WARNING this run produced no results` naming `holder pid=... - this terse server`. Measured this task: 42 s burned, zero tests run, and the A/B had to be re-run from a hand-made copy under a temp directory | `terse call`, `DotnetRunner`, `LoadedWorkspace` | run `terse call` from a shadow copy of its own directory when the target solution builds the running assembly, or refuse up front with a remedy naming the copy to make | one failed 42 s probe and one re-plan per verification of a just-built binary, on the one repository whose gate mandates that probe | — |
| **I345** [speed] every build under `run_tests`/`build` passes `-nodeReuse:false` (`DotnetRunner.BuiltAsync`), so each invocation pays a cold MSBuild start. Measured by patching it out over `fixtures/SelectionSolution`, five runs each beside `--no-restore`: **2 198 ms -> 1 941 ms**, so node reuse is worth about **257 ms per call** | `build`, `run_tests`, `DotnetRunner.BuiltAsync` | leave it as it is unless the lock story changes: the saving is small and the flag exists for the analyzer and output-file locks this repo fights | ~257 ms per call, against reintroducing the lock class `unload_workspace` and the build retry exist for | measured and declined this round - 257 ms does not buy back live MSBuild nodes holding this tree's outputs |
| **I346** [accuracy] `run_tests` now selects a solution's test projects with `TestScope` (a metadata-reference scan of the loaded compilation, the same classifier `changed=true` uses) while `list_tests` still selects them with `TestReporterProbe.TestProjectsAsync` (a text scan of the `.csproj` for `Microsoft.Testing.Extensions.TrxReport`, `MSTest.Sdk` or `xunit.v3`). The two can disagree about which projects hold tests in one solution, and neither says which it used | `run_tests`, `list_tests`, `TestScope`, `TestReporterProbe` | one classifier for both, or make each name the projects it selected | one confusing disagreement per solution whose test projects are declared through `Directory.Build.props` | — |
| **I347** [accuracy] `TestScope` classifies any project referencing a test-framework assembly as a test project, so a shared test-helper library that references xunit but sets `<IsTestProject>false</IsTestProject>` is now run by an expanded solution `run_tests`, produces no results, and `Merge` propagates its non-zero exit as the whole batch's verdict. Pre-existing for `changed=true`, newly reachable from a plain solution run | `run_tests`, `TestScope`, `DotnetRunner.Merge` | honour an evaluated `IsTestProject=false`, or do not let a project that reported zero tests turn a green batch red | one false red per solution holding a test-helper library | — |
| **I348** [speed] `run_tests` rebuilds on every call; Rider does not, because ReSharper Build tracks inputs/outputs itself and skips MSBuild when nothing moved. Re-measured on the direct-assembly path over `fixtures/SelectionSolution`, five pairs: **2 749 ms with the build against 421 ms with `noBuild=true`** - the up-to-date no-op build is **2 328 ms, 85 % of the call**, while the tests themselves cost 0.42 s. It is now the dominant cost of any solution whose tests are fast, and it is paid again on every repeat run. (The earlier 645 s -> 581 s figure was the old single-`dotnet test` path, where the same cost was spread across the per-project VSTest invocations.) **Re-measured at corpus scale in the 1-week, 421-transcript scan: `run_tests` is 54.5 h of the 156.4 h tool-wall week, and a `build` immediately followed by a `run_tests` that builds again ran 282 times for 57.9 m; 673 of 1 022 `build` calls were bare whole-solution builds at avg 12.7 s.** | `run_tests`, `build`, `DotnetRunner.BuiltAsync` | pass `--no-build` automatically when no document changed since the last successful build, using the change tracking `analyze changed=true` and `run_tests changed=true` already have | ~64 s per unchanged-tree run here, and more on a solution whose up-to-date check is slower | not attempted: this repo's CLAUDE.md names "a test result read before its build is the previous binary's" as a trap, so staleness detection has to be proven before it can be trusted |
| **I349** [speed] `run_tests` passes `--blame-hang-timeout` on every VSTest run above 30 s, which routes the run through a datacollector process; the xunit.v3 in-process runner offers `-longRunning <seconds>` for the same answer with no extra process. Now only on the `dotnet test` fallback path, since a directly executed assembly gets neither | `run_tests`, `DotnetRunner.VsTestArguments` | measure the datacollector's cost, and use `-longRunning` where the direct runner is in play so a stopped run can still name the test that was running | unmeasured; the hang report is currently the only thing keeping the datacollector on the VSTest path | — |
| **I350** [accuracy] the direct-assembly branch of an expanded `run_tests` is gated on the `xunit.v3.runner.inproc.console` metadata reference plus `Reporter is VsTestLogger`, but no fixture can exercise the refusal: `fixtures/MtpSolution` holds ONE test project and `MinimumExpandedProjects` is 2, so the gate that keeps an MTP entry point off `-trx/-noLogo/-reporter` is untested in both directions | `run_tests`, `TestScope.DirectlyRunnable`, `DotnetRunner.Expandable` | give `MtpSolution` a second test project, then assert the expansion falls back to `dotnet test` there | one class of silently-wrong invocation, currently held only by a code read | — |
| **I351** [accuracy] `TestScope.TestProjectsOf` can only see projects Roslyn loaded, so a test project in a language the MSBuild workspace does not open - an `.fsproj` under `SkipUnrecognizedProjects` - is invisible to the expansion, and it is not known whether that registers a load Failure. If it does not, the `Load.Failures.Count is 0` guard passes and the run covers fewer projects than `dotnet test <solution>` would, silently | `run_tests`, `TestScope`, `ToolContext.TestProjectsOf` | probe whether an unrecognized project registers a Failure; if not, compare the loaded project set against the solution file's own list and refuse to expand when they differ | one silently-narrowed run per mixed-language solution | — |
| **I352** [accuracy] the direct-assembly branch hard-codes `-trx <path> -noLogo -reporter quiet` and gates only on the PRESENCE of `xunit.v3.runner.inproc.console`, never its version. Proven against xunit.v3 3.2.2 only; an older v3 that rejects one switch would turn a whole solution red with no way back | `run_tests`, `TestScope.DirectlyRunnable`, `DotnetRunner.AssemblyArguments` | fall back once to `dotnet test <csproj> --no-build` when a direct invocation exits non-zero having produced no trx, so an unsupported switch degrades instead of failing the run | one unrecoverable red per consumer on an older xunit.v3 | — |
| **I353** [accuracy] a workspace loaded over a **never-restored** solution carries NO metadata references, so `TestScope` classifies none of its projects as tests - and a later `build` does not refresh the already-loaded Roslyn solution, so BOTH `changed=true` and the direct-assembly path stay silently disabled for the life of that server. Measured on CI run 32879507657, red on ubuntu and macos and green on windows purely by test ordering, then reproduced locally by deleting the fixture's `bin`/`obj`: the expanded run fell back to `dotnet test` and `changed=true` fell back to the whole solution (`total=2` against `total=1`). Only `load_workspace reload=true` after a restore recovers it | `run_tests`, `TestScope`, `ToolContext.Described`, `LoadedWorkspace` | re-resolve references after a build that restored the solution, or report the degraded classification instead of silently answering from an unreferenced compilation | the fast path and `changed=true` selection on every server started against an unrestored tree | joining `ChangedTestSelectionE2ETests` to `TerseServerCollection` to stabilise its tests - it is what turned CI run 31634146607 red and is recorded in `E2ECollectionCensusTests.Excluded` |
| **I355** [accuracy] the restore backstop - a build failing for missing assets being retried WITH restore - is the riskiest path of the restore-skip change and has no E2E test. It is covered by a unit test over a fabricated `ProcessRun` string and by one hand-run measurement (a fixture with every `bin`/`obj` deleted answered `PASSED` in 7 808 ms), not by anything CI runs | `run_tests`, `build`, `DotnetRunner.FailedForMissingAssets` | copy a fixture to a temp directory, delete its `obj`, call `build`, assert it answers ok - a temp copy so the shared checked-in fixtures are never emptied under a parallel class | one unverified recovery path per release, on the branch that turns a fresh clone from working into failing | deleting `obj` from a checked-in fixture in place: `SelectionSolution` is built by eight tests in one class and by `BacklogClosureE2ETests` from another collection |
| **I356** [accuracy] `RestoreInputStamp` covers the files MSBuild finds by directory walk, but not one a project reaches by an explicit `<Import Project="...Versions.props" />` outside that walk, nor a user- or machine-level `nuget.config`. A change to one of those leaves the stamp unmoved, so the session keeps skipping restore and the build resolves the old package versions - silently, because nothing in a `--no-restore` build re-validates them | `run_tests`, `build`, `DotnetRunner.RestoreInputStamp` | stamp the imports MSBuild actually evaluated, which the loaded workspace already knows, instead of a name list | one silent stale-package build per solution that keeps its versions in an imported file | — |
| **I357** [accuracy] `run_tests changed=true` was called **0 times in 2 375 calls** across 120 test-running sessions in a 1-week, 421-transcript scan of three projects. `run_tests` is **54.5 h of the 156.4 h tool-wall week (34.9 %)**, and **1 057 of those calls (25.8 h)** re-ran an invocation already run earlier in the same session, after an edit that in most cases could not reach that assembly. Measured shape cost: `project+timeoutSeconds` 398x avg **126.1 s** against `project+test+timeoutSeconds` 779x avg **20.3 s** - narrowing is **6.2x**. `impact_of tests=true`, the other narrowing path, was called **1** time | `run_tests`, `SKILL.md` | make `changed=true` the DEFAULT when a call names no `test=`/`filter=`, keeping the existing documented whole-solution fallback and naming both the projects it ran and the ones it skipped, so the selection is never silent | Meta measured a file-level dependency cone at ~33 % of transitively-dependent tests with >99.9 % regression catch (engineering.fb.com 2018-11-21); at the measured 6.2x shape ratio this is hours per week, not minutes | a content-addressed test-result cache keyed on assembly hash: **measured and refuted this round** - only **88 of 2 375** runs repeated an identical invocation with no edit at all in between (117.3 m), and only **4** of those were green-after-green, so there is almost no pure re-run to cache; also refuted, blanket `noBuild=true`, which the repo's own "a test result read before its build is the previous binary's" trap bans |
| **I358** [accuracy] a shipped tool nobody calls: **`explore_symbol` 0 calls and `impact_of` 1 call in 35 739 tool calls**, while the chains they exist to collapse ran adjacent - `get_file_outline`->`get_symbol_source` 19x, `get_symbol_source`->`read_text` 208x, `get_symbol_source`+`replace_symbol` on one target 23x. **65 of 88 advertised tools were called at all; 23 were never called and 24 more fewer than 10 times.** CLAUDE.md's own ranking rule says a shipped tool nobody calls is a defect to fix or delete, and this is the second consecutive scan to measure it | `explore_symbol`, `impact_of`, `SKILL.md` | stop advertising the two as standalone tools and fold their answer into the tool the agent already calls - a `usages=true` / `impact=true` parameter on `get_symbol_source` and `get_file_outline` - so the capability needs no discovery; measured adoption evidence says placement, not existence, is the lever (arXiv:2602.20048: 58 % of trials made **zero** MCP calls despite an explicit system-prompt instruction, recovered to 100 % by a checklist at the END of the prompt) | ~255 tokens of `tools/list` per session returned for two tools, against 227 adjacent chains that a parameter would have collapsed | re-wording the `[Description]`: this is the third surface where a description-only fix was tried and the selection rate stayed at zero |
| **I359** [accuracy] `read_text` returns **verbose=true on 4 087 of 6 154 calls (66 %)**, so the compression the tool exists for is opted out of two times in three - and `read_text` is **37.9 M chars = 10.1 M tokens = 59 % of all terse-sharp result volume** in the week, the single largest payload in the corpus by a factor of six. 2 030 of those calls named a `.cs` path, which is exactly the case where `verbose=true` discards the outline and takes ~3x the tokens | `read_text`, `SKILL.md` | find why `verbose=true` is being passed - the likeliest cause is the `.cs`-outline steer training callers to pass it reflexively on every path, including the 4 121 non-`.cs` ones where it only re-adds blank lines and per-line numbers - then either scope `verbose` to the `.cs` opt-in it was added for, or name a narrower parameter for it | 66 % of the largest payload in the corpus; even a third of those calls dropping back to the compressed form is ~1 M tokens/week | condensing the payload further: the repo already refuted editing payload text, and this is a caller-behaviour defect, not a format one |
| **I360** [cost] the `read_text` -> `edit_text` anchor round trip: **823 adjacent same-path pairs**, read line span p50 **21** lines, and the `oldText` the edit then carried was p50 **162** chars. **2 115 342 chars (~528 835 tokens) of read payload existed only to let the caller copy an anchor**, and the copy is error-prone - 34 `oldText matched 0 times` refusals re-paid 19 492 tokens of input | `edit_text`, `read_text` | let `edit_text` address a line range - `startLine=`/`endLine=` beside `newText=` - guarded by a mandatory `expect=` first-line check so a drifted line number is refused instead of silently replacing the wrong text, which is the only reason a line-addressed edit was not already offered | ~528 835 tokens/week of read payload plus 823 round trips at `read_text` p50 1 521 ms = ~21 min of wall clock | dropping the `expect=` guard to make the call shorter - an unverified line number is exactly the confidently-wrong answer this repo refuses, and `section=`/`row=`/`occurrence=` already cover the cases where an anchor is stable |
| **I361** [perf] the edit path reports errors and introduced **warnings**, but never the **info-severity** analyzer findings the new text introduced, so a performance rule is learned one or two calls later from a `build`. Measured over 7 935 edit calls in the week: only **124 (1.6 %)** were followed by an `analyze`, while the gate output that did run named **CA1859 x93** (change return type for performance), **CA2000 x63**, **CA1861 x43**, **CA2012 x37**, **CA1822 x18**. Confirmed with instrument (d): `analyze severity=info` over `TextSearchService.cs`, `FileService.cs` and `ResponseBuilder.cs` returns **0 diagnostics**, so this is a write-path feedback gap, not a defect in this repo's source | `replace_symbol`, `replace_symbol_body`, `add_member`, `write_text`, `EditGate` | return the info-severity diagnostics the changed declaration introduced, as a delta against the pre-edit state, in the same response as `changedLines=` - the compile gate already holds both compilations | static-analysis feedback returned in the same session measured reliability warnings >50 %->11 % and functional correctness +10 pp in 10 iterations (arXiv:2508.14419); round-1 compiler feedback is worth +21.39 % pass@k against +3.33 % for round 2 (arXiv:2512.15766), so it is the FIRST response that has to carry it | returning the whole `analyze` payload on every edit - the same paper measures 5 % of iterations introducing a NEW issue under unfocused multi-issue feedback, so only the newly-introduced delta may be returned; and stating the rule in `SKILL.md` instead, which AGENTIF measures at <30 % perfect rule-following |
| **I362** [modern] a diagnostic that names only its rule id is measurably weaker than one that names the replacement. The agent emitted **543 `.ToString()`/`string.Format(` without an `IFormatProvider` (1.96/kloc, 58 sessions)**, **227 hand-written backing fields where `field`/a primary constructor applies (0.82/kloc, 32 sessions)**, **131 `new List<T>()`/`Array.Empty<T>()` where a collection expression applies (0.47/kloc, 36 sessions)** and **49 block bodies that `IDE0022` kills on this repo's ubuntu-only CI leg (9 sessions)**, over 277 413 emitted lines | `replace_symbol`, `add_member`, `cleanup`, `[Description]` | where a Roslyn code fix exists, name the fixed form in the edit response - `IDE0300: use [..] here` - rather than the rule id alone, and offer to apply it | grounded replacement text moved updated-API adoption 74.64 %->92.87 % (+24.42 pp) while self-reflection moved it +11.33 pp (arXiv:2604.09515); an automatic fix scored 85.2-99.6 % against 25.7-97.2 % for injecting an instruction (arXiv:2406.09834) | `await` without `ConfigureAwait(false)` - **9 289 hits, by far the largest class, refuted this round**: `.editorconfig` sets `dotnet_diagnostic.CA2007.severity = none`, so it is deliberately off and proposing it would be asking the codebase to fight its own configuration |
| **I363** [speed] the agent achieved **no parallelism at all**: **35 731 of 35 735 assistant messages carrying a tool call carried exactly ONE `tool_use` block**, 2+ blocks appeared **4** times, and **8 697 calls sat inside runs of 3 or more consecutive calls of the same tool** (longest run: `Bash` 42, `edit_text` 20, `read_text` 19, `write_text` 14). The from-the-second-call plural steer shipped and did not move it | `SKILL.md`, `ResponseBuilder`, every plural-parameter tool | fire the plural steer on the FIRST response of a tool whose next call is predictably independent, and say in `SKILL.md` that independent calls go in ONE message rather than only that a plural parameter exists | 3 tool calls per turn measured **-40.6 % wall clock and -35.9 % cost** with +2 pp accuracy (arXiv:2602.07359); tool execution is 60 % of a coding agent's latency (arXiv:2603.18897) and 68 % here - 156.4 h tool against 228.6 h turn | firing the steer on every first call: measured previously to break the one-line success contract on six tools, so it must be conditional on the tool declaring a plural parameter |
| **I364** [cost] **collapsing internal multi-space runs to one space saves 2.36 % of all terse-sharp output - 406 472 tokens in the measured week** - verified with `tiktoken` before/after on the 65 widest real responses in the corpus (664 897 chars): **5.02 % on `o200k_base` and 5.03 % on `cl100k_base`**, so the saving survives both encodings. Per tool: `get_file_outline` **13.7 %**, `find_usages` **11.9 %**, `razor_outline` 16.7 %, `find_files` 6.7 %, `changed_files` 6.6 %, `get_symbol_source` 6.3 %, `search_text` 4.8 % | `ResponseBuilder`, `TextCompressor` | use a single space as the column separator in every record the server FRAMES (outlines, hit lists, listings, diagnostics); a multi-space run costs exactly 1 token whatever its width, so the alignment is paid per row and read by nobody | 406 472 tokens/week measured, ~2.36 % of terse output; assert it in `TokenBudgetE2ETests` against the widest fixture case so it cannot be given back | **five trims measured at zero this round and refuted**: trailing whitespace 0.00 %, blank lines 0.00 %, leading-indent collapse 0.00 %, thousands separators 0.01 %, absolute->bare filename 0.25 % - none may be proposed again; and collapsing runs inside PAYLOAD text, which would corrupt a source literal, so this is framing-only |
| **I365** [accuracy] the truncation steer is firing as a **wrong default**, not as a rare narrowing: **`diff_text` 421/498 = 84 %**, `razor_outline` 10/11 = 90 %, `history` 98/179 = 54 %, `changed_files` 305/770 = 39 %, `get_file_outline` 134/391 = 34 %, `find_files` 318/1 012 = 31 %, `search_regex` 207/814 = 25 %, `search_text` 445/2 127 = 20 %. A tool that truncates on five calls in six is not steering, it is mis-defaulted, and each steer is a second call | `diff_text`, `history`, `changed_files`, `find_files`, `search_text`, `search_regex` | re-derive each default from its own measured payload distribution - `diff_text maxLines` first, at 84 % - so the common call lands whole; keep the steer for the genuine tail | ~1 900 avoidable second calls per week at these six tools' p50 latencies = ~45 min of wall clock, plus the re-sent arguments | raising every cap uniformly: silent or over-eager truncation measured 78.1 %->32.6 % downstream accuracy, so a cap that is too HIGH is not a safe error either - each default has to be measured on its own tool |
| **I366** [accuracy] one parameter accounts for the largest single error class in the corpus: **`glob` was refused `InvalidArgument` 101 times** - `search_text` 41, `find_files` 40, `search_regex` 20 - and one rejection line appears 97 times in the boilerplate census. These three tools already accept `pattern`/`path`/`query` aliases, so the failure is the glob VALUE, not its name | `search_text`, `search_regex`, `find_files`, `Errors` | put a worked, copy-paste glob in the `remedy:` of every rejected glob - the mechanism `ToolExamples` already ships for `razor_*` and `package_add` - and say which construct was rejected rather than only that the value was invalid | 101 refusals/week, each a full round trip; embedded tool-use examples measured complex-parameter handling **72 % -> 90 %**, and Anthropic ships `input_examples` as a first-party schema field for exactly this case | widening the glob grammar to accept whatever was passed: a glob that silently matches something other than what was asked is the confidently-wrong answer the census gates exist to prevent |
| **I367** [cost] the 88-tool advertised surface costs **26 475 tokens on every request**, against Anthropic's own first-party statement - now VERIFIED as a primary source, closing a standing UNVERIFIED row - that *"Claude's ability to pick the right tool degrades once you exceed 30-50 available tools"*. Corroborated: LongFuncEval measures a 7-85 % selection drop from 49 to 741 tools (arXiv:2505.10570), and adaptive shortlisting beat a fixed list by **+6.0 to +15.9 pp on Claude Sonnet 4.6** (arXiv:2605.24660). Measured here: **23 of 88 advertised tools were never called in 35 739 calls** | `ToolProfile`, `ToolGroups`, `.terse.json`, `workspace_status` | narrow the DEFAULT profile to the measured-called set and leave the rest reachable by name, the way `--tools core` already can - the machinery exists and only the default is wrong | Anthropic measures deferred loading at ">85 %" context reduction and names <10 tools as the point below which eager loading wins; here the advertised payload is 26 475 tokens per request | `notifications/tools/list_changed` as the narrowing mechanism: Claude Code's client-side support for it is **UNVERIFIED**, so the narrowing has to happen at startup, not at runtime |
| **I368** [accuracy] the guard is firing but not converting: **225 `Bash` git denials in the week** - `git log` **90**, `git diff` **60**, `git status` **60**, `git show` 8, `git tag` 4, `git ls-files` 3 - every one of which `history`, `changed_files`, `diff_symbols`, `diff_text`, `read_text ref=` or `find_files tracked=true` already answers, and every one of which cost a turn. Alongside them ~950 `Bash` shell-text calls (`cd+grep` 213, `cd+tail` 144, `ls` 139, `cat` 129, `cd+cat` 101, `cd+ls` 85, `grep` 80, `cd+head` 58) and **153 built-in `Read` calls averaging 50 561 chars** | `ToolGuard`, `SKILL.md` | the `PreToolUse` schema now documents `hookSpecificOutput.additionalContext`, which injects text on an **allow** as well as a deny - use it to name the replacement BEFORE the habit fires, on the first `Bash` of a session in a .NET tree, rather than only after the 90th denial | 225 turns/week at this corpus's turn p50 of 743 s, plus ~950 shell-text calls the semantic tools answer for fewer tokens | denying more `git` verbs: `git blame` ran once in 683 sessions and history mutation has no replacement, so the deny list is already at its correct extent - the defect is the timing of the message, not its coverage |
| **I369** [accuracy] the user had to intervene on **126 of 302 post-first-turn prompts (41 %, 0.29/session)**, and the tool called immediately before an intervention was **`Bash` 50 times (40 %)** - four times the next entry (`changed_files` 10, `run_tests` 10). Cue mix: re-scope 74, **gate reminder 60**, correction 54, unblock 54, redo 26. A gate the user has to restate 60 times a week is a gate that is not reaching the agent at the moment of the call | `ToolGuard`, `SKILL.md`, `CLAUDE.md` | move each hard gate that is a mechanical invariant out of the instruction files and into the `PreToolUse` guard, and measure interventions/session again next scan - the number is only meaningful as a trend | AGENTIF measures the best model perfectly following **<30 %** of system-prompt rules, with 64.6 % violating at least one; an intervention costs a full turn plus the re-paid input plus the context already spent going the wrong way | "the agent should have known which tool to call" - banned as a diagnosis by this repo's own review rule; and shortening the instruction files without moving the rule somewhere enforcing, which would delete the rule rather than relocate it |
| **I370** [speed] `write_text` is the **third most expensive tool in the week by wall clock - 926.8 m (15.4 h) over 1 582 calls** - and almost all of it is tail: p50 **3 999 ms**, p90 11 262 ms, p99 **56 304 ms**, so p50 x calls accounts for 105 m of the 927 m and the other **88 %** is the tail. **251 of those 1 582 calls (15.9 %) carried a cold-compilation notice**, against 102 on `get_symbol_source` and 99 on `replace_symbol`. The same tail shape appears on every semantic read: `search_symbols` p99 **53 799 ms** against p50 1 819 ms, `get_file_outline` p99 45 739 ms, `analyze` p99 68 189 ms, `find_usages` p99 20 215 ms | `write_text`, `EditGate`, `LoadedWorkspace`, `WorkspaceRegistry` | attribute the cold realization in the response the way `load_workspace` already does, then attack the trigger: the idle drop and the LRU eviction are re-realizing compilations that a write immediately needs again, so a write should mark its own projects hot rather than paying a full realization behind a 4 s median | ~800 m/week of tail on one tool; `MSBuildWorkspace.LoadMetadataForReferencedProjects=true` lets a referenced project resolve from its built `.dll` instead of a design-time build, which is the documented highest-leverage load option | blaming `MSBuildWorkspace.OpenSolutionAsync` itself - dotnet/roslyn#76679 measures a 419-828 % regression there (0.33 s -> 2.71 s per project) but it is paid at LOAD, and this cost is on calls made long after the load, so the BuildHost regression is not the mechanism here |
| **I371** [cost] harness-injected context is **35 % of the corpus's entire tool-result volume and nobody calls for it**: 9 730 attachment records / 29.5 MB, of which `skill_listing` **468 records = 15.4 MB (~3.86 M tokens)** and `deferred_tools_delta` **584 records = 8.3 MB (~2.08 M tokens)**. Against that, terse-sharp's own advertised surface is 26 475 tokens per request, and the two compactions in the week ran 967 432 -> 29 821 and 969 523 -> 34 561 tokens | `.terse.json`, `ToolProfile`, `SKILL.md` | `SKILL.md` is shipped by `terse install --skill` and loaded whole; price each of its sections and move anything a caller only needs AFTER choosing a tool into the `[Description]` or the `remedy:` that already reaches them at the call | one-level progressive disclosure measured as the ceiling - a second routing level "never helps and sometimes breaks accuracy outright" (arXiv:2607.17598) - so a flat index plus at-the-call detail is the shape with evidence behind it | shortening the hard gates into prose summaries: measured at <30 % perfect rule-following for system-prompt rules (AGENTIF), so a compressed gate is a deleted gate - the content has to move to an enforcing surface, not shrink |



