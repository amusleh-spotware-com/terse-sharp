# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I485** [accuracy] the installed `SKILL.md` describes HEAD while the connected server is an older binary, so an agent following the skill sends parameters that binary does not declare. Measured live this run, on `terse=0.53.0` against a repo at 0.55.0: `find_files globs=[...]` and `run_tests tests=[...]` were each refused `unrecognized <name>` - **2 wasted calls and 2 model gaps (~12 s) purely from skill/binary drift**, and the refusal listed the accepted names without ever saying which version the caller was talking to | `workspace_status`, every tool's `InvalidArgument` remedy | end an `InvalidArgument` that names an unrecognized parameter with the running version - `terse=0.53.0; this build declares: ...` - so the drift is legible from the failure itself rather than inferred. `workspace_status` already prints `terse=<version>`; the refusal is where it is actually needed | ~2 rejected calls per session on any machine whose global tool lags the repo, which is the normal state here because the running server holds file locks on its own `terse.dll` and cannot self-update. **Sized 2026-09-04**: `InvalidArgument` was the *only* error code the whole 246-transcript week produced, **242 occurrences**, and the edit family alone re-paid **296 024 characters (~74 006 tokens) of rejected input across 121 of them** - the largest single trap class in the window by re-paid tokens | printing the version on every response is refused - it is framing on thousands of calls to serve a handful of refusals |
| **I486** [speed] `edit_text edits=` carries `force` only at the TOP level, so one batch cannot mix a markdown file with a `.cs` attribute edit. Measured live: a 5-entry batch across `DotnetRunner.cs`, `CHANGELOG.md`, `SKILL.md` and `IMPROVEMENTS-ARCHIVE.md` landed 4 entries and answered `REFUSED src/TerseSharp.Server/DotnetRunner.cs: InvalidArgument - is a C# file`, costing a second call for the one entry | `edit_text` | let a `edits=` entry carry its own `force`, exactly as it already carries its own `path`. The top-level value stays the default for entries that omit it, so nothing an existing call means changes | one call and one ~6 s gap per mixed batch; the docs gate makes a `.cs` + `.md` batch the SHAPE of every shipped change here, so it recurs once per row | a top-level `force=true` covering the whole batch is refused - it would silently un-gate every markdown entry's sibling `.cs` file in a batch the caller meant to be text-only |
| **I487** [cost] `find_files glob="src/**/*.cs"` answered **215 paths** when the question was which ~12 files ten backlog rows lived in - roughly 4 000 tokens of which ~3 500 were never read. `depth=` and `name=` both existed and neither is reachable from the question, because the caller does not yet know the names to filter by | `find_files` | when a listing saturates its steer threshold, end it with the `depth=` that would fold it - `next: depth=2 folds this to 11 rows` - computed from the paths already in hand, the way `changed_files` ends with the exact `diff_symbols` call. A steer that names a concrete argument is acted on; one that names a parameter is not | ~3 500 tokens on the first orientation call of any task in a repo of this size, which is one call per task | making `depth=` default non-zero is refused - a listing that folds by default cannot be pasted into `read_text paths=` |
| **I488** [cost] a truncated `get_file_outline` names `contains=` only after the payload has been paid. Measured: `BuildTools.cs` answered `44 of 62 members` and `DotnetRunner.cs` `47 of 125 members` - together ~3 000 tokens - to locate 6 members, and `contains=` could not have been passed first because the member names were what the call was for | `get_file_outline`, `get_type_outline` | when an unfiltered outline exceeds the per-type cap, answer the TYPE list plus each type's member count first and let `contains=`/`all=true` open one - the shape `get_symbol_source` already uses on a type id, where it answers the member list plus a steer instead of the whole source | ~2 500 tokens per wide-file orientation; `DotnetRunner.cs` alone has 125 members and is read at the start of any build/test task | dropping the cap is refused, and lowering it is refused - both trade one wrong payload size for another |
| **I489** [fallback] `build`'s `StillLocked` note ends by telling the agent to leave the server: `The build named no holding process, so nothing below identifies one - list the holders yourself before stopping anything.` Measured live: that produced **2 `Bash` PowerShell calls and ~2 000 tokens of process table** to learn that a concurrent session's `dotnet test` and its four `terse.dll` children held the output - and the answer was in the OS, not in MSBuild's text, which is the only place `LockHolders.Describe` looks | `build`, `run_tests`, `clean` | when MSBuild names no holder, enumerate the holders from the OS instead of delegating to a shell - on Windows the handle owner of the named file, elsewhere the process table filtered to this tree - and classify them with the wording `LockHolders` already produces. The tool already knows the exact path that failed to copy | 2 `Bash` calls and ~2 000 tokens per lock event; a shared tree with agent worktrees makes a lock the normal outcome of a concurrent build, and this is a fallback the server explicitly instructs | shelling out to `handle.exe` is refused - it is not present by default and needs elevation; the refusal to stop a process for the agent stands, only the identification moves in-server |
| **I490** [accuracy] the duplicate-anchor refusal and the per-file grouping both compare `edits=` entry paths as RAW STRINGS, so one file named two ways in one batch is neither grouped together nor collision-checked. Found by the reviewer on the I477 change: `edits=[{path:"docs/Foo.md", oldText:"x", occurrence:1}, {path:"C:\\repo\\docs\\Foo.md", oldText:"x", occurrence:2}]` compares unequal in `FileService.Same`, so `Colliding` returns null, and `FileTools.Grouped` keys its dictionary on the same raw string, so the file is opened, written, then RE-OPENED by the second group - which reads the post-write text, and `occurrence=2` then addresses something other than what the caller counted | `edit_text` | resolve each entry's path to its full form once, before grouping, and use that for both the group key and the `Same` comparison - `PathGuard.Resolve` already produces it and `EditText` already holds the workspace | (accuracy) a false negative today, so it degrades to the pre-I477 partial-application hazard rather than refusing a valid call; it also silently splits one file into two writes, which doubles the change count a caller reads back | normalizing only in `Same` is refused - the grouping would still open the file twice, which is the half that actually corrupts the second occurrence |
| **I491** [accuracy] the I477 duplicate-anchor check is anchor-only by construction: `FileService.Shadowed` returns early when `OldText` is empty, so two `section=`-addressed entries of one file - `edits=[{section:"### Added", occurrence:1}, {section:"### Added", occurrence:2}]` - bypass it entirely. The exposure is smaller than the `oldText` case because a section replacement keeps its heading, so heading ordinals usually stay stable, but `place=` and a replacement that drops or adds a heading both move them | `edit_text` | extend the check to entries addressed by `section=` + `occurrence=`, comparing the section heading the way it compares `oldText`, and refuse the pair for the same reason | (accuracy) one silent mis-addressed section write per batch that renumbers headings; unmeasured, because no observed call has hit it | refusing every repeated `section=` in a batch is refused - two entries writing INTO different occurrences of one heading with `place=append` are legal and land correctly today |
| **I492** [accuracy] `EditGate.Collect` records only `DiagnosticSeverity.Error` and `DiagnosticSeverity.Warning`, so an applied edit can never report the **info-severity** diagnostic it just introduced - while `SKILL.md` tells the agent the edit's own `errors=/warnings=` means "you do not need a separate `analyze` afterwards", which is a claim the gate cannot support. Instrument (d): `get_symbol_source EditGate.Collect` - `src/TerseSharp.Core/EditGate.cs:240-255`, two `if`s and no third. Measured over **5 957 edit calls in 247 transcripts**: the rules that did surface, one or two calls later in `analyze`/`cleanup`/`build` output, were `CS0246`x125 `CS0234`x79 `CS0117`x64 `CA1859`x36 `IDE0060`x20 `IDE0161`x19 `CA1861`x16 `CA2000`x15 `CA1822`x12 `CA2012`x12 `IDE0042`x7 - every `CA`/`IDE` one of those is info or suggestion severity, invisible to `build` as well, and `IDE0060`/`IDE0161` are CI-fatal in this repository | `EditGate`, every mutating tool | count them: add `info=N` to the gate's tally when an edit introduces info-severity diagnostics and name them under `verbose=true`. The `WARNING introduced` list already exists and is already capped at five, so the shape is there and only the severity filter moves | 414 `analyze` calls at mean **10.2 s** plus a **5 866 ms** gap each, and the class it closes is the one measured to respond to a tool-delivered diagnostic - 85-99.6% fix rate for a tool-applied correction against 25.7-97.2% for telling the model in a prompt (arXiv:2406.09834), and >40%-to-13% / >80%-to-11% for an analyzer diagnostic fed back in-loop (arXiv:2508.14419) | listing every info diagnostic on every edit is refused - it breaks the one-line success contract on 5 957 calls; a count plus `verbose=true` is the only shape that survives the minimum-response gate |
| **I493** [accuracy] `rename_symbol` constructs `new SymbolRenameOptions()` - every flag default-false - so `RenameInComments` and `RenameInStrings` are OFF and **no C# comment or string occurrence is renamed, and nothing in the response says so**; the `NOT rewritten` list the skill documents is the XAML path only. Instrument (d): `get_symbol_source RenameService.RenameAsync` - `src/TerseSharp.Core/RenameService.cs:8-35`. This is the exact failure a 2026 measurement of language-server tooling recorded: the semantic arm scored **Pass@1 0.67 and site-recall 0.930 against plain grep's 1.00 and 1.000** on multi-file rename, and the residual was "fundamental: both fail the rename by missing the same site, a *comment*" (arXiv:2608.13568) | `rename_symbol` | after the rename lands, scan the old name in comments and string literals of the affected documents and emit them as `NOT rewritten` records, exactly as the XAML path already does - report them, never rewrite them, because a match inside a string is not decidable | (accuracy) one silently incomplete rename per occurrence that the agent cannot detect - the precise class this repository's never-answer-what-you-cannot-prove rule exists to stop; 23 `rename_symbol` calls in the window, none of which could have known | turning `RenameInComments`/`RenameInStrings` on is refused - it rewrites text no compiler checks, so the compile gate cannot catch a wrong one, which is the lossy-edit class I92 and I127 already refused |
| **I494** [speed] the bare-`sleep` gate worked and the waiting moved somewhere the gate does not look: **15 bare sleeps totalling 175 s** in the whole window, down from a measured 156 calls and 25 307 s - but **`TaskOutput` cost 14.08 h, 13.0% of all tool wall time, over 193 calls**, p50 **182 s**, p90 and p99 both **600 s** which is the ceiling, and 24 of them byte-identical repeats. Background work re-invokes the agent when it finishes, so those seconds bought nothing that the notification did not already deliver | `SKILL.md`, `CLAUDE.md` | extend the sleep gate to name polling by tool, not only by command: reading `TaskOutput`/`TaskList` to obtain a result the harness will deliver by itself is the same breach as `sleep N`, and the legal move is the one already written down - end the turn | up to **14.08 h/week** of wall clock, lever (d), which is larger than every `analyze`, `cleanup`, `format`, `get_diagnostics`, `gate` and `build` call in the same window combined (5.05 h) | denying `TaskOutput` in `ToolGuard` is refused - it is the only way to read a background result the completion notification does not carry, and a status check after a real notification is legitimate |
| **I495** [accuracy] the guard leaks at scale. Across the five project trees that made a terse call, in one week: **463 shell text tools ran as a leading command word** - not downstream of a pipe, so not the legal stdin-reader shape - `ls` 101, `grep` 79, `cat` 56, `tail` 43, `wc` 37, `sed` 24, `find` 17, `awk` 12, `head` 11, `tree` 7, `nl` 2; and **308 plain `git` subcommands terse already serves** - `log` 167, `show` 59, `ls-files` 36, `status` 30, `diff` 9, `tag` 7 - beside 175 scripted `--format`/`--porcelain` forms that are deliberately legal. Against all of that the harness recorded only **252 tool denials of every kind**. Excluding `type`, a shell builtin nothing replaces, that is **697 replaced commands that reached `Bash`**, inside a `Bash` class costing 7.33 h at a 15.2% error rate | `ToolGuard` | diagnose before widening: have the guard record a one-line reason whenever it inspects a command in a tree it recognises as .NET and allows it anyway, so the next scan can separate "cwd not recognised" from "compound shape denied whole then re-issued piecemeal" from "no row covers it" | 697 replaced commands per week; the fallback class is the only defect that scales with every session, and the guard is the only thing that addresses it | widening the compound-command rewriter to catch more shapes is refused up front - three review rounds each found a false negative in it, a `#` comment, a backslash escape and a trailing `&&`, and a wrong rewrite is strictly worse than a leak |
| **I496** [speed] `resx_set` ran **329 times across 36 sessions with a longest unbroken run of 115 consecutive calls** and 256 calls inside runs of three or more - and **250 of those 329 responses had already named a batch parameter**. So this is not a discoverability gap and the repeat steer is not the fix: `entries=` is **same-file only**, while a localization sweep is across culture *files*, which no parameter the tool declares can express | `resx_set` | take `files=[{path, entries}, ...]`, the shape `write_text files=` already ships, or a `cultures=` that writes one key set across a whole family in a single pass. Until one of them exists the steer names a parameter that cannot answer the run it fired on | 277 of the 329 calls collapse to a handful; at p50 **8 939 ms** plus a **5 866 ms** model gap that is **~68 min/week**, lever (a) | pointing the existing steer harder is refused on measurement - it fired on 250 of 329 calls and the longest run still reached 115, because the parameter it names cannot express a cross-file sweep |
| **I497** [cost] the advertised surface has grown back past what **I201** cut it to. Measured this run against this repository's own workspace: **88 tools / 29 647 tokens**, split `toolDescriptions=10194 parameterDescriptions=11578 schemaFrame=7581 names=296`, against I201's shipped 56 tools / 16 962 tokens - **+74.8%** - because markup narrowing keys off mere *presence*, so 10 `.razor` files in a 396-document solution re-advertise all ten `razor_*` tools while the same `workspace_status` line reports `generator=unavailable`. And the re-measurement this gate demands: **32 of 88 advertised tools were called ZERO times in 262 sessions, and 17 more ten times or fewer**, so 56 tools carried all 23 752 calls; `explore_symbol` and `impact_of` are now **0 and 0**, down from the 7 and 1 that were already the reason to re-measure | `ToolProfile`, `ToolGroups` | narrow the razor family when the workspace itself reports `generator=unavailable` - in that state those ten tools cannot answer a component or parameter question at all, and they cost roughly 3 400 tokens of surface to advertise; `parameterDescriptions` at 11 578 tokens is the single largest component and has never been measured on its own | ~3 400 tokens per request on any solution whose Razor generator is dead, plus the accuracy side Anthropic now documents: selection "degrades once you exceed 30-50 available tools", and deferred loading measured 79.5%-to-88.1% on Opus 4.5 | deleting the zero-call tools is refused - `xaml_*` and `project_*` answer questions this corpus's projects did not ask, not questions nobody asks; and compressing a `[Description]` was already measured and refused as I203 at 11.8% |
| **I498** [modern] the CI-fatal modern-syntax class is invisible at every gate the agent runs before CI. Measured over **180 639 emitted C# lines in 247 transcripts** and confirmed on real source by terse's own `analyze`/`cleanup` output naming `IDE0161`x19, `IDE0060`x20 and `IDE0042`x7 in the same window; this repository's `.editorconfig` carries that family at `suggestion` while `TreatWarningsAsErrors` escalates warnings only, so `build` cannot see them, `EditGate` cannot see them either (I492), and the ubuntu `--severity info` format leg is the first thing that does. Emitted rates: `IDE0032` field-keyword/primary-constructor **0.57/kloc across 20 sessions**, `IDE0300` collection expression **0.54/kloc across 30**, `IDE0041` `is null` **0.17/kloc across 12**, `IDE0022` expression body **0.09/kloc across 5** | `EditGate` via I492, `SKILL.md` | once I492 counts info severity, *name* only the subset the format gate is fatal on, so the write path teaches exactly the class CI kills rather than emitting the whole info tier | the format-gate rework this window measured as **39 red `format` and 94 red `cleanup` runs, every one of them following an edit in the same session** | bumping the `.editorconfig` severity is refused - it is the project's deliberate setting, and fighting a codebase's own configuration file-by-file is banned by `CLAUDE.md` |
| **I499** [perf] the agent emits blocking and allocating C# at a rate no gate reports at the write. Over **180 639 emitted lines**: `.ToList()`/`.ToArray()` materialization **3.24/kloc across 76 sessions**, synchronous `File.*` / `XDocument.Load` / `new StreamReader` on a request path **1.17/kloc across 51 sessions**, `Substring`/`Split` where a span would slice **0.74/kloc across 39**, `.Result`/`.Wait()`/`GetAwaiter().GetResult()` **0.54/kloc across 17**. Instrument (d): terse's own gate output named `CA1859`x36, `CA1861`x16, `CA2000`x15 and `CA2012`x12 on real source in the same window, and this repository already compiles the first two classes away with `BannedApiAnalyzers` - a mechanism that exists only in a tree that adopted it. Independently, real .NET performance-bug fixing sits at **3% baseline agent success against 20% with performance-aware tooling** (arXiv:2509.24091) | `EditGate` via I492, `SKILL.md` | the same info-severity count, plus a `SKILL.md` line separating what terse can decide from what it cannot - allocation is analyzer-owned and reportable, host-conditional async is not | the measured lever is the diagnostic at the write and not the rule in the prompt: **62-89% fix rates once the tool hands the model its diagnostic** (arXiv:2412.14841) against prompt-only efficiency instructions that moved `eff@1` negligibly or negatively (arXiv:2406.06647) | mandating `ConfigureAwait(false)` is **refused on evidence**: 7 505 hits at 41.55/kloc were measured and dropped, because current guidance is host-conditional - omit it in ASP.NET Core and app code, keep it in libraries - so a universal nudge would be wrong more often than right |
| **I500** [cost] tool **input** has never been measured by any run of this scan, and it is not small: **20.8 M characters sent**, of which the four writers carry **14.19 M ch = ~3.55 M tokens** - `edit_text` 1.62 M, `write_text` 1.50 M, `replace_symbol` 0.27 M, `add_member` 0.16 M - which is **19% of the 18.46 M tokens of tool *output*** in the same window. `write_text` averages **7 151 ch/call** over 846 calls, and 729 of those are the session's first write of that path, so this is the cost of creating files rather than whole-file-rewrite waste; 151 of them still sat in runs of three or more that `files=` already collapses | `SKILL.md`, `write_text` | teach the input side in the skill's batching rule, which today argues only from response tokens and model gap: `write_text files=` and `edit_text edits=` also delete the repeated argument framing, measured at ~270 000 tokens for `write_text` runs alone | ~270 000 tokens/week of repeated `write_text` argument framing plus 0.25 h of model gap, lever (a) | a compression of the sent content is refused - it is payload the caller authored, and rewriting payload rather than framing is the class I92 and I127 already refused |
