# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I517** [accuracy] three token-budget numbers shipped in `README.md` and `NUGET_README.md` - a `29,700-token ceiling over 88 tools`, `57 tools, ≤22,650 tokens`, `64 tools, ≤23,280 tokens` - are asserted nowhere: a literal sweep of the whole tree matched only the two docs, while the real asserted ceilings are 24,600 (`MarkupProfileE2ETests`) and 25,400 (`ToolSettingsE2ETests`) and the full surface has no ceiling at all. A from-scratch rewrite carried all three forward before a manual sweep caught them | `DocsCoverageE2ETests` | census-gate the shipped docs the way the tool list already is: discover every `≤N tokens` / `N-token ceiling` claim from the README text itself and fail when no `Assert` under `tests/` names that number | one false public claim per docs rewrite - three were live on the repo front page and on nuget.org, each costing a `search_regex` sweep to disprove | — |
| **I518** [accuracy] `terse serve --help` contradicts the code on the default tool profile: `Program.cs` describes `--tools` as "core (default) ... an unrecognised value falls back to core", while `ToolProfile.Resolve` returns `new(null, MarkupDerived: true)` for null, empty **and** unrecognised - the whole markup-derived surface, never `core`. A user who trusts `--help` believes 21 tools are advertised when 88 are | `ToolProfile`, `Program.cs` | correct the option description to say the default is the markup-derived surface and that an unrecognised value falls back to it, and census-gate it: assert the `--tools` help text names the profile `Resolve(null, null)` actually returns | one wrong belief about the whole advertised surface, held from the first `--help` a user reads; found only because a docs review cross-checked the README against `Resolve` | — |
| **I519** [unanswerable] nothing stages part of a file, so a shared tree forces a mutate-stage-restore dance: `CHANGELOG.md` held this task's entry **and** a parallel session's unfinished one, and committing the path would have published a feature that was not in the commit. `git show HEAD:<path>` is guard-denied (correctly - `read_text ref=` replaces it), which also closes the `git hash-object`/`git apply --cached` route, so the fix was `edit_text` the other entry out, `git add`, `edit_text` it back - 4 extra calls and a window in which a concurrent write would have been clobbered | `changed_files`, a new `stage` | let `changed_files` stage by content: `changed_files stage=["<path>"] hunks="mine"` staging only the hunks whose lines this session wrote, from the undo history the workspace already keeps, with no worktree mutation | 4 calls and the clobber window per shared-tree commit; this repo is documented as shared with agent worktrees, and `git add -A` has already swept a stray note into a release commit once | `git add -p` is refused - interactive is unavailable to an agent; exporting the HEAD blob is refused - the guard denies `git show <ref>:<path>` and rerouting through `read_text ref=` costs the whole file in context |
| **I520** [speed] `explore_symbol` and `impact_of` were called **0 times** across sixteen backlog rows of pure symbol work in this run, while the chains they exist to collapse ran constantly: roughly 12 `get_symbol_source`, 6 `find_usages`, 4 `get_type_outline` and 8 `search_text containers=true` calls, several of them the literal `search_text` -> `get_symbol_source` -> `find_usages` triple `explore_symbol` answers in one. The repo already measured this at 7 calls in 683 sessions and shipped the tools anyway; a second independent run confirms nothing changed | `SKILL.md`, `explore_symbol`, `find_usages` | make the composite reachable from the answer instead of from memory: have `find_usages` and `get_symbol_source` end with `explore_symbol symbolId=<the id you just used>` when the same id has now been asked about twice in a session, the way the repeat steer already names a plural parameter | 2 calls and ~12 s of model gap per symbol investigated; this run investigated about a dozen | adding a third navigation tool is refused - the two that exist are not called, so the defect is discovery, not capability |
| **I521** [accuracy] a locked-output `holder` line names `pid`, `startedUtc` and `exe`, and then says "wait for it rather than stopping it" without giving anything to judge HOW LONG to wait - so telling a live E2E run from a stranded fixture host took **3 `powershell Get-CimInstance` fallbacks** in this run, reading each holder's age and command line. Measured live: a `build` was blocked twice over ~3 minutes by another session's suite, and the decision to wait rather than kill could not be made from the `holder` lines alone | `build`, `run_tests`, `LockHolders` | give each holder line its age and its command line tail - `holder pid=41612 dotnet age=5.8m cmd=... terse.dll serve --workspace fixtures/FixtureSolution` - which is exactly what the three shell calls went and fetched | 3 shell fallbacks per locked build on a shared tree; this repo documents itself as shared with agent worktrees, so the case is routine rather than rare | printing the full command line is refused - it can carry an absolute path per `PositionFormat`; the tail after the assembly name is enough to identify the fixture |
| **I522** [accuracy] a truncated `read_text section=` reports the **file's** length as the section's: reading `## [Unreleased]` out of `CHANGELOG.md` answered `40/5603 lines truncated` and `next: startLine=49 (total=5603)`, where 5603 is the whole 179-section changelog and the section itself is about 90 lines. The count line and the continuation both describe a range the caller did not ask for, so a bounded section read looks like a bounded whole-file read | `read_text`, `FileService` | when `section=` is passed, count and report against the SECTION's own line span - `40/90 lines truncated`, `next: startLine=49 (total=90)` - so the steer names the range that was actually read | (accuracy) one wrong total per truncated section read; the number is currently 62x the real one on this repo's own changelog | reporting no total is refused - the continuation line needs one, and `I236`-era work added it deliberately |
| **I523** [accuracy] the `I507` case-sensitivity fix has no test that fails when it is reverted. `Comparer_IsCaseSensitiveExactlyWhereTheFilesystemIs` pins `PathBoundary`, a different type from the two changed lines: put `StringComparer.OrdinalIgnoreCase` back into `FileTools.Grouped` and `FileTools.TrackedAsync` and the whole suite stays green. Found by the R2a reviewer's mutation check on the `I501`-`I516` pass | `FileToolsE2ETests`, `fixtures` | the honest test needs a case-only twin, which cannot exist on the Windows and macOS legs - so add the case to the fixture the only way it can be added: a `Directory.Build.targets` step that creates `a.md`/`A.md` on a case-sensitive filesystem only, and assert the changed-file count equals `PathBoundary.Comparer.Equals("a.md", "A.md") ? 1 : 2` | (accuracy) the fix is correct but unlocked; a future edit to either line reverts it silently on the two CI legs where it matters | a platform-conditional early return in the test body is refused - it is a skip in disguise and this repo bans weakening a test to make a suite green |
