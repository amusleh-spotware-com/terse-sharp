# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I269** fixing the 12 `RS0030` sites this task introduced meant turning `file(line,col)` into the declaration that contains it: `analyze` and `build` name a position, nothing names a symbol, so it cost one `get_file_outline paths=[5]` (~2 900 tokens, of which 9 lines were used) plus a `get_symbol_source` batch | `analyze`, `get_diagnostics`, `build` | tag each finding with the containing declaration's addressable id, exactly as `diff_symbols` already maps a hunk onto its declaration - the fix list is symbols, not coordinates | one outline call and its unused payload per multi-site diagnostic sweep, measured at ~3 000 tokens over 12 sites | printing the whole declaration beside the finding - `analyze` is a listing tool and rule 0 bans payload nobody asked for |
| **I270** `search_text glob={README.md,NUGET_README.md,src/.../SKILL.md}` answered `0 matches` rather than refusing, so a brace expansion the matcher does not implement read as "the string occurs nowhere" - a confidently wrong negative, and one wasted call plus a retry. Observed a second time on `**/*.{md,yml,yaml,json,cs,slnx}` during the backlog split, where the empty answer was nearly read as "no document mentions the file" | `FileGlob` | refuse a glob containing `{`/`}` naming the syntax that is supported, the way an unknown parameter name is refused before the call runs | one wasted call per unsupported glob, and the far larger cost of trusting an empty result | implementing brace expansion - the glob surface is deliberately small, and a refusal is what makes a zero result trustworthy |
| **I272** `edit_text section="### Added" place=append` answered `'### Added' names 36 sections`, and its remedy - "pass the heading with its level" - is what the call already did; `occurrence=` is refused beside `section=`, so the only way in is an anchor | `FileService` | accept `occurrence=` with `section=`, and make the remedy name it instead of restating the level rule | one wasted call per repeated-heading section edit, on a file such as `CHANGELOG.md` where every release repeats the same four headings | matching the first occurrence by default - a changelog edit landing in the wrong release is silent and expensive |
| **I273** the whole evidence base for `I266` - that 65 % of the advertised payload is descriptions - had to be measured by driving the server over stdio from a python script, because `workspace_status` reports `advertised=88 tools 25959 tokens` and nothing about what those tokens are | `workspace_status` | under `verbose=true`, break the advertised total into tool descriptions, parameter descriptions, schema frame and names | the stdio harness and its ~15 minutes per measurement, on the one number this repository optimises against | printing the breakdown by default - it is a maintainer question, not an agent one |
| **I274** a concurrent session reverted an applied edit and `changedLines=51` had already been reported, so verifying what was really on disk fell back to `Bash: grep -c` twice: every terse read answers from the workspace, which is exactly the thing that could not be trusted | `workspace_status`, `changed_files` | report the documents whose in-memory text differs from disk - the watcher already compares content before it changes anything, so the divergence is known and simply not surfaced | two shell fallbacks per suspected divergence, and the far larger cost of an edit believed applied that never landed | re-reading the file with `read_text` - it answers from the same snapshot, so it cannot detect the case |
| **I275** the `guard=absent` warning is published by a fire-and-forget maintenance task started one line before the transport, so the first `workspace_status` of a process can answer without it - the orientation call the warning exists for | `AssetBanner`, `McpHost` | compute the asset state synchronously before the transport starts, or await the refresh inside the first `workspace_status`/`load_workspace` | the whole value of `I256` on the one call an agent makes first; unquantified, because no reproduction exists | awaiting the probe on every call - it would put four file probes on the request path the 60 s handshake budget protects |
| **I276** the metadata fallback ignores `kind=` and `scope=`: `search_symbols query=X kind=method` falls through to a types-only assembly search, so it can answer with a class the caller excluded | `SymbolSearch`, `NavigationTools` | skip the fallback when `kind=` names something the metadata half cannot return, and say so instead of answering off-filter | one confidently off-filter answer per filtered zero-result query | filtering after the fact - the metadata half only ever holds types, so the honest answer is to decline |
| **I277** `SchemaCompactor` now drops any property named `default` at any depth; a nested item-schema member named `default` inside `files=`/`edits=` would vanish silently, and the parameter census only reaches top-level properties | `SchemaCompactor` | drop the key only at parameter-schema depth, or extend the census to nested item schemas | latent - no such member exists today; the cost is a silently missing nested parameter | dropping the compaction - it is worth 2 672 characters per `tools/list` |
| **I278** `changed_files` reports a `capped` row count against a total file count once an untracked directory folds, so the two numbers in one summary are different units | `GitTools` | count files for both halves, the way `find_files` already does with `Covered(shown)` | one misleading summary per capped fold | printing both units - a summary carrying two counts of different things is the confusion, not the fix |
| **I279** `addTo=` matches a containing type by leaf name only, so two targets whose containers share a leaf name in different namespaces resolve to the first, silently | `SymbolEditService` | compare the qualified name when the leaf is ambiguous, and refuse naming both candidates | one member landing in the wrong type, undetectable from the response | requiring a full documentation id - the leaf name is what an outline prints and what the remedy offers |
| **I280** `CLAUDE.md` mandates that every `CHANGELOG.md` link definition name a tag that exists, and no census gate reads it: the `[0.37.0]` heading, its link and the repointed `[Unreleased]` compare all sat in `main` against a tag that did not exist | `ChangelogReferenceTests` | assert every `## [` heading has a link definition and every definition names an existing tag or the one being cut | one dead nuget.org and GitHub release link per release cut ahead of its tag | resolving tags over the network - `git tag --list` is local and is what the release gate already reads |
| **I281** `BacklogClosureE2ETests.WriteText_WithTwoInterdependentCSharpFiles_LandsThemUnderOneCompileGate` failed once in four full E2E runs with `ERROR Internal: RemoteInvocationException`, and passed alone and on the next full run - the same MSBuild project-evaluation race `0.37.0` hardened with one shared lock, so the lock does not cover every path that loads a `ProjectCollection` | `ProjectGlobs`, `EditGate` | reproduce it deterministically - two concurrent new-`.cs` writes into one project - and either widen the gate to every MSBuild load or answer the failure as a retriable condition instead of `ERROR Internal` | one red full-suite run per four, each ~21 minutes, plus the far larger cost of reading a real failure as a flake | calling it a flake and re-running - a one-runner red is not a diagnosis, and this one has now been observed three times |
| **I282** `edit_text edits=[{path,oldText,newText}, ...]` where **every** entry carries its own path is refused with `missing path`, so the whole 1 200-token batch had to be re-sent with an arbitrary top-level `path` chosen from one of the entries | `edit_text`, `FileService` | make top-level `path` optional when every entry declares one, and keep the refusal only for a batch with at least one path-less entry | one rejected call plus a full re-send of the batch payload per cross-file edit - ~1 200 tokens here, and it scales with the batch | making `path` required in the schema and saying so in the description only - the binder already accepts the call, and the cost is the re-send, not the wording |
| **I283** the `N read_text calls in a row - pass paths=[...] with the next N+ in ONE call` steer fired on three consecutive reads that each needed a **different line range**, which `paths=` cannot express, so the steer asked for a call that would answer the wrong lines | `read_text` | suppress the steer when the preceding calls used `startLine`/`endLine`/`tail`/`section`, or teach `paths=` per-entry ranges so the steer is actionable | one misleading steer per multi-range read sequence, and the re-read it would cause if followed | dropping the steer - the batch it advertises is real and measured for whole-file reads |
| **I284** `read_text ref=HEAD` returns every non-ASCII character as mojibake - an em dash came back as `ΓÇö` on Windows - so a historical read cannot be compared with the working tree, and text copied out of one would write the corruption into the file | `read_text`, `GitRunner` | decode the `git show` child process's output as UTF-8 explicitly instead of inheriting the console code page | one unusable comparison per `ref=` read of any file carrying an em dash, plus the far larger cost of a corrupted write-back; every document in this repo uses em dashes | reading the working-tree copy instead - the whole point of `ref=` is the text at that revision |
| **I285** moving one markdown section from one file into another has no tool: splitting the 205 KB backlog cost a 13 KB `read_text section="## Open"` plus a 9 KB `write_text` that re-sent text the server already held, ~5 500 tokens for a move that touches no content | `edit_text`, `FileService` | `edit_text section="## Open" toPath=other.md place=append` - cut the section from one file and append it to another in one compile-gated write, returning only the two changed-line counts | the whole payload of a section move - 5 500 tokens here, and it scales with the section | `write_text ref=` on the source file - it restores a whole file from git, and cannot address a section or a second path |
