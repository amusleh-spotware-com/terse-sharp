# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I540** [round trips] `cleanup verify=true fix=style` and `cleanup verify=true fix=analyzers` are the CI pair this project's own `CLAUDE.md` mandates back-to-back, so they are ALWAYS two calls and two model gaps; this run made them 4 times (2 rounds x 2 modes) and the server's own repeat steer fired on them | `cleanup` | a `fix=ci` mode - or `fix=["style","analyzers"]` - that applies both CI rule sets in one pass and answers one verdict, naming which mode each named file belongs to | 2 calls and ~12 s of gap per end-of-task gate, on every task in this repo | — |
| **I541** [fallbacks] the guard correctly denied `cat > fixtures/.../RegionTail.cs <<'EOF'` but its `Call this instead:` named `get_file_outline path="…"` - a READ tool - for what was plainly a WRITE of a new `.cs` file; the usable answer was `write_text path=… force=true`, which the hook never named | `ToolGuard` | route by direction as well as file kind: a redirect (`>`, `>>`), a heredoc, or a `tee`/`cp`/`mv` target makes the replacement `write_text` (with `force=true` for a `.cs`), and only a bare read routes to the outline/`read_text` family | one wrong-tool suggestion per denied shell write; the hook's whole value is that its suggestion is runnable verbatim | — |
| **I542** [failures] a whole-solution `run_tests` was launched, then code was edited, so the in-flight verdict became stale and had to be `TaskStop`ped - twice in this run, ~19 min of wall clock abandoned; the second time it also held the build output and cost an `MSB3027` round trip to diagnose | `run_tests` | record the loaded workspaces' generation counters when a run starts and, when a document changes before it finishes, end the verdict with `STALE n document(s) changed after this run started` - or abandon it outright; the memo already tracks "nothing was written in between" for the opposite case | ~9 min per abandoned whole-solution run, plus the `build` lock round trip it causes | — |
| **I543** [unanswerable] a `TokenBudgetE2ETests` failure names the WHOLE 88-tool surface cost, but nothing answers that number from a working-tree build: the `terse call workspace_status` probe reports only the narrowed advertised profile (78 tools here), so re-measuring a 4-test budget failure cost 3 E2E runs (~90 s) instead of one 3 s probe | `workspace_status` | print the whole-surface cost beside the advertised one under `verbose=true` - `advertised=78 tools 28727 tokens  surface=88 tools 30565 tokens` - so the number the budget census asserts is readable without running the census | ~90 s and 3 test runs per budget re-baseline; the existing `advertised=` line already measures the wrong set for that question | — |

