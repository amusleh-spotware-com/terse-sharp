# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I438** [speed] **36 140 of 36 140** tool-bearing assistant messages issued exactly ONE tool call in the week to 2026-08-29 - **100.000%**, worse than the 99.997% of 2026-08-26. Zero messages across 284 transcripts spent the harness's parallel `tool_use` capability, which is free and needs no server change, while the model gap cost **103.1 h** at p50 6 062 ms over 36 032 gaps. The two dominant independent pairs are `search_text` -> `read_text` 819x and `read_text` -> `search_text` 576x, both **same-target 0%** | `SKILL.md`, `CLAUDE.md` | name the parallel case with the concrete shapes the corpus shows instead of the current generic phrasing that measured zero adoption: an independent read beside an independent search, and every `find_files`/`search_symbols` preceding a read of a different file | (b) 1 395 measured adjacent same-target-0% read/search pairs = 2.38 h/week, against a ceiling of the whole 103.1 h of model gap | a server-side change cannot reach this: parallelism is a property of the assistant message, not of any tool schema. The PROMPT lever is also already spent and measured failing - this session's own system prompt carries the `<use_parallel_tool_calls>` instruction Anthropic's prompting guide claims boosts the rate to ~100%, and this corpus still measured 100.000% single-call. Published rates corroborate a low ceiling: 12.7% of turns for the best model and under 1.5% for most (arXiv:2603.20576, 2026-03-21), so a server-side batch surface is the only lever left. Claude Code's own prompt already carries *"When making multiple bash tool calls, you MUST send a single message with multiple tools calls"* and this corpus still measured zero. The measured payoff of forcing a per-turn quota instead is turns **45.7 -> 23.8**, wall clock **-41%**, cost **-36%** and accuracy 66% -> 68% (arXiv:2602.07359, 2026-02). No published source gives an in-the-wild parallel rate for any production coding agent, so this 100.000% is novel and has no external comparator |
| **I439** [speed] **1 485 of 3 050 `run_tests` calls (48.7%) and 772 of 1 040 `build` calls (74.2%)** were byte-identical repeats inside one session - **15.50 h and 3.33 h = 18.83 h/week** on tools whose p50 is 31 506 ms and 9 446 ms. `run_tests` alone is 103.94 h = **56.8% of all tool wall time** and the verification family is 111.11 h = 61%, at 7.8 verification calls per task cycle | `run_tests`, `build` | make the repeat visible rather than cached - prefix an identical in-session call with `repeat #N of this exact call, Ns ago, previous verdict X; N documents changed since` - and extend `changed=true` to per-project granularity so a re-run skips the test projects whose transitive inputs did not move since the previous identical run | (d) up to 18.83 h/week; the per-project gate is the provable half, the visibility prefix is ~20 tokens on a call that already costs 31 s | a whole-call result cache is refuted by this run's own measurement: only 58 of 1 488 `run_tests` repeats and 8 of 772 `build` repeats had NO mutating call in between, so it would fire on 3.9% and 1.0% and would have to be unsound to do better. If it is ever built the key must cover input CONTENT HASHES not timestamps, plus the `terse` version and the `dotnet` arguments - Gradle counts the task's own code as an input and names an under-specified key as the cause of incorrect hits - the E2E tier must be excluded outright because it spawns a real child `terse.dll` and is not side-effect free (Nx), and a red verdict is never reused (Bazel `auto`). .NET ships no test-result cache at all - dotnet/sdk#20071 open since 2021-08-24, and the MTP `dotnet test` surface has no cache option - so this is unclaimed territory rather than a wheel to reinvent |
| **I440** [accuracy] **2 389 terse responses ended truncated in one week = 6.6% of all terse calls**, each a round trip the agent must re-issue: `diff_text` **369 of 432 = 85.4%**, `history` 123 of 255 = 48.2%, `changed_files` 317 of 772 = 41.1%, `get_file_outline` 157 of 448 = 35.0%, `find_files` 256 of 804 = 31.8%, `search_regex` 289 of 1 122 = 25.8%, `search_text` 466 of 2 206 = 21.1%. At the corpus model gap that is **4.04 h/week** before the re-issued call runs - and the re-issue mostly clips too: 440 of `search_text`'s 466 steers, 315 of `diff_text`'s 369 and 265 of `search_regex`'s 289 already carried an explicit cap | `diff_text`, `changed_files`, `get_file_outline`, `find_files` | raise the one default that is provably wrong - `diff_text maxLines=1000` truncates 85.4% of real calls at a mean payload of 10 032 chars - and make every steer name the cap value that returns the rest, which `diff_text` already does and the other six do not | (a) 4.04 h/week of model gap plus the clipped payload paid for and discarded | raising every default is refused: the caps exist to bound a response, and `search_text`'s steer correctly points at `glob=` rather than at a bigger cap, which is why 94% of its steered calls had already set one |
| **I441** [accuracy] the `PreToolUse` guard fired **297 times in one week** on shell commands terse already replaces - `git log` 122, `git status` 72, `git diff` 59, `git show` 28, `git ls-files` 7 - every one a denied `Bash` call the agent then re-issued as a terse call, costing a 6 062 ms model gap plus ~110 tokens of deny text each = **0.50 h and ~33 000 tokens per week**. The guard works; discoverability does not, and `history`, `changed_files`, `read_text ref=` and `find_files tracked=true` all shipped before this window | `ToolGuard`, `SKILL.md` | make the deny text a worked example rather than a sentence: translate the denied command's own arguments into the replacing call (`git log --oneline -1` -> `history maxResults=1`), which is the schema-example lever measured at 72% -> 90% on complex parameter handling | (d) 297 denied round trips = 0.50 h and ~33 000 tokens per 284-session week | widening the guard is not the lever - it already denies all four commands; the failure is that the agent reaches for the shell first, so the fix is in what the denial teaches |
| **I442** [modern] every mutating tool answers `errors=N (+D) warnings=N (+D)` and nothing else - confirmed at `src/TerseSharp.Core/ResponseCompression.cs:15`, which renders errors and warnings only, while `DiagnosticSeverity.Info` appears nowhere outside `GateService` (`src/TerseSharp.Core/GateService.cs:35,48`) - so an edit that introduces an info-severity CA/IDE diagnostic answers clean and the agent learns two calls later. Measured over 7 517 edit calls in the week to 2026-08-29: **202 info-severity rule mentions in gate output that followed an edit in the same session** (CA1859x83, CA2000x36, CA1822x30, CA1861x30, IDE0060x24, CA2012x23, IDE0161x20) and **1 042 targets edited more than once in one session** (2x541, 3x216, 4x107, 5x66, 6+x112) | `EditGate`, `ResponseCompression.VerboseCounters` | the compile gate already builds the changed projects' compilation to count errors - report the info-severity ids that the changed declaration introduced on the same line (`info=+2 CA1859,IDE0060`), ids and count only, full text behind `verbose=true` | (a) one `analyze` round trip per edit that would otherwise be clean - `analyze` p50 4 335 ms plus a 6 062 ms gap - against 202 measured post-edit info findings per week | returning full diagnostic text on every edit is refused by the minimum-response gate: a success is one line, so this is an id list and a count, never a report. And the row must NOT claim a code-quality gain: an ablation of five self-refinement loops measures compilation-error feedback at **+4.75 pp** pass@1 against static-analysis-quality feedback at **+0.06 pp** (LLMloop, arXiv:2603.23613, 2026-03-24), so the compile gate this repo already ships is where the correctness value is and this row buys only the deleted round trip. A bare diagnostic is also the WRONG shape - across six feedback types bare compiler feedback ranked lowest at 49.2% repair and mixed diagnostic-plus-guidance highest at 63.6% (FeedbackEval, arXiv:2504.06939v2) - so each id carries its remedy, and the loop stops at one round, because gains concentrate in the first two (arXiv:2604.10508) |
| **I443** [cost] multi-space alignment padding in terse's OWN responses cost **763 296 tokens = 4.82%** of the 15.85 M tokens terse returned in the week to 2026-08-29, a multi-space run being exactly 1 token whatever its width; every non-terse tool together contributed 24 247. Tokenizer-confirmed on the widest real response of each of 16 tools: 143 278 -> 135 941 `o200k_base` tokens = **5.12%**, worst `get_file_outline` **13.7%**, `find_usages` 11.9%, `search_regex` 10.8%, `resx_validate` 8.4%, `razor_validate` 7.8%, `changed_files` 6.8%, `write_text` 6.6%, `get_symbol_source` 6.3% | `ResponseBuilder`, `PositionFormat` | emit ONE space between columns on every machine-read record instead of an aligned pad; keep the alignment only under `verbose=true` | (cost) ~763 000 tokens per 284-session week, tokenizer-verified at 5.12% on the widest real payload of 16 tools | abbreviation, blank-line and trailing-whitespace trims are refuted for this row - 19 of 20 abbreviations and both whitespace classes measure <=0.1%; and a record's own text is never rewritten, so this touches framing only |
| **I444** [cost] `read_text` is **42.3% of all tool-result volume** in the window - 33.93 M chars over 5 868 calls at 5 784 ch/call - and **57.9% of that volume is whole-file reads**: 2 471 calls, 19.67 M chars, **7 959 ch/call**, against 3 058 ranged calls at 4 074 ch/call. The projection parameters that exist to avoid it (`headings`, `section`, `columns`, `tail`) were used on **341 calls = 5.5%**. By extension the expensive half is markdown: 2 179 `.md` paths at **5 699 ch/path** against 5 352 `.cs` paths at 3 278, where the outline steer already fires | `read_text`, `FileService` | give a whole-file markdown read the steer a whole `.cs` read already gets - answer the heading map plus a one-line steer instead of the text once the file passes a size threshold, with `verbose=true` or any range opting back into the text | (cost) the markdown half of 19.67 M chars per week; the `.cs` path that already steers measures 3 278 ch/path against markdown's 5 699 | applying it to every markdown file regardless of size is refused: a short `.md` read whole is the correct answer, and a steer there would cost a second call to recover the text |
| **I445** [speed] `find_files` and `get_type_outline` are the only two listing tools with NO array parameter while every sibling has one - `get_file_outline paths=`, `get_symbol_source symbolIds=`, `analyze paths=`, `search_text queries=`. Measured in the week to 2026-08-29: `find_files` 809 calls **0% batched**, 93 consecutive runs totalling 221 calls, longest 6; `get_type_outline` 129 calls 0% batched, 16 runs totalling 38 calls, longest 4 | `find_files`, `get_type_outline` | add `globs=[...]` to `find_files` and `symbolIds=[...]` to `get_type_outline`, each answering per entry under its own header line with an inline NOT_FOUND - the contract `read_text paths=` and `get_symbol_source symbolIds=` already keep | (a) 150 calls and 0.25 h of model gap per 284-session week, plus the schema asymmetry that costs a rejected call the way I433 measured for `add_member` | `find_files glob="{a,b}"` alternation is not a substitute: it cannot report a per-entry count, and a glob that matched nothing is indistinguishable from one that was never asked |
| **I446** [speed] **3 057 `Bash` calls carrying a shell text tool** (`grep`/`rg`/`cat`/`head`/`tail`/`sed`/`awk`/`ls`/`find`) ran in the week to 2026-08-29 - **16.58 h = 61% of all `Bash` wall time**, at a 12.8% error rate - against 2 206 `search_text`, 1 122 `search_regex` and 804 `find_files` calls. `Bash` p50 is 3 646 ms against `search_text` 108 ms and `search_regex` 70 ms, so the fallback is **34x slower** than the tool it replaces as well as costing more tokens | `ToolGuard`, `SKILL.md` | establish why the guard did not deny these - its text-command rows are scoped to a cwd holding a .NET tree, so a repository no loaded workspace serves escapes them - then either widen the scope test to any loaded workspace root containing the cwd, or emit the worked-example deny text I441 proposes | (d) up to 16.58 h/week, at a measured 34x latency ratio against the replacing tool | denying shell text tools unconditionally is refused: the guard is installed user-wide and `grep` in a non-.NET repository has no replacement, which is the shipped scoping rule |
| **I447** [perf] the analyzer ids gate output named most in the window are performance rules the agent's own edits introduced - **CA1859x83** (change return type for performance), CA2000x36 (dispose), CA1861x30 (constant array argument), CA1822x30 (mark static), CA2012x23 (ValueTask) - 202 mentions, every one after an edit in the same session, against 7 517 edit calls. In the 244 583 lines of C# the agent emitted, the matching rates are materializing LINQ 3.01/kloc over 77 sessions, sync file I/O 0.87/kloc over 50 sessions and sync-over-async 0.37/kloc over 13 sessions; this repo compiles the last of those away with `BannedApiAnalyzers` (`src/BannedSymbols.txt`), which is why it surfaces at `build` (p50 9 446 ms) rather than at the edit | `EditGate`, `.terse.json` code policy | let the project's `.terse.json` code policy carry a performance rule set the edit gate evaluates on the changed declaration and reports as `policy=CA1859` on the edit's own line, the way `allowPolicy` already names a bypassed rule, so a per-file or per-line allocation is named where it is written | (a) the 202 measured post-edit performance findings per week, each currently costing a `build` or `analyze` round trip | running the full analyzer set on every edit is refused: `analyze` p50 is 4 335 ms against `edit_text`'s 1 146 ms, so the gate carries an id list from the compilation it has already built, not a second analyzer pass. Stating the rule in `SKILL.md` instead is refused on measured grounds: on the closest published analogue, a mechanical intervention fixed **over 85%** of outdated-API emissions while zero-shot prompt engineering ranged 25.7-97.2%, and the authors conclude *"Direct interventions in the decoding process are more effective than zero-shot prompt engineering"* (ICSE 2025, arXiv:2406.09834v3); an interception layer measured **89.42%** (APILOT, arXiv:2409.16526). A newer model will not fix it either - the cause is measured as corpus FREQUENCY, not training cutoff (RustEvo2, arXiv:2503.16922). Note no published work measures C# 12/13/14 syntax adoption, `IFormatProvider`/CA1305, or .NET async correctness in LLM output, so the rates in this row have no external comparator |
| **I448** [cost] the sub-floor trim classes, aggregated per gate 2 rather than dropped: absolute path text ~51 004 tok, constant tags (`EXACT`, `remedy:`, `errors=0 warnings=0`, `(truncated=false`) ~48 319 tok, repeated identical lines ~47 188 tok and leading indent ~18 888 tok = **165 399 tokens per week = 0.83% of tool-result output**, of which the terse share of the absolute-path class is ~19 000 (`read_text` 6 469, `run_tests` 5 191, `build` 2 329) against `Bash`'s 24 007. Separately the guard's own deny text was re-emitted 42 times for two command shapes at ~110 tokens each | `ResponseBuilder`, `Errors` | drop `remedy:` and the `(truncated=false` marker from responses that carry no failure and no truncation, and print the constant confidence tag once per response rather than once per record where every record shares it | (cost) ~165 000 tokens per 284-session week, ranked below I443 because it is 0.83% against 4.82% | hoisting a shared confidence tag by REWRITING record text is refused and already reverted once - it corrupted a payload that legitimately contained the literal tag; the hoist must be a framing change only |
| **I449** [cost] terse advertises **88 tools costing 28 921 tokens on EVERY request** (`workspace_status`: toolDescriptions=9 758, parameterDescriptions=11 566, schemaFrame=7 301, names=296), which is past both thresholds Anthropic publishes for deferring a tool surface - *"Use tool search when any of the following apply: You have 10 or more tools available. Your tool definitions consume more than 10k tokens"* - and past the stated point where *"Claude's ability to pick the right tool degrades once you exceed 30-50 available tools"*. The same source measures 85% definition-token reduction and MCP-eval accuracy 79.5% -> 88.1% on Opus 4.5 with deferral, and states deferral does NOT cost a cache re-prime because deferred definitions are excluded from the system-prompt prefix. The corpus shows the selection cost locally: 297 guard denials plus 3 057 shell-text fallbacks in one week are the agent not reaching the tool that exists | `ToolProfile`, `ToolGroups`, `.terse.json` | measure the per-tool selection rate from the corpus, then make a narrowed profile the DEFAULT rather than an opt-in - the machinery already exists in `--tools`, `ToolGroups` and `.terse.json`, and the census gates already discover the group set from the `[McpServerToolType]` classes | (cost) up to ~24 000 tokens per request against a measured 28 921, and the accuracy lever is the larger half: 79.5% -> 88.1% published | deleting tools to shrink the surface is refused: the break-even is 32 calls per 508 sessions and most of the 88 clear it. Shortening a `Replaces Bash ...` prefix is refused - it un-enrols the tool from `ToolCensusE2ETests`. Cutting description text generally is refused too: OSWorld-MCP measures excess description suppressing tool use, but embedded worked examples measure 72% -> 90%, so the fix is deferral, not brevity |
| **I450** [speed] the corpus's **26 205 adjacent tool-pair transitions** are cross-tool runs no single tool's array parameter can collapse: `read_text` -> `edit_text` 1 025x (same-target **68%**), `search_text` -> `read_text` 819x, `read_text` -> `search_text` 576x, `search_regex` -> `read_text` 418x, `edit_text` -> `read_text` 413x (same-target 30%), `find_files` -> `read_text` 314x. Anthropic ships a `batch_tool` meta-tool for exactly this, describing the cause as *"a tendency to make tool calls serially, instead of in parallel"* (anthropic-cookbook PR #139, 2025-03), and measures the payoff as round trips rather than payload - *"When Claude orchestrates 20+ tool calls in a single code block, you eliminate 19+ inference passes"* and a 37% token reduction on complex research tasks | new tool `batch` | one tool taking `invocations=[{tool, arguments}, ...]`, each dispatched through the same `ToolBoundary` and `ToolContext` the direct call uses, answered per entry under its own header line with its own error code so one bad entry never fails the batch - the contract `read_text paths=` already keeps - and refused for the mutating family unless every entry names the same workspace | (a) the ceiling is the whole 103.1 h/week of model gap; the six named cross-tool pairs alone are 3 565 transitions = 6.00 h. Surface cost is ~255 tokens against a break-even of 32 calls per 508 sessions | letting `batch` carry mutating tools without a same-workspace check is refused: a helper that re-derives scope from a raw path answers wrongly and silently, which is a shipped trap here. Nesting `batch` inside `batch` is refused outright. Note that no published controlled study measures delta-calls-per-task from adding a batch parameter - the number would have to be instrumented here |
| **I451** [speed] the verification ladder is measured INVERTED - the cheap rungs run unscoped while the expensive one is scoped. In the week to 2026-08-29 `run_tests` was scoped on **3 003 of 3 050** calls, yet `build` ran whole-solution on **771 of 1 039** (74%), `get_diagnostics` on **142 of 160** (89%), `cleanup` on **240 of 372** (65%) and `analyze` on **276 of 437** (63%), while `gate` - which exists to BE the scoped end-of-task sweep - was called only 131 times against `build`'s 1 039. The verification family is 5 391 calls, 111.11 h and **7.8 calls per task cycle**, and 3 167 of its 4 811 in-session repeats came under five minutes apart | `build`, `analyze`, `cleanup`, `get_diagnostics` | give the four cheap rungs the `changed=true` default `analyze` already declares - scope to the documents modified since the workspace loaded and name what was skipped - so an unscoped sweep is the opt-in rather than the default | (c) `build` mean 14.9 s over 1 039 calls and `cleanup` 7.6 s over 372, of which the 74% and 65% whole-solution share is the scopeable part | flipping `run_tests` the same way is refused - it is already scoped on 98.5% of calls, and its waste is the 48.7% identical-repeat rate that I439 covers, not its scope |
| **I452** [accuracy] the rewrite fence is a whole-command CHARACTER blacklist, so the most common benign shell suffix there is disables it for every segment. the nine-character `Hazards` set at `ToolGuard.cs:856` - parentheses, braces, backtick, angle brackets, dollar and hash - is scanned by `Unfenceable` (`ToolGuard.cs:1073`) over the ENTIRE command, and `Compound` (`ToolGuard.cs:837`) calls `Fenced(command)` before it will strip anything - so one `2>&1` anywhere forces `Blocking`, and NO part of the command runs. Measured over 4 608 `Bash` calls in the week to 2026-08-29: **2 128 (46.2%) carry `2>&1`**, `>` is the first hazard on **1 903 of 3 811** hazard-carrying commands, and of the **3 328** compound commands that name a command terse replaces, **2 966 (89.1%) are fenced off entirely** - with a redirection the ONLY hazard on **1 434 of them (48.3%)**. Observed: **179 whole-command refusals = 44.5% of 402 guard messages** | `ToolGuard.Masked`, `ToolGuard.Unfenceable` | mask the FD-DUPLICATION redirections (`2>&1`, `N>&M`) in `Masked` before the `Hazards` scan, the way quoted spans are already masked - they bind to one pipeline and touch no file, so masking them narrows the blacklist by SHAPE without widening one character of what may be rewritten | (a) up to 1 434 commands per week that are refused whole today and could be stripped and re-issued as a terse call, plus the 179 observed full re-derivations at a 6 062 ms gap each | dropping `>` from `Hazards` outright is refused: `> file` redirects output, and stripping the pipeline carrying it would silently discard the write - only the FD-duplication form has no filesystem effect. Per-segment fencing is refused for `&&` and `\|\|`, which short-circuit, so removing one segment changes whether its sibling runs. Any change here must add its shape to `Guard_ForABatchWhoseShapeCannotBeRewrittenSoundly_DeniesItWhole` first, because three review rounds each found a false negative in this exact path |
| **I453** [accuracy] a command substitution SHADOWS the real command, so the guard denies the wrong thing and prints the wrong remedy. `Command` (`ToolGuard.cs:525`) replaces `$(` with a single space instead of masking the substituted span, so `VAR="$(cat secrets-file)" dotnet test ...` tokenises to `cat` once `IsAssignment` has skipped the leading assignment - the segment is reported as a shell-text-tool denial quoting shell-text statistics, when its actual command is `dotnet test` and its actual replacement is `run_tests`. Observed live on a command whose only text-tool use was reading a credential into an environment variable, where `read_text` is not merely a worse answer but the wrong one - it would put the secret in the transcript. Measured: **749 `Bash` calls (16.3%) carry `$( )`** and `$` is the first hazard on **820** of them | `ToolGuard.Command`, `ToolGuard.Masked` | mask the whole `$(...)` span rather than deleting its opening delimiter, so the segment's own command is the one found, denied and routed - which fixes the mis-routed remedy and the credential case in one change, because the guard then never sees the substituted `cat` at all | (a) the mis-routed denials inside the 749 substitution-carrying calls per week, each one a denial naming a replacement that does not answer the command the agent actually ran | exempting credential-shaped paths from the text-tool rows is refused as a separate mechanism: it is a second list to maintain and it still leaves every other substitution mis-routed, whereas masking the span fixes the whole class. Inspecting the substituted command deliberately - to catch a `$(grep ...)` that really is a text scan - is refused: the substitution's own replacement cannot be spliced back into the outer command soundly |
| **I454** [speed] `run_tests` has `projects=[...]` for many PROJECTS and `test=` for ONE filter, but no way to run several test CLASSES of the same project in one call. Measured this run: after a token-budget fix reddened four E2E classes, re-verifying them cost four sequential single-class runs - `SchemaCensusE2ETests` 90 s, `MarkupProfileE2ETests` 591 s, `ToolSettingsE2ETests` 210 s, `TestingPlatformE2ETests` 630 s = **25.4 minutes of wall clock** - each paying its own fixture spin-up, against ~600 s for one whole-project run | `run_tests`, `list_tests` | accept `tests=[...]`: several `test=` filters combined into ONE filter expression, answered as one run with one verdict line, the way `projects=[...]` already batches projects | (a) four runs collapsed to one per targeted re-verification; here ~25 min of wall clock and three deleted 6.1 s model gaps | widening `test=` to accept a comma-separated string is refused: `test=` is a documented SUBSTRING match and a comma is legal inside a fully-qualified test name, so overloading it changes the meaning of a shipped argument |
| **I455** [failure] `get_symbol_source` on a FIELD with an initializer returns only the initializer clause, not the declaration - `Plural = new Dictionary<string, string>(...)` with `public static readonly FrozenDictionary<string, string>` dropped - so feeding it straight back to `replace_symbol` is refused: `ERROR InvalidArgument: the declaration did not parse: Invalid token '=' in a member declaration`. Measured this run: one rejected call on `RepeatSteer.Plural`, recovered with `edit_text force=true` | `get_symbol_source`, `replace_symbol` | return the whole field declaration - modifiers, type, declarator, initializer, semicolon - for a field symbol, the way a method already round-trips, so the documented read-then-edit contract holds for fields | (a) one rejected `replace_symbol` plus one fallback call per field edit; hand-written declarations are already the repo's top rejection class | refusing the edit with a clearer message is not the fix: the PAYLOAD is what is wrong. `BacklogClosureE2ETests.ReplaceSymbol_WithADeclarationReadBackFromGetSymbolSource_ChangesNothing` already asserts this round trip for a property, so the contract exists and fields simply escape it |
| **I456** [payload] `diff_symbols` emits one record per HUNK, not per declaration, so a declaration edited in several places is listed several times. Measured this run on a 14-file change set: 37 records for 20 distinct declarations - `SnippetSearch.Adopted` x4, `MatchedRegion` x4, `SameLine` x3, `LocateReindented` x2, `FileService.NoRow` x2, `EditTools.AddMember` x2 - each repeating the full documentation id, which for `MatchedRegion` is 138 characters. 17 of 37 records are an id already printed | `diff_symbols` | fold hunks mapping to the SAME declaration onto one record carrying the id once and its hunk line ranges after it, exactly the way `analyze` already folds findings sharing an id onto one line carrying every position | (cost) ~45% of `diff_symbols` records on a multi-hunk change set; here 17 of 37 records at up to 138 characters of repeated id each | folding across DIFFERENT declarations is refused: the id is the unit a reviewer feeds to `get_symbol_source`, so one record per declaration is the contract; the `HEURISTIC` unmapped hunks must also stay per-hunk, having no id to fold onto |
| **I457** [accuracy] nothing answers how much room is left against the shipped token budgets before an edit spends it. Measured this run: adding TWO parameter descriptions overshot four budgets at once - `TokenBudgetE2ETests` 29 100 over 29 000, `MarkupProfileE2ETests` 23 978 over 23 900, `ToolSettingsE2ETests` 24 496 over 24 400, and then `DocsCoverageE2ETests` 27 812 over 27 700 for `SKILL.md` - and the only way to see it was a ~9 min full E2E run, twice, plus two rounds of description trimming. The real headroom was **79 tokens** and nothing said so | `workspace_status`, `read_text tokens=` | print the headroom beside the cost: `workspace_status` already reports `advertised=<n> tools <t> tokens`, so report it against the asserted ceiling (`t/29000, 79 left`), and let `read_text tokens=true` on a budgeted document do the same, so a surface-widening edit is visible BEFORE the suite runs | (a) two ~9 min full-suite runs plus two trim-and-rebuild cycles per surface-widening change | hard-coding the ceilings in the server is refused: they are test assertions and belong to the tests, so the server must read them from a checked-in manifest the tests also read, or the number becomes a second source of truth that drifts |
