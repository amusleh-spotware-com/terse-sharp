# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I294** `find_files` is the only listing tool with no `root=`: `search_text`, `search_regex`, `changed_files`, `diff_text` and `history` all take one. Inspecting `%LOCALAPPDATA%/terse-analyzers` and `%TEMP%` while diagnosing a red suite cost **3 `Bash: ls`/`find` fallbacks** in this run | `find_files` | accept `root=<absolute directory>` exactly as `search_text` does, tagged `outside-workspace`, so a cache or log directory needs no shell | 3 shell fallbacks per diagnosis that leaves the workspace, and it is the one gap that forces `ls` on an agent already inside the gate | putting it behind `search_text` - that answers "which lines match", and the question here is "which files exist and how big are they" |
| **I295** an `add_member` or `replace_symbol add=` whose new member name is already declared in the type is applied, compiled, rejected `CompileRegression` and rolled back - **3 times in this run** (`Listing`, `Listed`, plus one more), each costing the whole declaration payload, ~4 000 characters re-sent, plus a full compile round trip | `add_member`, `replace_symbol`, `SymbolEditService` | check the target type's existing member names before applying and answer `ERROR NameTaken` naming the declaration that already has it, with its line - a duplicate name is decidable from syntax and needs no compile | one compile round trip and one re-sent declaration per collision; 3 in this run | refusing every same-name member - an overload is a legitimate same-name addition, so the check must compare parameter lists too |
| **I296** `write_text files=[...]` declares no `force`, so a batch mixing a new `.cs` file with `.props`/`.slnx`/`.csproj` entries is refused outright and has to be split into two calls - one rejected call plus one extra call, observed writing the `HangSolution` fixture | `write_text`, `FileService` | let `files=` carry the top-level `force`, or accept `force` per entry, so one batch writes a fixture whose files are of mixed kinds | 1 rejected call and 1 extra call per mixed batch; the compile gate already handles `.cs` entries inside a batch | dropping the `.cs` guard - `force` exists so a `.cs` write is deliberate, and a batch must not weaken that |
| **I297** `edit_text row=` moves exactly one row per call, so closing a backlog is one call per row - **8 calls in this run**, where the rows were known up front | `edit_text`, `FileService` | `rows=[{row, newText}, ...]` against one `toPath=`, applied as one write per file the way `edits=` already is | 7 of 8 calls per backlog closure, and the same shape every release cuts | reusing `edits=` - those are anchored on `oldText`, and the whole point of `row=` is that the old text never crosses the wire |
| **I301** reopened from **I281**, exactly as that row's own closing condition demanded: `BacklogClosureE2ETests.WriteText_WithTwoInterdependentCSharpFiles_LandsThemUnderOneCompileGate` answered `ERROR Transient: RemoteInvocationException ... Fixture.Trading.csproj because it is being used by another process` on the windows-latest leg of CI run 32262661458 (commit b09f642), while ubuntu and macos were green and two full local Windows suites had passed - so the widened `ProjectGlobs.EvaluationGate` does not cover every path that evaluates that project, and the `Transient` remedy is masking the race rather than fixing it | `EditGate`, `ProjectGlobs`, `LoadedWorkspace.Applied` | find the remaining evaluation path - the failing write adds two interdependent `.cs` files, so Roslyn's build host evaluates the project while `ProjectGlobs.CompilesByGlob` reads it; either take the gate around that read too, or serialize on the project file itself rather than on the workspace | one red CI leg per ~4 full runs, each costing a re-run of a 13-minute Windows E2E job, plus the standing risk that a real `write_text` failure is dismissed as this one | calling it a flake - I281 rejected that explicitly and made a fresh observation the trigger to reopen; and retrying inside the tool, which would hide the race the row exists to remove |
| **I300** an unscoped `changed_files` in a tree carrying 42 untracked `.research/**` files clipped at "52 files" and spent the whole budget on untracked entries, dropping a tracked, modified `.cs` file from the listing; the review subagent read that as the change set and had to be corrected by a second scoped call - 1 wrong conclusion plus 2 extra calls in this run | `changed_files` | order tracked, modified files ahead of untracked ones before clipping, or report the two counts separately (`9 tracked, 43 untracked - narrow with exclude=`), so a clipped listing never hides a file the diff actually changed | one call instead of three, and it removes the failure mode where a reviewer reviews a change set that is missing a file | dropping untracked files entirely - `changed_files` with an empty `baseRef` is documented to include them, and a new file is exactly what a review must see |
| **I307** a run that the Microsoft.Testing.Platform runner itself aborted on `--timeout` loses its deadline steer: terse sets that timeout ~15 s inside its own, so the runner ends the session first, `run.TimedOut` is false, `HangSequence` is VSTest-only so `hung` is empty, and `AppendDeadlineRemedy` - gated on `run.TimedOut` - never fires. The run still prints the runner's own timeout line through `AppendTail`, so the fact survives; the `remedy: raise timeoutSeconds` does not | `DotnetRunner.RanAsync`, `AppendDeadlineRemedy` | derive the timed-out signal from evidence rather than the process flag: the elapsed time reaching the `--timeout` window terse itself passed is proof enough, and needs no exit-code table | one steer per timed-out MTP run, which is the run where the steer matters most | asserting MTP's exit code for a timeout - the documented table gives 3 for an aborted session, unverified here, and guessing an exit code is what produced the `--timeout` millisecond bug this release already had to fix once |
| **I308** `--ignore-exit-code 8` is forwarded to every test application of a `--solution` MTP run, so a solution where project A passes and project B matched zero tests aggregates to exit 0 with failures=0 and `IsGreen` true - `Unfinished` counts per **invocation**, not per project inside one solution invocation, so nothing names B. The VSTest baseline for this case is **not** verified, so this is newly-guaranteed invisible rather than a proven regression | `DotnetRunner`, `TestPlatformArguments`, `Unfinished` | count the `.trx` files a solution invocation produced against the test projects it should have run, and warn naming the projects that wrote none - the same evidence `Finished(slot)` already uses per project | the one case where a green verdict can hide a whole project, on the runner terse has just started driving | dropping `--ignore-exit-code 8` - without it a zero-test run fails the session outright, which is a bigger and more common wrong answer than this one, and it breaks parity with the VSTest path |
| **I305** the same glob means different things to `find_files` and `search_text`: `glob="tests/TerseSharp.E2ETests/*.cs"` listed **55 files** through `find_files` and matched **0** through `search_text` in this run, twice, because `search_text`'s glob is applied to the file name unless it carries `**`. A zero result from a *counting* tool is the one answer an agent cannot tell from absence, and this one is silent | `search_text`, `search_regex`, `FileGlob` | apply one glob semantic across all four tools - the `find_files` one, which matches on the workspace-relative path - or, failing that, answer `0 matches` from a path-shaped glob that selected no files with the same `try name=` steer `find_files` already emits when its glob matches nothing | 2 wasted calls here, and it silently invalidates any sweep phrased that way - the failure mode is a confident zero, not an error | making `search_text` reject a path-shaped glob: `**/Views/*.xaml` is documented to work, so the shape itself is legitimate and only the anchoring differs |
| **I306** neither `build` nor `run_tests` will show the command line it ran, at any verbosity. Diagnosing why an MTP run answered `Zero tests ran / Exit code: 5` meant reading `DotnetRunner.Arguments` in source and then isolating the rejected option with **4 `Bash` probes** of the test application's own `--help`, because the response says only that the child refused - never with what | `build`, `run_tests`, `rerun_failed`, `list_tests`, `DotnetRunner` | echo the resolved child command line under `verbose=true`, and unconditionally on a run that produced no results - that is exactly the case where the arguments are the answer and the payload is otherwise empty | the 4 probes plus the source read here; and it is the difference between an agent fixing a rejected argument and an agent concluding the tool is broken, which is how this defect was reported in the first place | echoing it on every run - a green run has nothing to diagnose, and rule 4 of the minimum-response gate keeps a success at one line |
| **I303** `run_tests projects=` under MTP builds each project then runs it, but `TestReporterProbe.DetectAsync` re-walks `global.json` and every `*.csproj` under the root **once per call** rather than once per workspace - on a solution target the csproj walk is `Directory.EnumerateFiles(root, "*.csproj", recursive)` capped at 200, repeated for every `run_tests` in the session | `TestReporterProbe`, `LoadedWorkspace` | memoize the detected `TestReporter` on the workspace the way `ProjectGlobs.Memoized` memoizes glob evaluation, invalidated by a `global.json` or `.csproj` write | one directory walk per test call after the first; it is small next to a build, and it is pure repeated I/O on the request path | detecting from the Roslyn workspace instead - `run_tests` goes through `WithTargetAsync`, which releases the lease before the runner is reached, so the compilation is not available at that point |
| **I304** `write_text files=[...]` still declares no `force`, re-observed writing the `MtpSolution` fixture: one batch of five non-`.cs` files landed, then the two `.cs` files cost one call each because the batch form refuses them - 3 calls where 1 would do. This is **I296** observed a second time in a second release | `write_text`, `FileService` | as I296: let `files=` carry the top-level `force`, or accept `force` per entry | 2 calls per mixed fixture write; every new fixture in this repo pays it | dropping the `.cs` guard - `force` exists so a `.cs` write is deliberate |
| **I298** five tests assert behaviour "outside a .NET tree" from a sandbox under `%TEMP%`, so a stray `.csproj` left anywhere above it by another session turns them red with no hint of the cause; diagnosing that cost roughly **40 minutes** of this run and the litter could not be removed | `ToolGuardTests`, `UpdateE2ETests` | give the sandbox a root the walk cannot escape, or have the failure name the marker file `IsDotNetTree` actually found, so the message says `%TEMP%/automate-head.csproj` instead of only "denied" | the whole diagnosis, and it recurs on any developer machine that has ever written a scratch project to the temp directory | asserting the tests are flaky and re-running - the failure is deterministic on that machine and the tests are simply not hermetic |
| **I309** the E2E suite's build-spawning classes share one `TerseServerCollection`, which is a **551 s serial critical path** of the 723 s CI leg — measured from the `.trx` of run 32352033133, where 41 % of wall time ran with exactly one test in flight. Splitting the two single-owner fixtures out took the local wall from 576.7 s to 476.9 s; the remaining ~467 s floor is `FixtureSolution` itself, which 30+ classes build. Re-measured over the 1-week session scan of 2026-08-24: `run_tests` is the largest single wall-time consumer in the surface at **2 019 minutes over 1 487 calls**, p50 26.9 s, p99 1 058 s | `E2ECollectionCensusTests`, `TerseServerFixture` | give each *test* its own copy of `FixtureSolution` the way `TerseTempSolution` already does, so the serialization constraint disappears instead of being partitioned; or shard `FixtureSolution` into per-concern fixtures small enough that no two classes contend | the remaining 467 s of the critical path, on every local run and every CI leg | splitting `HangReportE2ETests` out - `BacklogClosureE2ETests` builds `HangSolution` too, and the second census test now fails that move; and raising `maxParallelThreads`, which was tried at `1.5x` and **measured worse on CI run 32369954276** - E2E 723 s to 773 s and the unit assembly beside it 305 s to 482 s - because a lane starts a Roslyn load that is CPU-bound rather than an idle wait on a child, so it cannot help a floor that is serial by construction |
| **I310** a `.cs` file that must live outside every workspace root cannot be written at all: `write_text` answers `ERROR OutOfWorkspace`, and the `PreToolUse` guard denies `Write`, `cat` and a heredoc on the same path because it is C# source. Writing a 7-line MSBuildLocator probe to `%TEMP%` cost **3 refused calls** and ended in a scratch directory inside the repository, which then had to be excluded from the commit | `write_text`, `ToolGuard` | let `write_text` take an absolute path outside every root when `force=true`, tagged `outside-workspace` exactly as `read_text` already tags such a read - the read half of that asymmetry already works | 3 refused calls per scratch probe, and it removes the one case where the gate has no legal path at all | relaxing the guard instead - the guard is right that a `.cs` write should go through the compile-gated tool; it is `write_text` that refuses the path |
| **I311** `run_tests` reports `durationMs` as the **summed** test time and `elapsedMs` as the wall, but nothing reports concurrency, so a parallelism change reads as a regression: the E2E split raised summed time from 1 402 s to 1 608 s while cutting wall from 576.7 s to 476.9 s. Answering "did that get faster" needed the CI `.trx` and a hand-written scheduler analysis | `run_tests` | print `concurrency=<summed/elapsed>` beside the two numbers it already has, and name the class that held the longest solo stretch when concurrency is under 2 - both are one division and one scan of data the runner already holds | the whole external `.trx` analysis this task needed to answer a question the tool had the data for | reporting summed time alone - it moves the wrong way when parallelism improves, which is exactly how it misled here |
| **I312** `list_tests` under MTP resolves a test module with `dotnet msbuild -getProperty:TargetPath`, which on a **multi-targeted** test project evaluates the outer build and yields `bin/Debug/Foo.Tests.dll` with no TFM segment: the project is dropped silently, and worse, a stale dll left there by a single-TFM era would be executed and its tests reported as the project's. No fixture in this repository multi-targets, so the path cannot be covered today | `DotnetRunner.ModulesAsync`, `TestReporterProbe` | give a fixture `<TargetFrameworks>`, then ask for `TargetPath` and `TargetFrameworks` together and refuse by name when the project multi-targets and `targetFramework=` was not passed | the one case where the tool can list the tests of a *different* binary than the one it built - a confident wrong answer | shipping the two-property JSON query now: its output shape could not be verified in this session because the guard denies `dotnet msbuild`, and this repository bans a claim about tool behaviour that was not observed |
| **I313** `list_tests` under MTP decides what to *execute* from `TestReporterProbe.Projects`, which walks every `*.csproj` under the **workspace root** capped at 200 rather than the projects the target contains - so a `samples/Foo.Tests.csproj` outside the solution can be listed as if it were part of it. That walk previously only classified the reporter, where being over-broad was harmless | `TestReporterProbe.Projects`, `DotnetRunner.ModulesAsync` | keep only the modules the build just wrote, or resolve the target's own project list before `WithTargetAsync` releases the lease | a listing that names tests `run_tests` on the same target will not run | bounding to the target's directory - a solution at the repository root contains `samples/` too, so it excludes nothing; the workspace-root bound that shipped stops an arbitrary `TargetPath` override, not this |
| **I314** a .NET-shaped file outside every loaded root is reachable by **no legal call**: `ToolGuard.OnPath` denies `Read`/`Edit`/`Write` on `.cs`, `.csproj`, `.xaml`, `.resx`, `.razor` by extension alone - no `cwd` or root check, unlike the git rows which scope on `IsDotNetTree` - and routes to `get_file_outline`, which answers `ERROR OutOfWorkspace ... remedy: pass a path inside the loaded workspace` (probed on the installed 0.42.0; the refusal is `OutlineService.FromDiskAsync` line 319 calling `PathGuard.Resolve(workspace.Root, path)`). The same refusal is in `XamlService.Located`, `ResxTarget.Locate`, `RazorOpen.AtAsync`, `RazorValidation.Single`, `ProjectTools.Reading`/`ReadingAsync` and `DocumentScope.UnderDirectory`. `read_text` (`FileService.Readable`), `search_text`/`search_regex` `root=` and the three git tools already answer outside a root and tag it - the .NET-shaped readers are the only half that does not, so an SDK source file, a sibling repository or a scratch probe costs one denied built-in plus one dead-end `ERROR` and ends in the agent arguing itself out of the gate | `OutlineService.FromDiskAsync`, `PathGuard`, `XamlService`, `ResxTarget`, `RazorContext`, `ProjectTools`, `ToolGuard.OnPath` | lift `FileService.Readable`'s rule into `PathGuard` as `Resolve(workspace, path, outside: true)`: an absolute path that `PathBoundary.Contains` places under **no** loaded root resolves to itself instead of failing, and every other caller keeps today's behaviour by not passing the flag. Then, one call site at a time: **(1)** `OutlineService.FromDiskAsync` - it already parses with `CSharpSyntaxTree.ParseText` plus `CSharpCompilation.Create("terse-ref")` and stamps `ParsedFromText`, so `get_file_outline` on an outside `.cs` is the guard swap and nothing else; **(2)** `get_symbol_source path=` gains the same disk fallback, resolving the name inside that one parsed tree; **(3)** the markup and project readers - `xaml_outline`/`xaml_find`/`xaml_names`/`xaml_bindings`, `resx_files`/`resx_find`/`resx_get`, `razor_outline`/`razor_find`, `project_properties`, `package_list` - are XML and text parsers that never needed a `Project`, so they take the same overload; **(4)** every such response is tagged `outside-workspace  <full path>` exactly as `FileService.Outside` and `TextSearchService` line 650 already do, plus a `syntax-only - no project, so types and references are unresolved` note, and its records are `HEURISTIC`, never `EXACT`; **(5)** writers and compile-gated tools keep refusing - `EditGate` has no compilation to gate on outside a project - except the `write_text force=true` half already logged as **I310**; **(6)** `ToolGuard.OnPath` learns the scoping `Git` already has: when the path is under no loaded root **and** `IsDotNetTree` of its directory is false, ALLOW the built-in, so the guard never denies a call whose named replacement cannot answer. E2E: copy a `.cs`, a `.csproj` and a `.xaml` from `fixtures/FixtureSolution` into the test sandbox, outline and read each while only `FixtureSolution` is loaded, assert the `outside-workspace` tag and the syntax-only note; `ToolGuardTests` gets both directions - denied inside a .NET tree, allowed outside one | the only dead end in the whole gate: today the sequence is 1 denied built-in + 1 unusable `ERROR` + a fallback the agent has to talk itself into, and it recurs on every cross-repo read, every SDK or decompiled source read and every scratch probe. It also closes the guard's worst failure mode - denying with a remedy that cannot be followed | `load_workspace` on the other file's solution - a loose `.cs`, an SDK source file or a temp probe has no solution to load, and loading one to read one file costs a full MSBuild load and makes every later un-hinted call `AmbiguousWorkspace`; giving these reads full semantics - with no project there are no references and no resolved types, so anything but `HEURISTIC` would be the confident wrong answer this repo bans; and relaxing the guard alone, which was rejected in **I310** for the write half and is wrong here too - the guard is only wrong when the replacement it names cannot answer |
| **I314** [accuracy] the guard denies the whole `git diff --cached` family - **33 denials in one week** (23 `--name-only`, 6 `--stat`, 2 `--name-status`, 2 other) - but `changed_files` declares no staged mode, so this is a deny with no replacement, the exact case the guard gate calls "worse than no guard"; a further 8 denials were `--untracked-files=no`, which `exclude=` only approximates. Claude Code stops the model at 3 consecutive or 20 total denials, so these are spent from a scarce budget | `changed_files`, `ToolGuard` | give `changed_files` `staged=true` and `untracked=false`, then keep denying; until it exists, drop the `--cached` rows from `Replaced` | 33 denied calls plus 33 retries per week, and it removes the only guard rows that redirect an agent to a tool that cannot answer | steering `--cached` to `changed_files` as it stands - it compares the working tree or a ref, never the index, so the redirect is a confidently wrong answer |
| **I315** [accuracy] `ToolGuard` matches a driver name inside a **quoted string literal**: a PowerShell process query whose `-match` pattern contained the word MSBuild was denied and redirected to `build`, in a command that invoked no build at all - observed once in this scan, on the only call in it that carried a build-tool name as data | `ToolGuard` | match `Replaced` drivers in **command position** only, the way the git rows already resolve a `-C` target or a directory operand, and never inside a quoted argument | one false denial plus one rewrite per occurrence, and a false deny spends the same 3-consecutive budget a real one does | dropping MSBuild from the driver list - it is a real driver and must stay denied when it is the command |
| **I316** [accuracy] the brace-glob refusal is now **78 refusals in one week = 51% of all 152 terse `InvalidArgument` results**, the single most frequent error in the surface, and every one is a rejected call plus a retry | `find_files`, `search_text`, `search_regex`, `FileGlob` | expand `{a,b}` into the alternative globs before matching - the expansion is decidable from the pattern alone and needs no file system | 78 rejected calls and 78 retries per week | answering `0 matches` from a brace glob - that silent zero is exactly what the refusal was introduced to prevent, and it stays refused until expansion is implemented |
| **I317** [accuracy] `edit_text section=` was used **45 times against 2 795 `edit_text` calls (1.6%)** while **391 of the 751 adjacent `read_text` to `edit_text` pairs targeted a `.md` file**; at `read_text`'s measured 1 608 tokens per call that is ~**628 000 tokens a week** spent reading markdown only to compose an anchor `section=` makes unnecessary, and the same anchor approach produced **29 `oldText matched 0 times` rejections re-paying 14 185 tokens** | `read_text`, `SKILL.md` | end a `.md` read that carried no `headings=`, `section=` or line range with `sections=N - replace one with edit_text section="..."`, naming the headings the read covered | ~390 read calls a week, plus the 29 anchor misses that a section address cannot have | making `section=` the default on `.md` - reading a whole document is a legitimate call, and the steer has to ride on the read it already paid for |
| **I318** [accuracy] `analyze` declares only `path=` while `read_text`, `get_file_outline`, `diff_text` and `write_text` all declare a plural, so generalising from the neighbouring read tools costs a rejected call - it cost one in this scan | `analyze` | accept `paths=` the way the other read tools do; `analyze` already scopes per document, so the batch is free and it removes a surface inconsistency the swap table cannot show | one rejected call per agent that generalises, and it makes the end-of-task per-file sweep one call instead of N | `changed=true` as the substitute - it answers "what I touched", not "these three files", and a scan of named files is the commoner case |
| **I319** [modern] [perf] the compile gate reports errors and up to five introduced warnings but **no info-severity diagnostic**, which is precisely the class the build cannot see and the ubuntu format leg kills: the agent emitted **104 block-bodied single-return members (IDE0022) across 13 sessions and 377 hand-written backing fields (IDE0032) across 34**, both carried at `suggestion` in this repository's `.editorconfig` (read to confirm), and gate output afterwards named **CA1859x85, CA2000x57, CA2012x36, CA1861x35, IDE0161x20**; `src/BannedSymbols.txt` catches the 162 sync-file-I/O and 42 sync-over-async emissions only at build. Five primary sources agree the fix is a diagnostic at the write: static-analysis feedback took readability violations from over 80% to 11% in ten iterations (arXiv 2508.14419), write-time guidance lifts compilation rate 21.8-24.7% relative (arXiv 2306.10763), and error feedback alone moves version-correct generation only 39.9% to 43.7% (arXiv 2411.05830) | `EditGate`, `replace_symbol`, `replace_symbol_body`, `add_member`, `write_text` | run the referenced analyzers at `info` over the changed declaration inside the gate that already holds the compilation, and append them exactly as `WARNING introduced` already appends warnings | the `analyze` round trip after an edit - **434 analyze calls and 81.5 minutes of wall clock in the week**, p50 3 712 ms - plus the build round trip for the banned-API class, `build` p50 8 247 ms over 741 calls | rolling back on an info diagnostic - informational rules are not errors and a gate that rejects them blocks legitimate edits; and teaching it in `SKILL.md` instead, refuted by arXiv 2606.25402: "simply specifying the target version provides no benefit, while relevant documentation significantly boosts models' accuracy" |
| **I320** [speed] every read and search tool sits at a **p50 near 1.65 s** - `read_text` 1 648 ms over 4 479 calls, `search_text` 1 737, `find_usages` 1 663, `find_files` 1 678, `get_symbol_source` 1 870, `search_regex` 1 870, `get_file_outline` 1 903, `search_symbols` 1 968 - while `changed_files`, which shells out to git over the same transport, answers in **217 ms** and the built-in `Read` it replaces in **197 ms**, an 8.4x gap; `read_text` alone burned **527.8 minutes** of wall clock in the week | `ToolContext`, `WorkspaceRegistry` | find the ~1.45 s the read path pays and `changed_files` does not - lease resolution, index refresh or a per-call workspace probe - and take it off the read family | ~105 minutes a week on `read_text` alone and ~250 across the family; the prime directive is tokens **and** speed, and a tool slower than the built-in it replaces fails half of it | blaming MCP stdio round trip - `changed_files` at 217 ms and `ToolSearch` at 4 ms cross the same transport, and no published stdio benchmark makes protocol overhead a credible driver |
| **I321** [speed] batch-parameter uptake splits sharply: **`run_tests projects=` 19% (1 196 single calls), `read_text paths=` 24% (3 365 single), `get_file_outline paths=` 33%, `replace_symbol symbolIds=` 37%, `write_text files=` 39%** against `search_text queries=` 69% and `get_symbol_source symbolIds=` 60%; **6 558 calls sat inside runs of 3 or more consecutive same-tool calls** and the corpus holds **5 parallel assistant messages out of 25 912** | `SKILL.md`, the consecutive-call steer | name the plural parameter in the opening sentence of each low-uptake tool's `[Description]`, where a positive obligation survives a long session - measured omission compliance falls 73% to 33% by turn 16 while commission holds at 100% (arXiv 2604.20911) | `read_text` alone: 1 420 run-calls batched at the observed mean of 3.9 is ~1 056 fewer calls, **29 minutes a week** plus their argument framing | firing the steer on the first call - measured to break the one-line success contract on six tools, which is why it starts at the second |
| **I322** [speed] `workspace_status verbose=true` is advertised as the in-server replacement for `terse doctor` but prints no process or memory line: `WorkingSet64` occurs exactly once in the source, in `Doctor.cs:228`, so the only route to "how much is terse holding" is the shell-out the gate forbids. Measured on this machine mid-scan: **8 live `terse` processes, 5.87 GB resident in total, largest 2.12 GB** | `workspace_status`, `Doctor` | fold Doctor's live-process line into `workspace_status verbose=true`, which already carries the other four self-checks | the one number an agent cannot obtain without breaching the gate, and 5.87 GB across eight servers is invisible today | printing it unconditionally - the minimum-response gate keeps a status success short, and the number only matters while someone is diagnosing memory |
| **I323** [cost] `explore_symbol` was called **0 times and `impact_of` once in 360 sessions**, the second measurement below the bar **I224** itself set - "treat a shipped tool with a near-zero call count as a defect to fix or delete" - after 7 and 1 calls in 683 sessions; at the measured 25 857 tokens for 88 tools (294 per tool) the pair costs ~**2.1 M base-input-equivalent tokens** across the week's 36 047 terse-attributed records, for one call | `explore_symbol`, `impact_of`, `SKILL.md` | execute I224's own rule: fold both into `get_symbol` and `find_usages` as `usages=true` and `impact=true`, or delete them - pruning a surface is a measured accuracy win as well as a token one, 79.5% to 88.1% on Opus 4.5 with tool search enabled | ~2.1 M BIE tokens a week of advertised surface, and two fewer near-duplicate descriptions competing for selection | another `SKILL.md` paragraph - the skill already teaches both in the swap table and in a dedicated row, and two measurements 683 and 360 sessions apart show teaching has not moved them |
| **I324** [cost] the fixed per-session cost of this server is **25 857 tokens of advertised surface plus a 91 053-byte `SKILL.md` (~22 800 tokens)**, about 48 600 tokens before any work happens; the server's own split puts **65% of the surface on the schema side** - `parameterDescriptions=9792`, `schemaFrame=6921` - against `toolDescriptions=8849` and `names=296`, which matches the only published breakdown of an MCP surface at 97% `inputSchema` | `[Description]` on parameters, `SKILL.md` | cut parameter prose before tool prose: the 3-4 sentence floor vendor guidance names is for the **tool** description, and nothing asks for it per parameter | a 30% cut of `parameterDescriptions` is ~2 940 tokens per request; the corpus carried 518 `deferred_tools_delta` records worth 1.81 M tokens, which is the observed floor | shortening any `Replaces Bash ...` prefix - `ToolCensusE2ETests` enrols the guard from it, so cutting one silently un-enrols the tool; and cutting tool descriptions to one line, below the measured 3-4 sentence floor |
| **I325** [cost] `diff_text` answered **295 of 340 calls (87%) with a `maxLines=` continuation** at 8 741 characters per call, while `diff_symbols` - the tool the skill puts ahead of it - was called **30 times, 11x less than the last resort it is supposed to precede**; the 743 000 tokens a week `diff_text` returns are mostly incomplete answers | `changed_files`, `diff_text`, `SKILL.md` | have `changed_files` end with the ready `diff_symbols` call for the paths it just listed, the way `search_text` already ends with `paths=[...]` | 743 000 tokens a week currently spent on diffs that were truncated anyway, replaced by declaration ids the agent can read selectively | raising `maxLines` alone - the cap is what keeps `diff_text` bounded, and an unbounded raw diff is the most expensive answer in the surface |
| **I326** [cost] aggregated micro-trim, measured with a tokenizer on the 15 captured real terse responses and confirmed in both encodings: **multi-space column padding costs 1.98% (o200k) / 2.01% (cl100k)**, and the corpus estimate is **578 942 tokens = 3.74% of 15.5 M result tokens**; residual absolute-path text costs **7.95% / 8.01%** on the same samples and 56 808 tokens in the corpus, with `workspace_status` alone printing the absolute solution path on each of its first two lines | `ResponseBuilder`, `workspace_status` | emit one space between columns wherever the value is machine-read, and print the solution path once | ~580 000 tokens a week of padding and ~57 000 of path text | blank lines (0.04%) and trailing whitespace (0.07%), both re-measured to zero in both encodings this run exactly as the standing verdict table says, and abbreviation, 19 of 20 of which save nothing |
| **I327** [accuracy] a compound `git add ... && git commit ... && git show --stat HEAD` is denied **whole** because of its last segment, so the two index-mutating segments the guard explicitly allows never run - and the denial names only the replacement for the offending command, never which segment tripped it or that nothing else executed. Observed once in this scan, on the commit this scan exists to make; an agent that reads the denial as "the commit landed, use `history` to view it" ships a confident wrong answer | `ToolGuard` | name the offending segment in the deny reason and state that no part of the command ran - the inspector already locates the segment in order to match it | one lost commit plus one re-issued call per occurrence, and it closes the case where a deny is mistaken for a partial success | allowing a compound whose other segments are legal - the `cd src && dotnet test` case is exactly why compounds are inspected at all, and relaxing it reopens the most common breach in this repository's own log |
