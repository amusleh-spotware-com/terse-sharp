# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I475** [speed] a `replace_symbol symbolIds=[...]` batch rejected for a defect in ONE declaration costs the WHOLE payload to retry, and `retryWith` cannot help because it replays the rejected text unchanged. Measured this run: a 5-declaration batch was rejected twice - once for a `??` / `is` precedence error in one declaration (CS0019), once for a malformed `declarations` array - and re-sending it cost ~9 400 characters for two one-line fixes | `replace_symbol`, `add_member`, `replace_symbol_body` | let `retryWith` take a PARTIAL override: `retryWith="r2" declarations={"2": "<fixed text>"}` replaces only the entry at that index and replays the rest from the token. The index is already known - the rejection names the failing declaration by index for a syntax error | (a) the whole batch payload per one-declaration fix; measured at ~4 700 characters per retry on a 5-entry batch, and batches are the shape this server steers callers toward | re-sending the batch is what happens today and is what the token was added to avoid; a per-call `allowErrors=true` is not a substitute, because the point is to land a CORRECT batch |
| **I476** [cost] `read_text columns="Finding"` on a backlog answers the FULL text of every first cell - 16 rows of finding prose, ~6 000 tokens - when the question that projection is used for is almost always "which ids are open". There is no way to bound a cell | `read_text` | add a per-cell width cap, e.g. `columns="Finding" cellChars=60`, truncating each projected cell with an ellipsis and saying so once in the summary. The row identifier lives in the first ~10 characters of the cell, so a cap answers the id question for a twentieth of the payload | (b) ~5 700 tokens per backlog projection; the same shape applies to any checked-in table whose first column carries prose | dropping to `search_regex matchesOnly=true` is what the truncation steer already names, but it cannot preserve the row-to-column mapping that `columns=` exists for |
| **I477** [accuracy] two `edits=` entries targeting `occurrence=1` and `occurrence=2` of the SAME anchor cannot both land: the entries are applied in order against the progressively rewritten text, so after the first lands there is only one match left and the second is refused `occurrence=2 does not exist: oldText matched 1 times`. Observed live this run on two identical parameter descriptions in `FileTools.cs` | `edit_text` | evaluate every entry's `occurrence=` against the text as it was at the START of the batch, which is what the caller can see, or refuse the pair up front naming both entries - either is answerable; silently shifting the index under the caller is not | (accuracy) one refused entry plus one extra call per duplicated anchor; the failure is invisible until the batch reports a partial application | making `occurrence=` batch-relative without saying so is refused - it would silently change what an existing single-entry call means |
| **I478** [accuracy] nothing tells a caller that a file it read has been rewritten by ANOTHER session before its write lands, so a read-modify-write on a shared file silently races. Measured this run: 15 backlog rows were closed correctly, a concurrent session read the resulting table, misread it as a tool defect, and restored both files from git - reverting the closure, and costing the whole 15-row move plus five verification calls to re-derive | `changed_files`, `read_text`, `edit_text` | stamp every read of a tracked file with the mtime the server saw, and let `edit_text`/`write_text` take `ifUnchangedSince=` so a write whose file moved under it is refused naming the writer rather than landing. `find_files stamps=true` already returns exactly that stamp | (accuracy) one silently lost change set per race; this repo's `CLAUDE.md` already names the shared-tree trap as recurring, which means prose has not fixed it | detecting the race after the fact from `changed_files` is refused: by then the write has landed and the other session's change is gone |
| **I479** [accuracy] `replace_symbol add=` reported the containing type of a NESTED TYPE target as the target itself: for `symbolIds=["ResponseBuilder.EntryKind", ...]` the refusal read `these targets do not share one: the enum EntryKind, ResponseBuilder, ResponseBuilder`, when `EntryKind`'s container is `ResponseBuilder` and all three DO share it. The remedy `pass addTo=` was still actionable, but the stated reason was wrong | `replace_symbol` | resolve the container of a nested type to its declaring type, so a batch mixing a nested type with members of the same outer type is not refused on a container set that has an extra element in it | (accuracy) one wrong refusal reason plus one extra call per batch that mixes a nested type with its siblings | refusing whenever a target is a type is refused - the batch is legal and the outer type is a real, single container |
| **I480** [speed] a test verdict is bought twice. Mined every Claude Code transcript on this machine - 381 files, 371 sessions, 73 114 tool calls, 415.1 h of tool wall clock, 2026-07-05 to 2026-08-29: `run_tests` is **41.7% of ALL tool wall time (173.1 h of 415.1 h, n=5 235, mean 119.0 s, p99 948.3 s)**, and **2 588 of 5 709 test calls (45.3%) were byte-identical repeats of an earlier call in the SAME session, costing 95.5 h - 53.7% of all test wall time**. A further 1 784 test runs (75.4 h, 42.4% of test wall) had **zero edit calls since the previous test run**, so the answer provably could not have moved | `run_tests`, `rerun_failed` | Remember the last verdict per (project, configuration, targetFramework, filter) with the workspace generation counter it was taken at; when a repeat call arrives and no document in the reachable project closure changed since, answer that verdict tagged `cached gen=N` in one line instead of spawning the runner. `force=true` re-runs | ~95 h of 178 h test wall over 8 weeks; at the measured p50 of 30.8 s a cached answer is <100 ms | `noBuild=true` - it skips the build, not the run, and 45.3% of the repeats already passed it |
| **I481** [speed] `run_tests` blocks the agent for its whole duration and there is no way to fire it and keep working. Measured over the same corpus: 178.0 h of blocked wall clock, p50 30.8 s, p90 372.3 s, p99 909.4 s, max 3 001.6 s; the median test-running session spends **33.5 min and 44% of its tool wall clock inside the test tool**, over 22 test calls. Every other verification rung (`analyze` 13.7 s mean, `build` 17.1 s) is cheap enough to fill that gap | `run_tests` | `async=true` returns a run handle immediately and the verdict arrives as a completion steer, the way a background `Bash` does; a second call with the handle blocks only for what is left. The harness already re-invokes on background completion, so nothing new is needed client-side | the agent keeps editing during a p90 of 372 s instead of idling; on the measured 5 235 runs the recoverable idle is bounded by 173 h | a `Bash: dotnet test &` shell-out - it leaves the MSBuild file locks and the result parsing outside the server |
| **I482** [accuracy] nothing tells a caller a suite is pathologically slow **for its own size**, so a 31x regression can sit in a repo for weeks with every gate green. Measured live this run: AIBrain `ArchitectureTests`, 494 tests, `elapsedMs=534506` and `durationMs=8587053` - **17.4 s of CPU per test** - because ~100 census gates each re-walked and re-read the whole `src/` corpus, and the per-line relative-path helper re-located the repository root once per source LINE. After memoizing the corpus and the root: `elapsedMs=17148`, `durationMs=101448`, same 494 passing - **31.2x wall, 84.6x CPU**. `slowest` had to be asked for by name to see it | `run_tests`, `rerun_failed` | Print one steer line when the run's mean ms/test crosses a threshold, naming the worst assembly and its ms/test - the same shape as the existing `concurrency=Nx` line, which is already computed from `durationMs`/`elapsedMs` | one call surfaces what cost 43.9 h across 1 306 runs of this one project in 8 weeks | making `slowest` default-on: the payload is per-test lines, and rule 4 of the minimum-response gate keeps that opt-in |
| **I483** [speed] `changed=true` selects by PROJECT reachability, so on a solution whose every test project references the core library it selects the whole suite and is indistinguishable from passing nothing. Measured: three `changed=true` calls in cTraderDev cost 1 615 s, 1 829 s and 1 927 s - all of them full-solution runs. The published state of the art narrows the same set further with a per-test map: 15% of the suite at >95% failure recall (ICSME 2025, arXiv:2509.10279) and ~33% at >99.9% faulty-change recall (Meta, arXiv:1810.05286) | `run_tests` | Inside the projects reachability already selected, narrow to a `--filter` from the changed SYMBOLS: one pass over the changed documents' syntax trees plus `SemanticModel` to build a member edge list, invert it, walk up to test methods. Always union in the tests whose own file changed, the previously failing tests, and the whole project when a non-`.cs` file changed | a full-solution fallback becomes a filtered run; the published analogues report 3x-27x fewer tests per change | per-symbol `SymbolFinder.FindReferencesAsync` in a loop - documented to scale badly with codebase size |
| **I484** [speed] `run_tests` refuses its own automatic lock retry whenever a SECOND workspace is loaded, even though `workspace:` names exactly which one to unload. Measured live this run: `run_tests workspace="TerseSharp"` died `MSB3027 ... locked by ".NET Host"` after **21.9 s**, answering `more than one workspace is loaded, so the test run was not retried; unload_workspace the one you are targeting and try again` - then `unload_workspace` + the same call succeeded. **Three calls and two model gaps for one verdict**, and the second workspace (`AIBrain`) held no lock on the TerseSharp output at all. The repo's own trap list already names this as the one legitimate unload | `run_tests`, `build`, `unload_workspace` | Retry against the workspace the call resolved to, not only when it is the sole one loaded: unload that one, retry, reload it - the sequence the tool already performs and already reports. Widen only when the resolved workspace is unambiguous, so an un-hinted call on two loaded roots still refuses | one call instead of three; 21.9 s plus two ~6 s gaps per occurrence, and `.claude/worktrees/agent-*` makes two loaded roots the normal state here | unloading every loaded workspace - it destroys a sibling session's undo history and its realized compilations |

