# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I531** [accuracy] `package_add` wrote its `<PackageVersion>` element into the `<PropertyGroup>` of `Directory.Packages.props` instead of an `<ItemGroup>`, breaking CPM for EVERY project: the next build failed `NU1015` on all four projects with every PackageReference reported version-less. Hit live adding `System.Management`; cost a broken build, a props read and a two-edit hand-fix | `package_add`, `ProjectFile` | insert the `<PackageVersion>` into the first `<ItemGroup>` that already holds one (or create an `<ItemGroup>` when none exists), never into a `<PropertyGroup>`; pin with a unit test whose props file leads with a `<PropertyGroup>` | one corrupted CPM props + one failed build per first `package_add` on a props file shaped like this repo's | — |
| **I532** [accuracy] the `run_tests UNCHANGED` memo never primes on this machine: `RunTests_RepeatedWithNothingWrittenInBetween_AnswersUnchangedInsteadOfRunningAgain` is red at a CLEAN HEAD worktree here (verified) while green in CI at v0.58.0, and the new sibling `build UNCHANGED` test passes in the same session - so the first run_tests response either carries a hidden extra line (the memo refuses multi-line) or the stamp moves during the run on this Windows shared tree | `run_tests`, `UnchangedRun` | instrument the miss: have the memo (under verbose or an env flag) say WHY it did not replay - verdict-not-single-line vs stamp-moved, naming the generation that moved - then diagnose from that; the lock-flavoured asymmetry against `build` is the lead | one full fixture suite re-run per repeat on affected machines, and a permanently-red local test that costs a worktree probe per session to re-classify | re-running until green is refused - the failure is deterministic here; widening the memo predicate blind is refused - a wrong UNCHANGED is a confidently stale answer |
| **I533** [speed] closing one backlog row costs two `edit_text` calls by construction: `row=`/`rows=` (the archive move) is refused beside `edits=`, so the row move and its CHANGELOG entry cannot land as one call - this run closed 14 rows and paid the second call every time | `edit_text` | let an `edits=` entry carry `row=` and `toPath=` (grouped per file like any other entry), or let `rows=` batches carry additional plain-edit entries for OTHER files | 1 call + ~6 s of gap per closed backlog row; 14 in this run alone | — |
| **I534** [accuracy] a LEADING redirect defeats guard classification entirely: `> out.log dotnet build X` tokenizes with head `>` and is ALLOWED, before and after the I524 widening - the fence only converts denials to whole-denials, it never denies an otherwise-allowed segment. Found by the I517-I530 review round tracing both revisions | `ToolGuard` | teach `Tokenized` to skip a redirect operator plus its target when they PREFIX the command word, so `> f cmd` classifies as `cmd`; pin with both a denied (`> f dotnet build`) and an allowed (`> f npm run x`) shape | one guard bypass per prefix-redirect spelling of a replaced command; rare shape but a standing hole in the deny list | — |
| **I535** [accuracy] the `build`/`run_tests` UNCHANGED stamp cannot see an external in-place `.cs` edit that sits undrained in `pending` between the remembered run and the repeat - the memo replays a green verdict against changed disk in that window. The I517-I530 review flagged it; the run's own fix attempt is refuted below | `UnchangedRun`, `BuildTools.Stamp` | drain (or reflect) the pending queue before stamping: make `Replayable` await a workspace sync so the stamp is computed over a settled tree, or include a per-path pending fingerprint rather than a count | one stale green verdict per repeat inside the undrained window; the `State`/`Gaps` guard shipped this run already refuses the memo when the watcher is Off/Degraded or lossy | appending the raw `Events` counter to the BUILD stamp is refuted - measured live: the first build's own watcher events land after its stamp is captured, so the two stamps never match and the memo goes dead (`gen=c0/p0/x0/r0/rz0/f0 pending=0 gaps=0` stable across the build while the test reddened); a `PendingCount>0` null-guard is refused too - a freshly loaded workspace holds an undrained queue and no build call ever drains it |
