# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I458** [speed] `rerun_failed` replays EVERY previous failure and takes no filter, so a red run whose failures you have deliberately re-pointed cannot be re-verified selectively. Measured this run: a 12-failure full suite (elapsed 603 s) was followed by four targeted `run_tests filter=` calls of 135 s, 148 s, 44 s and 9 s - 336 s - because `rerun_failed` would have re-run all twelve, including the four whose expectations the same edit changed | `rerun_failed` | accept `tests=[...]` and `exclude=[...]` on `rerun_failed`, filtering the remembered failure list rather than replaying it whole - the same shape `run_tests tests=` now takes | (a) one targeted rerun instead of a full replay per red round; measured here at 336 s of re-verification against a 603 s full run | replaying the whole list stays the default: a failure you did not name is exactly the one a partial rerun hides |
| **I459** [accuracy] a `search_text` scoped to ONE `.cs` path does not steer to `containers=true`, which is the parameter that turns its hits into ids. Measured this run on `ToolGuard.cs` (1 095 lines): 3 `search_text` calls plus 3 ranged `read_text` calls - 6 round trips, ~4 400 tokens - to locate four declarations that `containers=true` would have named on the first call | `search_text`, `search_regex` | when the search is scoped to a single `.cs` path or glob and `containers` was not passed, end the answer with `containers=true names the declaration each hit sits in` - the steer already exists for `paths=` and for the repeat run, and this is the same shape | (a) up to 5 of 6 calls on a single-file search; ~700 tokens per avoided ranged read | steering on every search is refused: over a multi-file text sweep the container is noise, and `containers=` is `.cs`-only |
| **I460** [speed] `get_symbol` is the last symbol-addressed read tool with no array parameter - `get_symbol_source` has `symbolIds=`, `get_type_outline` gained it in I445, `replace_symbol` has it. Measured this run: one `get_symbol symbolIds=[...]` call was refused outright (`unrecognized symbolIds`), costing a round trip plus a re-issue as two separate calls | `get_symbol` | accept `symbolIds=[...]`, answering one signature block per id with `NOT_RESOLVED` inline, exactly as `get_symbol_source` and `get_type_outline` do | (a) one rejected call plus one extra call per multi-symbol question; the asymmetry is what earns the rejection, as I433 measured for `add_member` | inferring the batch from the sibling tool is refused: the binder validates against the declared schema, so the parameter has to exist |
| **I461** [accuracy] the one-line success contract is spelled per test rather than censused, so a new framing line is only discovered by the assertions that happen to spell it - and they are ORDER-DEPENDENT, so the local suite and the three CI legs disagree. Measured this run: appending the I439 repeat note reddened four `DoesNotContain("\n")` assertions locally and then two MORE on CI that had passed locally (`ToolCensusE2ETests.EveryVerdictPrefixedTool_...` on all three legs, `TestToolsE2ETests.RunTests_GreenRun_StaysTiny` on ubuntu only), costing a 603 s full-suite run plus a 16-minute red CI round. Partly closed: the six sites now assert `first line is the verdict, every later line is framing` through the shared `ToolCensus.IsFraming`, and `EveryVerdictPrefixedTool_...` censuses that shape over `ToolCensus.VerdictPrefixed`; what is still missing is discovering the verdict-answering set from `tools/list` rather than from a checked-in list | `ToolCensus`, `BuildWarningsE2ETests` | census the contract instead of spelling it per test: discover every tool that answers a verdict line from `tools/list`, call it on a clean fixture, and assert the FIRST line is the verdict and every later line is framing (a steer or a note), so a new framing line is legal and a new payload line is not | (a) one 603 s full-suite round trip per response-shape change; the current spelling also makes framing indistinguishable from payload | asserting `DoesNotContain("\n")` per tool is what already exists and is what broke: it cannot tell a steer from a second payload line |
| **I462** [accuracy] a batched symbol read reports the ids it was ASKED for, not the ones it answered: `OutlineService.TypesAsync` (`src/TerseSharp.Core/OutlineService.cs:443`) and `SourceService.OfSymbolsAsync` (`src/TerseSharp.Core/SourceService.cs:78`) both call `Summary(symbolIds.Count, symbolIds.Count, ...)`, so `get_type_outline symbolIds=["A","NoSuchType"]` answers `2 types` having outlined one. Observed live this run on a 4-id `get_symbol_source` that answered `4 symbols` with one `NOT_RESOLVED` | `get_type_outline`, `get_symbol_source` | count the ids that RESOLVED and let the summary read `1/2 types`, which `ResponseBuilder` already renders, keeping the `NOT_RESOLVED` line per unresolved id; both call sites must move together or the two batches disagree | (accuracy) one unprovable count per batched read; the payload is unchanged, so this is a framing fix, not a payload one | leaving it is defensible and was the shipped decision for one round: the unresolved id is named inline, so the inflated count cannot be acted on without seeing what contradicts it. It is still a claim the response cannot support, which is why it is logged rather than closed |
| **I463** [accuracy] every shipped `[speed]` fix persisted at its pre-fix rate, and the instruction-file literature says why. Re-measured over 2 weeks / 637 transcripts / 60 551 calls: `run_tests` byte-identical repeats **46.9%** (I439 shipped, measured 48.7%), bare `sleep` **230 calls** (the guard denies it), whole-solution `build` **1 279 of 1 763 = 73%** (I451 shipped, measured 74%). The corpus also carries **97 `gate reminder` interventions** in the window. IFScale (arXiv:2507.11538, 15 Jul 2025) measures the best frontier model at **68% compliance at 500 instructions**, omission over modification **>30:1**, primacy bias rising with load; HANDBOOK.md (arXiv:2607.25398) puts a 20-124 page policy at **36.2% of agentic trials**; arXiv:2607.19257 puts perfect compliance at **zero by N=80** simultaneous instructions | `SKILL.md`, `CLAUDE.md`, `ToolGuard` | stop adding prose rules and convert the four persisting classes to mechanism: a guard row, a tool default or a refusal. Budget the two documents by rule count and delete or mechanise every rule past the measured ceiling, rather than adding a stronger wording of one already there | (accuracy) the three persisting classes are worth **22.0 h + 10.2 h + 30.7 h** in this 2-week window alone; it is the multiplier on every other row | adding a stronger prose gate is refuted twice in this corpus - I439/I377/I378 shipped guidance for `run_tests` repeats and the rate moved 48.7% -> 46.9%, and the bare-`sleep` guard still let 230 through. The parallelism class was withdrawn from this row: it rested on the counting artifact I473 names |
| **I464** [speed] `run_tests` is **149.55 h = 49.8% of ALL tool wall time** (4 409 calls, mean 122.2 s, p99 961 924 ms) and its cadence, not its scope, is the defect: **4 317 of 4 409 already carried `project=`**, yet the gap between two consecutive `run_tests` in one session is **p50 1.9 min, p25 0.7 min**, with **2 140 of 4 187 pairs under 2 minutes apart** and 3 049 under 5. 2 069 are byte-identical repeats. The server can prove the answer could not have changed: it already tracks and prints the per-kind change-generation counters (`watch=active gen=c0/p0/x0/r0/rz0/f0`) | `run_tests`, `rerun_failed` | when the arguments match a run earlier in this session AND no generation counter has moved since it, answer `run_tests UNCHANGED  nothing was written since the previous run at <time> - previous: PASSED passed=N ...` instead of re-running; `force=true` opts out | (d) **22.00 h** of tool time in 2 weeks on the byte-identical class alone, plus 2 069 x 6 136 ms = 3.5 h of model gap; at the tool's 122.2 s mean rather than its p50 the same class is ~70 h | telling the agent not to re-run is refuted: I439, I377, I378 and I451 all shipped that guidance and the repeat rate moved 48.7% -> 46.9% |
| **I465** [speed] the parallelism baseline this repo has acted on three times is a **measurement artifact**, and the true rate is ~1 800x better. The transcript JSONL splits one API message carrying N `tool_use` blocks into N records of ONE block each, so any per-record tally answers exactly 1.0 - which is how I376 measured `99.997% unspent`, I363 `35 731 of 35 735` and I166 `17 567 of 17 567`. Grouped by `message.id` over a fortnight and 647 transcripts the truth is **1.165 calls per assistant message, 14.3% carrying two or more** (histogram 1:44 810 2:6 768 3:470 4:170 5+:74) - **8 620 round trips already deleted, 14.7 h of gap already not paid**. Headroom is real but finite: one session in the same corpus reached **1.78 calls/message, 47.1% multi**, and 13 820 calls still sat in runs of >=3 consecutive same-tool calls (`Bash` 4 137, `read_text` 2 719, `edit_text` 1 214, `write_text` 632) | `SKILL.md`, `ToolGuard` | two changes, in this order. (1) `SKILL.md` gains an explicit `use_parallel_tool_calls` section carrying a CONCRETE worked example and the dependent-call exception - Anthropic's prompting guidance states that models have "a high success rate in using parallel tool calling without any prompting" and that "minor prompting" raises it to "~100% parallel tool use success rate", and the shipped mechanisms tried here were annotations and a steer, never that block. (2) extend the guard, which does see `Bash`, to answer a run of consecutive same-shape `Bash` calls with the batch or the tool that replaces them, since `Bash` - the largest run family at 4 137 - can never receive the per-response plural steer | (b) **measured A/B in-loop this run: the same eight files cost 151.4 s wall as eight one-per-message calls (2.9 s tool + 148.5 s model gap) against 10.2 s as one `paths=[...]` call - 14.79x, with 98% of the saving being gap, not tool time**. At the demonstrated 1.78 calls/message the corpus's 60 912 calls need 34 220 messages instead of 52 292 - **18 072 fewer round trips = 30.8 h per fortnight**; at the 8:1 this experiment achieved, the 13 820 calls sitting in same-tool runs alone collapse to ~1 730, deleting **12 090 round trips = 20.6 h**. Corroborating external measurements: 3 tool calls per turn cut wall clock **40.6%** and turns 45.7 -> 23.8 while accuracy ROSE 66% -> 68% (arXiv:2602.07359, 7 Feb 2026), bulk-in-one-call measured 10 calls / 104.5 s against 38 calls / 245.1 s = **57% faster and 41% fewer tokens** (arXiv:2511.19477), and LLMCompiler measured **up to 3.7x** end to end (arXiv:2312.04511, ICML 2024) with native parallel `tool_use` blocks alone capturing ~60-75% of it and no planner | counting fan-out per transcript RECORD is refuted outright - it IS the artifact, and every conclusion drawn from it must be re-derived. `readOnlyHint` (I191) and the per-response plural steer have never been measured against the corrected baseline, so neither is confirmed nor refuted here. Firing the steer on the FIRST call was measured to break the one-line success contract on six tools |
| **I466** [speed] shell text tools are **5 383 `Bash` calls = 30.68 h**, the largest single fallback class, at a **12.2% error rate** over `Bash`'s 7 908 calls: `tail` 2 638, `head` 2 628, `grep` 2 322, `ls` 776, `cat` 562, `sed` 339, `wc` 247, `type` 187, `find` 122. The guard denies a bare `grep`/`cat`, but **5 266 of the 5 383 are `head`/`tail` used as a PAGER on the end of a command nothing replaces** - a shape the guard does not deny and that buys nothing, because the harness already bounds a `Bash` result. Caveat: the per-verb hours bill the whole `Bash` call to its first text verb, so 15.82 h for `tail` is an upper bound; the counts are exact | `ToolGuard`, `SKILL.md` | add a guard row for a trailing `\| head`/`\| tail`/`\| wc -l` on an otherwise-allowed command: strip the pager, run the rest, and say the output is already bounded and that `read_text tail=N` and `search_text countOnly=true` answer the file case | (d) 5 266 calls of pager framing plus the retry share of a 12.2% error rate; the `ls`/`cat`/`type`/`find` remainder is 1 647 calls the guard already denies and that still reached `Bash` | denying the pager outright is refused: `docker logs \| tail` has no replacement here, so the row is a rewrite, not a refusal |
| **I467** [accuracy] `Bash` is the tool immediately before **81 of 209 interventions (39%)** - more than the next four predecessors combined (`changed_files` 20, `run_tests` 18, `Agent` 16, `TaskList` 8) - and it carries the corpus's **highest error rate, 12.2% over 7 908 calls**. Interventions are **209 of 526 post-first-turn prompts = 39.7%**, 0.33 per session; cue mix `re-scope` 123, `gate reminder` 97, `correction` 89. So the fallback class is not only the biggest `[speed]` cost, it is the biggest `[accuracy]` cost | `ToolGuard` | rank the guard's remaining gaps by what precedes an intervention rather than by what it already denies: the compound-command whole-refusal path, and the `cwd`-scoped rows that go quiet outside a .NET tree, are where the 81 sit | (accuracy) 81 interventions in 2 weeks, each costing a whole turn at the corpus turn p50 of **955 s** plus the re-issued call | "the agent should have known" is banned as a diagnosis here and is not the proposal; the row is a guard-coverage row |
| **I468** [accuracy] **412 `InvalidArgument`** in the window, and the top shapes are parameter names the agent guesses from a SIBLING tool. Confirmed live against `terse=0.53.0` schemas this run: `add_member` is the only symbol-addressed edit tool that rejects `symbolId` (it takes `typeSymbolId`/`symbol`) - **41 rejected calls**; `find_registrations` declares `query` with no `symbol`/`name` alias - **15**; `format` declares `path` with no `paths=` though `analyze` and `read_text` both have one - **12**; `get_diagnostics` declares `minSeverity` with no `severity` alias though `analyze` declares exactly that alias - **7** | `add_member`, `find_registrations`, `format`, `get_diagnostics` | add the four aliases. Every one is a parameter an existing sibling already declares, so the asymmetry itself is what earns the rejection - the same finding I460 makes for `get_symbol symbolIds=` and I433 for `add_member` | (a) **75 rejected calls** plus the re-paid input of each retry; `add_member`'s rejection rate is 3.8% of 1 072 calls against `edit_text`'s 2.8% of 6 370 | inferring the alias is impossible for the caller: the MCP binder validates against the declared schema, so the parameter has to exist. `analyze paths=` shapes in the corpus were an OLDER binary and are excluded from the 75 |
| **I469** [cost] a successful `edit_text` answers `changedLines=N` and nothing else, so the agent re-reads the file to see what landed: **`edit_text -> read_text` 771 times with 238 (31%) on the same path**, and `edit_text + read_text` is the top re-fetch pair at **1 121 sessions**. `read_text` is already **42% of all tool-result volume** - 9 786 calls, 57.44 M chars, ~14.36 M tokens, 5 869 ch/call | `edit_text`, `write_text` | mechanism 1: `context=N` returning the N lines around each applied change in their POST-edit state - not a diff, which rule 4 keeps behind `verbose=true`, but the text the confirm-read was for | (a) **771 round trips = 1.57 h** and the read half of a 5 869 ch/call average, against N lines of added framing on an opt-in parameter | defaulting it on is refused: it would charge every edit for a confirm-read that 69% of them did not want |
| **I470** [cost] the advertised surface is **28 921 tokens on every request** (`advertised=88 tools`), and the split says the prose to cut is not where it was assumed: **`parameterDescriptions=11 566` (40%) > `toolDescriptions=9 758` (34%) > `schemaFrame=7 301` (25%) > `names=296` (1%)**. Independent measurement agrees the schema half is the compressible one - a 9-server stdio benchmark found *"for the worst server, 97% is inputSchema, not descriptions"* and cut one server 17 161 -> 773 tokens, and arXiv:2605.26165 (24 May 2026) reports rule-based JSON-Schema compression at **44-50% token savings with no accuracy loss** | `[Description]` on parameters | audit the longest PARAMETER descriptions rather than the tool descriptions - several run to full paragraphs of behaviour that belongs in `SKILL.md` or in the `remedy:` of a rejected call | (cost) 40% of a 28 921-token per-request floor; at 44-50% on that component alone it is ~5 100 tokens off every request of every session | shortening a tool `[Description]` is capped by the opposite evidence: EasyTool (arXiv:2401.06201) raised success 46.0% -> 70.0% by RESTRUCTURING rather than cutting, and Anthropic's floor is 3-4 sentences. Dropping the `Replaces Bash ...` prefix is refused outright - it un-enrols the tool from the guard census |
| **I471** [speed] `AskUserQuestion` cost **12.91 h over 90 calls** - p50 97 s, p90 **1 011 s**, max **4.23 h on one call** - which is the agent stopping the loop to ask rather than taking a defensible default and recording it. For scale, that is more wall clock than every `analyze`, `cleanup`, `format`, `get_diagnostics`, `gate` and `list_tests` call in the window combined (5.51 h) | `SKILL.md`, `CLAUDE.md` | name the measured cost at the point the rule is stated: a question with a defensible default costs a p90 of 17 minutes of dead loop, so the default is taken and logged as an ASSUMPTION | (d) **12.91 h** in 2 weeks, of which the single 4.23 h call is 33% | this is a behavioural row and inherits I463's ceiling - it is logged as a measurement to re-check, not as a rule expected to hold on its own |
| **I472** [speed] the shipped bare-`sleep` denial is being reached around: **230 bare `sleep` calls** (no `while`/`until`/`for` guard) declaring **36 819 s = 10.2 h**, beside 266 correctly guarded ones; all sleep-bearing `Bash` calls declared **244 167 s = 67.8 h** against **21.46 h** of actual wall. The guard denies a segment whose command word is `sleep` outside a loop, so 230 arrived either inside a compound shape it refuses whole and the agent re-issued, or from a `cwd` outside a .NET tree where the row is scoped off | `ToolGuard` | count which of the two paths the 230 took before widening anything - the compound-refusal path is a rewrite candidate, the `cwd`-scoped path is not, because a user-wide guard has no replacement to name outside a workspace | (d) **10.2 h** of declared blind wait in 2 weeks | widening the row to every `sleep` everywhere is refused: `terse guard` is installed user-wide and a bare `sleep` outside a .NET tree has no replacement to point at |
| **I474** [accuracy] `edit_text row=<id> toPath=` moved **every row of the table**, not the one named, and answered `changedLines=1` on each file - so the response was byte-identical to a correct single-row move while the whole backlog had been emptied. Observed live this run: `row="I473"` on `IMPROVEMENTS.md` -> `IMPROVEMENTS-ARCHIVE.md` left the Open table with its header and **zero** of its 16 rows, moved all 16 into the archive, and the state reached a commit before the next read caught it. `rows=[...]` shares the same cut path | `edit_text` | cut exactly the rows whose first cell equals a named identifier, and derive `changedLines` from the rows actually cut so the answer cannot under-report a bulk move; refuse the write when the cut count differs from the number of identifiers named | (accuracy) one silent whole-table move per call; recovery here cost two `write_text ref=` restores plus five verification calls, and the bad state was committed first | reading the response is not a defence - `changedLines=1` is exactly what a correct single-row move answers, which is why the count must be derived from the cut rather than assumed |
