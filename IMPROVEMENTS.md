# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I551** [failures] `changed_files` replayed a memoized listing that no longer matched the tree: it answered `fixtures/**  x7 untracked` while `changed_files path=fixtures` answered 2, because five of those files had been deleted by a child test-host process under a directory the watcher excludes - so the memo's "no watcher event since" guard held while the fact it guards did not. Two answers to one question, 5 files apart, and the stale one is the cheap unscoped call every end-of-task review opens with; it cost ~12 calls and 3 `powershell Test-Path` fallbacks to establish which was true | `changed_files` | watcher events cannot be the whole freshness test for paths the watcher does not watch: either re-shell whenever the previous listing named a path under an excluded directory, or leave those paths out of the listing entirely, so the memo only ever covers what the watcher can invalidate | one silently stale change set per review that follows a test run or a build - which is every review; `I548` narrows the excluded set but `bin/`, `obj/` and `artifacts/` still reach it | a plain time-to-live: it shortens the staleness window without making the answer provable, and this repo does not ship an answer it cannot prove |
| **I552** [round trips] `edit_text edits=[...]` refuses two entries of one file that share an `oldText` with `occurrence=1` ("entries are applied in order against the text the previous one produced"), so replacing the SAME sentence in `search_text` and `search_regex` cost 3 calls instead of 1 - twice in this run, and the same shape recurs on every description sweep across sibling tools | `edit_text` | resolve every entry's `occurrence` against the ORIGINAL text and apply the edits by descending offset, so `occurrence=1` and `occurrence=2` of one anchor land in one call; the current rule is an implementation artefact of applying in order, not a property the caller can reason about | 2 calls and ~12 s of gap per repeated-anchor sweep, and it removes the one batch shape the tool currently rejects up front | — |
| **I553** [fallbacks] nothing answers "does this path exist ON DISK", independent of the workspace index: when a write claimed success and every read answered `is not a document in the loaded workspace`, the only way to tell a missing file from an excluded one was `Bash: powershell Test-Path` / `Get-ChildItem -Force`, 3 times in this run. `find_files`, `read_text` and `changed_files` all answer from the index or from git, so all three are silent about a file the index refuses to hold | `find_files` | make the `DocumentNotFound`/`0 files` answer distinguish *absent* from *excluded*: when the path exists on disk but the walk excludes it, say so and name the rule, so the agent never needs a shell to tell the two apart | 3 shell fallbacks per diagnosis of this class, and the fallback is a built-in on a question the server is uniquely placed to answer | — |
| **I554** [payload] `diff_symbols path=src` returned 71 records of which 12 were `HEURISTIC  not a C# document in this workspace` lines for one `.md` file - one per changed hunk, carrying a line range and a constant string, and nothing `changed_files` had not already said. ~15% of the response was that repetition | `diff_symbols` | fold every hunk of a non-C# file onto ONE record per file - `SKILL.md  12 hunks  not a C# document` - since the per-hunk line range of a file the tool cannot map is not an address anything consumes | ~15% of a mixed-change-set `diff_symbols`, on every review round, which is where the tool is always called | — |
| **I556** [payload] `read_text path="CHANGELOG.md" headings=true` answered 240 section lines - about 3 000 tokens - to locate one heading at line 9. The heading map is the documented cheap way to find a section, and on this file it costs more than reading the section would; the same shape hits every release task and every changelog entry | `read_text` | bound the heading map the way every other listing is bounded: default to the top heading levels, or cap it with the usual `N/T truncated - narrow with maxLevel=` steer, so a 240-section file answers its version list rather than its whole outline | ~2 700 tokens per changelog navigation, which every release and every task that adds an entry performs | a hard `maxLevel=2` default with no opt-out: a `.md` whose sections are all `###` would then answer nothing |
| **I557** [failures] `replace_symbol_body` refused `declaration=` with `unrecognized declaration` - that is the parameter name `replace_symbol` and `add_member` both take, so the wrong guess costs one rejected call plus re-sending the whole body | `replace_symbol_body` | accept `declaration` as an alias for `body`, exactly as every symbol-addressed tool already accepts `symbol` for `symbolId`; the remedy already lists the accepted set, but the alias removes the class | one rejected call and one re-sent body per occurrence, on the guess the sibling tools make obvious | — |
| **I558** [failures] `get_symbol_source` on a **type** id answers the type's OUTLINE plus a steer, not its source, while `SKILL.md`'s own row reads "Read a whole class's source -> `get_symbol_source(symbolId)` on a type id". Following the documented row cost a second call with `verbose=true` for two types in this run | `get_symbol_source` | make the tool and the doc agree: either have a type id return the source the tool is named for, or change the SKILL row to `get_symbol_source(symbolId, verbose=true)` and say the default is an outline | one round trip per whole-type read, on the one tool whose name promises the payload it withholds | — |
| **I559** [failures] once a second workspace was loaded, `search_text path="src/TerseSharp.Server/Assets/SKILL.md"` answered `ERROR AmbiguousWorkspace` - yet that relative path exists under exactly ONE of the two loaded roots, so the hint was decidable and was not used. Cost one rejected call plus a retry on every relative-path call for the rest of the session | `search_text` | rank a RELATIVE path hint the way an absolute one is ranked: resolve it against each loaded root and take the workspace where the file actually exists, refusing only when it exists under two or none - the resolver already refuses to guess, and this is not a guess | one error plus one retry per relative-path call whenever a fixture workspace is also loaded, which is every task that probes a fixture - `AmbiguousWorkspace` was already the second most frequent error code in the session logs | resolving by root-name prefix alone: two worktrees of the same repo share every relative path, so it would guess |
| **I560** [failures] the guard routes `git diff --stat` to `diff_symbols`, while `changed_files`'s own advertised description opens `Replaces Bash git status and git diff --stat` - so the tool that claims the command and the tool the agent is sent to disagree. Found by this task's fresh-context reviewer; pre-existing, and invisible to the gates because `ToolCensusE2ETests.EveryToolThatAdvertisesItReplacesAShellCommand_IsDeniedByTheGuard` asserts only that the command is DENIED, never where it is routed | `changed_files` | make `Replaced`/`Diffing` treat `--stat` (and `--numstat`, `--name-only`, `--name-status`) as the `status` subcommand so the routing is `changed_files`, and extend the census to assert the routing names the advertising tool, not merely that a denial happened | one wrong tool choice per `--stat` diff, plus the round trip to correct it; and it closes the census hole that let the two drift | routing `--stat` to `diff_text`: the whole point of `--stat` is that the caller does NOT want hunk text |
| **I561** [unanswerable] `read_text` and `get_file_outline` take `ref=`, but `get_symbol_source` does not - so "what did this member look like before the change" has no symbol-addressed answer and must be done as a raw line range against a revision. In this run that cost a real near-miss: a `ref=HEAD ranges=[...]` read clipped `ToolGuard.Direct` mid-method, the body was rewritten from the clipped text, and its last return silently lost `Priced`, `Reason("Bash", ...)` and `BashRouting(trimmed)`. `build`, `analyze`, `get_diagnostics` and 389 guard tests were all green on the corruption; only a second diff against `ref=HEAD` found it | `get_symbol_source` | give `get_symbol_source`, `get_symbol` and `get_type_outline` the same `ref=` parameter `read_text` already has, resolved against that revision's syntax tree, so the pre-change body of a member is one symbol-addressed call that cannot be clipped mid-declaration | the whole class of rewrite-from-a-clipped-revision-read defects, which no local gate can catch; plus the line-range guessing on every R4 introduced-vs-pre-existing check, which is every review round | reading the whole file at a revision instead: on this repo's 1 600-line `ToolGuard.cs` that is the payload `get_symbol_source` exists to avoid |
| **I555** [failures] `BuildTools.Replayable` wires `Watched` around the runner in ONE line and nothing proves it: substitute `run` for `watched` and every test still passes, because the only E2E assertion is `Build_WithNothingWrittenWhileItRan_CarriesNoStaleMarker` - an ABSENCE - and the unit tests exercise `StaleRun` directly. Raised by this run's own fresh-context reviewer and left open deliberately: the obvious probe - `fixtures/SelectionSolution`'s 90 s `TerseStallBuild` target plus a concurrent `write_text` - does NOT remove the race, because `Watched` snapshots `EditPulse.Changed` only after `ReadyAsync`, `SyncedAsync` and `Stamp`, so the write can land before the snapshot and redden the test for a reason unrelated to the code | `run_tests` | give the run a deterministic seam rather than a wall-clock one: have `Watched` publish its snapshot through a test-visible hook, or let the stall target signal a file the test waits on before it writes, so the write is provably after the snapshot | one unproven wiring line on a verdict every task reads; the coupling it depends on - `UnchangedRun.MemoRefusal` refusing a multi-line verdict - is pinned, the wiring is not | the `TerseStallBuild` plus concurrent-`write_text` probe: it races the snapshot, and a flaky test on a repo with a documented history of one-legged CI reds is worse than a stated gap |



