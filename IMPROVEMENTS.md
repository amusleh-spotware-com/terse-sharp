# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I328** [cost] `read_text path=CHANGELOG.md section="## [Unreleased]"` returned **531 lines** - the heading and everything after it to the character cap - instead of the 120-line section, ~13 000 tokens against ~300, and it was the single largest response of this run. The file carries fenced code blocks and bracketed headings, so either `DocumentOutline.IsFence` left the scanner fenced and found no later heading, or `Close`/`Seal` did not bound the section; the observation is certain, the cause is not | `read_text`, `DocumentOutline` | reproduce against a fixture markdown file that carries BOTH a `[bracketed]` heading and a fenced block, then bound the section at the next heading of the same or lower level in every case; the fixture has neither today, so no test can currently fail | ~12 700 tokens per over-read, on the one file every release reads | assuming the brackets are the cause - the fence path is equally consistent with the observation and this repo bans a claim it has not proven |
| **I329** [speed] `replace_symbol symbolIds=[...] add=[...]` refuses when the targets do not share one containing type and names `addTo=`, but `addTo=` takes exactly ONE container - so a batch whose members live in two types cannot carry its helpers at all. It cost **2 extra `add_member` calls** in this run (`GitTools.ChangeScope`+`Steer`, then the `DotnetRunner` helpers), each re-sending nothing but paying a full round trip | `replace_symbol`, `SymbolEditService` | let each `add=` entry carry its own `addTo`, or accept `addTo=` as an array paired with `add=`, so one compile-gated edit covers a multi-container change | 2 calls per multi-container edit; the callee-first rule makes this the common shape | inferring the container from the declaration - a helper's container is a choice, and guessing it lands a member in the wrong type undetectably |
| **I330** [speed] a `CompileRegression` whose only error is a missing import cost **3 round trips** in this run (`System.Text` twice, `System.Globalization` once) even though the remedy named `usings=` exactly: the rejection, the retry, and the payload that had to be held. `usings=` is documented in the parameter, not in the opening sentence of the tool description, which is where a long session still reads | `add_member`, `replace_symbol`, `replace_symbol_body` | name `usings=` in the opening sentence of all three descriptions, beside the plural steer - or add the directive automatically when every new error is a `CS0246`/`CS0103` the project resolves in exactly one namespace, behind an explicit `autoUsings` the response reports | 3 rejected edits per session that writes new code, at the full declaration payload each | adding the import silently - the current behaviour is deliberate, and an import the caller did not ask for is a change they cannot see in a one-line success |
| **I331** [cost] `get_symbol_source` on a one-line `readonly record struct` answers `1 types  <name>  struct  :742-742` plus `steer: ... verbose=true for the whole type` - it withholds a declaration that is *shorter than the steer*. Measured **3 wasted calls** in this run (`FileService.FileWrite`, `PendingWrite`, `RowCut`), each answered again with `verbose=true` | `get_symbol_source`, `SourceService` | return the source when the type's whole declaration is at most a handful of lines, and steer only when withholding it actually saves tokens | 3 calls per run that reads record structs, which is every run that edits this codebase | dropping the type steer - on a 200-line class it is the whole point |
| **I332** [accuracy] `unload_workspace` warns `2 analyzer or source-generator assembly(ies) ... still mapped` naming `Microsoft.CodeAnalysis.Analyzers.dll` and `...CSharp.Analyzers.dll` **from the NuGet cache**, not from any project's `bin/`. `AnalyzerLockE2ETests.UnloadWorkspace_AfterTheAnalyzerRan_ReportsNothingStillMapped` fails on it deterministically on this machine - reproduced identically on a clean `1f8055a` worktree - while the **same commit's CI run 32732013720 passed that test on all three legs**, so it is environment-shaped rather than universal | `unload_workspace`, the analyzer shadow-copy loader, `AnalyzerLockE2ETests` | find what maps those two package assemblies by their original path when every analyzer is meant to load from the `terse-analyzers/` shadow copy - a second load context, a transitive Roslyn dependency resolved by identity, or a probe that reports a file the process merely opened - then either release them or stop counting a file the loader never mapped | the warning is a **false positive today on at least one machine**: it tells a user to restart a server that is holding nothing, which is the confidently-wrong-answer class this repository exists to remove | calling it a flake - it is deterministic here across three runs and on a clean HEAD worktree; and deleting the assertion, which is the only thing that detects the regression the shadow-copy loader was built to prevent |

