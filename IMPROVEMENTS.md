# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I595** [failures] `write_text path=... content=...` silently OVERWROTE two files that already existed in this run - `fixtures/.../WideSurface.cs` (45 members, referenced by four tests) and `tests/.../ResultCapTests.cs` (4 tests) - because nothing in the request or the response says the path was occupied. The first cost a 50-failure, 10-minute whole-suite run plus 8 repair calls before the cause was visible; the second was caught only by reading the diff the tool printed | `write_text` | when a single `path=` write REPLACES an existing file, say so in the one-line success (`overwrote existing  <path>  changedLines=N`), and refuse a write whose content shares no line with the file it replaces unless `force=true` or `overwrite=true` - a wholesale replacement of an unrelated file is the shape that costs a suite run | one whole-suite round trip plus the repair calls per occurrence; it happened TWICE in one session, both times because the agent believed it was creating a new file | a `create=true` flag the caller must remember: the failure is precisely that the caller did not know the file existed, so the check has to be on the server side |
| **I606** [round trips] `analyze ids=["CS8019", ...]` answered `NOT_ENABLED ["CS8019", ...] - no analyzer these projects reference declares it, so this pass could not have found it` - for an id the SAME tool had reported two calls earlier in the same session (`CS8019 info DeadCode src\TerseSharp.Core\SymbolFormat.cs:1:1`). The filter is checked against analyzer DESCRIPTORS only, so every compiler and dead-code id is declared unfindable, and the one narrowing parameter that would have cut a 174-record response to 2 cannot be used | `analyze` · `get_diagnostics` | treat an id the compiler or the dead-code engine can emit as enabled - `IsCompilerId` already exists in `ProjectDiagnostics` and `DeadCodeService.RuleId` is a constant - so `NOT_ENABLED` names only ids nothing in the run could produce | the whole 174-record payload per narrowing attempt, ~6 000 tokens, twice in this session | widening the filter to accept any id: the refusal is right for a genuinely absent analyzer, and that is the case it was built for |
| **I607** [payload] `analyze changed=true severity=info` scopes to changed FILES, not changed DECLARATIONS: 174 records came back, 172 of them `TERSE1xx` policy findings on members this task never touched (`EditTools.AddMember - 18 parameters`, `SymbolEditService - 118 methods`, 60+ `TERSE107` test-name rows), against 2 actionable `CS8019`. `gate baseRef=` scopes to changed lines and is the tool that gets this right; plain `analyze changed=true` does not | `analyze` | give `analyze` the same line scoping `gate baseRef=` already has - `changed=true` should mean the declarations the diff touched, with the rest behind a count - or default `includePolicy=false` when the run was scoped by `changed=` | ~5 900 tokens per call, paid twice here and on every task that ends with the mandated info-severity sweep | raising the severity floor: the 2 actionable records were `info` too, so the floor cannot separate them |
| **I608** [round trips] there is no batched delete: `delete_symbol` takes one `symbolId` and no `symbolIds=`, while `replace_symbol` refuses a declaration whose name differs from its paired id. Removing 12 members from `SymbolFormat` after extracting them into `AttributeText`/`ConstantText` therefore cost one `replace_symbol` re-sending the WHOLE type - 172 changed lines, ~1 900 tokens - because 12 `delete_symbol` calls was the only alternative | `delete_symbol` | accept `symbolIds=[...]` on `delete_symbol`, applied as ONE compile-gated edit the way `replace_symbol symbolIds=` and `add_member typeSymbolIds=` already are - extracting a helper into a new file and deleting the originals is the common shape | 11 calls, or ~1 900 tokens of re-sent declaration, per extraction refactor | `replace_symbol` with `rename=true`: it replaces rather than removes, and the compile gate still sees the old members |
| **I609** [failures] `read_text path=... lines=40-200` was rejected `unrecognized lines`; the parameter pair is `startLine`/`endLine`. The rejection named the whole accepted set and the retry landed, so the cost was one call - but `lines=` is the spelling the tool's own steers use in prose (`raise maxChars`, `a line range`) and the one an agent reaches for first; re-measured 2026-09-26: 2 more `unrecognized lines` refusals in one week | `read_text` | accept `lines="40-200"` as an alias that splits into `startLine`/`endLine`, or name `startLine=`/`endLine=` in the `path` description so the schema teaches it without a rejection | one rejected call per session that reads a range, which is most of them | adding it to `ranges=`: that takes a list of ranges and is the multi-range form, so a single `lines=` still has to resolve to the scalar pair |
| **I605** [payload] `find_implementations` and `get_symbol usages=true` on a METHOD now pay two whole-solution scans - `SymbolFinder.FindImplementationsAsync` plus `FindOverridesAsync` - where they paid one; the second is what made a base-class member answer its overrides at all (**I600**), but its cost was never measured, and `ExploreService.ReachAsync` pays it on every `get_symbol usages=true` as well, on the 148-project solution this targets | `find_implementations` · `get_symbol` · `SymbolDerivation` | measure both calls on a wide member, then skip `FindOverridesAsync` when the symbol can have no overrides - not `virtual`, `abstract` or `override`, or declared in a sealed type - which is the common case and decidable from the symbol alone | one whole-solution reference scan per call on every non-virtual member, which is most of them | dropping `FindImplementationsAsync` for members and keeping only `FindOverridesAsync`: it is the one that resolves INTERFACE implementations, which was never broken |
| **I610** [latency] field report on a 148-project WPF solution: two symbol queries issued while a test build ran sat 3-4 minutes each, and the workspace was unloaded and reloaded twice for MSBuild locks; each reload threw the realized compilations away, so the next query paid realization again (47 s, against 119 s for the first) while competing with the build for CPU | `build` · `run_tests` · `WorkspaceRegistry` | keep the last realized `Solution` across the lock-retry unload/reload and answer read-only symbol queries from it tagged `snapshot=pre-build` until the reload finishes, or skip the unload when no holder the build names is this process | 47-119 s per symbol query issued during a build on a large solution | answering from a stale snapshot WITHOUT saying so: a confident answer about code the build may have changed is the defect this server exists to prevent |
| **I611** [payload] `add_member typeSymbolIds=[5 types] usings=["Microsoft.CodeAnalysis.CSharp", "...Syntax", "System.Globalization"]` added all three directives to EVERY file of the batch, although each namespace was needed by one or two of them; a `cleanup fix=usings` then had to remove 13 lines across 5 files | `add_member` · `replace_symbol` · `SymbolEditService` | land each `usings=` entry only in the documents whose new declarations need it - the per-document CS0246/CS0103 set the gate already computes names them - and drop the rest | one `cleanup` call plus 13 dead lines per multi-file batch carrying usings | — |
| **I613** [payload] `gate` with no `baseRef=` over 13 changed files returned 90.7 KB - spilled to a file by the client - because 369 pre-existing policy findings on those files were listed; `analyze baseRef=HEAD` answered the same question in 19 records | `gate` | when the workspace is a git tree, default `gate` to the changed-lines view `baseRef=HEAD` gives and fold the rest to one count, with `baseRef=""` opting back into every finding in the changed files | one wasted ~23 000-token call and a re-run per end-of-task gate on a file set with pre-existing findings | — |
| **I614** [round trips] a `replace_symbol symbolIds=[10 across 4 types] add=[7 helpers] addTo="BuildTools,EditTools,MemberDeclarationTests,CompileGateE2ETests"` was refused because `addTo=` may only name the targets' own containers, so helpers for types the batch did not replace needed a separate `add_member typeSymbolIds=` call plus a `retryWith add=[]` replay - 3 calls where one edit was meant | `replace_symbol` | let `addTo=` name any type in the workspace (resolved as `add_member`'s `typeSymbolId` is), so a batch whose replaced members and new helpers live in different types stays ONE compile-gated edit | 2 calls and one re-sent helper set per cross-type batch | — |
| **I630** [round trips] 0.67.0 field report D2: a refactor whose N declarations only compile together (fields + interface member + impl + ctor across 4 files) pays one full realization per rejected step, because the gate is per call while the refactor is per set | `replace_symbol` · `add_member` · `EditGate` | a staged edit - `begin_edit`, per-symbol stage calls, `commit` running the gate once over the set; `I617`'s `add_member` adoption covers the fields-plus-constructor case only | 2-4 rejected calls and 20-370 s realizations each per multi-declaration refactor | a fourth tool was deferred until `I617`'s adoption rate is measured - it may cover most of the class |
| **I631** [latency] `WorkspaceRegistry.PressureBytes` is a fixed 2 GB, below what one large workspace holds, so pressure is judged against a constant instead of the machine | `WorkspaceRegistry` | scale the threshold to `GC.GetGCMemoryInfo().TotalAvailableMemoryBytes` (e.g. 60 %), keeping 2 GB as the floor | avoids needless drops of non-MRU workspaces on a 64 GB box | — |
| **I632** [failures] a suite that parses 148 projects needs more than any foreground call can wait for; the client moved 11 calls to background tasks and each cost a `TaskOutput` poll turn; re-measured 2026-09-26 over 176 transcripts: `run_tests` p90 **120 013 ms** and p99 122 730 ms sit on that ceiling, 17 `run_tests` calls answered `RunInFlight`, and `TaskOutput` still cost 24 calls / 0.98 h - the MCP 2026-07-28 release candidate adds `tools/call` task handles driven by `tasks/get`, the protocol form of this row | `run_tests` | a detached mode - `run_tests detach=true` answering a run id at once, and `rerun_failed`/`run_tests status=<id>` reading its verdict | one poll turn per long run, and runs past the client's 120 s foreground limit | — |
| **I633** [payload] `replace_symbol symbolIds=[...]` re-indented the untouched parameter lists of `FileService.InsideAsync`, `WriteTextManyAsync`, `GitTools.Render`, `ListAsync` and `DiagnosticsService.CollectAsync` - 5 collateral hunks, +20 -20 lines of pure whitespace in one task's diff | `replace_symbol` · `SymbolEditService` | preserve the original indentation of a replaced declaration's parameter list when the sent text only differs in leading whitespace, or re-indent the sent declaration to the target's column before splicing | ~40 diff lines of review noise per multi-symbol replace | — |
| **I634** [round trips] making a nested private record a namespace-level type cost `delete_symbol force allowErrors` + `add_member path=` - 2 calls and a transiently broken tree | `move_type_to_file` · `move_type_to_namespace` | let `move_type_to_namespace` lift a nested type to namespace level in the same file | 1 call and no broken intermediate state | — |
| **I635** [accuracy] `gate baseRef="HEAD" path="src/**/*.cs"` REFORMATTED six files outside the change set (`ProjectDiagnostics.cs`, `RazorGeneratedMap.cs`, `ReleaseVersion.cs`, `SolutionFile.cs`, `TestResultParser.cs`, `TestScope.cs`) - `baseRef=` narrows the findings it reports but not the files its format step writes; restoring them cost 6 `write_text ref=HEAD` calls | `gate` | under `baseRef=`, limit the format and cleanup writes to the files the working tree changed against that ref, the same set the findings are narrowed to | 6 restore calls and a collateral diff per scoped gate run | — |
| **I638** [failures] `gh run view <id> --log-failed 2>&1 \| grep -E "..." \| head -60` was stripped by the guard as a replaced command, though every text tool in it reads STDIN - the skill says a stdin reader is untouched; recovering cost a redirect to a scratch file plus 3 `search_text root=` calls | `ToolGuard` | treat `grep`/`head` fed by a pipe from a command nothing replaces as a stdin reader, as `git branch -a \| head -40` already is - the `2>&1` or the `-E` flag is the likely trigger | 4 calls per CI-log triage | — |
| **I615** [failures] the review subagent called `search_regex paths=[...]` and got `InvalidArgument` - `analyze`, `cleanup`, `format`, `read_text` and `get_file_outline` all take `paths=`, so the plural is the spelling an agent carries over; it re-issued with `glob=` | `search_regex` · `search_text` | accept `paths=` on both searches as a list of globs, OR-ed the way `find_files globs=` already does, so the batch spelling every sibling takes is not a refused call | one refused call per cross-tool habit, observed in a 22-call review | — |
| **I616** [failures] three consecutive `write_text delete=true recursive=true` calls on three `obj/` directories drew the run steer `pass files=[...] ... each entry is {path, content}`, but a `files=` entry cannot delete - the steer names a batch that cannot express the calls it is batching; six parallel `write_text ref=HEAD` restores drew the same steer, and `files=` is refused beside `ref=` too | `write_text` | either accept `delete`/`recursive`/`ref` on a `files=` entry, or suppress the run steer when the run's calls carried `delete=true` or `ref=` | one misleading steer per delete run, and a refused call for an agent that obeys it literally | — |
| **I639** [correctness] the guard's out-of-tree carve-out (**I549**) never expands `~`: `wc -c ~/notes/hooks.md` and `find ~/.claude/projects -name '*.jsonl'` are DENIED as "C#/.NET source" on 0.68.0 (probed with `workspace_status guard=`), while the same file spelled as an absolute out-of-tree path is ALLOWED - and the replacement the denial names, `search_text`, searches the workspace and cannot answer. This run's first transcript count hit it; the 1-week scan (176 transcripts) counted **268 guard denials of `Bash`, 127 of the "C#/.NET source" kind, 9 with a `~` operand** (text heuristic), and `Bash` was the very next call after 120 of the 268 | `ToolGuard` | expand a leading `~`, `~/` and `$HOME` to the user profile before resolving operands in the **I549** scope test, so a home-relative path outside the tree is allowed exactly like its absolute spelling | one denied call plus one re-issue (~4.5 s model gap each) per occurrence, and it removes a denial whose named replacement cannot serve the call | treating `~` as unresolvable and denying: it always names the home directory, never a workspace path |
| **I640** [accuracy] 17 `read_text` calls answered `ERROR InvalidArgument: IOException: The process cannot access the file ...` in the 1-week window, and **11 `read_text` refusals were followed directly by a `Bash` call** - the terse read tool measurably losing its caller to the shell. `FileService.PresentFileAsync` reads with `File.ReadAllTextAsync`, which opens `FileShare.Read`, so any file a live process holds open for writing (a test log, a build output, a transcript) is unreadable - while `FileService.CharacterLengthAsync` and `TextSearchService.Options` already open `FileShare.ReadWrite` (confirmed by `search_regex containers=true`) | `read_text` · `FileService` | read through a `FileStream` opened `FileShare.ReadWrite \| FileShare.Delete` with `FileOptions.Asynchronous \| FileOptions.SequentialScan`, and map a residual sharing violation to its own code (`FileLocked`) whose remedy names `tail=` - it is not an argument error | 17 refused calls and 11 shell fallbacks a week | — |
| **I641** [accuracy] **22 `find_files` calls were refused `'globs' was passed with 'root', and a batch answers about the loaded workspace only`** in the 1-week window, and 15 `find_files` refusals were followed by another `find_files` retry; `find_files` carries a **7.1%** error rate (34 of 478). `root=` already answers ONE glob outside the workspace, so the refusal is an implementation gap the caller cannot predict from the schema | `find_files` | run the `globs=` batch against `root=` the way a single glob already runs there, OR-ing the globs and tagging the answer outside-workspace | 22 refused calls plus their retries a week | — |
| **I643** [speed] **9 966 of 13 305 assistant messages (74.9%) carried exactly one tool call** in the 1-week window (1.328 calls/message). Anthropic's Fable 5.1 prompting guide (fetched 2026-09-26) says of coding loops that the model "may issue them one per turn instead" and recommends re-sending an independent-calls nudge after tool results; Claude Code's `PostToolBatch` hook "fires exactly once with the full batch" and its `additionalContext` is injected before the next model call. `terse install --guard` registers `PreToolUse` only - `PostToolBatch` appears nowhere under `src/` | `ClientRegistrar` · `ToolGuard` · `terse install --guard` | register a `PostToolBatch` hook that, when the batch was ONE read-only terse call, injects one sentence - list what you need next, and request every item that does not depend on another's result in one response - placed after the tool results, never before them | lever (e): moving the multi-call share from 25.1% to 35% of 13 305 messages deletes ~1 300 round trips, **~1.6 h a week** of model gap at the 4 543 ms p50 | a `CLAUDE.md` or `SKILL.md` line: a rule stated once did not hold (W&D, ICLR 2026), and the global `CLAUDE.md` SPEED gate is exactly that rule; text placed before the tool results breaks parallel tool use per Anthropic's own formatting rule |
| **I644** [accuracy] 20 `InvalidArgument ... unrecognized <parameter>` refusals in the 1-week window on spellings an agent carries over from a sibling tool or from `Grep`: `gate paths` 4, `get_type_outline typeName` 4, `load_workspace name` 3, `add_member code` 2, `replace_symbol content` 2 and `path` 1, `list_projects contains` 2, `search_text output_mode`/`-A`/`-C` 2 - each a re-issued call; `read_text lines` (**I609**) and the searches' `paths` (**I615**) are the same family and already open. Parameter mistakes cost more than a wrong tool: "task success rates drop by over 12%" and "agents tend to blindly trust the erroneous response" (arXiv 2506.21967) | `gate` · `get_type_outline` · `load_workspace` · `add_member` · `replace_symbol` · `list_projects` · `search_text` | bind each observed spelling as an alias of its canonical parameter - `gate paths=` as a path list, `typeName` to `symbol`, `load_workspace name` to `path`, `code`/`content` to `declaration`, `list_projects contains` to a name filter, `search_text -C` to `context` - and keep `InvalidArgument` for a name nothing maps | ~20 calls a week, plus the retries each refusal triggers | silently dropping an unknown parameter: a parameter mistake is the one an agent does not notice, so the refusal stays for true unknowns; every alias is a declared parameter, so `EveryAdvertisedSchema_DeclaresExactlyTheParametersItsToolMethodDeclares` stays green and each costs ~15 schema tokens |
| **I645** [cost] **I588**'s column-separator half, closed pending "the only change of its release", measured again with `tiktoken` on 360 real success responses (40 each of 9 listing tools, 1-week window): collapsing every internal run of 2+ spaces to one saves **exactly 1.00 token per run in both encodings - 4.78% o200k / 4.81% cl100k overall, 8.0% on `find_usages`, 7.0% `changed_files`, 5.4% `get_type_outline`, 5.1% `search_symbols`, 5.0% `search_regex`** - against **I588**'s 2.49% on 15 payloads; the scan counted ~96 000 such runs a week across those tools | `ResponseBuilder` · every listing tool | ship the one-space separator as the ONLY change of its release, with each tool's `TokenBudgetE2ETests` ceiling lowered in the same commit | ~96 000 tokens a week | bundling it with other rows (**I326** and **I588** refused exactly that); markdown pipes or TSV, which cost more per row |
| **I646** [accuracy] this command's own M2 script mis-scores five numbers every run reports, found reading its 2026-09-26 output: (1) unspent parallelism, **5 818 pairs / 62.6 h**, treats a write followed by its verify as independent because its proof is textual - `edit_text`+`run_tests` 144, `run_tests`+`edit_text` 88, `Write`+`Bash` 138, `Bash`+`Write` 103 - so it OVER-reports where it claims to under-report, and its gap runs message start to message start, tool time included; (2) the redundant share (8.9%) counts **222 `run_tests` and 123 `build` repeats, of which 214 and 122 followed a write or `Bash`** - write-aware, 9 of 345 were redundant; (3) `NARROW` lacks `paths`/`changed`/`baseRef`, so 153 of 195 `analyze` calls read as whole-solution; (4) "mid-session re-prime" labels 151 M of 156 M cache-write tokens - every incremental turn write - as a broken prefix; (5) 31 of 103 "gate reminder" intervention cues were slash-command expansions | `.claude/commands/mine-sessions.md` | drop pairs whose first call mutates or whose second verifies; measure the gap from the first call's RESULT; add `paths`/`changed`/`baseRef` to `NARROW`; count a verification repeat as redundant only with no write between (the instrument `CLAUDE.md`'s definition of done already uses); count a cache write as re-prime only when `cache_read` FELL against the previous message; skip user records carrying `command-name`/`command-message` | corrects the `[speed]` headline metrics every run reports and every row is ranked on | — |
