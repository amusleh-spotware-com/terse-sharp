# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I410** [failure] `get_symbol_source` DEDENTS its payload, so pasting it back as an `edit_text` anchor fails - measured this run: 6 `edit_text` entries in 2 batches answered `oldText matched 0 times` on text copied straight from a `get_symbol_source` read, each costing a `read_text verbose=true` re-read plus a re-issued batch (~1 500 tokens and 4 extra calls in total). The remedy names the cause exactly, which is why it was recoverable, but the round trip is paid every time | `get_symbol_source`, `edit_text` | let `edit_text` match a dedented anchor when the dedented form is unique in the file - the same normalization it already does for line endings - or give `get_symbol_source` an `indent=true` that returns the text as it sits | 2 calls and ~250 tokens per anchor edit taken from a symbol read, which is the normal way to find the text to anchor on | rendering every symbol read verbatim is refused by I-series measurement: the dedent is worth 18-31% at member scope |
| **I411** [fallback] nothing removes a DIRECTORY, so an empty folder a `write_text delete=true` leaves behind needs `Bash: rmdir` - measured this run once, after a probe file was written to the wrong fixture path and deleted again | `write_text`, `find_files` | `write_text delete=true` could remove a directory it just emptied, or take `directory=true` for an explicitly empty one; a non-empty directory stays refused | one `Bash` fallback per stray directory, and it closes the last file-system action with no tool - every other create, write and delete is served | deleting a non-empty directory is refused outright: no tool in this server removes more than it was pointed at |
| **I412** [round trip] `analyze paths=` caps at 10, but a release change set is 20+ `.cs` files, so the end-of-task sweep costs 3 calls - measured this run: 21 touched files answered in 3 calls of 10, 10 and 7 | `analyze` | `analyze changed=true` already exists and answers the same question in ONE call; the defect is that nothing steers you there when a `paths=` batch saturates - a saturated batch should end with `next: analyze changed=true` | 2 calls and ~120 tokens per end-of-task sweep, and it makes the cheaper form discoverable at the moment the expensive one is being used | raising the 10-path cap is refused: the cap bounds the response, and `changed=true` already answers the unbounded case |
| **I413** [unanswerable] no tool answers a question ABOUT the advertised surface - "which tools declare `dryRun` but not `allowPolicy`?" cost a full E2E test run to enumerate (I402), and "does this tool take `staged`?" is answered only by calling it or reading the source | `workspace_status`, a new schema query | `workspace_status` could take `tool=<name>` or `declares=<parameter>` and answer from the same advertised list it already counts - the surface is in memory, and this is the question every census test asks in code | one test run (~30 s) per surface question, and it turns a census a human writes into a query the agent can make | reading `tools/list` in the client is not an answer: the agent cannot query it, and the server already holds the list it prints the token count for |
| **I414** [payload] the surface is 88 advertised tools - 89 `[McpServerTool(Name` declarations across 12 `Tools` classes - at this repo's own measured ~255 tokens of `tools/list` each, so ~22 400 tokens sit in the prefix of every request of every session. Anthropic measures tool-selection accuracy degrading past 30-50 tools, and its Tool Search beta recovering Opus 4.5 from 79.5% to 88.1% on large libraries while cutting the definition block 85% (72k to 8.7k); GitHub cut Copilot's MCP from 40 tools to 13 for +2 to +5pp on SWE-bench Verified and -400 ms of latency | `ToolProfile`, `ToolGroups`, `.terse.json` | make the DEFAULT profile a ~12-tool core - outline, symbol source, search_symbols, find_usages, read_text, edit_text, replace_symbol_body, changed_files, analyze, build, run_tests, workspace_status - and put every other group behind an opt-in the core tools name in their `remedy:` lines, so the deferred groups stay discoverable at the moment they are needed | ~19 000 prefix tokens per session, plus the measured selection-accuracy band between a 13-tool and a 40-tool surface | deleting tools is refused: this repo's own break-even is 32 calls per 508 sessions, so the cost of a tool is discovery, not existence |
| **I415** [payload] the advertised set is filtered per loaded workspace - the workspace's own markup families plus `.terse.json` - and `McpHost` emits a list-changed notification, so loading a SECOND workspace mid-session rewrites the tools array. Anthropic's cache key renders tools, then system, then messages, and a tool-definition change rebuilds ALL THREE tiers, so one mid-session load pays a full ~22 400-token prefix rewrite at the 1.25x write price instead of a 0.1x read | `ToolProfile`, `McpHost` | decide the advertised set ONCE at startup from every root the session may load, and never re-emit list-changed; MCP 2026-07-28 adds `ttlMs`, `cacheScope` and deterministic list ordering for exactly this case | one full prefix rebuild per extra workspace load - ~22 400 tokens at write price, on a tree whose CLAUDE.md already warns more than one workspace is usually loadable here | narrowing the surface per workspace at load time is the feature that causes the rewrite: it cannot be kept and cached both |
| **I416** [failure] no MCP tool annotation is emitted anywhere - `readOnlyHint`, `destructiveHint`, `idempotentHint` and `openWorldHint` all occur ZERO times in `src/**/*.cs` - so every one of the 88 tools presents to a client as destructive, because `destructiveHint` defaults to true in the spec, even though roughly half are pure reads and `ToolContext.RejectWrite` already knows exactly which | every `[McpServerTool]`, `ToolContext.RejectWrite` | emit annotations from the same read/write split `--read-only` already enforces, and census-gate it: every tool that does not call `RejectWrite` is `readOnlyHint: true`, every tool that does is not | an approval prompt per read call on any client that uses annotations for graduated confirmation, and it is derived from a split the server already computes | treating the hints as a security boundary is refused - the spec says clients must not trust them; `RejectWrite` stays the gate |
| **I417** [round trip] Anthropic's programmatic tool calling measured 37% fewer input tokens (43 588 to 27 297) and +11% on agentic search by keeping intermediate tool results out of context, while this repo's own scan found 1 922 adjacent navigation pairs against 7 lifetime calls of `explore_symbol` - the composite exists and is not selected, so the fix is not another composite | `find_usages`, `get_symbol_source`, `explore_symbol` | let ONE tool consume the previous result instead of the agent relaying it - `get_symbol_source fromUsagesOf=<id>` - so the intermediate usage listing never enters the transcript at all | one full listing per navigation chain, on the 1 922 pairs already measured | a general code-execution or scripting tool is refused: it bypasses `EditGate` and carries the same discoverability veto that left `explore_symbol` at 7 calls |
| **I418** [payload] responses pad records into aligned columns, and alignment padding is pure whitespace charged per column boundary per record. Notation Matters (9 649 prompt-completion trials, 11 models, 4 formats) found format did NOT significantly change frontier accuracy (chi-squared 2.45, p=0.484), while markdown pipe tables cost up to +60% tokens against YAML in retrieval-heavy agents and JSON runs about 2x TSV | `ResponseBuilder` | measure a single-space or tab-separated variant of the WIDEST listing - `find_usages` on the widest fixture symbol - against the aligned form, and if it wins, drop the padding and assert the new number in `TokenBudgetE2ETests` | every column boundary in every record of every listing, at a measured-zero accuracy cost on a frontier model | JSON is refused outright - measured the most expensive of the four formats - and so is any change that rewrites payload text rather than framing |
| **I419** [unanswerable] no tool answers "does a member like this already exist?" - `search_symbols` needs the name you do not have yet. GitClear 2026 measures block duplication rising from 40.3 to 73.0 per million changed lines (+81% since 2023), copy/paste from 9.4% to 15.7% in H1 2026 and moved-and-refactored code falling from 21% to 3.8%, with cloned blocks carrying 15-50% more defects; the named mechanism is that an assistant finds inserting a new block trivial and proposing reuse of an existing one unlikely | `add_member`, `replace_symbol`, a new similarity query | have `add_member` and `replace_symbol` report near-duplicate existing members on `dryRun` - a normalized syntax-tree hash over the containing project, identifiers erased - so the duplicate is named before it is written | the duplicate this server currently helps write, on the one code-quality signal with four years of longitudinal measurement behind it | text similarity is refused: a parameter rename defeats it. Hash the normalized tree, not the text |
| **I420** [unanswerable] `analyze` reports the compiler plus the analyzers the project REFERENCES, so a solution referencing no security analyzer gets no security rule at all - and Veracode measures 45% of AI-generated samples introducing an OWASP Top 10 flaw, flat from 2025 into 2026, with CodeRabbit measuring AI-written code 1.88x likelier than human-written to introduce a vulnerability | `analyze`, `get_diagnostics` | `analyze security=true` loading the SDK's CA2100 / CA3xxx / CA5xxx set regardless of what the project references, reported separately from the project's own analyzer set | the entire class of finding this repo's definition-of-done cannot currently produce on a solution that ships no security analyzer | enabling them by default is refused: it would report on files the agent never touched, which the touched-files rule already forbids |
| **I421** [failure] the anchor class `edit_text` does NOT handle is the one that matches ZERO times - 102 measured `oldText matched 0 times` here, each costing a `read_text` re-read, plus the 6 dedent cases logged as I410. The 2026 format-theoretic harness round-trips search/replace at 0.81-0.94 and ALL 11 of its failures were anchor AMBIGUITY, which this server already refuses by name with `occurrence=`, so the remaining measured failure is the opposite one | `edit_text` | on zero matches, return the closest region and its line range - a token-level diff over the file - so the retry is a corrected anchor rather than a re-read | one `read_text` per failed anchor, against 102 measured occurrences | switching the edit format to unified diff is refused: measured worst of the three in 2026 (Pass@1 -2.2 and -4.0) and the most tokens, and it would give up the line-drift immunity symbol addressing exists for |
| **I422** [round trip] proving a behavioural edit costs edit then `build` then `run_tests`, because `EditGate` returns compiler errors only. FeedbackEval measures compiler feedback alone repairing 49.2%, test feedback 57.9% and mixed compiler-plus-test 63.6%, with the marginal gain of further iterations gone after two or three - so this server hands the agent the measured-weakest single signal | `EditGate`, `replace_symbol_body`, `replace_symbol`, `add_member` | `verify=tests` on the symbol writers: once the compile gate passes, run the AFFECTED project's tests and return the failures in the same response | 2 calls and one measured 6.1 s model gap per behavioural edit, and it moves the edit signal from 49.2% to 63.6% repair on the published numbers | running tests on every edit is refused - `run_tests` means 85 s and a 16-minute p99, so this is opt-in and scoped to the affected project only |
| **I423** [payload] every compression path here is tuned against redundancy - outline instead of file text, minimum success response, dedent - but SWE-Explore measures the opposite asymmetry: agents reach about 65% file-level hit rate and only 15-19% line-level recall, and synthetically REMOVING core evidence collapses resolve rate while padding with irrelevant regions barely moves it | `read_text`, `get_file_outline`, `ResponseBuilder` | audit every truncation path for a SILENT drop - an outline returned in place of file text should name the members it did not print, and every clipped listing should name the axis it clipped, not only the count | no direct token saving: the row prices the measured-more-expensive of the two errors, which nothing in the current budget assertions can see | returning file text by default is refused - measured about 3x the tokens of the outline, and the outline is almost always the question |
| **I424** [unanswerable] `search_symbols` and `search_text` return records in discovery order with no ranking layer, so a truncated listing is truncated arbitrarily. Code Isn't Memory (91 instances, Claude Opus 4.7, 3 seeds) measures a graph-plus-vector-plus-BM25 index at 84.5% acc@5 against 44.3% with the index off and 75.3% for agentic grep, at 10.1k against 14.0k mean tokens and $2.30 against $2.92 per solve, and CODESTRUCT's ablation costs -7.8 Pass@1 AND raises token use when structure-aware read is removed | `search_symbols`, `search_text`, `find_usages` | rank records by an identifier-level BM25 score before the cap applies, so the first 20 are the likely 20 | the widened re-query after a truncated listing: the measured line-level recall problem is a ranking problem, not a capacity one | embeddings are refused - they add a model dependency this server does not have, and grep-first baselines match them on this workload |
| **I425** [failure] the literal `next: ` steer that names the widening call appears in only 2 of the 14 tool source files, so most truncated answers end at a count. MCP-Atlas measures nearly two thirds of diagnosed agent failures as COGNITIVE - the agent enters the right evidence-gathering regime and stops before covering every claim - and Help or Hurdle measured MCP integration degrading six LLMs by 9.5% on average, which is what a partial answer that does not name its own completion produces | `ResponseBuilder`, every listing tool | census-gate it: every response carrying a truncation marker must also carry a `next:` naming the exact call that widens it, discovered from `tools/list` the way the other census gates are | the re-query the agent currently has to invent, on every truncated listing the server returns | raising the caps is refused: a cap that bounds the response is the point, and I412 already closed the one case where a batch cap hid a cheaper whole-set call |
| **I426** [payload] the dominant cost is the accumulated transcript, not any single response, and nothing this server returns helps a client prune it. arXiv 2606.10209 (50-task tool-use benchmark, cross-checked on Claude Sonnet 4.5) measures full-history retention at 71.0% completion, 1 480 996 tokens and 14.56 h; pruning to the last 5 tool calls at 79.0%, 535 274 tokens and 5.39 h - 64% fewer tokens and 8pp MORE accurate; pruning plus summarization at 91.6% and 553 374 tokens | `ResponseBuilder` | end every response with the exact call that reproduces it, so a pruning client can drop the body and keep one line without losing the ability to re-derive it | the -64% the pruning study measures is unavailable to any client of this server today, because no response says how to get itself back | returning payloads as MCP resource links is refused: the client fetches them back and pays twice |
| **I427** [payload] `ToolExamples` reaches the agent ONLY inside the `remedy:` of an already-rejected call, so the example is paid for AFTER the failure rather than spent preventing it - while Anthropic measures Tool Use Examples lifting complex-parameter accuracy from 72% to 90% with 1-5 realistic examples, recommended exactly where the schema alone is ambiguous | `ToolExamples`, the `[Description]` of the ambiguous-parameter tools | promote the examples of the few tools with the most `InvalidArgument` history into their advertised description, priced against this repo's own ~255-token-per-tool budget | one rejected call per ambiguous-parameter tool, against a measured 18pp accuracy gap on complex parameters | putting examples on all 88 tools is refused - that is the ~22 400-token prefix I414 exists to cut |
| **I428** [failure] the allocation gate mandates pre-sizing the one collection you must build and the style gate mandates collection expressions over `new List<T>()`, so pre-sizing today forces the banned form and the agent has to reason about the contradiction at every builder. C# 15 collection expression arguments resolve it - `[with(capacity: n), .. items]` - shipping in the .NET 11 SDK behind `LangVersion=preview` | `Directory.Build.props`, the style and allocation sections of CLAUDE.md | adopt `with(...)` on the pre-sized builders when the SDK pin moves, and record the resolution in the style section so the contradiction stops being re-derived | one standing exemption the agent currently reasons about on every pre-sized collection | adopting it now is refused: `global.json` pins 10.0.300 and it needs the .NET 11 SDK plus `LangVersion=preview`, which CI does not have on three runners |
| **I429** [round trip] the async hard gate is argued on correctness alone and has no measured payoff recorded, while .NET 11 Preview 5 measures runtime-async resuming an on-stack-replaced method directly into optimized code at 6 357.1 ms down to 457.1 ms on a suspension-heavy benchmark, about 10-20x less transition overhead - on a server that is one stdio client, entirely async by gate, against a fixed 60 s handshake ceiling | `BannedApiTests`, `McpHost`, the async gate | measure cold start and one wide `find_usages` on a .NET 11 preview runtime before the pin moves, and record the number beside the gate | unmeasured by construction: the row exists to replace a correctness argument with a number, the way every other accepted improvement here had to | moving the SDK pin now is refused - .NET 11 is preview and CI runs three OSes, one of which already misses the 60 s handshake ceiling cold |
