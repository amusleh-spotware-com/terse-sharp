# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I386** [failure] `get_symbol_source symbolIds=` answers `NOT_RESOLVED <id>` with no nearest-name suggestion, while the singular path's `SymbolNotFound` names them. Measured this run: `symbolIds=["NavigationTools.FileOutline", "NavigationTools.TypeOutline"]` answered two bare `NOT_RESOLVED` lines for members really called `GetFileOutline`/`GetTypeOutline`, and recovering them cost 2 further `read_text` line-range calls | `get_symbol_source`, `SourceService` | give each `NOT_RESOLVED` entry the same nearest-name list `Errors.SymbolNotFound` already computes, so a near-miss id is one call rather than three | 2 calls and ~1 100 tokens per near-miss in a batch; the batch path is the one that cannot fall through to the singular remedy | — |
| **I387** [payload] `search_symbols` ranks by name match with no way to scope to a file, so a common helper name buries the one declaration wanted. Measured this run: `search_symbols query="Report" scope=test` returned `50/168 symbols truncated` (~1 900 tokens) and not one of them was the `TokenBudgetE2ETests.Report` helper being looked for; `get_file_outline contains="Report"` answered it next call for ~120 | `search_symbols` | take `path=` the way `get_symbol`, `get_symbol_source` and `get_type_outline` already do - resolve inside that document first and fall back to the solution only when it holds no match | ~1 800 tokens and one call per common-name lookup; the parameter already exists on three sibling tools, so it costs no new concept | — |
| **I388** [unanswerable] nothing answers what `tools/list` costs without an MCP session. Measured this run: converging three token budgets needed the advertised total, `workspace_status` prints it only in-session, and the one-shot `terse call workspace_status --workspace <sln> --json '{"verbose":true}'` probe returned every self-check line and no `advertised=` at all - so the ~3 s probe I383 shipped as a steer cannot answer the very number the budget tests assert | `workspace_status`, `terse call` | have the one-shot `call` path build the advertised surface the way the stdio filter does and print `advertised=<n> tools <t> tokens` with its verbose split, so the budget number is measurable in ~3 s instead of a ~90 s `build` + `run_tests` cycle | ~90 s per budget iteration, on every release that touches a description; measured at 3 iterations this run alone | printing it unconditionally from a shared cache - the number is a property of the session's filter, not of the workspace, so a cached value would be a confidently wrong answer |
