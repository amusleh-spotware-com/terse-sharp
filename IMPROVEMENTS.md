# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I647** [failures] a trailing note is appended with no line break: `replace_symbol verbose=true` answered `changedLines=412 replace_symbol calls in a row - ...` and `rerun_failed` answered `... warnings=0repeat #3 of this exact rerun_failed call ...` - the steer and the identical-call note glue onto the last payload line, so `changedLines=41` reads as `412` | `ResponseBuilder` · `RepeatSteer` · `IdenticalCall` | append every trailing framing line through one helper that guarantees a preceding `\n`, and assert in a census test that no advertised tool's response ends a payload line and starts a note on the same line | removes a misread number per occurrence; seen 4 times in this run | — |
| **I648** [accuracy] the repeat-steer counter is process-wide, not per client: a planner subagent's `replace_symbol dryRun` made the main thread's next `replace_symbol` "2 replace_symbol calls in a row - these are ONE call: symbolIds=[\"SymbolEditService.Rewritten\", ...]", naming an id the main thread never sent | `RepeatSteer` | key the run counter by MCP session (the stdio client id / request context), so concurrent agents sharing one server never steer each other | one misleading steer per interleaved agent call; 12 concurrent planners shared one server in this run | — |
| **I649** [round trips] `edit_text row="I609" toPath=` was refused `matches the first cell of 2 table rows` because I644's cell mentions I609; the id had to be re-sent as `row="**I609** [failures]"` | `edit_text` | match `row=` first against the leading bold token of the first cell (`**I609**`) and fall back to substring only when no row starts with it | one refused call plus a re-sent 4-row payload per collision | — |
| **I650** [round trips] `rerun_failed` answered `no failing test is remembered` after a narrower `run_tests test=` (green) overwrote the remembered failures of the wider red run it was meant to re-verify | `rerun_failed` · `LastTestRun` | keep the failure list of the last RED run until a run covering those tests is green, instead of replacing it with any later run's result | one full re-run of the red suite per occurrence | — |
| **I651** [accuracy] `replace_symbol` on a record whose sent declaration was the positional header ending in `;` silently dropped the record body's 5 members; only the compile gate's `CS1061` rollback revealed it, where `write_text` warns `this overwrite drops N declaration(s)` | `replace_symbol` · `DroppedDeclarations` | run the same dropped-declaration check on a replaced TYPE and print `WARNING this replace drops N member(s): ...` (or keep the body, as the bodiless-header re-head already does for a class) | one rejected call plus the re-send of the whole type per occurrence | — |
| **I652** [payload] `add_member` inserted nested collection-initializer lines at the wrong column (`new[] {` then entries at the outer indent and `},` dedented) in E2E test bodies, and `format` left them | `add_member` · `SymbolEditService` | re-indent the inserted declaration as one block relative to its first line before splicing, the way I633 now keeps a parameter list's layout | a manual re-format or review noise per multi-line initializer | — |
| **I653** [failures] concurrent E2E servers leave the shared fixture dirty: `RenameEcho.cs` survived its own `finally` delete and `Order.cs` kept a `using System.Text;` after a green run, then pushed `gate solution=true` past its 900-token budget in another collection; isolated reruns stayed clean | `LoadedWorkspace.TryApplyAsync` · E2E fixtures | find which server re-writes an absorbed document after an external delete (memory: TryApplyChanges writes absorbed text) and make it re-read disk instead; add a census assertion that the fixture tree is clean after every collection | one false-red budget test and a manual restore per occurrence | — |
| **I654** [payload] every end-of-task `gate`/`analyze baseRef=` over this repo's test files lists ~100 `TERSE107` rows because the repo's own `.terse.json` naming pattern `^[A-Z][A-Za-z0-9]*$` contradicts its `Method_Scenario_Outcome` test convention; this run's final gate printed 120 records of which 1 was actionable | `.terse.json` · `PolicyService` | give test methods their own naming pattern (or exempt `[Fact]`/`[Theory]` methods from `TERSE107` by default) so the sweep reports what a reviewer would act on | ~5 000 tokens per end-of-task sweep on this repo | raising the severity floor: the one actionable `IDE0037` was info too |
| **I655** [unanswerable] the one-shot probe cannot measure a per-call cost: `terse call find_implementations` took 11.8 s before and 6.9 s after I605 while the no-usages control moved 12.9 s to 6.7 s - load time dominates and no tool reports load vs call time | `terse call` · `Program` | print `loadMs=` and `callMs=` on stderr (or a `--timing` flag) so a before/after of one tool is a measurement, not a guess | turns every "structural, unmeasured" saving into a number | — |
| **I656** [accuracy] `get_file_outline contains="Pressure"` answered `0 of 45 members` for `WorkspaceRegistry.cs` although `PressureBytes` exists (a planner's report, a const field in a names-only outline) | `get_file_outline` | apply `contains=` to fields and constants in the names-only view as well | one false-empty answer and a fallback read per occurrence | — |
| **I657** [failures] `get_symbol_source BuildTools..ctor` answered `SaturatedName` for a primary constructor; the planner fell back to `read_text` of the class header | `get_symbol_source` · `SymbolLookup` | resolve `Type..ctor` / `Type.#ctor` to a primary constructor's parameter list (the type header) | one refused call plus a ranged read per primary-constructor question | — |
| **I658** [accuracy] `analyze ids=TERSE107` shows the TERSE107 records AND `NOT_ENABLED TERSE107` in one answer, because policy findings are not analyzer descriptors (found while planning I606) | `analyze` · `ProjectDiagnostics.Unsupported` | treat an id the active policy can emit as enabled, the way compiler and `TERSE001` ids already are | removes a self-contradicting answer | — |
| **I659** [speed] after an idle drop the next `search_symbols` re-realizes every compilation (46 of 84 slow warm calls in the I642 scan carried the realization note), and 38 slow warm calls carried no note at all | `WorkspaceRegistry` · `SymbolSearch` | profile the 38 unexplained slow warm calls, and consider re-warming a dropped workspace in the background only when heap pressure has cleared | up to ~1 h a week of `search_symbols` wall time (I642's measurement) | answering from syntax (I642, refuted: generated declarations and the bound id format need compilations) |
| **I660** [correctness] `terse uninstall` removes only the `mcpServers` entry and leaves both the `PreToolUse` guard and the new `PostToolBatch` hook in `settings.json` (a planner's finding while building I643) | `ClientRegistrar` · `terse uninstall` | remove every hook entry whose command starts with `terse guard` on uninstall, keeping every other hook | a stale hook calling a removed binary on every tool call after uninstall | — |
| **I661** [test-coverage] `delete_symbol symbolIds=` excludes a reference that sits inside another deleted member, but FixtureSolution has no member referenced only by another unused member, so that exclusion is untested end to end | `delete_symbol` · FixtureSolution | add a private helper called only by an unused member to the fixture (checking no count-asserting test moves) and a test deleting both in one batch | locks the I608 exclusion | — |
| **I662** [round trips] this run's mechanical cost: ~200 main-thread calls for 30 rows (~6.7 per row) plus ~450 planner calls; the largest avoidable share was per-row E2E slices (9 slice runs of 30-400 s) that the final whole-suite run repeats, and SKILL.md token-budget trimming that cost 5 edit/test rounds because the budget is only checked by a test | `/ship-improvements` · `DocsCoverageE2ETests` | have `workspace_status tools=true` (or `read_text tokens=true`) report SKILL.md against its budget so the trim is sized in one call, and batch the per-wave E2E slices into one `projects=`/`tests=` run per wave | ~5 calls and 2-3 slice runs per release run | — |
| **I663** [diagnosability] the v0.69.0 release run (36233328021) failed on `RunTests_RepeatedAfterAnExternalEditTheWatcherHasNotDrained_RunsAgainInsteadOfReplayingStale`: its second identical `run_tests` answered PASSED, not UNCHANGED, and the reason - which stamp component moved (pulse, loaded, LoadedUtc or generations) - is only rendered when `TERSE_UNCHANGED_TRACE=1`, which CI does not set, so the neighbour that moved it could not be named and the fix had to settle the primer instead | `run_tests` · `UnchangedRun.MissReason` | when a repeat of a key already remembered is re-run instead of replayed, append one line naming the moved stamp component (`NOTE re-ran: stamp moved generations 41->42`), unconditionally | one diagnosis round (~6 calls) per replay miss, and CI flakes become attributable from the log alone | — |
| **I664** [fallback] the guard stripped `gh run view … --log-failed \| grep -A 25 "…\[FAIL\]" \| cut -c 60- \| head -40`, although grep reads the gh pipe and names no file - the `-A` option argument or the quoted pattern was taken as a path operand inside the workspace | `ToolGuard` (`ReadsStdin` / `NamesAFile`) | skip the option arguments of `-A`/`-B`/`-C`/`-m`/`-e`/`-f` and the first non-option operand (the pattern) before deciding a fed grep names a file | 2 calls plus a scratch-file detour per CI log triage | — |
| **I665** [fallback] the guard stripped `wc -l "$S/release-fail.log"` where `S` was assigned a scratchpad path earlier in the same command: an unexpanded `$VAR/…` operand is resolved against the cwd as a workspace-relative path | `ToolGuard.Inside` / `Expanded` | expand variables assigned earlier in the same command line, else treat an operand whose root is an unexpanded variable as unknown and allow it unless it names a .NET source extension | 1 call per scratch-file probe | — |
