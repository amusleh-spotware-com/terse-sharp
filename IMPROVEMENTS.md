# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I501** [speed] `read_text` takes ONE line range, so reading four separate anchors out of one file is four calls. Measured this run on `src/TerseSharp.Server/Assets/SKILL.md`: lines 42, 101-102, 167-168 and 306-316 were four `read_text` calls, ~4 model gaps and ~24 s, purely to copy four `oldText` anchors. `paths=` cannot express it - it is one file - and `section=` cannot either, because three of the four anchors are table rows inside one enormous section | `read_text` | take `ranges=["42", "101-102", "167-168", "306-316"]`, rendered under one path with the existing gutter that already prints a number only where the numbering jumps, so a discontinuous read is one call. `columns=` and `headings=` stay refused beside it, exactly as they are refused beside `startLine=` today | 3 calls and ~18 s of model gap per anchor-gathering pass; every docs-gate task in this repository does one, and this run did it three times | raising `maxLines` to read the whole file instead is refused - a 1 150-line `SKILL.md` is ~30 000 tokens against ~400 for four ranges |
| **I502** [speed] a `retryWith` token holds exactly the batch that was rejected, and there is no way to ADD a pair to it. Measured this run: a 4-entry `replace_symbol` batch was rolled back `CS7036` because the caller `FileTools.EditText` also had to change; the remedy correctly named that caller, but adding it meant re-sending all four held declarations plus the new one - so the token saved nothing and the recovery cost ~3 100 characters. The workaround used was an `edit_text force=true` on the caller first, which is not compile-gated and is therefore strictly worse | `replace_symbol`, `add_member` | let `retryWith=` take `add`-shaped extra pairs - `symbolIds=[...] declarations=[...]` beside a token APPENDS to what the token holds rather than replacing it, the way `fix=` already edits it in place. The `CS7036` remedy already names the callers, so the token plus those callers is the whole call | the whole held payload per signature-change rollback; `CompileRegression` on a caller is the single most common rejection class this repo logs | making a bare `symbolIds=` beside a token append rather than override is refused - overriding is how a mis-typed id is corrected today, and silently changing that meaning would break the documented recovery |
| **I503** [accuracy] `fix=["<index>=<declaration>"]` corrects held `declarations=` entries only, and a rejection also holds `add=` and `usings=`. Measured this run: a 6-entry batch was refused `InvalidArgument: the declaration did not parse` for a `"` inside an interpolation hole in ONE `add=` entry; `fix=` could not name it, so the retry re-sent all four `add=` entries - ~1 400 characters - to correct one | `replace_symbol`, `add_member` | let `fix=` address the held `add=` array too, e.g. `fix=["add:1=<corrected>"]`, and have the parse rejection print the index in that same namespace so the correction is copyable from the failure | ~1 200 characters per malformed-`add` retry; the parse rejection already names the offset, so the only thing missing is the addressable index | a second `fixAdd=` parameter is refused - one addressing scheme, prefixed, keeps the schema flat |
| **I504** [cost] `parameterDescriptions` is the largest single component of the advertised surface and has never been measured on its own: `workspace_status verbose=true` reports `advertised=88 tools 29647 tokens` split `toolDescriptions=10194 parameterDescriptions=11578 schemaFrame=7581 names=296`, so the per-parameter prose is 39% of every request's tool frame - more than the tool descriptions I203 already measured and refused to compress. Carried over from **I497**, which shipped the razor half and named this as its NOT-done | `ToolProfile`, every `[Description]` on a parameter | measure it before changing it: report the per-TOOL parameter-description cost from `workspace_status verbose=true` so the ten worst are visible, then decide per tool whether a parameter's prose is doing work an example would do better | unknown until measured, which is the point; the total at risk is 11 578 tokens per request - re-measured 2026-09-12 at 78 advertised tools / 28 021 tokens split `toolDescriptions=9700 parameterDescriptions=11152 schemaFrame=6914 names=257`, so the SCHEMA half (parameters + frame) is 64% and the prose half 35%; but Claude Code's own docs now state MCP tool definitions are DEFERRED by default - "only tool names and server instructions enter context until Claude uses a specific tool" - so "per request" is the premise to verify before compressing anything | compressing every parameter description across the board is refused up front - I203 measured that class on tool descriptions at 11.8% and refused it, and a parameter whose description is the only place a valid value is named cannot be cut at all |
| **I505** [perf] `RenameService.WithTextAsync` materializes every changed document's whole text before checking whether the old name occurs in it at all - `var text = source.ToString();` at `src/TerseSharp.Core/RenameService.cs:124`. Every document in `changed` is by definition renamed, so its `SourceText` is a `ChangedText` whose `ToString()` builds a fresh string; a rename touching 50 documents copies all 50. Found by the I493 review round | `rename_symbol` | walk `syntax.DescendantTrivia()` and the string-literal tokens directly - the set `Carrier` classifies anyway - so nothing but the matched trivia and literals is materialized, and the line number comes from `source.Lines.GetLinePosition(token.SpanStart)` as it does now | one whole-file string per changed document per rename, on a path this repo's allocation gate governs explicitly; 23 `rename_symbol` calls in the measured window, so the total is small but the shape is the banned one | leaving it is refused only if the token walk proves slower - measure both before switching, because `DescendantTrivia()` over a large file is not free either |
| **I506** [perf] `TextSearchService.Folds` runs a full pass per candidate depth, each allocating a `Dictionary<string, int>` plus its alternate lookup, and on a listing where no depth qualifies it runs all four - `src/TerseSharp.Core/TextSearchService.cs:1015-1038`. It early-exits on the first qualifying depth, so the common case is one pass, but the worst case is 4xN `Prefix` computations on a listing that may be thousands of files. Found by the I487 review round | `find_files` | compute every candidate depth's prefix counts in ONE pass over the paths - a `Dictionary<(depth, prefix), int>` or four parallel dictionaries filled together - so the steer costs one walk whatever depth wins | 3 of 4 passes on the worst case, on the first orientation call of a task; unmeasured, which is why it is a row rather than a fix | dropping `MaxFoldDepth` to 2 is refused - the measured useful fold in this repo was `depth=2`, so 2 must remain reachable |
| **I507** [accuracy] `edit_text edits=` groups entries with `StringComparer.OrdinalIgnoreCase`, so on a case-sensitive filesystem `edits=[{path:"a.md"},{path:"A.md"}]` collapses two DISTINCT files into one group, applies both edits to whichever spelling arrived first, and answers one line as though one file changed. Pre-existing - HEAD keyed the raw string the same way and I490 only widened the key to the resolved path - and surfaced by the I490 review round | `edit_text`, `FileTools.Collect` | key the group with the platform's own comparison: `OrdinalIgnoreCase` on Windows and macOS, `Ordinal` elsewhere, from a single `PathComparison` helper the writers share, so two spellings are one file exactly where the filesystem says they are | (accuracy) a silent wrong write on Linux and macOS, which is two of the three CI legs; no measured occurrence, because the shape needs a caller that names one file two ways | a blanket `Ordinal` is refused - it would split `C:\Repo\a.md` from `c:\repo\a.md` on Windows, where they ARE one file, which is the bug I490 just fixed |
| **I508** [accuracy] the guard's text-tool denial names the wrong reason for a path OUTSIDE every workspace. Measured live this run: `cat C:/Users/.../Temp/claude/.../tasks/bc9yb2rt8.output` from a repo cwd was denied with `Bash on 'cat ...' is C#/.NET source`, which is false - the file is a background task's stdout in the OS temp directory. The denial itself is by design (`IsDotNetTree(cwd) && Operanded(segment)` denies shell text tools anywhere under a .NET tree's cwd) and `read_text` answered it in one call, so only the REASON is wrong | `ToolGuard`, `Direct`, `Reason` | when the operand resolves outside every loaded workspace root, say so - `the cwd is a .NET tree, so shell text tools are denied here; this path is outside the workspace and read_text takes it` - instead of asserting the file is C#/.NET source | (accuracy) one wrong claim per denial of an out-of-tree operand; the agent is told the file is something it demonstrably is not, which is the confident-wrong-answer class this project exists to remove | allowing the command when the operand is outside the tree is refused - the cwd rule is what stops `cat`/`head` on a repo file reached by a relative path, and an operand check cannot see through a shell variable |
| **I509** [accuracy] `cleanup` refuses `paths=` - **30 `InvalidArgument` rejections in the window, the single most-refused parameter of any tool** - while `analyze` and `format` both take `paths=` (10 entries). The consequence is measured twice over: 101 `cleanup` calls sat in runs of >=3 consecutive calls (longest 10), and **0 of 219** `cleanup` responses carried the run-batching steer, because that steer names a plural parameter this tool does not declare | `cleanup` | declare `paths=` exactly as `analyze` and `format` do, so the end-of-task sweep over a task's touched files is one call and the run steer has something to name | 30 rejected round trips at 5 961 ms each plus ~67 calls deleted = **~0.16 h/week**; (a) | aliasing `paths=` onto `path=` and silently taking the first entry is refused - a batch argument that answers about one file is the confident-wrong-answer class this project exists to remove |
| **I510** [speed] the bare-`sleep` guard row does not cover PowerShell, and the waiting moved there - **126 `Start-Sleep`-bearing `Bash` calls, 117 with no `while`/`until`/`for` guard, 0.90 h of wall clock**, against 6 bare bash `sleep`s and 0.83 h in the same window. Third destination of one class: I379 closed bash `sleep`, I494 measured it reappearing as `TaskOutput` | `ToolGuard` | add `Start-Sleep` to the sleep row's command words and match it inside a `powershell -Command` string, with the same loop carve-out the bash row already has | **0.90 h/week**; (d) | denying `powershell` wholesale is refused - 163 `Get-Process`, 93 `Stop-Process` and 71 `Get-CimInstance` calls in the same window are the lock-holder investigation `build`'s own `holder pid=` lines send the agent to do, and no tool replaces them |
| **I511** [speed] `TaskOutput` is now **22.48 h = 29.7% of ALL tool wall time** over 272 calls, p50 230 s with p90 and p99 both pinned at the 600 s ceiling - I494 measured this class at 14.08 h/13.0% and shipped documentation only, and it has grown **60%** since. Of the 272, **65 were issued with no completion notification since the task was launched** and 207 followed one (HEURISTIC - the notification is matched in the transcript, not in the hook payload) | `ToolGuard` | the `PreToolUse` hook sees every tool, not only `Bash`: deny a `TaskOutput` for a task that has not notified, exactly as the sleep row denies a wait, and say `end the turn - the completion notification re-invokes you` | 65 calls at p50 230 s = **~4.2 h/week**; (d) | denying `TaskOutput` outright stays refused as I494 decided - 207 of 272 followed a real notification and are a legitimate status read; and if the hook payload carries no task state this degrades to a `SKILL.md` line rather than a guard row |
| **I512** [accuracy] `delete_symbol` declares neither `allowErrors=` nor `symbolIds=`, and the agent assumes both - **19 of 101 calls rejected = 18.8%, the highest rejection rate of any edit tool** (`edit_text` 2.7%, `write_text` 3.8%, `add_member` 3.9%, `replace_symbol` 4.7%); 11 rejections were `unrecognized allowErrors` and 3 `unrecognized symbolIds`, so **14 of the 19** named parameters its siblings declare. One session issued a run of **16 consecutive** `delete_symbol` calls | `delete_symbol` | declare `allowErrors` and `symbolIds=` the way `replace_symbol` does, applying the batch as one compile-gated edit | 14 rejected round trips plus a 16-call run collapsing to one edit = **~0.03 h/week**; (a) | - |
| **I513** [speed] the run-batching steer rides on **7.1% of `edit_text` responses** (156 of 2 204) and names a parameter that cannot express the run it steers: **496 `edit_text` and 103 `write_text` calls sat in runs of >=3 (longest 10 and 6), and 439 of the 496 crossed DIFFERENT paths** - which `edits=[{path, oldText, newText}, ...]` (25 entries) and `files=[{path, content}]` (10) already serve today. `write_text` steers at 21.0%, `replace_symbol` at 14.2%, and `search_symbols`, `changed_files`, `add_member`, `find_usages` and `cleanup` at 0.0% | `edit_text`, `write_text`, `ResponseBuilder` | fire the run steer on a CROSS-file run too, naming the per-entry shape rather than the same-file one, and say the entry may carry its own `path` | 599 calls in runs = **~0.99 h of model gap per week**; (b) | filling the arguments in is refused for these two - the steer can only fill in short identifier arguments, and an `oldText` cannot be recovered from a call already answered |
| **I514** [perf] the compile gate already builds a compilation over the changed declaration and throws its ANALYZER diagnostics away, so every quality rule costs a separate `analyze` round trip - p50 **5 751 ms** plus **5 961 ms** of model gap = ~11.7 s, over 271 calls of which **254 were whole-solution**. Instrument (d): `CLAUDE.md` states "Analyzers are NOT run on an edit, so a CA/IDE rule still needs `analyze`", `workspace_status verbose=true` reports the gate as semantic-only, and the rules that came back red AFTER an edit in the same session were CA1859x22, IDE0060x15, CA2012x7, CA1816x5, CA2000x4, IDE0042x4 across **189 post-edit gate reds** | `EditGate`, every mutating tool | report the info-severity ANALYZER diagnostics the changed declaration introduced, beside the `info=N introduced` the gate already reports for compiler diagnostics | one `analyze` round trip deleted per edit that would have reported one, at **~11.7 s** each; (a). Corroborated: an analyzer feedback loop at the write cut quality findings from >50% to 11% and raised functional correctness ~10.7 pts (arXiv:2508.14419, 2025-08-20), and models self-detect their own defects at F1 <0.3 but repair 62-89% when handed the report (arXiv:2412.14841, 2024-12-19) | running the full referenced analyzer set on every edit is refused - that is `analyze`'s own 7.0 s cost and would make the edit slower than the round trip it saves; scope it to diagnostics whose span falls inside the changed declaration |
| **I515** [accuracy] `CLAUDE.md` and `SKILL.md` both teach "48% of `run_tests` calls and 75% of `build` calls in a measured week were byte-identical repeats", and a mutation-aware instrument refutes it: splitting every identical repeat by whether ANY mutating call landed in between gives **`run_tests` 36 provably-redundant of 594 repeats (6.1%) and `build` 20 of 201 (10.0%)** - 58 stale against 893 that legitimately followed an edit. The old figures counted argument-identical calls only, so a correct re-run after an edit was scored as waste | `CLAUDE.md`, `SKILL.md` | replace both figures with the stale/after-an-edit split and name the instrument, so the ladder argues from what is actually redundant | (accuracy) the standing instructions currently overstate the waste by **8x** on `run_tests` and **7.5x** on `build`, teaching the agent to distrust verification tools that already memoize | deleting the sentence is refused - the ladder's ordering argument is unaffected and still correct; only the number is wrong |
| **I516** [cost] parameter-name guessing costs one round trip apiece while the `remedy:` already names the right answer - **218 `InvalidArgument` in the window**; beyond I509's 30 and I512's 14 the measured guesses are `add_member code=` 9, `replace_symbol content=` 7, `edit_text find=`/`replace=` 7, `find_registrations symbolId=` 6, `search_text globs=` 3 and `head_limit=` 3, `run_tests run_in_background=` 4 and `contains=` 2, `list_tests filter=` 3, `resx_usages path=` 3, `read_text maxBytes=` 2, `diff_text maxResults=` 2, `replace_symbol_body declaration=` 2, `xaml_validate paths=` 2 - ~48 calls, each one 5 961 ms of model gap plus the whole re-paid argument | `ToolArgumentFilter` | accept as aliases ONLY the guesses whose concept the tool really has - `declaration`->`declarations`, `symbolId`->`query` on `find_registrations` (which already aliases `symbol` and `name`), `globs`->`glob`, `filter`->`contains` on `list_tests`, `maxResults`->`maxLines` on `diff_text` - and leave the rest to the remedy | ~48 round trips = **~0.08 h/week** plus the re-paid arguments; (a) | aliasing every measured guess is refused - `run_in_background` and `code` name concepts these tools do not have, and inventing them would answer a question the caller did not ask |
| **I517** [accuracy] three token-budget numbers shipped in `README.md` and `NUGET_README.md` - a `29,700-token ceiling over 88 tools`, `57 tools, ≤22,650 tokens`, `64 tools, ≤23,280 tokens` - are asserted nowhere: a literal sweep of the whole tree matched only the two docs, while the real asserted ceilings are 24,600 (`MarkupProfileE2ETests`) and 25,400 (`ToolSettingsE2ETests`) and the full surface has no ceiling at all. A from-scratch rewrite carried all three forward before a manual sweep caught them | `DocsCoverageE2ETests` | census-gate the shipped docs the way the tool list already is: discover every `≤N tokens` / `N-token ceiling` claim from the README text itself and fail when no `Assert` under `tests/` names that number | one false public claim per docs rewrite - three were live on the repo front page and on nuget.org, each costing a `search_regex` sweep to disprove | — |
| **I518** [accuracy] `terse serve --help` contradicts the code on the default tool profile: `Program.cs` describes `--tools` as "core (default) ... an unrecognised value falls back to core", while `ToolProfile.Resolve` returns `new(null, MarkupDerived: true)` for null, empty **and** unrecognised - the whole markup-derived surface, never `core`. A user who trusts `--help` believes 21 tools are advertised when 88 are | `ToolProfile`, `Program.cs` | correct the option description to say the default is the markup-derived surface and that an unrecognised value falls back to it, and census-gate it: assert the `--tools` help text names the profile `Resolve(null, null)` actually returns | one wrong belief about the whole advertised surface, held from the first `--help` a user reads; found only because a docs review cross-checked the README against `Resolve` | — |


