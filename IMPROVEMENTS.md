# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I595** [failures] `write_text path=... content=...` silently OVERWROTE two files that already existed in this run - `fixtures/.../WideSurface.cs` (45 members, referenced by four tests) and `tests/.../ResultCapTests.cs` (4 tests) - because nothing in the request or the response says the path was occupied. The first cost a 50-failure, 10-minute whole-suite run plus 8 repair calls before the cause was visible; the second was caught only by reading the diff the tool printed | `write_text` | when a single `path=` write REPLACES an existing file, say so in the one-line success (`overwrote existing  <path>  changedLines=N`), and refuse a write whose content shares no line with the file it replaces unless `force=true` or `overwrite=true` - a wholesale replacement of an unrelated file is the shape that costs a suite run | one whole-suite round trip plus the repair calls per occurrence; it happened TWICE in one session, both times because the agent believed it was creating a new file | a `create=true` flag the caller must remember: the failure is precisely that the caller did not know the file existed, so the check has to be on the server side |
| **I609** [failures] `read_text path=... lines=40-200` was rejected `unrecognized lines`; the parameter pair is `startLine`/`endLine`. The rejection named the whole accepted set and the retry landed, so the cost was one call - but `lines=` is the spelling the tool's own steers use in prose (`raise maxChars`, `a line range`) and the one an agent reaches for first; re-measured 2026-09-26: 2 more `unrecognized lines` refusals in one week | `read_text` | accept `lines="40-200"` as an alias that splits into `startLine`/`endLine`, or name `startLine=`/`endLine=` in the `path` description so the schema teaches it without a rejection | one rejected call per session that reads a range, which is most of them | adding it to `ranges=`: that takes a list of ranges and is the multi-range form, so a single `lines=` still has to resolve to the scalar pair |
| **I605** [payload] `find_implementations` and `get_symbol usages=true` on a METHOD now pay two whole-solution scans - `SymbolFinder.FindImplementationsAsync` plus `FindOverridesAsync` - where they paid one; the second is what made a base-class member answer its overrides at all (**I600**), but its cost was never measured, and `ExploreService.ReachAsync` pays it on every `get_symbol usages=true` as well, on the 148-project solution this targets | `find_implementations` · `get_symbol` · `SymbolDerivation` | measure both calls on a wide member, then skip `FindOverridesAsync` when the symbol can have no overrides - not `virtual`, `abstract` or `override`, or declared in a sealed type - which is the common case and decidable from the symbol alone | one whole-solution reference scan per call on every non-virtual member, which is most of them | dropping `FindImplementationsAsync` for members and keeping only `FindOverridesAsync`: it is the one that resolves INTERFACE implementations, which was never broken |
| **I610** [latency] field report on a 148-project WPF solution: two symbol queries issued while a test build ran sat 3-4 minutes each, and the workspace was unloaded and reloaded twice for MSBuild locks; each reload threw the realized compilations away, so the next query paid realization again (47 s, against 119 s for the first) while competing with the build for CPU | `build` · `run_tests` · `WorkspaceRegistry` | keep the last realized `Solution` across the lock-retry unload/reload and answer read-only symbol queries from it tagged `snapshot=pre-build` until the reload finishes, or skip the unload when no holder the build names is this process | 47-119 s per symbol query issued during a build on a large solution | answering from a stale snapshot WITHOUT saying so: a confident answer about code the build may have changed is the defect this server exists to prevent |
| **I632** [failures] a suite that parses 148 projects needs more than any foreground call can wait for; the client moved 11 calls to background tasks and each cost a `TaskOutput` poll turn; re-measured 2026-09-26 over 176 transcripts: `run_tests` p90 **120 013 ms** and p99 122 730 ms sit on that ceiling, 17 `run_tests` calls answered `RunInFlight`, and `TaskOutput` still cost 24 calls / 0.98 h - the MCP 2026-07-28 release candidate adds `tools/call` task handles driven by `tasks/get`, the protocol form of this row | `run_tests` | a detached mode - `run_tests detach=true` answering a run id at once, and `rerun_failed`/`run_tests status=<id>` reading its verdict | one poll turn per long run, and runs past the client's 120 s foreground limit | — |
| **I634** [round trips] making a nested private record a namespace-level type cost `delete_symbol force allowErrors` + `add_member path=` - 2 calls and a transiently broken tree | `move_type_to_file` · `move_type_to_namespace` | let `move_type_to_namespace` lift a nested type to namespace level in the same file | 1 call and no broken intermediate state | — |
| **I615** [failures] the review subagent called `search_regex paths=[...]` and got `InvalidArgument` - `analyze`, `cleanup`, `format`, `read_text` and `get_file_outline` all take `paths=`, so the plural is the spelling an agent carries over; it re-issued with `glob=` | `search_regex` · `search_text` | accept `paths=` on both searches as a list of globs, OR-ed the way `find_files globs=` already does, so the batch spelling every sibling takes is not a refused call | one refused call per cross-tool habit, observed in a 22-call review | — |
| **I616** [failures] three consecutive `write_text delete=true recursive=true` calls on three `obj/` directories drew the run steer `pass files=[...] ... each entry is {path, content}`, but a `files=` entry cannot delete - the steer names a batch that cannot express the calls it is batching; six parallel `write_text ref=HEAD` restores drew the same steer, and `files=` is refused beside `ref=` too | `write_text` | either accept `delete`/`recursive`/`ref` on a `files=` entry, or suppress the run steer when the run's calls carried `delete=true` or `ref=` | one misleading steer per delete run, and a refused call for an agent that obeys it literally | — |
| **I640** [accuracy] 17 `read_text` calls answered `ERROR InvalidArgument: IOException: The process cannot access the file ...` in the 1-week window, and **11 `read_text` refusals were followed directly by a `Bash` call** - the terse read tool measurably losing its caller to the shell. `FileService.PresentFileAsync` reads with `File.ReadAllTextAsync`, which opens `FileShare.Read`, so any file a live process holds open for writing (a test log, a build output, a transcript) is unreadable - while `FileService.CharacterLengthAsync` and `TextSearchService.Options` already open `FileShare.ReadWrite` (confirmed by `search_regex containers=true`) | `read_text` · `FileService` | read through a `FileStream` opened `FileShare.ReadWrite \| FileShare.Delete` with `FileOptions.Asynchronous \| FileOptions.SequentialScan`, and map a residual sharing violation to its own code (`FileLocked`) whose remedy names `tail=` - it is not an argument error | 17 refused calls and 11 shell fallbacks a week | — |
| **I641** [accuracy] **22 `find_files` calls were refused `'globs' was passed with 'root', and a batch answers about the loaded workspace only`** in the 1-week window, and 15 `find_files` refusals were followed by another `find_files` retry; `find_files` carries a **7.1%** error rate (34 of 478). `root=` already answers ONE glob outside the workspace, so the refusal is an implementation gap the caller cannot predict from the schema | `find_files` | run the `globs=` batch against `root=` the way a single glob already runs there, OR-ing the globs and tagging the answer outside-workspace | 22 refused calls plus their retries a week | — |
| **I644** [accuracy] 20 `InvalidArgument ... unrecognized <parameter>` refusals in the 1-week window on spellings an agent carries over from a sibling tool or from `Grep`: `gate paths` 4, `get_type_outline typeName` 4, `load_workspace name` 3, `add_member code` 2, `replace_symbol content` 2 and `path` 1, `list_projects contains` 2, `search_text output_mode`/`-A`/`-C` 2 - each a re-issued call; `read_text lines` (**I609**) and the searches' `paths` (**I615**) are the same family and already open. Parameter mistakes cost more than a wrong tool: "task success rates drop by over 12%" and "agents tend to blindly trust the erroneous response" (arXiv 2506.21967) | `gate` · `get_type_outline` · `load_workspace` · `add_member` · `replace_symbol` · `list_projects` · `search_text` | bind each observed spelling as an alias of its canonical parameter - `gate paths=` as a path list, `typeName` to `symbol`, `load_workspace name` to `path`, `code`/`content` to `declaration`, `list_projects contains` to a name filter, `search_text -C` to `context` - and keep `InvalidArgument` for a name nothing maps | ~20 calls a week, plus the retries each refusal triggers | silently dropping an unknown parameter: a parameter mistake is the one an agent does not notice, so the refusal stays for true unknowns; every alias is a declared parameter, so `EveryAdvertisedSchema_DeclaresExactlyTheParametersItsToolMethodDeclares` stays green and each costs ~15 schema tokens |
