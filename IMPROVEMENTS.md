# Improvements backlog

Closed rows: [IMPROVEMENTS-ARCHIVE.md](IMPROVEMENTS-ARCHIVE.md).

## Open

| Finding | Tool | Proposed change | Expected saving | Rejected |
|---|---|---|---|---|
| **I372** [cost] a `replace_symbol symbolIds=[...] declarations=[...]` batch rejected for ONE unresolvable id costs the WHOLE payload. Measured twice this run: `ToolArgumentFilter.Unrecognized` answered `AmbiguousSymbol` (two overloads) and `T:TerseSharp.Server.FileTools.TextQuery` answered `SymbolNotFound` (one wrong namespace segment), and each time the other three declarations - **~2 400 and ~1 500 characters** - had to be re-sent verbatim. A `CompileRegression` holds the declarations behind a `retryWith` token; a RESOLUTION failure holds nothing | `replace_symbol`, `add_member`, `SymbolLookup` | issue a `retryWith` token for a resolution failure too, holding the declarations and pairing them by index, so the retry names the token plus the one corrected id - the mechanism and the 8-slot store already exist | ~2 000 characters of re-sent payload per mis-typed id, on the batch shape the repeat steer actively pushes callers towards | resolving the ambiguity by picking the first candidate - `AmbiguousSymbol` exists precisely because a wrong pick is undetectable |
| **I373** [accuracy] a `ToolGuard` denial of ONE segment of a compound `Bash` command rejects the WHOLE command, and the denial says so (`NO part of the command ran - re-issue the segments that are allowed`). Measured this run: `git fetch && git rev-parse && git pull && git log --oneline -1 && gh auth status` was denied for the `git log` segment alone, costing a full turn and a re-issue of four allowed segments | `ToolGuard` | when every denied segment has a replacement, put the rewritten command in `hookSpecificOutput.additionalContext` - `... && history maxResults=1` is not shell, so instead name the allowed prefix to re-issue and the tool call to make for the denied segment, rather than leaving the caller to split it | one turn per compound command containing a single denied segment; `cd X && <tool>` is the single most common `Bash` shape in the corpus (I368: `cd+grep` 213, `cd+tail` 144, `cd+cat` 101, `cd+ls` 85) | denying only the offending segment and running the rest: a `PreToolUse` hook can allow or deny, it cannot rewrite the command |
| **I374** [accuracy] `replace_symbol_body` answers `the body did not parse` with the remedy `pass a block starting with '{' or an expression body` for at least two causes it does not distinguish. Measured twice this run: (a) a block that DID start with `{` but was truncated mid-method, where the real fault was an unbalanced brace, and (b) a call targeting `ToolExamples.Worked`, a FIELD - `replace_symbol_body` has no body to replace there at all. Both got the same line, and the first read as a contradiction of what had just been sent | `replace_symbol_body`, `MemberDeclaration` | say which: report the unbalanced-brace position the way the array-parameter `InvalidArgument` now reports its offset (`I336`), and refuse a non-member target by KIND, naming `replace_symbol` or `edit_text force=true` for a field initializer | two misdirected retries measured this run, each re-sending the whole body - the `McpHost.RunAsync` body alone was ~1 900 characters | — |
| **I375** [unanswerable] no tool answers one MSBuild property across every project of a solution. `project_properties` answers one project's evaluated properties, so `IsTestProject` over an N-project solution is N calls - which is why `I347` could not be fixed by honouring the evaluated value and had to stay open. The same gap blocks any "which projects set X" question (`TargetFramework`, `IsPackable`, `EnableDefaultItems`) | `project_properties`, `list_projects` | let `list_projects` take `properties="IsTestProject,TargetFramework"` and print each project's evaluated value beside its name, from the evaluation the workspace already holds | turns an N-call sweep into one, and unblocks `I347`; on this solution N=4, on a 148-project tree N=148 | reading the `.csproj` text: the value routinely comes from `Directory.Build.props`, which is the whole reason `project_properties` reads MSBuild's evaluated set |
| **I376** [speed] lever (b) — the harness's parallel `tool_use` capability is **99.997% unspent**. 1-week window to 2026-08-26, 305 transcripts, 36 075 calls: **36 070 of 36 071** tool-bearing assistant messages issued exactly ONE tool call; exactly one issued two. Every serial call pays the corpus model gap — `tool_result` to the next `tool_use`, **p50 6 097 ms**, **102.9 h** in the window — before its tool runs at all. The existing "N calls in a row" nudge fires only AFTER the serial calls were paid, and only for the SAME tool, so it can never cover two different tools that had no dependency between them | `SKILL.md` + `ResponseBuilder` nudge | State the parallel rule in the skill's hard gate as a first-class instruction — independent calls go in ONE assistant message — and make the run-detection nudge predictive and cross-tool: when a read tool answers, name the sibling calls that could have been issued beside it | 3 060+ calls sat in same-tool runs of ≥3 (`read_text` 1 801, `edit_text` 799, `write_text` 460) = **~5.2 h/week** of model gap at p50, before any cross-tool pair is counted | A batch parameter alone — `paths=`, `edits=` and `files=` already exist and the runs persisted anyway, and a batch cannot fuse two DIFFERENT tools; a retrospective nudge — it fires after the cost is paid |
| **I377** [speed] lever (a)/(d) — verification is **41% of all tool wall time** and half of it is provably redundant. 1-week window to 2026-08-26: `run_tests` + `build` + `analyze` + `cleanup` + `format` + `get_diagnostics` + `gate` = **4 856 calls / 64.5 h**, **7.3 verification calls per task cycle** over 663 cycles. `run_tests` alone is **2 454 calls / 58.0 h**, mean **85 s**, p99 **965 s** — and **1 183 of them (48%)** are byte-identical repeats inside one session, as are **787 of 1 053 `build` calls (75%)**. 1 154 identical verification calls were re-issued **under 5 minutes apart** | `run_tests` / `build` | Fingerprint the inputs a run covered — document versions, project files, resolved package set — and answer an identical call from the previous result when nothing in that fingerprint moved, as `CACHED age=4m inputs unchanged`, with `force=true` to opt out. The server already holds the file watcher and the Roslyn document versions that PROVE it, which is the only reason this is not a guess | **~27 h/week** at the measured duplicate count × mean duration | A clock-based TTL — an answer that could have changed in 30 s is exactly the confident wrong answer this repo bans, so the key must be input-derived; telling the agent not to re-run — the definition-of-done already says so and the duplicate share is still 48% |
| **I378** [speed] lever (d) — the agent runs the full suite per EDIT, not per task. 1-week window to 2026-08-26: `edit_text -> run_tests` **708x** and `run_tests -> edit_text` **569x** are among the most frequent adjacent pairs in the corpus, giving **3.7 full suite runs per task cycle** at mean 85 s. The end-of-task tail is NOT where the hours are — after the last edit it is only **15%** of post-first-edit wall time (mean 6.8 calls, p90 11.1 min); the interleaved edit-then-test loop is the other 85% | `SKILL.md` / `CLAUDE.md` definition-of-done | Write the verification ladder explicitly with its measured cost per rung: after an EDIT run `analyze` on the touched file (mean **7.0 s**) and a scoped `build` (mean **13.0 s**); run the full suite ONCE at the end of the task, and `rerun_failed` after a red one. Naming the seconds is what makes the rung a cost decision rather than a habit | 2 of the 3.7 suite runs per cycle = **~31 h/week** | Dropping a test tier — the tiers are the gate; the lever is WHEN the suite runs, never whether it does |
| **I379** [speed] lever (d) — the written `sleep` gate is breached ~156 times a week. 1-week window to 2026-08-26: 293 `Bash` calls contain a `sleep`, declaring **229 797 s**; **156 of them are BARE** — no `while`/`until`/`for` guard — declaring **25 307 s = 7.0 h** and costing **7.0 h** of real wall time, largest single **580 s**. The guarded shape works (137 calls declare 204 490 s and cost 5.7 h, because they exit when the process dies); the bare shape is pure burn | `ToolGuard` | Deny a `Bash` command whose `sleep` is not inside a loop that also tests the process, with a remedy naming the two alternatives that already exist — a background run notifies on completion, and ending the turn is free | **7.0 h/week** | Prose — the gate is already written in `CLAUDE.md` and was breached 156 times in the same week it was in context |
| **I380** [speed] lever (c)/(d) — the two remaining wall-clock sinks, aggregated. 1-week window to 2026-08-26: `Bash` is **4 547 calls / 35.2 h** at a **13.2% error rate** (602 failures, **341 retried with the same tool**), of which shell text tools are **2 369 calls / 18.1 h = 51.5%** of all `Bash` wall time; and the `Agent` subagent is **184 calls / 6.4 h**, p99 **2 303 s**, max **6 589 s** — 110 minutes inside one call — with no scope stated in the spawn | `ToolGuard` / `SKILL.md` reviewer brief | Name the shell-text class in the guard remedy with its terse replacement AND the wall time it costs, so the substitution is priced rather than merely forbidden; and require every spawn prompt to carry the `changed_files` output as its scope plus a stated call ceiling, so a delegate cannot walk the whole tree | The `Bash` text class is **18.1 h/week**; the subagent tail is **6.4 h/week** with p99 at 38 min | Denying `grep`/`cat` user-wide — the guard is installed user-wide and outside a .NET tree there is no replacement, which is a shipped trap in this repo |

