# Improvements archive

Open rows: [IMPROVEMENTS.md](IMPROVEMENTS.md).

## Closed

| Finding | Tool | Change | Outcome |
|---|---|---|---|
| **I285** moving one markdown section from one file into another has no tool: splitting the 205 KB backlog cost a 13 KB `read_text section="## Open"` plus a 9 KB `write_text` that re-sent text the server already held, ~5 500 tokens for a move that touches no content | `edit_text`, `FileService` | `edit_text section= toPath=` cuts the section - heading and body, `occurrence=` picking it when the heading repeats - and lands it in the target through `WriteTextManyAsync`, so both files go through one write and the answer is one changed-line count per file | **shipped.** The section text never crosses the wire in either direction: measured against the 5 500 tokens the `IMPROVEMENTS.md` split cost, the same move is now two response lines. Locked by three `EditErgonomicsE2ETests` cases: the move itself (source loses the section and keeps its other one, target gains it, no diff in the answer), `toPath` beside `oldText`/`newText`, and a non-markdown target. `write_text ref=` stays rejected: it restores a whole file from git and cannot address a section or a second path |
| **I284** `read_text ref=HEAD` returns every non-ASCII character as mojibake - an em dash came back as `ΓÇö` on Windows - so a historical read cannot be compared with the working tree, and text copied out of one would write the corruption into the file | `read_text`, `GitRunner` | `ChildProcess.RunAsync`/`StartInfo` take an optional encoding and `GitRunner` passes a BOM-less UTF-8, so `git show`, `git diff`, `git status` and `git log` are all decoded correctly | **shipped.** Makes `ref=`, `history`, `diff_text` and `changed_files` usable on every document in this repository - all of which carry em dashes - and removes the corrupted-write-back risk entirely. Locked by `GitToolsE2ETests.ReadText_AtARef_DecodesGitsOutputAsUtf8`, which builds a real temp git repository through a new `TerseTempSolution.StartAsync(prepare:)` hook, commits a UTF-8 file carrying an em dash, and asserts the round trip carries it with no `Γ`, `Ã` or replacement character. `dotnet` is **excluded as a measured decision**: MSBuild writes in the console code page and forcing UTF-8 there would corrupt what it currently gets right. Reading the working-tree copy stays rejected: the whole point of `ref=` is the text at that revision |
| **I283** the `N read_text calls in a row - pass paths=[...] with the next N+ in ONE call` steer fired on three consecutive reads that each needed a **different line range**, which `paths=` cannot express, so the steer asked for a call that would answer the wrong lines | `read_text` | `RepeatSteer.Unbatchable` recognises a `read_text` carrying `startLine`, `endLine`, `tail` or `section` and `Steer` forgets the run for it, so such a call neither steers nor counts toward the next steer | **shipped.** Removes one misleading steer per multi-range read sequence and the wrong-lines re-read it would cause if followed. Locked by three `RepeatSteerTests` cases: a ranged read breaks a run that had already steered, five ranged reads in a row never steer, and a theory pinning `startLine`/`endLine`/`tail`/`section` as unbatchable against `maxLines`/`verbose`/`bytes` which are not, and against `get_file_outline` which has no ranges. Teaching `paths=` per-entry ranges was not attempted; dropping the steer stays rejected, because the batch it advertises is real and measured for whole-file reads |
| **I282** `edit_text edits=[{path,oldText,newText}, ...]` where **every** entry carries its own path is refused with `missing path`, so the whole 1 200-token batch had to be re-sent with an arbitrary top-level `path` chosen from one of the entries | `edit_text`, `FileService` | `path` is now `string? path = null` and `FileTools.Anchor` resolves the workspace anchor from the first entry when every entry declares one; a batch carrying a path-less entry is refused naming it by index, and a call with neither is refused naming both ways in | **shipped, and it fired against 0.37.0 twice during this very run** - once on the I274 docs edit, once on the I269 one, each costing a rejected call plus a full re-send. Saves the re-send of the whole batch payload per cross-file edit, ~1 200 tokens on the call that produced the row and more as the batch grows. Locked by three `EditErgonomicsE2ETests` cases: a two-file batch with no top-level path, a batch whose second entry has none, and a call with neither. Keeping `path` required and saying so in the description stays rejected: the cost was the re-send, not the wording |
| **I281** `BacklogClosureE2ETests.WriteText_WithTwoInterdependentCSharpFiles_LandsThemUnderOneCompileGate` failed once in four full E2E runs with `ERROR Internal: RemoteInvocationException`, and passed alone and on the next full run - the same MSBuild project-evaluation race `0.37.0` hardened with one shared lock, so the lock does not cover every path that loads a `ProjectCollection` | `ProjectGlobs`, `EditGate` | **both halves.** `LoadedWorkspace.Applied` now takes `ProjectGlobs.EvaluationGate` across `TryApplyChanges`, so Roslyn's build-host evaluation can no longer overlap a `ProjectCollection` evaluation - the only MSBuild load the 0.37.0 lock did not cover; and `Errors.IsBuildHostFailure` maps any chain carrying `RemoteInvocationException`/`ConnectionLostException`/`RemoteRpcException` onto the new `TerseErrorCode.Transient`, whose remedy is "the project file was restored, a file the edit was adding may already be on disk, retry the same call" - the most the call site can prove - instead of "this is a server defect" | **shipped, with Phase 1 DEGRADED**: the race was never reproduced deterministically - it was observed three times in ~21-minute full-suite runs and not once in isolation - so the fix is the two structural halves plus a substitute test. Locked by `TransientFailureTests` (three cases: nested recognition, non-recognition, and the rendered code and remedy) and `WorkspaceSyncE2ETests.ConcurrentNewCSharpWrites_IntoOneProject_AnswerOnlyASuccessOrAnEditConflict`, which fires six concurrent new-`.cs` writes into one project, asserts at least one landed and every answer is either a landed write or the benign `EditConflict` an optimistic apply is entitled to - never `ERROR Internal` and never `ERROR Transient` - then retries each sequentially expecting no error at all, and proves all six files exist by outlining them. **Reopen if `ERROR Transient` is ever observed in a run**: that is the same race surviving the widened lock, and the retry remedy would then be masking it. Calling it a flake stays rejected |
| **I280** `CLAUDE.md` mandates that every `CHANGELOG.md` link definition name a tag that exists, and no census gate reads it: the `[0.37.0]` heading, its link and the repointed `[Unreleased]` compare all sat in `main` against a tag that did not exist | `ChangelogReferenceTests` | four new census tests read the changelog and `git tag --list v*`: every heading has a link definition, every definition names a heading, `[Unreleased]` compares against the newest version, and every tag has a heading - the last minus a reasoned, ratcheted `v0.15.1` | **shipped, and it found two live defects on `main` the moment it ran**: a `[0.28.0]` link definition pointing at a heading that did not exist, and the shipped `v0.28.0` release having no section at all. The `## [0.28.0]` section is reconstructed from `git log v0.27.0..v0.28.0` and says so. The newest heading is exempt from the tag check because the release flow commits the changelog before it tags - which is exactly why the `0.37.0` case needed the *older-than-newest* rule to catch it on the next cut. Resolving tags over the network stays rejected: `git tag --list` is local and is what the release gate already reads |
| **I279** `addTo=` matches a containing type by leaf name only, so two targets whose containers share a leaf name in different namespaces resolve to the first, silently | `SymbolEditService` | `Addresses` compares the **qualified** name by dot-boundary suffix whenever the reference carries a `.`, and `AmbiguousContainer` refuses a bare leaf that matches more than one container, naming every candidate's qualified name; `LeafName` is gone | **shipped.** Turns one member landing in the wrong type - undetectable from the response - into a refusal that carries the disambiguating spelling. Locked by `CompileGateE2ETests.AddTo_WhenTwoContainersShareALeafName_RefusesInsteadOfPickingTheFirst` against two new `Fixture.Broken.Alpha/Beta.Duplicate` fixture types, asserting both directions: `addTo=Duplicate` refuses naming both, `addTo=Fixture.Broken.Alpha.Duplicate` applies. Requiring a full documentation id stays rejected: the leaf name is what an outline prints and what the remedy offers |
| **I278** `changed_files` reports a `capped` row count against a total file count once an untracked directory folds, so the two numbers in one summary are different units | `GitTools` | `GitTools.Covered` sums the files each shown row covers - one per plain row, and the `xN untracked` count for a folded one - and the capped summary reports that instead of the row count | **shipped.** Removes one misleading summary per capped fold; the uncapped summary is unchanged. Locked by `GitToolsE2ETests.ChangedFiles_WhenTheListIsCapped_CountsFilesOnBothHalvesOfTheSummary`, which builds two untracked directories of 8 and 7 files and asserts `8/15 files truncated` where the old code answered `1/15`. Printing both units stays rejected: a summary carrying two counts of different things is the confusion, not the fix |
| **I277** `SchemaCompactor` now drops any property named `default` at any depth; a nested item-schema member named `default` inside `files=`/`edits=` would vanish silently, and the parameter census only reaches top-level properties | `SchemaCompactor` | the writer is split into `WriteRoot` -> `WriteParameters` -> `WriteParameter`, and only the last removes the `default` key; every deeper object is copied through unchanged, union collapsing included | **shipped.** Closes a latent silent-drop with no cost: the compaction is still worth its 2 672 characters per `tools/list`, and the eight `SchemaCompactorTests` cases still pass. Locked by `SchemaCompactorTests.Compact_KeepsAMemberNamedDefaultInsideANestedItemSchema`, which asserts the parameter's own `default` is gone and an item-schema member of the same name survives with its type. Dropping the compaction stays rejected |
| **I276** the metadata fallback ignores `kind=` and `scope=`: `search_symbols query=X kind=method` falls through to a types-only assembly search, so it can answer with a class the caller excluded | `SymbolSearch`, `NavigationTools` | `NavigationTools.Declined` refuses the fallback when `kind=` names something other than a type kind, or when any `scope=` was passed, and answers `0 symbols` plus the reason and the parameter to drop | **shipped.** Removes one confidently off-filter answer per filtered zero-result query - the failure an agent cannot detect. Locked by `NavigationToolsE2ETests.SearchSymbols_WithAFilterTheMetadataFallbackCannotHonour_DeclinesInsteadOfAnsweringOffFilter`, which asserts `kind=method` and `scope=src` both answer `0 symbols` with no `EXACT` record while the unfiltered query still reaches `JsonSerializer`, and by `...WithATypeKind_StillReachesTheReferencedAssemblies`. Filtering after the fact stays rejected: the metadata half only ever holds types, so declining is the honest answer |
| **I275** the `guard=absent` warning is published by a fire-and-forget maintenance task started one line before the transport, so the first `workspace_status` of a process can answer without it - the orientation call the warning exists for | `AssetBanner`, `McpHost` | `McpHost.RunAsync` awaits `ClientRegistrar.ProbeAsync` and publishes the asset state **before** `host.RunAsync` starts the transport; the maintenance pass still refreshes and re-publishes, and `ProbeAsync` swallows everything but cancellation so a hand-edited settings file cannot fail startup | **shipped.** Restores the whole value of `I256` on the one call an agent makes first. Locked by `InstallCommandE2ETests.TheVeryFirstCallOfAProcess_AlreadyCarriesTheAbsentGuardWarning`, which sandboxes `TERSE_HOME`/`CLAUDE_CONFIG_DIR` onto an empty profile so the warning is deterministic, and asserts the **first** call carries it. Awaiting the probe on every call stays rejected: it would put four file probes on the request path the 60 s handshake budget protects - one probe at startup costs that budget nothing measurable |
| **I274** a concurrent session reverted an applied edit and `changedLines=51` had already been reported, so verifying what was really on disk fell back to `Bash: grep -c` twice: every terse read answers from the workspace, which is exactly the thing that could not be trusted | `workspace_status`, `changed_files` | `WorkspaceDivergence` compares the in-memory `SourceText` of the files **this server wrote** - taken from the undo history, capped at 50 - against their bytes, so the orientation call never walks the solution and a read-only session pays nothing; `workspace_status` answers `WARNING workspace=diverged - N document(s) differ from disk: <paths>` with the reload to run, and `verbose=true` adds `disk=in-sync probed=N of M` | **shipped.** Removes the two `Bash: grep -c` fallbacks per suspected divergence and, more importantly, makes the case detectable at all - `read_text` answers from the same snapshot and cannot. Locked by `WorkspaceSyncE2ETests.WorkspaceStatus_WhenDiskNoLongerMatchesTheWorkspace_NamesTheDivergedDocument`, which asserts both directions on a `--no-watch` temp solution: `disk=in-sync` after the edit, `workspace=diverged` after an external revert. `changed_files` is **excluded as a measured decision**: it answers from git rather than from the workspace, so a Roslyn-versus-disk verdict there would break its one-line-per-file contract |
| **I273** the whole evidence base for `I266` - that 65 % of the advertised payload is descriptions - had to be measured by driving the server over stdio from a python script, because `workspace_status` reports `advertised=88 tools 25959 tokens` and nothing about what those tokens are | `workspace_status` | `AdvertisedCost` records four character totals while it measures the advertised list - tool descriptions, parameter descriptions (walked out of each input schema), schema frame and names - and `verbose=true` prints them as a second line under the total | **shipped.** Replaces a ~15-minute stdio harness per measurement with one call. Locked by `RemainingToolsE2ETests.WorkspaceStatus_Verbose_BreaksTheAdvertisedTotalIntoTheFourThingsItIsSpentOn`, which asserts the four parts are each non-zero, sum to the advertised total within the four per-part roundings, and that the two description halves are the majority - the `I266` claim, now a test. Printing it by default stays rejected: it is a maintainer question, and the plain status is asserted not to carry it |
| **I272** `edit_text section="### Added" place=append` answered `'### Added' names 36 sections`, and its remedy - "pass the heading with its level" - is what the call already did; `occurrence=` was ignored beside `section=`, so the only way in was an anchor | `FileService` | `DocumentOutline.Locate` takes `occurrence`, and its multi-match remedy now names `occurrence=1..N` plus each candidate's start line instead of restating the level rule; `edit_text` wires its existing `occurrence=` into the `section=` branch and `read_text` gains the same parameter, refusing it when no `section=` was passed | **shipped.** Turns one wasted call plus a re-read into one refusal that carries the index to pass - the candidate start lines mean the retry needs no `headings=true` call. Locked by `DocumentOutlineTests` (four cases: repeated heading, index picked, index past the last, single heading unaffected) and `EditErgonomicsE2ETests.Section_WhenTheHeadingRepeats_IsPickedWithOccurrenceRatherThanAnAnchor`, which edits section 1 and reads back both, and by `DocumentOutlineTests.Locate_ForMoreCandidatesThanItLists_SaysHowManyItDropped`, because the candidate list caps at 12 and must say so. Matching the first occurrence by default stays rejected: a changelog edit landing in the wrong release is silent and expensive |
| **I270** `search_text glob={README.md,NUGET_README.md,src/.../SKILL.md}` answered `0 matches` rather than refusing, so a brace expansion the matcher does not implement read as "the string occurs nowhere" - a confidently wrong negative, and one wasted call plus a retry. Observed a second time on `**/*.{md,yml,yaml,json,cs,slnx}` during the backlog split, where the empty answer was nearly read as "no document mentions the file" | `FileGlob` | `FileGlob.Unsupported(glob, parameter)` returns an `InvalidArgument` naming the offending parameter and the syntax that is supported; it guards `find_files glob=`, `search_text`/`search_regex` `glob=` and `exclude=`, and `changed_files exclude=` | **shipped.** Observed a third time on this very run - `search_text glob={README.md,NUGET_README.md,src/.../SKILL.md}` against 0.37.0 answered `0 matches` while all three files carried the text. Turns one wasted call plus a retry, and an untrustworthy zero, into one refusal that names the fix. Locked by `FileGlobTests.Unsupported_ForAGlobCarryingABraceExpansion_RefusesInsteadOfMatchingNothing`, `FileGlobTests.Unsupported_ForTheSyntaxTheMatcherImplements_AllowsIt` and `FileToolsE2ETests.EveryGlobTakingTool_RefusesABraceExpansionInsteadOfAnsweringZero`, which drives all four parameters. Brace expansion itself stays unimplemented: the glob surface is deliberately small, and a refusal is what makes a zero result trustworthy |
| **I269** fixing the 12 `RS0030` sites this task introduced meant turning `file(line,col)` into the declaration that contains it: `analyze` and `build` name a position, nothing names a symbol, so it cost one `get_file_outline paths=[5]` (~2 900 tokens, of which 9 lines were used) plus a `get_symbol_source` batch | `analyze`, `get_diagnostics`, `build` | `DiagnosticDeclarations.ResolverAsync` maps every diagnostic location onto the declaration containing it - reusing `UsageContainer`, the same syntax-only walk `find_usages` already uses - and `DiagnosticFold` appends that addressable id to each position, so a record reads `OrderService.cs:15:16 OrderService.Unused: Member 'Unused' ...`. A location with no source tree keeps the bare position | **shipped.** Removes the outline call between a diagnostic sweep and its fix list, measured at ~2 900 tokens over the 12-site `RS0030` sweep that produced the row, against ~2 tokens per tagged position (5 positions on the fixture scope). `build` is **excluded as a measured decision**: `ToolContext.WithTargetAsync` releases the workspace lease before it shells out, so no compilation exists to map its MSBuild-parsed positions onto. Locked by `AnalysisToolsE2ETests.Analyze_NamesTheDeclarationEachFindingSitsIn`, `CompileGateE2ETests.GetDiagnostics_NamesTheDeclarationTheErrorSitsIn` and `DiagnosticFoldTests.Findings_WithADeclarationResolver_NamesTheDeclarationEachPositionSitsIn`, the last observed failing by mutating `DiagnosticFold.Positioned` to drop the resolver |
| **I211** a `CompileRegression` caused by a signature change named the broken call site's file but not the members that call it; finding the one caller missed cost 4 calls after the rejection | `EditGate`, `Errors.CompileRegression` | when every new error is `CS7036`/`CS1501`/`CS1503`/`CS1729`, resolve each error location to the `MemberDeclarationSyntax` containing it and answer `send these callers in the same replace_symbol symbolIds/declarations batch: <addressable ids>`; a `dryRun` prints the same line | shipped - 4 calls to 1 per occurrence, on the second most common rollback class. Nothing is named when one error is not a call shape, when a caller cannot be resolved, or when more than five declarations call it. Locked by `EditToolsE2ETests.ReplaceSymbol_RolledBackByASignatureChange_NamesTheDeclarationsThatCallIt`, `…ReplaceSymbol_DryRunOfASignatureChange_NamesTheCallersItWouldBreak` and the negative `…AddMember_RolledBackForARegressionWithNoImport_NamesNoImportItCannotProve` |
| **I212** a complete outline never named `contains=`: 49 members at ~1 100 tokens when 6 were wanted, because the `- narrow with` steer fires only on a truncation and an outline does not truncate | `get_file_outline`, `ResponseBuilder` | an unfiltered outline of 25 members or more ends with `104 members - narrow with contains=`; silent below the threshold and silent when `contains=` was already passed | shipped - ~900 tokens on a wide file, on the most-called navigation tool in this repository's logs. Locked by `NavigationToolsE2ETests.GetFileOutline_OfAWideFile_NamesTheContainsParameterItNeverTruncatedInto`, `…GetFileOutline_OfANarrowFile_NamesNoParameterItDoesNotNeed` and `…GetFileOutline_FilteredOnAWideFile_DoesNotSteerToTheParameterItAlreadyUsed` |
| **I213** a qualified name whose type declares no such member answered `AmbiguousSymbol: matches more than 100 symbols`, blaming the name's popularity and telling the caller to qualify a name that was already qualified | `SymbolLookup`, `Errors` | when the qualifier resolves to exactly one type with no matching member, answer `SymbolNotFound` naming the type and up to five of its own members, preferring the ones whose name contains what was asked for | shipped - 1 error plus 1 retry per occurrence, and it removes a remedy that actively misdirected. Two or more types of that name still fall through, because the type is then unproven. Locked by `NavigationToolsE2ETests.GetSymbolSource_QualifiedByATypeThatDeclaresNoSuchMember_BlamesTheQualifierNotTheName` |
| **I214** nothing reported the advertised `tools/list` token cost; the only reader of that number was a test, so three description edits each cost a full E2E run to discover they had blown the budget | `workspace_status`, `AdvertisedCost` | `workspace_status` prints `advertised=<n> tools <t> tokens`, measured on the payload the client actually received - after schema compaction and both list filters | shipped - 3 × 120 s of wall clock in the run that logged it, and the prime directive's own number now sits in front of the agent about to grow it. Locked by `TokenBudgetE2ETests.WorkspaceStatus_ReportsTheAdvertisedPayloadTheClientActuallyReceived`, which recomputes it client-side rather than matching a literal |
| **I215** verifying a behaviour change against the freshly built binary had no tool, and the mandated one-shot probe took 2 attempts because the path had to be guessed | `build` | `build verbose=true` prints one `wrote <path>` note per assembly MSBuild reported and, for `terse.dll`, the exact probe command for it | shipped - 1 wasted attempt per verification, on the workflow this repository's own hard gate makes mandatory. A successful build without `verbose=true` is still one line. Locked by `DotnetRunnerTests.OutputNotes_ForTheServersOwnAssembly_NamesTheProbeCommandForTheBinaryJustBuilt` and `…OutputNotes_ForABuildThatWroteNothingElse_NamesNoProbeForAnotherAssembly` |
| **I216** `CHANGELOG.md` named its locking tests in back-ticks and nothing resolved them; a rename left a dead reference in the release notes, caught by a reviewer rather than a gate | `ChangelogReferenceTests` | a census test extracting every back-ticked test-name-shaped identifier from the two newest `## [` sections and resolving each against the method declarations of both test projects | shipped - and it immediately caught a second dead reference, `…AddMember_DryRunForAMissingUsing_StillNamesTheImportItWouldNeed`, already shipped in 0.33.0. The discriminator is itself covered so `TERSE_RESULTS_DIRECTORY` and `get_file_outline` are not mistaken for tests, the referenced set is asserted non-empty, and the exclusion set is empty and ratcheted |
| **I216** a 12-week scan of 509 transcripts found 4 932 of 28 978 tool calls (17.0 %) going to a built-in or shell command terse replaces, in trees whose own instructions carried the ban | `doctor`, `ToolGuard` | `doctor` reports per-tree guard coverage - the four measured breach classes run through `ToolGuard` with this workspace's directory as the hook's `cwd` | shipped - `read-cs=denied bash-text=denied dotnet-build=denied dotnet-test=denied git-status=denied git-diff=denied`. The `assets guard=` line says the hook is installed; this one says what it denies **here**, which is what varies, because the git rows are cwd-scoped. Locked by `ToolGuardTests.Coverage_InADotNetTree_ReportsEveryMeasuredBreachClassDenied`, `…Coverage_OutsideADotNetTree_ReportsTheGitRowsAsAllowedBecauseNothingReplacesThem` and `InstallCommandE2ETests.Doctor_ReportsWhetherTheGuardCoversThisTreesMeasuredBreachClasses` |
| **I217** the shipped Roslyn lags the SDK and the server only said so per workspace; a 10.0.4xx SDK silently disabled the Razor generator and every IDE code fix `cleanup fix=style` applies, and `cleanup` answered `0 files changed  no change` for a fix set that never ran | `doctor`, `CodeFixService` | `doctor` compares the carried `Microsoft.CodeAnalysis` against the selected SDK's own Roslyn on a `roslyn:` line and `FAIL`s when the SDK is ahead; `cleanup fix=style`/`fix=all` report `UNAVAILABLE` when the project registers no IDE code fixer | shipped - one whole CI cycle was lost to a diagnosis the server had the information to make, and a clean `no change` for a fix set that never ran is exactly the confident wrong answer this server refuses. Locked by `InstallCommandE2ETests.Doctor_ComparesTheRoslynThisBuildCarriesAgainstTheOneTheSdkCarries`, `CodeFixServiceTests.StyleUnavailable_WhenNoIdeFixerIsRegistered_SaysNothingWasChecked` and `…StyleUnavailable_WhenTheFixersAreThereOrTheModeDoesNotUseThem_SaysNothing`. **Degraded**: the no-fixer condition needs an SDK skew this machine cannot create, so the decision is covered by unit tests over the seam and the positive control by the repository's own clean `cleanup fix=style` |
| **I217** `read_text paths=` was the largest fusion opportunity and the only plural parameter with no uptake - 679 of 1 710 calls fusable (39.7 %), `paths=` passed on 5 of 190 - because a `search_text` hit is a `path:line text` record the caller must extract and dedupe first, while `get_symbol_source` batches at 55 % after `get_file_outline`, whose records already read as arguments | `ArgumentLine`, `search_text`, `search_regex`, `find_files`, `changed_files`, `find_usages`, `get_file_outline` | every hit list ends with `paths=["src/A.cs","src/B.cs"]`, deduped and capped at 10; an outline of at most ten members ends with `symbolIds=[…]` | shipped - the opportunity is 679 calls × 46 817 BIE = 31.8 M BIE and 1.9 h of model think in that corpus. Neither line appears with fewer than two entries, a wide outline offers `contains=` instead, and a **ref'd** outline emits no ids at all because they address the loaded solution. Locked by five `BacklogClosureE2ETests` cases, one of which feeds the emitted ids straight back into `get_symbol_source symbolIds=` and asserts none answers `NOT_RESOLVED` |
| **I218** the repeat steer's reach was structurally capped: of 1 735 fusable calls a third-call steer reaches 432 (24.9 %), because 571 runs are exactly two calls long and end before it can be read | `RepeatSteer` | `Threshold` 3 → 2 | shipped - +370 calls recovered = 17.3 M BIE and 1.0 h of model think in that corpus. `Threshold=1` stays refused: it would fire on the first call of every plural-capable tool |
| **I219** the per-call floor looked 3-5x worse on a large solution than the 1.1 s it was scoped against - `read_text` p50 3 049 ms against `Read` 27 ms | `PhaseProbe`, `doctor` | time the compilation realization separately from the outline, and report `realizeMs= outlineMs= gateMs= diffMs=` with the verdict on the two per-call phases | shipped, and it **refuted the premise**: measured on this repository's own solution, `outlineMs` fell **2 948.41 ms → 54.50 ms** once realization was timed on its own, reappearing as `realizeMs=2 748.47`, with `resolveMs=0.07 syncMs=0.15`. The corpus reading is realization re-paid after every load and idle drop, not a per-call floor. Locked by `InstallCommandE2ETests.Doctor_SeparatesTheOncePerLoadCompilationFromThePerCallPhases` |
| **I220** `diff_text` truncated on 105 of 133 calls (78.9 %), the highest steer rate of any tool measured, and the caller paid a second call at p50 3 595 ms | `diff_text` | default line cap 400 → 1 000, and the steer names the exact `maxLines=<total>` that returns the rest | shipped - one retry instead of a widening ladder. The other three caps are untouched at 11-23 % steer rates, because a wider default there pays the payload on every call that did not need it. Locked by `GitToolsE2ETests.DiffText_WhenItTruncates_NamesTheExactMaxLinesThatReturnsTheRest` |
| **I221** reading a file at a git ref had no tool: `git show <ref>:<path>` ran 363 times across 683 sessions, returning 133 873 tokens of raw uncompressed file text, and it is the one shell fallback this repository's review gate mandates | `read_text`, `get_file_outline`, `GitRunner` | `ref=` on both, resolved through `GitRunner`; the historical text gets the numbering gutter, line ranges, `tail=`, `headings=`, `section=` and the `maxChars` budget, and a whole `.cs` file answers its outline parsed from that revision's own text | shipped - the working-tree path already measures the outline at about a third of the text. `ref=` takes one `path=`, is refused by name beside `paths=`, and a ref that does not exist is refused rather than answered from the working tree. Locked by five `GitToolsE2ETests` cases |
| **I222** git history was the largest legal-`Bash` class left: `git log` 725 times and `git show --stat` 103 times across 683 sessions, ~1.9 calls per session and 258 K tokens | a new `history` tool, `ToolGuard` | one read-only tool over the shared `ChildProcess` runner - commits for a path or pathspec, oneline by default, `baseRef` and ranges, `contains=` for the pickaxe, `message=` for the subject grep, `commit=` for one commit's stat - after which the guard denies `git log` and `git show` in a .NET tree | shipped as the 88th tool - 828 calls per 683 sessions against a 32-call break-even. `git show <ref>:<path>` routes to `read_text ref=` instead, and `git blame` stays on the shell at 1 call in 683 sessions. The advertised payload budget moves 23 450 → 24 200 tokens on that evidence. Locked by `ToolGuardTests.Inspect_ForGitHistoryInADotNetTree_NamesTheToolThatReplacesIt`, the widened denial theory and its allow-list control |
| **I223** the agent preferred a competing server's fuzzy file finder to `find_files` roughly 2:1 - 363 calls against 191 - and `find_files` carried the worst error rate of any terse tool measured, 7.3 % (14/191) | `find_files` | a `name=` file-name substring that needs no glob syntax, combining with `glob=`, plus a steer to `name=` when a glob matched nothing | shipped - loosening `glob=` itself stays refused, because a glob that silently matches more than it says is the confident wrong answer this server prevents, and a separate parameter records which semantics were asked for. Locked by four `FileToolsE2ETests` cases including the empty-glob steer and the refusal when neither is passed |
| **I224** two premises behind every tool decision here were wrong by measurement: one extra tool costs 1.51 M BIE across 508 sessions against 46 817 BIE per removed API turn - a 32-call break-even - while `explore_symbol` was called 7 times and `impact_of` once in 683 sessions | `CLAUDE.md`, `README.md`, `SKILL.md` | replace "a 57th tool must beat the one it splits" with the break-even and a **measured per-tool selection rate**, and treat a shipped tool with a near-zero call count as a defect to fix or delete | shipped - it re-aims every later tool decision, and this release's own 88th tool was justified against it at 828 calls per 683 sessions. Pricing the surface by the "accuracy degrades past 30-50 tools" figure stays refused: it exists in a primary source but carries no eval, dataset, curve or sample size, and two independent measurements bracket it badly. Re-measured 2026-08-24 over 360 sessions: **`explore_symbol` 0 calls, `impact_of` 1** - the rule's own delete-or-fix branch is now due, tracked as I323 |
| **I225** the surface was defended by count when the measured driver is semantic redundancy: merging semantically redundant tools is worth +8.4 % to +38.6 % tool-selection accuracy (ToolScope, arXiv:2510.20036) while merely similar tools cost 8-19 % (arXiv:2504.00914) | a new census test, every `[Description]` | compute pairwise description overlap over the advertised surface and fail above a threshold, with a reasoned, ratcheted exclusion set | shipped - 3 828 pairs at 88 tools, failing above 0.45, with seven excluded-by-design pairs each carrying a written reason and ratcheted by its own test. They are opposite verbs on one addressing scheme or a literal/pattern pair, which is not the same-thing-twice hazard ToolScope measured. Deleting the low-traffic near-synonyms outright stays refused: a client can only call what its list carries |
| **I226** the repeat steer stated a fact where the only published technique that raises batch width states an imperative with a number - W&D (arXiv:2602.07359) moved BrowseComp 66 % → 74 % with "you MUST make at least m but not more than m+1 function calls" and measured that a model cannot self-select parallel width | `RepeatSteer` | restate it imperatively with the count the server already computes: `2 read_text calls in a row - pass paths=[...] with the next 2+ in ONE call` | shipped inside the same ~14 tokens, on the 1 735 fusable calls I218 reaches. Batch-parameter utilisation is unmeasured anywhere in the published literature, so this repository's own rate is the only number that exists and the next scan re-measures it |
| **I227** every token measurement in this space, including this repository's own `TokenBudgetE2ETests`, measures payload size, which is silent by construction on whether the answer was right; the one published study measuring both found the trade negative - 83 % answer quality against 92 % for a plain grep-and-read agent (arXiv:2603.27277) | `AnswerQualityE2ETests`, `fixtures/` | a fixed question set over the fixture solution with verified reference answers, scoring **answers** rather than bytes | shipped - 17 questions a human would actually ask, each with the facts its answer must carry; it caught two real gaps while being written. The set is asserted ≥ 14 questions so it cannot go vacuous, and a second test reports that answering all of it costs ≤ 4 000 tokens, so quality and cost are read together. Trusting the payload budgets as a proxy stays refused: their silence on accuracy is the mechanism behind the 83 %-versus-92 % result |
| **I210** `E2ETelemetry` (shipped as I186) wrote its attribution line to stderr on process exit, and `run_tests` drops the test host's output on a green run - so the three numbers the row existed to produce were invisible through the tool that runs the suite | `run_tests`, `E2ETelemetry` | `run_tests` sets `TERSE_RESULTS_DIRECTORY` on the `dotnet test` process and, under `verbose=true`, returns whatever the run wrote to `$TERSE_RESULTS_DIRECTORY/terse-notes*.txt` under a `run notes:` heading, bounded to 20 lines; `E2ETelemetry` writes through it | shipped - the attribution line is now readable through the tool that runs the suite (`run notes: e2e attribution: starts=1 startMs=553 calls=1 callMs=1959`, probed against the freshly built Debug `terse.dll`), instead of costing a `dotnet test` shell-out. Locked by `TestToolsE2ETests.RunTests_GreenAndVerbose_ShowsWhatTheTestHostPrinted` and the control `…RunTests_GreenWithoutVerbose_HidesWhatTheTestHostPrinted`, both observed failing first. **Measured refutation of the cheaper option**: surfacing the test host's own console output on a green verbose run was implemented and reverted - a marker written exactly the way `E2ETelemetry` wrote it never reaches `run_tests`' captured output at any verbosity, because the runner captures test-host console output per test. Printing the note on every green run stays refused: the one-line green verdict is a hard gate |
| **I209** a member whose new body calls a helper that does not exist yet was rolled back: **8** `CompileRegression` rejections in one run, 16 calls, every one of them *add the callee, then retry*. Nothing could say *replace this member and add this new private helper in the same compile-gated edit* | `replace_symbol`, `SymbolEditService` | `replace_symbol add=[...]` appends the new members to the containing type inside the one `EditGate.ApplyAsync`. The appended members are tracked as a syntax annotation and applied **after** the member replacements, so the type node the append rewrites is the already-edited one; a batch whose targets do not share one containing type, and a call that replaces that type itself, are refused naming what they found rather than guessing | shipped - **3 calls to 1** per occurrence (rejection, `add_member`, `retryWith`), on the most frequent error code in this repository's own session log (35 rejections in one release). Locked by `EditToolsE2ETests.ReplaceSymbol_WithAdd_LandsTheNewHelperInTheSameCompileGatedEdit`, `…ReplaceSymbol_WithAdd_WritesBothMembersInOneEdit`, `…ReplaceSymbol_WithAddAcrossTwoTypes_IsRefusedRatherThanGuessingTheContainer` and the control `…ReplaceSymbol_WithoutAdd_IsStillRolledBackForTheHelperThatDoesNotExistYet`, all observed failing first. Callee-first hand ordering stays refuted: the callee is discovered *while* writing the caller |
| **I208** nothing answered *how big is this file* while you were reading it. Measuring `SKILL.md` across a rewrite took **3** `Bash: wc -c` calls plus one `git show HEAD:<path> \| wc -c` | `read_text`, `find_files` | `read_text bytes=true` ends the answer with `bytes=N`, uniformly on every shape the tool returns - text, heading map, `section=`, and the outline a whole `.cs` read answers with - and once per entry of a `paths=` batch, so an unknown-shape response never drops the argument silently. The `find_files` half needed no code: `find_files glob="<a concrete path>" stamps=true` already answered it in one call, which `SKILL.md` never said - now stated and locked by a test | shipped - **4 shell fallbacks removed**, and the cheaper of the two answers (`find_files`, which does not read the file) is now the documented one. Locked by `FileToolsE2ETests.ReadText_WithBytes_ReportsTheSameByteLengthThatFindFilesStampsDoes`, `…ReadText_WithBytesOnAWholeCSharpRead_ReportsThemBesideTheOutlineSteer`, `…ReadText_WithBytesOverSeveralPaths_ReportsTheLengthOfEachEntry` and the negative control `…ReadText_WithoutBytes_ReportsNoByteLength`, all observed failing first. The row's own "on the count line" was **not** taken: a suffix on the count line has no home in the `.cs`-outline answer, and an argument that is silently ignored there is the confidently-wrong answer this repo refuses - so `bytes=N` is its own last line everywhere. The git-blob half stays on `Bash`; terse-sharp does not model history |
| **I207** a `CompileRegression` whose only new errors are `CS0246` for a name the project resolves in one namespace answered `remedy: add: using X; then replay the rejected text with retryWith` - the **three-call** recovery (rejected edit, `edit_text force=true` on the header, `retryWith`), while the `usings=` parameter that lands the import inside the same compile-gated write already existed on `replace_symbol_body`, `replace_symbol` and `add_member` and was never named. Hit twice in one run, 4 calls | `EditGate`, `Errors.CompileRegression` | `GateReport.Import` became `Imports` (the namespaces, not the rendered directives) and the remedy now reads `retry with usings=["System.Collections.Immutable"] and the retryWith token below, which lands the import in the same compile-gated edit`. The `dryRun` note keeps `add: using X;`, because nothing was rejected there and there is no token to replay | shipped - **2 calls saved per occurrence** (3 -> 1) on the commonest rollback class after callee-after-caller. Locked by `EditToolsE2ETests.AddMember_RolledBackForAMissingUsing_NamesTheUsingsParameterAndTheRetryToken` (the new wording) and `…AddMember_DryRunForAMissingUsing_StillNamesTheImportItWouldNeed` (the preserved `dryRun` note), both observed failing first |
| **I200** the advertised `tools/list` payload carried 1 779 tokens of serializer noise - 227 `"default": null` and 227 `["string","null"]` unions whose null arm says nothing | `McpHost`, `SchemaCompactor` | a `ListTools` filter rewrites every advertised schema: a null `default` is dropped and a two-arm union whose other arm is `null` collapses to the single type, recursively into `items`. Non-null defaults are kept, and the server still binds an explicit null because only the advertised copy is rewritten | shipped - measured both directions with the same method, **23 613 -> 22 193 tokens (-1 420, -6.0 %)** on every request of every session, no description reworded. Locked by `SchemaCensusE2ETests.NoAdvertisedSchema_CarriesANullDefaultOrANullTypeArm` and `TokenBudgetE2ETests.TheAdvertisedToolPayload_StaysWithinItsBudget` |
| **I201** 19 of the 87 advertised tools were called zero times in six weeks and 18 of them are `xaml_*`, `razor_*` or `resx_*`; not one of the three measured workspaces held such a file, so a quarter of the surface was pure input cost | `ToolProfile`, `WorkspaceMarkup`, `McpHost` | the advertised set is derived from the loaded workspace's own file kinds, unioned across every loaded workspace, with `notifications/tools/list_changed` on a load or unload that changes the answer; `--tools all` opts out and `--tools core` is unchanged. `WorkspaceMarkup.Of` walks the existing `PathIndex` and stops as soon as all three kinds are seen | shipped - **87 -> 56 tools and 22 193 -> 16 962 tokens (-23.6 %)** on `fixtures/SelectionSolution`. Locked by `MarkupProfileE2ETests`, which also proves a hidden tool still answers by name and that loading a markup-bearing solution re-advertises the families. The I160 `--tools core` hole (33 guard-named tools absent from the profile) is closed structurally: a solution holding no `.xaml` cannot produce a XAML guard denial |
| **I202** `SKILL.md` was 17 164 tokens and two of its eleven sections enumerated the same tool set twice - 9 025 tokens, 53 % of the file | `src/TerseSharp.Server/Assets/SKILL.md`, `DocsCoverageE2ETests` | the swap table and *The whole surface, by job* are merged into one job-keyed table carrying the replaced built-in as a column, so every tool appears in exactly one row; what remains of the second section is the behaviour a table cannot carry | **shipped, predicted saving refuted.** Measured 68 971 -> 68 999 bytes, of which I201 added ~330: the two sections' bulk was non-duplicated behavioural prose, not a repeated enumeration, so the 3 500-4 000 token estimate was wrong by an order of magnitude. The durable value is the single enumeration plus two new census gates - `TheShippedSkill_StaysWithinItsTokenBudget` (17 600) and `…EnumeratesEveryToolExactlyOnceInOneTable`. Reopen only with a measurement that names which specific sentences duplicate each other |
| **I192** `read_text` was the most serialized tool in the corpus: 667 adjacent same-tool round trips, 82.5 % independent, 78 % spanning more than one file | `read_text`, `FileTools` | `paths=[...]`, at most 10, each file under its own path line with its own count and `next:` note, an unresolved path reported inline as `NOT_FOUND`, and `maxChars` a budget shared across the batch that names the entry it clipped. `path` became optional and combines with `paths`, taken first | shipped - one call replaces up to 10, removing an inference each. Measured at p50 7 652 ms of model think and 199 281 cache-read tokens per fused pair. The batch costs at most ~20 tokens per extra file over the same reads sent separately, asserted in `BacklogClosureE2ETests.ReadText_WithSeveralPaths_AnswersThemAllInOneResponseUnderTheirOwnPathLines` |
| **I193** `edit_text edits=` was same-file only, and 50 % of the 621 measured runs spanned more than one file, so half of its 946 round-trip pairs could not be fused | `edit_text`, `FileService` | each `edits[]` entry takes an optional `path` defaulting to the top-level one; entries are grouped by file, each group applied in order as one write, and the answer is one line per changed file. Caps: 10 per file, 25 in total, each refused by name | shipped - `EditText_WithAPathPerEntry_EditsSeveralFilesInOneCallAndAnswersOneLinePerFile` asserts the exact two-line answer and both files' new content; the two cap refusals are asserted by their own tests |
| **I194** `write_text` cost 166 round-trip pairs, 85.5 % independent, 82 % multi-file, at a p50 gap of 16 440 ms - the slowest of any terse tool measured | `write_text`, `FileService` | `files=[{path,content}]`, at most 10, prepared together and applied as **one** `EditGate.ApplyAsync` over every `.cs` document among them, so the compile gate sees the whole set; a rollback writes nothing at all, including the plain files | shipped - `WriteText_WithTwoInterdependentCSharpFiles_LandsThemUnderOneCompileGate` writes a caller before its callee in one call and both land, which is exactly the `CompileRegression` class this repo's traps section records 35 instances of |
| **I195** `run_tests` cost 169 round-trip pairs, 94.1 % independent, with `project` the varying key in 114 of 146 runs, at a p50 of 34 433 ms per call | `run_tests`, `DotnetRunner` | `projects=[...]` resolves each entry through the existing project resolver and reuses the `Targets` path `changed=true` already had, so one results directory and one merged verdict line. The per-invocation timeout became **per project** instead of a shared deadline that silently skipped the rest, and any project that timed out is named in a `WARNING` | shipped - the shared deadline previously broke out of the loop and could report a merged run as passed while later projects never ran. `RunTests_WithSeveralProjects_RunsThemAllUnderOneVerdictLine` asserts total=2 for one project and total=4 for two |
| **I196** `diff_text` cost 53 round-trip pairs at 94.3 % independent and `get_file_outline` 11 at 100 %, and both were **100 % multi-file** | `diff_text`, `get_file_outline` | `paths=[...]` on both. `diff_text` hands every entry to the same `git diff` invocation as its own pathspec, so the answer is one unified diff already labelled per file; `get_file_outline` renders one group per path and reports an unresolved path inline as `NOT_FOUND` | shipped - both assert values against the fixture, and the outline batch is asserted to cost at most 20 tokens per extra file over the same outlines sent separately |
| **I198** the agent had no signal it was repeating itself: 3 260 same-tool pairs cost a round trip, 71.4 % independent, and `gate` measured 0/0/2 calls against `analyze`'s 164/335/64 despite existing to collapse exactly that run | `RepeatSteer`, `McpHost` | a `CallTool` filter counts consecutive calls of the same tool and, from the **third**, appends one framing line naming the plural parameter that tool declares - nothing when it declares none, nothing when the call already used it, and the counter resets on any different tool | shipped - ~12 tokens on a call that has already proved the pattern, nothing on any other. Census-gated by `SchemaCensusE2ETests.EveryToolWithAPluralParameter_IsKnownToTheRepeatSteerAndNoOtherIs`, which derives the plural set from `tools/list` in both directions, so a new plural parameter cannot ship without its steer. Next scan measures the same-tool pair count, 3 260 today |
| **I204** `add_member typeSymbolId=Errors` answered `AmbiguousSymbol` listing five properties and one type, although `add_member` takes a containing type; the retry cost one call plus the full documentation id | `SymbolLookup`, `add_member`, `extract_interface`, `move_type_to_file`, `move_type_to_namespace` | a `typesOnly` flag threaded from the four tools that declare `typeSymbolId` filters the candidate set to `INamedTypeSymbol` before the ambiguity check; a name matching no type says so and counts the non-type matches instead of hiding them | shipped - one call plus one documentation id per collision, and collisions are the norm for a short domain name that is also a property name. The fixture gained `SideHolder`, whose `OrderSide` property collides with the `OrderSide` enum, because the case could not be made to fail there before |
| **I205** the only way to see whether a behaviour change works was an E2E run or a hand-run stdio probe, and the `call` sub-command that makes the probe cheap was named in `CLAUDE.md` and in no shipped surface | `Program.cs` `call`, `SKILL.md`, `doctor` | `doctor`'s `version` line now prints the running assembly path and the exact `dotnet <assembly> call <tool> --workspace <solution> --json '{...}'` shape, and `SKILL.md` gained a paragraph saying a behaviour claim is proven against a freshly built binary, never against the connected server | shipped - 3 s against 13 s for the narrowest filtered E2E run and 1 200 s for the suite, on the single most expensive recurring confusion this repo records. Asserted by `InstallCommandE2ETests.Doctor_NamesTheRunningAssemblyAndTheOneShotProbeCommand` |
| **I206** `get_file_outline` had no way to ask for part of a file: reading `TextSearchService.cs` to find five members cost 45 members and ~1 100 tokens, repeated on two more files in one run | `get_file_outline`, `get_type_outline` | a `contains=` filter keeps only the members whose name matches, printed under their declaring type with an `N of M members` line so the omission is never silent; it composes with `ids`, `signatures` and `parameterNames` | shipped - asserted to cost less than half the unfiltered outline on the widest fixture type, and `get_type_outline` takes the same filter |
| **I186** the suite runs 1 641 tests in 1 102 s, of which `TerseSharp.E2ETests` is 718 tests in 884 s, and the number was unattributed | `TerseServerFixture`, `TerseServerProcess` | `E2ETelemetry` counts server starts, total start time, tool calls and total call time; the numbers are written to stderr on process exit and asserted from a test that prints them | shipped as the **measurement**, not as an optimization - the row existed to make the 884 s attributable before anything was changed, and it now is. Reopen with the numbers a full run prints |
| **I188** round-tripping a `get_symbol_source` payload through `replace_symbol` silently re-indented the member: the read dedents, the write landed the dedented text, and continuation lines ended up shifted left, invisible to `format`, `analyze` and the build | `replace_symbol`, `MemberDeclaration` | `MemberDeclaration.Reindented` shifts a declaration whose first line arrives at column 0 by the target's own column **before** it is parsed, skipping every line that begins inside a multi-line literal, which is found from the parsed token spans | shipped - the earlier trivia-rewriter attempt is recorded as refuted in this row's history and was not retried. `ReplaceSymbol_WithADeclarationReadBackFromGetSymbolSource_ChangesNothing` was observed failing on `OrderBook.For` before the fix and passing after; `OrderBook.TotalVolume` does **not** reproduce it, because a block-bodied member is re-indented correctly by Roslyn and only an expression-bodied continuation is affected |
| **I183** `workspace_status` printed **no version**, although this repo's own hard gate says it does, and every *which binary answered* argument rests on it | `workspace_status`, `doctor` | `workspace_status` ends with `terse=<informational version>`, build metadata after `+` trimmed; `doctor` opens with a first-class `version:` line instead of leaving it inside `update:` | shipped - one line, 4 tokens, on the once-per-session question that cost three documented sessions of arguing with a binary three releases old. Locked by `SteersAndRemediesE2ETests.WorkspaceStatus_ReportsTheRunningVersion` and by `TokenBudgetE2ETests.WorkspaceStatus_KeepsItsTelemetryBehindVerbose`, which now pins the quiet form at exactly 4 lines |
| **I184** `get_file_outline path=src/TerseSharp.Core/Errors.cs` cost a wasted call: `Errors` lives in `Result.cs`, and the `DocumentNotFound` remedy said *use find_files to locate it*, which cannot find a type that does not name its file | `get_file_outline`, `read_text`, `DocumentNotFound` | new `MissingDocument.ReadAsync` resolves the path's file-name stem against the workspace's declared types and answers `'Errors' is declared in <path> - use get_file_outline or get_type_outline on that path`; `Errors.DocumentNotFound` gained a remedy overload | shipped - one call per wrong-file guess, which is the default guess in a codebase that puts several types in one file. Locked by `SteersAndRemediesE2ETests.GetFileOutline_OnAPathNamedAfterAType_NamesTheFileThatDeclaresIt` |
| **I187** `add_member path=<new file>.cs` answered `DocumentNotFound ... use find_files to locate it`, which is the wrong instruction: the answer is `write_text force=true`, and `find_files` will never find a file nobody has written | `add_member` | `MissingDocument.Write` names `write_text path=... force=true` when the path is a `.cs` file inside the workspace root that does not exist yet | shipped - one wasted call per new file, which is every new type this repo adds. Locked by `SteersAndRemediesE2ETests.AddMember_OnAFileThatDoesNotExistYet_NamesWriteText` |
| **I180** `get_symbol_source symbolIds=["ProjectTools"]` returned the whole 176-line class - about 2 500 tokens - when 15 lines of one member were wanted | `get_symbol_source` | a **type** symbol id with no `verbose=true` answers `get_type_outline`'s payload plus one steer naming `get_symbol_source symbolId=Type.Member`; a member id and `verbose=true` are unchanged, and a delegate falls through to its source | shipped - ~2 400 tokens on the measured call, and it removes the last way to ask for a whole file's text by accident. Locked by `SteersAndRemediesE2ETests.GetSymbolSource_OnATypeId_AnswersTheOutlineAndSteersToOneMember` and its `verbose=true` counterpart |
| **I185** `search_symbols query=SearchText` returned **21 records, 20 of them tests**, burying the single production declaration | `search_symbols` | `SymbolSearch` now carries an `IsTest` flag per declaration, ranks `src` above `test`, and - only for `search_symbols`, never for `SymbolLookup`, which must still resolve a test symbol by name - folds the test half to one `N more in test projects - scope=test` line when the production half is non-empty and the count is exact | shipped - 20 of 21 records on the measured call. Nothing is hidden silently: the withheld count is always named, and a name that matches only test projects is still answered in full. Locked by `SteersAndRemediesE2ETests.SearchSymbols_KeepsTheProductionHalfAndNamesTheTestHalf` and by the rewritten `BuildWarningsE2ETests.SearchSymbols_WithScope_KeepsOnlyThatHalfOfTheSolution` |
| **I190** `solution_projects path=<x>.slnf` answered `0 projects` for a filter naming projects, and the capability was then withdrawn by name on four shipped surfaces | `SolutionFile.ProjectsAsync`, `solution_projects` | `solution.projects[]` is parsed with `JsonDocument` over an async `FileStream`; `.slnf` is a solution file to `IsSolutionFile` again; malformed JSON is refused with a remedy rather than answering `0 projects`. `fixtures/FixtureSolution/FixtureSolution.slnf` added | shipped - restores an advertised capability. Locked by `SteersAndRemediesE2ETests.SolutionProjects_ReadsASolutionFilter`, and `ProjectToolsE2ETests` now asserts the *missing* filter case instead of the refusal it used to assert |
| **I182** a run fell back to `Bash: rm -rf fixtures/SelectionSolution/**/{bin,obj}` to reproduce a cold build, because `clean` only served a **loaded** workspace | `clean` | `path=` takes a `.slnx`, `.sln`, `.slnf` or project file that is not loaded and sweeps from its own directory; a path that does not exist, or is not a solution or project file, is refused by name | shipped - one shell fallback per fixture-cold-build cycle, and it closes the last `rm -rf` this repo's own sessions reached for. Locked by `SteersAndRemediesE2ETests.Clean_WithAPath_SweepsASolutionThatIsNotLoaded` and its refusal counterpart |
| **I189** `search_text`/`search_regex` `queries=[...]` advanced past the **end of the match**, not the end of the reported line, so an entry matching across a line break consumed the lines its match spanned and the other entries' hits on them were never emitted | `TextSearchService.Collect` | when `SeveralPatterns`, the scan resumes at the end of the line the match **starts** on, so every line is still offered to every entry; a multi-line entry is reported once, at the line its text starts on | shipped - closes the last silent false negative in `queries=`, and the *keep the entries line-local* caveat is removed from all six surfaces. Reproduced on the stock fixture first: `queries=["class OrderRouter\n{", "{"]` answered 1 match and now answers 2. Locked by `SteersAndRemediesE2ETests.SearchText_WithAMultilineEntry_StillOffersEveryLineToTheOtherEntries` |
| **I181** naming the missing namespace fixed the *diagnosis*, not the *action*: adding it was still `edit_text force=true` on the file header, so the shape was 3 calls - rejected edit, header edit, `retryWith` | `edit_text`, `add_member`, `replace_symbol` | `usings=[...]` on `replace_symbol_body`, `replace_symbol` and `add_member`, carried on `EditOptions` and applied by a new `UsingDirectives.Ensured` inside the same compile-gated write; the block is left sorted System-first and a namespace already present is ignored | shipped - 3 calls to 1 on the commonest rollback class, and it removes the `force=true` text edit on a `.cs` file, which is the one edit path that bypasses symbol addressing. Locked by `SteersAndRemediesE2ETests.AddMember_WithUsings_AddsTheImportInTheSameEdit` |
| **I191** every one of the 87 `[McpServerTool]` attributes declared **no MCP annotation**, and Claude Code gates parallel dispatch on `readOnlyHint`, which defaults to false when absent | every `[McpServerTool]` in `src/TerseSharp.Server/Tools/*.cs`, `SchemaCensusE2ETests` | `ReadOnly = true` on 45 non-mutating tools and `Destructive = true` on the 9 deleting ones | shipped - one word per tool, no response-format change. **Deviation from the proposal, deliberately:** `build`, `run_tests`, `rerun_failed`, `list_tests` and `gate` are **not** marked read-only although the row named `list_tests` - they run a build, and a build dispatched concurrently with an edit is a race, not a saving. Census-gated in both directions by `EveryReadOnlyTool_DeclaresItsReadOnlyAnnotation` and `EveryDestructiveTool_DeclaresItsDestructiveAnnotation` |
| **I197** the position and mood of a plural parameter's mention in a `[Description]` predicts its adoption: `symbolIds`, an imperative in sentence two, reached **48 %** of `get_symbol_source` calls; `queries`, a statement of fact, sat at **2.6 %** and `search_regex.queries` at **0 of 502** | `get_symbol_source`, `search_text`, `search_regex`, `replace_symbol` | every plural mention rewritten as *Pass X to ... . Replaces one call per ...*, in both the tool description and the parameter's own, matching the wording that measures 48 % | shipped - no code path changed. Census-gated by `SchemaCensusE2ETests.EveryToolWithAPluralParameter_NamesItImperativelyInItsDescription`, which discovers every array-typed property whose name is the plural of a scalar sibling from `tools/list` and refuses to go vacuous, so every plural parameter I192-I196 adds is enrolled automatically |
| **I199** I166's premise was a measurement artifact - Claude Code writes one JSONL record per content block, so counting `tool_use` blocks per record always yields 1 - and its rejection clause rested on it | `IMPROVEMENTS.md` I166 | the correction is appended to I166's `Outcome` cell, which is never deleted: the corrected histogram grouped by `message.id` is 1:46 424, 2:7 875, 3:1 481, 4:735 up to 12, so **18.6 %** of tool-calling messages already carry two or more and the rate never moved | shipped - it removes a wrong number that was load-bearing for a rejection, and unblocks I192-I196. `SKILL.md` working rule 13 was re-read against the corrected number and its claim - that runs of three or more consecutive same-tool calls cost wall clock nothing depended on - is still true, so it stays |
| **I203** proposal to cut the advertised-surface cost by caveman-compressing the tool and parameter descriptions - drop articles, hedges and justifying clauses - on the theory that the 12 376 tokens of prose inside a 24 050-token payload are the compressible mass | every `[Description]` | measured instead of assumed: the five largest descriptions were hand-compressed keeping every parameter name, every refusal rule and the replaced built-in, then tokenized with o200k_base against the originals taken from a live `tools/list` | **rejected on measurement.** The squeeze is **11.8 %** - 970 tokens to 856, by tool `search_regex` 18.4 %, `run_tests` 13.3 %, `changed_files` 11.8 %, `write_text` 6.3 %, `load_workspace` 4.1 % - because this repo's prose is already dense and the yield falls as the description gets more technical. Extrapolated across all 87 tool descriptions and all 463 parameter descriptions that is ~1 450 tokens, **6 % of the payload**, bought at the highest risk in the list: I197 measured that description **wording** moves plural-parameter adoption from 2.6 % to 48 %, and Anthropic names wrong tool selection and wrong parameters as the dominant tool-use failure, so rewriting every description to save 6 % spends the one lever proven to work on this surface. Both levers it was compared against are strictly better - I200 takes 7.4 % with no prose touched at all, I201 takes 26.4 % by not advertising tools the workspace cannot use. Kept from the attempt: the payload composition, not previously known - 25 % tool descriptions, 27 % parameter descriptions, 24 % schema metadata, 21 % JSON punctuation and names - and the tokenizing scripts under `.research/batch/`. Caveat recorded with the number: o200k_base is a proxy because Anthropic publishes no tokenizer, but it lands within 4 % of the 12 849 figure I160 measured independently |
| **I175** a `Bash: grep -n` fallback on `IMPROVEMENTS.md` to locate 8 row ids at once - a gate breach, acknowledged at the call - because one `search_text` for the shared prefix answered `60/82 matches truncated` with every record carrying the identical matched text, and there was no way to ask for several literals in one call | `search_text`, `search_regex` | `queries=[...]` takes up to 10 literals or expressions in ONE pass over the same file set, every record tagged `q1`..`qN` by the position of its query in the array; `query` and `queries` combine with `query` first, an 11th entry is refused naming the cap and a blank entry is refused rather than matching everything | shipped - N calls -> 1 on every *where are these N things* question. A line matching several queries is **one** record carrying every matching tag, comma-separated in query order; the first cut emitted only the earliest query's tag, which the review round caught as a false negative the caller could not detect (`IMPROVEMENTS.md:31` matched two ids and reported one), and the second cut emitted them unsorted (`q2,q1`). The **legend was implemented and removed**: printing `q1=<literal>` cost exactly what the N-1 saved response headers saved (batched **187** tokens against **186** for four separate calls, measured on the fixture), and `q1` is `queries[0]`, which the caller passed, so it was ceremony under the response rule. Without it the same case measures strictly fewer tokens, locked by `SearchText_WithSeveralSparseQueries_CostsLessThanOneCallPerQuery`; `SearchText_TaggingRecordsByQuery_CostsAtMostTwoTokensPerRecord` bounds the tag on the widest fixture case and `SearchText_WithASingleQueriesEntry_IsByteIdenticalToTheQueryForm` locks the single-query answer byte-for-byte. All four E2E tests observed failing first |
| **I176** locating a member by a common name cost an extra round trip: `get_symbol_source symbolId=DotnetRunner.Report` answered `matches more than 100 symbols, so it cannot be resolved safely` for a private method of a named class, and the remedy then advised qualifying a name that already was | `SymbolLookup` | a saturated **qualified** name falls back to the members of the types called `Type`, which cannot saturate on a normal type name; a bare name still refuses, and the fallback itself refuses rather than resolving from a truncated set when the type name saturates too | shipped - one call instead of one call plus one retry on every common member name. Reproduced live in this run on `TextSearchService.Text`, then in the fixture: `fixtures/FixtureSolution` gained `ProbeSaturation.cs` (101 `Probe###` constants plus `ProbeHost.Probe`) so `GetSymbolSource_ForAQualifiedNameWhoseMemberSaturates_ResolvesThroughTheContainingType` was observed failing `ERROR AmbiguousSymbol`, with `GetSymbolSource_ForAnUnqualifiedNameThatSaturates_StillRefusesRatherThanGuessing` green in both directions as the control |
| **I177** a rolled-back edit whose only fault is a missing using directive cost the same round trip as a genuine compile regression - hit twice in one run on `ImmutableArray` | `EditGate`, `replace_symbol`, `add_member` | when every new error is `CS0246`/`CS0103` for a name the changed project resolves in exactly one namespace, the remedy reads `add: using System.Collections.Immutable; then replay the rejected text with retryWith` (up to three, sorted) | shipped - 3 calls -> 2 on a common edit shape. The candidate name is parsed from the diagnostic and then **verified** against the project's own declarations, so a name resolving nowhere, in two namespaces, or alongside a real regression yields no hint rather than a wrong one; `AddMember_RolledBackForAMissingUsing_NamesTheNamespaceToImport` was observed failing and `AddMember_RolledBackForARegressionWithNoImport_NamesNoImportItCannotProve` is the negative control. Adding the using automatically stays rejected: it edits a region the caller did not address |
| **I178** *which projects does this solution contain* had no cheap answer for a solution that is not loaded; establishing that `FixtureSolution.slnx` holds one project took a failing E2E test, a failing unit test and a throwaway diagnostic test | `list_projects`, `solution_projects` | `path=` on **`solution_projects`**, not on `list_projects`: it is already the read-the-file-not-the-workspace tool, and `list_projects` cannot report `Language`/`documents=` for a solution it has not loaded, so a reduced record shape under the same tool name would be the confusing answer | shipped - ~4 calls -> 1. A path that is not a solution file, or that does not exist, is refused with a remedy instead of answering `0 projects`, and `list_projects` now names the tool for the unloaded case so it stays discoverable. `SolutionFile` also went async (`XDocument.LoadAsync`, `File.ReadAllLinesAsync`) with the token threaded from all three solution tools. Three E2E tests, two observed failing first. Loading the fixture as a second workspace stays rejected - it makes every un-hinted call ambiguous |
| **I179** `edit_text` reported `changedLines=2426` for an edit git measures at `+89 -1` - a 27x overcount, and under the condensed success response that count is the only evidence the caller gets | `edit_text`, `UnifiedDiff` | above the 2 000 000-cell alignment ceiling the diff now splits recursively on a line occurring exactly once on each side, nearest the middle, and aligns each half; only a region with no such anchor, or more than 24 levels, still falls back to one block | shipped - measured **2 496 -> 2** on a 3 000-line file with two one-line edits 2 495 lines apart, and the `verbose=true` diff carries two small hunks instead of one 2 496-line hunk. Root cause was not the counter but the fallback block: `Counted` was right about a wrong `Blocks`. Two unit tests observed failing at 2 496, plus `EditText_OnALargeFileWithTwoFarApartEdits_ReportsTheChangedLinesTheDiffShows`, whose first draft asserted `Contains("changedLines=2")` - a substring of `2496` - and was tightened after a mutation run with `MaxSplitDepth=0` showed it passing against the bug |
| **I172** the ~1.1 s per-call floor is real in the transcripts but is not in workspace resolution - I161 measured resolve+sync at 0.21 ms, flat from 1 to 4 projects | `ToolContext`, `Doctor`, `PhaseProbe` | `terse doctor` prints `phases: widest=<file> outlineMs= gateMs= diffMs=`, timing a real `get_file_outline`, a real `EditGate` dry-run compile gate and a real `git diff` spawn against the widest `.cs` document of the loaded solution | shipped - the floor is now attributed to the phase that holds it rather than guessed, so the next optimization is aimed. Two unit tests over `ToolContext.MeasurePhasesAsync` (loaded and nothing-loaded) and `Doctor_AttributesThePerCallFloorToTheOutlineTheCompileGateAndTheGitSpawn`, which asserts the line exists and that `outlineMs` is not `0.00`. The whole `ToolContext`/`WorkspaceSync` hypothesis stays **measured out** at 0.21 ms |
| **I173** the positive path of `run_tests changed=true` - a selective run that genuinely skips a test project - had no end-to-end coverage, because `FixtureSolution.slnx` contains one project and does not include `Fixture.Trading.Tests` | `FixtureSolution`, `ChangedTestSelectionE2ETests` | a **dedicated** `fixtures/SelectionSolution`: one source project and two test projects, only one of which references it | shipped - `RunTests_WithChanged_RunsOnlyTheTestProjectThatDependsOnTheChangeAndNamesTheSkippedOne` edits the source project and asserts `total=1` of the fixture's two tests with the skipped project named; its control asserts `total=2` and the stated fallback reason when nothing changed, which is the built-in mutation check. Each test starts its own server so the two cannot couple through the workspace change set. Adding `Fixture.Trading.Tests` to `FixtureSolution.slnx` stays rejected - it moves `projects=1` to `projects=2` in five checked-in assertions plus the token budgets |
| **I174** the load-time MSBuild diagnostics of `workspace_status` and `load_workspace` were still absolute - 36 876 and 17 643 characters in the measured week | `workspace_status`, `load_workspace`, `LoadFailureSummary` | `LoadFailureSummary.Relative` strips the workspace root from each failure and warning **after** `Group` has parsed it, and from an unattributed failure's group key | shipped - a failing project's path is now re-usable as an argument like every other path in a response. `fixtures/UnloadableSolution` was added because no existing fixture reports a load failure at all (`BrokenSolution` loads cleanly and breaks at compile time), and two of its three E2E tests were observed failing on the absolute path. The ordering worry that stopped I163 is **refuted in writing**: `Group_AttributesTheSameProject_WhetherTheMessageIsAbsoluteOrAlreadyRelativised` shows the parser reads the quoted `*proj` file name and is indifferent to the path shape, so relativising after grouping is belt-and-braces, not load-bearing |
| **I171** `gate` answered `clean  analyzed=0 fixed=0 remaining=0` over a scope it had genuinely analyzed, byte-identical to a gate that ran over nothing - observed twice in one scan, including on an explicit `path=` naming a file `analyze` was answering about at the same moment | `gate` | `GateService.Render` bound `analyzed=` to `before.Length`, the **pre-fix diagnostic count**, so any clean scope rendered `analyzed=0`. It now counts the documents the gate had in scope (`DocumentScope.Select`, or `Editable` when unscoped), computed once before the write steps move mtimes | shipped - `analyzed=0` is now unreachable: a scoped gate errors below one document and an unscoped one counts every editable document, so the confident wrong answer is gone rather than re-labelled. `Gate_AnalyzedCountsTheDocumentsInScope_NotTheDiagnosticsItFound` locks it and was observed failing `Expected: 1 / Actual: 0` on the clean single-file scope. The row's other two halves are **refuted on evidence**: the `path=` scoping was never broken (`CollectedAsync` selects the named document and `Targets` its project - the `analyze` control run agreeing at `0 diagnostics` is the same file being analyzed, not a different one), and the proposed `NOTHING ANALYZED` line is already served better by `ERROR DocumentNotFound` / `ERROR Invalid` with a remedy, now locked by `Gate_OverAScopeMatchingNoDocument_AnswersAnErrorWithARemedy`, which passed before the fix. No `NOTHING ANALYZED` branch was shipped: it would be unreachable on every fixture, and a test the fixture cannot fail is not coverage |
| **I161** every workspace-resolving call was believed to pay a ~1.1 s floor, with the sync drain running per call as the prime candidate | `doctor`, `ToolContext` | `ToolContext.MeasureAsync` times `Registry.Resolve`, `SyncedAsync` and the action separately on the **real** request path, and `doctor` runs a 20-call loop and prints `latency: calls=N resolveMs=.. syncMs=.. actionMs=..`, failing above a 100 ms resolve+sync floor | shipped, and it **refuted the hypothesis it was built to confirm**: `resolveMs=0.07 syncMs=0.14 actionMs=0.00` on the freshly built 0.28.1-pre binary - **0.21 ms**, identical on a 1-project fixture and this 4-project / 308-document solution, four orders of magnitude below the 1 118 ms transcript minimum. `WorkspaceSync.SyncAsync` short-circuits on `pending.IsEmpty`, so the drain does not run per call. The transcripts' own numbers already contained the refutation - `workspace_status` min 9 ms goes through the same resolver. Two unit tests lock the split, including the nothing-loaded path. The residual, correctly located, is **I172** |
| **I160** the server advertises **87 tools / 12 849 tokens** of `[Description]` text on every request, past every published inflection point for selection accuracy; `--tools core` shipped in I140 but stayed opt-in and the built-in fallback share **rose** 29 % -> 31 % across two scans | `ToolProfile`, `GuardProfileCoherenceE2ETests` | make `core` the default. **Implemented, then reverted inside the same release on measured evidence** - the profile stays opt-in | closed as a measured decision, not shipped. The new census `GuardProfileCoherenceE2ETests` collects every tool the guard names across file kinds and replaced commands and finds **33** of them absent from `CoreTools` - every `xaml_*`, `resx_*` and `razor_*`, plus `cleanup`, `format`, `clean`, `gate`, `list_tests`, `rerun_failed`, `diff_text`, `rename_symbol`, `get_symbol`. The premise the row rests on - a hidden tool "still answers when called by name" - is proven for the **server** and unproven for the **client**: an agent can only call what its tool list carries, so defaulting to `core` would point every guard denial on a XAML, Blazor or localized codebase at a tool the agent cannot reach. Reopen only with evidence that a client can invoke an MCP tool absent from `tools/list`, or after the 33-tool debt is paid down; the census ratchet records it. Kept from the attempt: an explicit environment parameter on `Resolve` with 9 unit tests, `--tools all` on the census fixture and `BuildWarningsE2ETests`, and `ToolListDiscoveryCensusTests`, which caught `BuildWarningsE2ETests` silently shrinking from four build/test tools to two. Also rejected: two extra spawned probe servers, which hung the test host 300 s past a 9.8 s run |
| **I164** `run_tests` was **15.99 h of the 34.5 h** of terse wall clock in the window - 751 calls, p50 34 433 ms - and 101 of them ran the whole solution with no filter | `run_tests`, `ToolContext`, `DotnetRunner` | `changed=true` selects test projects from the `ChangedSinceUtc` set: changed documents -> their projects -> the test projects that transitively reference them, at **assembly** granularity. `ChangedTestSelection` computes it under the lease in `ToolContext.Described`, and `DotnetRunner` runs each selected project into one shared TRX directory so `TestResultParser` merges the per-project report unchanged | shipped, opt-in and fail-safe - it falls back to the whole solution **naming the reason** when nothing changed, when a changed file belongs to no project, or when no test project depends on the change, and it always names the skipped set, so a selective run is never mistaken for a full one and a silent under-run is impossible. 5 unit tests over a synthetic `AdhocWorkspace` solution assert the exact ran/skipped sets, including the test-file-changed and orphan-project cases; the E2E test locks the fallback. One real defect was found and fixed on the way: `WithTargetAsync` resolved **without** the sync drain, so a newly written document was invisible to `changed=true` - `TargetAsync` now drains when, and only when, a selection is asked for. Coverage gap tracked as **I173**. Symbol-level selection stays rejected (static RTS measured unsafe under reflection, OOPSLA 2019) |
| **I167** a sibling worktree was unreachable without loading it, so its change set was read in the shell: **420 `git` calls** in one week, **112 of them `git -C <other worktree>`** | `changed_files`, `diff_text`, `ToolGuard` | `root=` on `changed_files` and `diff_text` - any absolute directory, answered without loading it and tagged `outside-workspace`, exactly as `search_text root=` already worked. The guard's `status` and `diff` deny reasons now name the `root=` form | shipped - 5 E2E tests cover the tag, the relative-path refusal, the missing-directory refusal and the `diff_symbols` refusal. **`diff_symbols root=` was deliberately not shipped and the reason is in the response**: mapping a hunk onto a declaration needs that directory's Roslyn compilation, so it refuses and names `changed_files root=` and `diff_text root=` rather than returning a degraded answer. The guard needed no new row - it already resolves a `-C` target and denies `status`/`diff`/`ls-files` when that directory is a .NET tree; what it lacked was a replacement to name, which now exists. Loading each worktree stays rejected: p50 52 492 ms and ~3 GB resident |
| **I163** the child-process passthrough was not relativised: **227 835 characters of absolute path** across `build`, `run_tests`, `workspace_status` and `load_workspace` in one week | `run_tests`, `list_tests`, `DotnetRunner` | `AppendFailureOutput` rendered its error lines raw and called `AppendTail` with `root` hard-coded to `string.Empty`; it now takes the workspace root, and `RenderTest`, `RenderNoResults`, `RenderTestNames`, `TestAsync` and `ListTestNamesAsync` thread it through | shipped for the test half - a failed `run_tests` or `list_tests` now answers workspace-relative paths that are re-usable as arguments. Two paired unit tests prove both directions: with a root the absolute prefix is gone, without one it survives, so the pair fails if `Relative` ever becomes a no-op. `build` needed no change - `RenderBuild` already relativised both its diagnostics and its tail, so the row's attribution of 113 513 characters to `build` is **not** reproduced here and no number is claimed for it. The load-time half is deliberately out of scope and tracked as **I174**. Dropping the path stays rejected: the caller passed none of them, so it cannot derive them |
| **I170** the guard answered a denial with a `permissionDecisionReason` string only - the weakest documented lever (negated instructions inverse-scale with model size, arXiv:2209.12711; instruction adherence decays 0.877 -> 0.707 over three turns, arXiv:2410.15553; restating recovers only 15-20 %, arXiv:2505.06120), measured here as 2 278 shell-text `Bash` calls surviving a guard installed and current all week | `ToolGuard` | a denial now also returns `hookSpecificOutput.additionalContext` - `Call this instead: <the complete replacement call, arguments filled in from the denied command>` - which Claude Code places beside the tool result | shipped - the routing is chosen from the **file kind**, so a `.xaml` read routes to `xaml_outline`, a `.resx` read to `resx_get` and a `.razor` read to `razor_outline`, never to `get_file_outline`; a replaced shell command routes to `build`, `run_tests`, `changed_files`, `diff_symbols`, `clean` or the exact `cleanup fix=` form. 8 unit tests, including that an **allowed** call carries no `additionalContext`. Observable next scan as the shell-text call count. `updatedInput` stays rejected: it rewrites the arguments of the same tool and cannot reroute a `Bash` call to an MCP tool |
| **I168** the shell-text fallback class did not shrink after I139 and I144: **2 278 `Bash` shell-text calls** corpus-wide, and whether the guard denied them, never saw them, or allowed them as out-of-workspace is **not knowable from a transcript** | `ToolGuard` | opt-in `TERSE_GUARD_LOG=<path>` appends one JSON line per decision - tool, verdict, routing, reason, `cwd`, `session_id`, `transcript_path` | shipped - the session and transcript fields are what split subagent traffic from the main thread, which matters because claude-code#59309 documents that a subagent does not inherit the parent's `CLAUDE.md` and "then uses unrestricted tools (e.g. `grep` ...)". A dedicated subagent boolean was **not** shipped: the `PreToolUse` payload carries no such field, and inventing one would be a value the guard cannot prove - the session/transcript pair is the honest form of the same split. Opt-in and best-effort: a malformed payload still produces one line, and a write failure never changes the verdict. 4 unit tests. Proposing a guard rule from the current data stays rejected - four candidate causes each need a different fix |
| **I166** the agent never issued a parallel tool call: **17 567 of 17 567** assistant messages carrying a tool call carried exactly **one** (fan-out histogram `1:17567`, nothing above), while 5 989 calls sat inside runs of >=3 consecutive calls of the same tool - longest `edit_text` 26, `read_text` 17, `get_symbol_source` 14, `replace_symbol` 12 | `SKILL.md` | working rule 13 states the positive form - independent calls go in one assistant message, with worked examples - and spells out the dependency carve-out so a symbol id from an outline or a `retryWith` token is still awaited. Parallel tool use is on by default from Claude 4 and off only via `tool_choice.disable_parallel_tool_use`, so a histogram of exactly 1 is a prompt defect, not a client limit | shipped - the next scan verifies it by the fan-out histogram alone, which is the whole measurement. At the measured 2 926 ms p50 the serialized runs were ~4.9 h per week that had no dependency. A list parameter on these tools stays rejected: the client already supports parallel `tool_use` blocks and I141 has batched `edit_text`, so duplicating it in the schema pays surface for a mechanism that exists. **CORRECTION (I199):** the `1:17567` histogram is a measurement artifact - Claude Code writes one JSONL record per content block, so counting `tool_use` blocks per record always yields 1. Grouped by `message.id` over the same corpus the histogram is 1:46 424, 2:7 875, 3:1 481, 4:735 and up to 12, i.e. **18.6 %** of tool-calling messages carried two or more, weekly 16.0 to 24.1 % from 2026-W28 to W33 - the rate never moved and was never zero. The shipped `SKILL.md` change stands, but the *a list parameter on these tools stays rejected* clause is unsupported and is what I192-I196 act on. Any future fan-out measurement groups by `message.id` |
| **I165** `gate` was called **0 times** in 11 325 terse calls, three releases after I148 shipped it to collapse exactly this sequence: `analyze` 356 calls (1.46 h), of which only 56 passed `changed=true` and 204 re-passed `path=` + `minSeverity` | `SKILL.md`, `analyze` | `analyze changed=true` - the end-of-task shape - appends `gate runs this, format and cleanup fix=all as one call`, and the skill's Analyse section now **leads** with `gate` instead of listing it last | shipped - a tool that existed for three releases and was never discovered is now named at the moment it applies, for ~11 tokens on the ~56 calls per week that carry the shape; I148 measured the collapse at 3 of every 4 calls on that path. `Analyze_WithChanged_NamesGateAsTheOneCallForm` writes a probe document first, because `changed=true` over an unmodified fixture errors and the steer is unreachable - the test was observed failing that way before it was fixed. Auto-running `gate` stays rejected: `build` must still be read before any test result |
| **I169** framing residue: **13 629 `HEURISTIC` tags** across 1 151 `search_text`/`search_regex` calls in a one-week scan, on two tools documented to answer nothing else - one tag per record, on a value that can never vary. The row also claimed a `1 files changed` line still opening `replace_symbol` (47), `add_member` (43) and `replace_symbol_body` (17) responses | `search_text`, `search_regex` | **half shipped, half refuted.** Shipped: `TextSearchService` emits `path:line  <payload>` and states `HEURISTIC  text match` once per response; the dedup key moved from the tag to the record separator. Refuted: `EditGate.Render` already suppresses `N files changed` whenever the answer is condensed, and `Condensed` is false only for `verbose`, `dryRun`, a zero-diff run, a new compile error or an unresolved symbol - every one a caveat the success-costs-nothing rule **requires** showing, so the observed occurrences were correct, not residue | shipped - ~4-5 tokens per record removed from every `search_text`/`search_regex` record, against one 4-token note per response; `SearchText_TagsTheResponseHeuristicOnceInsteadOfEveryRecord` asserts exactly one tagged line and more than one record. `EXACT`/`HEURISTIC` on the tools that genuinely answer both is untouched. The `1 files changed` half is closed as refuted with the `Condensed` predicate as the evidence. Still rejected: editing record text to hoist a shared tag - the defect that silently rewrote a `get_symbol_source` of the constant defining `EXACT`; collapsing the two-space column separator, refuted in I143; and suppressing `N files changed` on the symbol editors, refuted here |
| **I162** `read_text` called a read **truncated** that it never clipped: **1 771 of 2 320 responses (76.3 %)** in a one-week scan carried a truncation claim, and on the shipped 0.28.0 an explicit `startLine=1 endLine=3` over a 237-line file answered `3/237 lines truncated`, a complete `section=` answered `2/2347 lines truncated`. `SKILL.md` documented the opposite - "a read your own `startLine`/`endLine` ended says nothing" | `read_text` | root cause was `FileService.Render` passing the **file's** line count as the summary total whatever the caller asked for; a new `ReachableLines` counts against what the caller could still reach - `CoveredLines` when `NextLine is 0`, the file total only when the read was genuinely cut | shipped - a false steer removed from 76 % of the calls of the most-called tool in the surface, restoring the invariant that a truncation count is always real. `ReadText_WithACallerChosenRange_CountsWhatArrivedInsteadOfClaimingTruncation` and `ReadText_WithACompleteSection_NeverCallsItTruncated` were observed failing with the exact reported strings (`2/5 lines truncated`, `4/15 lines truncated`) and pass after; `ReadText_WithMaxLinesOne_ReturnsOneLineAndSaysItTruncated` and `ReadText_WhenTheToolClipsTheRead_NamesTheLineToContinueFrom` stayed green as the no-regression control. Four existing tests asserted the old string and were judged **assumption, not requirement** (they encoded the defect); each was inverted, and the two `ToolEdgeCaseE2ETests` cases gained a `(total=5)` assertion proving the file total survives on the past-the-end note. The reviewer's WARNING that the `tail` and mid-line-cut shapes had no summary assertion was fixed in the same round. Dropping the count line stays rejected - it is the one value proving how much arrived |
| **I156** the guard denied `dotnet format analyzers` and `dotnet format style` with one generic message - "use format, cleanup fix=all, or cleanup verify=true" - so the agent still had to guess which mode matches, and `fix=all` is a superset that names files CI accepts (`ReleaseVersion.cs` here). This run shelled out to both commands twice under the old carve-out | `ToolGuard` | the `dotnet format` row resolves its sub-command and names the exact equivalent: `cleanup fix=analyzers` / `cleanup verify=true fix=analyzers`, `cleanup fix=style` / `cleanup verify=true fix=style`, and the whitespace-plus-cleanup pair for a bare `dotnet format` | shipped - two ~90 s solution-wide `dotnet format` runs per task replaced by two in-server verify calls answering one line each; `ToolGuardTests.Inspect_ForDotnetFormatWithASubcommand_NamesTheExactCleanupThatVerifiesTheSameThing` covers all four forms and the bare-`format` fallback. Denying `dotnet format` outright with no named replacement stays rejected: a deny that does not name the tool is what sends an agent back to the shell |
| **I154** `changedLines` reported the **span between** the first and last change, not the changes: a 3-edit `edit_text edits=[...]` over `README.md` answered `changedLines=125` where git reports `+3 -3`, `NUGET_README.md` 101 for `+3 -3` and `CLAUDE.md` 135 for `+2 -2`. The same bracket is the diff, so `verbose=true` would have printed 125 removed and 125 added lines | `UnifiedDiff`, and therefore every mutating tool | line alignment is a real LCS over the region between the common prefix and suffix, falling back to the old single block above 2 000 000 cells so memory stays bounded; `changedLines` sums `max(removed, added)` per block and `Between` emits one `@@` hunk per block | shipped - a confidently wrong number removed and a `verbose`/`dryRun` diff bounded by the edit instead of the file: 36 -> 2 on the 40-line regression case, observed failing first in `UnifiedDiffTests.ChangedLines_ForTwoDistantEdits_CountsTheEditsNotTheSpanBetweenThem` and `Between_ForTwoDistantEdits_EmitsOneHunkPerEditInsteadOfOneSpanningBoth`. Context lines around each hunk stay rejected: the hunk header already locates it and the lines are pure payload |
| **I155** `search_symbols` could not tell the two halves of a solution apart: `query=Same kind=method` returned 28 declarations of which 26 were test methods, burying the one production `PathBoundary.SameFile` the question was about | `search_symbols` | `scope=src\|test` filters the projects before the search, using the same `TestScope` split `find_usages` already tags usages with; an unknown value is refused naming the two accepted ones | shipped - ~24 unusable records (~600 tokens) on the measured call; `BuildWarningsE2ETests.SearchSymbols_WithScope_KeepsOnlyThatHalfOfTheSolution` runs against `WarningSolution`, the one fixture holding both a src and a test project, and was observed failing with the filter disabled. Filtering the rendered records instead of the projects stays rejected: it pays for the search it then throws away |
| **I147** a `CompileRegression` printed the **absolute** path of every rejected diagnostic - `CS0103 C:\Users\...\GateHost.cs: ...` - while every other record in the surface is workspace-relative | `EditGate`, `Errors.CompileRegression` | the diagnostic key is built through `PositionFormat.Relative(root, ...)`, threaded from `ApplyAsync` down through `AnalyseAsync`/`TallyAsync`/`Collect`/`Record`; `Arrived` records relative paths too, so the unresolved-name classification still matches | shipped - ~10 tokens per rejected diagnostic, and the rollback payload is now re-usable as an argument; `CompileGateE2ETests.ARejectedDiagnostic_NamesItsFileRelativeToTheWorkspaceRoot` asserts the relative path is present and the absolute root is absent. Dropping the path stays rejected: it is the one value the caller cannot derive |
| **I152** a `retryWith` token recorded no workspace, so replaying it with a different `workspace=` replayed the held declaration into another worktree - this tree keeps whole copies under `.claude/worktrees/agent-*`, where the same symbol id resolves in both | `RejectedEdits`, `EditTools` | `RejectedEdit` carries the `Root` it was rejected in; `WithSymbolAsync` gained an optional guard that runs **before** symbol resolution, so the refusal names both roots instead of failing later as `SymbolNotFound` | shipped - one wrong-worktree edit per occurrence, unbounded cost; `CompileGateE2ETests.ARetryToken_ReplayedAgainstAnotherWorkspace_IsRefusedInsteadOfEditingIt` loads a second workspace into the same server and asserts the refusal. Binding the token to the `workspace=` string the caller passed stays rejected: it is usually null on both calls |
| **I153** `terse guard` denied `git -C ../other-repo ls-files`, but `find_files tracked=true` answers about the **loaded workspace**, not `<dir>`, so the replacement could not answer what was denied | `ToolGuard` | all three git rows resolve the directory the command actually addresses - the `-C` target, then a directory operand, then the hook payload's `cwd` - and deny only when *that* sits under a `.sln`/`.slnx`/`.slnf`/`.csproj` | shipped - removes a deny with no replacement; `ToolGuardTests` covers both directions, `git -C <plain> ls-files`, `git -C ../notes status` and `git status ../notes` allowed, `git status` and `git -C . diff` in a `.csproj` tree still denied. Dropping the `-C` form from the guard stays rejected: `git -C src status` inside the same tree is the breach the row exists to stop |
| **I149** `search_regex` returned records with an **empty payload**: 33 of 66 records were `path:NN  HEURISTIC  ` with nothing after the tag, one blank record immediately before each real hit | `search_regex` | root cause was the scanner advancing past the line holding the match's *start*: with `RegexOptions.Multiline`, `^\s*(public\|internal\|private)` matches at the blank line above and spans onto the declaration, so it was reported at the blank line and then found again on the real one. `TextMatcher.Next` now returns the match length, a hit is reported at the first line carrying its text, and the scan resumes after the line the match *ends* on | shipped - half of a `search_regex` response on a multi-alternative pattern; `FileToolsE2ETests.SearchRegex_WithAPatternThatCanStartOnTheBlankLineAbove_LeavesNoRecordWithoutAPayload` asserts every record has a payload. Asserting it is the caller's regex stays rejected: the real hit follows on the next line, so the empty record was additional |
| **I150** an array parameter that failed to bind named neither the parameter nor the offending fragment - `JsonException ... BytePositionInLine: 9079` on a 9 079-character argument, followed by the list of every accepted parameter | `ToolArgumentFilter` | a `JsonException` carrying a byte position now also names each supplied parameter whose schema declares `items`, and quotes the 80 characters around that offset | shipped - one re-send of the whole call per occurrence; `ToolEdgeCaseE2ETests.ReplaceSymbol_WithAnArrayEntryThatCannotConvert_NamesTheArrayParameterAndQuotesTheOffendingText` asserts the parameter name and the quoted window. Echoing the whole argument back stays rejected: it doubles the payload that already failed |
| **I151** `search_text`/`search_regex` returned whole lines only, so extracting just the matched substrings needed `Bash: grep -o` over an XML in the NuGet package cache - a self-declared fallback made twice in one run | `search_text`, `search_regex` | `matchesOnly=true` prints the matched span instead of the line, composing with `unique=true`; a match that is only whitespace still prints its line, so I149's no-empty-record invariant holds | shipped - the two calls that fell back returned ~40 lines to read 13 names; `FileToolsE2ETests` asserts the span form is shorter than the line form and that `matchesOnly` + `unique` collapses to the distinct values. Printing both the line and the match stays rejected: strictly more tokens than the line alone |
| **I157** the review round found `gate` answering a bare `clean` line while hiding a `VERIFY_FAILED` from its own `cleanup verify=true fix=all` step - `FormatService` returns a failed verify inside a successful `Result`, so `.IsOk` was blind to it. Reproduced against the freshly built `terse.dll` on `ReleaseVersion.cs`: `analyze` 0 diagnostics, `cleanup verify` VERIFY_FAILED, `gate dryRun` `dryRun  analyzed=0 fixed=0 remaining=0` | `gate` | the verdict is `clean` or `FAILED`, and the one-line form is emitted only when every step is provably quiet - its text is exactly `clean` or starts with `0 files changed`; a dryRun whose tree would change reports `FAILED` | shipped - the tool whose whole purpose is the pre-push gate can no longer report a clean tree over a step that said otherwise; `AnalysisToolsE2ETests.Gate_WhenAWriteStepWouldChangeAFile_NeverCondensesThatAway` covers both the preview and the applied run and was observed failing on the pre-fix predicate. A caveat blacklist stays rejected: a whitelist of quiet shapes cannot silently gain a new caveat it does not know about |
| **I158** `UnifiedDiff` was computed twice per document - `Between` and `ChangedLines` each ran the alignment over the same pair, up to ~7.9 MB of transient `int[,]` each at the 2 000 000-cell ceiling, unconditionally in `DocumentDiff.CreateAsync` and on the verbose path of five renderers | `UnifiedDiff`, and therefore every mutating tool | `Report(path, before, after)` returns the text and the count from one alignment; the seven sites that needed both now call it, and the sites that need only the count still call `ChangedLines` | shipped - one alignment per document instead of two on every edit; `UnifiedDiffTests.Report_AnswersTheSameTextAndCountAsTheTwoSeparateCalls` locks the equivalence, and the 2 000 000-cell fallback, the empty file and the single-line file gained the tests they never had. Caching the last pair inside `UnifiedDiff` stays rejected: a static one-entry cache is shared state on a concurrent request path |
| **I159** an `InvalidArgument` carrying a `JsonException` emitted a byte-offset window for **every** supplied array parameter, asserting a location inside arguments that had bound correctly; the shipped test only passed because the second parameter's JSON was shorter than the offset | `ToolArgumentFilter` | the failing parameter is resolved from `JsonException.Path` when it names one; with exactly one candidate the window is emitted, and with several the response says the exception does not say which rather than guessing | shipped - removes a confidently wrong location from the one message an agent reads to fix a 9 000-character call; `ToolEdgeCaseE2ETests.ReplaceSymbol_WithTwoArrayParametersBothLongerThanTheOffset_NeverAssertsAnOffsetItCannotAttribute` covers the ambiguous case. Echoing the whole argument stays rejected for the same reason as I150 |
| **I148** `analyze`, `format`, `cleanup` and `get_diagnostics` were run 6 times in one task to gate 13 changes, always in the same fixed order, which this repo's own `CLAUDE.md` mandates | `analyze`, `format`, `cleanup` | `gate` runs `analyze` at `info` -> `format` -> `cleanup fix=all` -> `analyze` over the changed set and answers **one verdict line** plus only the diagnostics still unfixed - never a diff; `dryRun=true` makes both write steps verify instead of write, `solution=true` widens the scope, `verbose=true` adds each step's report. `AnalysisService` gained `FindingsAsync`, so the sequence reuses the analyze path instead of parsing its rendered output, and gate's internal passes do not disturb `sinceLast` history | shipped - 3 of every 4 calls on the end-of-task path, ~2 per task; tool surface 86 -> 87, `AnalysisToolsE2ETests` asserts the verdict line and that `remaining=` equals `analyze`'s own count for the same scope. A composite that also builds and tests stays rejected: `build` must be read *before* the test result |
| **I134** a member name unique inside the file just outlined still answered `SaturatedName`/`AmbiguousSymbol` solution-wide - 3 wasted calls in one run, always `get_file_outline path=X` then `get_symbol_source Type.Member` | `get_symbol_source`, `get_symbol`, `get_type_outline` | `path=` resolves the name inside that document first (project-scoped `FindSourceDeclarations`, filtered to the file) and falls back to the solution only when the file holds no match; `symbolIds=` scopes every id in the batch, and a `path=` naming no document answers `DocumentNotFound` | shipped - removes the one case where an id an outline just printed does not round-trip; 4 E2E tests in `NavigationToolsE2ETests`, incl. `Route` ambiguous without `path=` and resolved with it, the no-match fall-back, and the unknown-path refusal. Widening outlines to always print documentation ids stays rejected (I129 measured it at 10-13 % of outline tokens) |
| **I135** `changed_files` could not exclude a path: 27 of 63 rows on this shared tree were another session's untracked `.research/**` and `.serena/**` notes, paid on every call | `changed_files` | `exclude=` glob applied after `path=` has selected, as `search_text`/`search_regex` already have; an excluded file is not counted | shipped - ~30 rows per call on a shared tree; `ChangedFiles_WithExclude_DropsThePathsAPathspecCannotLeaveOut` asserts the probe is in the plain listing, absent from the filtered one, and that the record count fell. Several `path=` pathspecs stays rejected: it enumerates what you want, and another session's file set is not knowable in advance |
| **I136** two new interdependent `.cs` files could not be created in either order - `write_text` was rolled back with `CS0103` although the callee had just been written and reported `changedLines=55` | `write_text`, `edit_text` | root cause was `FileTools.Guarded` passing `semantic: false`, so the pending notice for the new file was never drained and the compile gate ran against a stale solution; a `.cs` target now synchronises first | shipped - `WriteText_OverAKnownFileReferencingAFileJustCreated_IsNotRolledBack` observed red with the report's exact `CS0103 ... was rolled back`, then green; a non-`.cs` write still pays no sync. Ordering the two writes stays rejected - the dependency is mutual in the general case. Interaction with the still-open **I104** adopt race, raised in review and judged not an exposure widening: the drain that runs the adopt is triggered by a non-empty pending queue, so the same drain would have run on the very next semantic call; routing a `.cs` write through it moves that drain earlier by one call, it does not add one. The observable consequence that *is* new - an `edit_text` on a `.cs` file can now move the mtime of an unrelated adopted file, which `analyze changed=true` then reports - is recorded here rather than fixed |
| **I137** nothing answered "which files does git **track** under this path", so picking a checked-in fixture cost a `Bash: git ls-files \| grep` fallback | `find_files` | `tracked=true`, answered from `git ls-files --cached` through the existing `GitRunner`, paths normalised to the index's separator; a bare `git ls-files` is denied by `terse guard`, every flagged form left alone | shipped - one `Bash` fallback per task removed; `FindFiles_WithTracked_ListsOnlyWhatGitTracks` plus `ToolGuardTests` in both directions (3 denied forms, 4 allowed). Reading `.gitignore` stays rejected: it answers ignoring, not tracking |
| **I138** the first semantic call on a freshly loaded workspace cost **7 414 ms** and no response said why, which is exactly how a per-call latency claim was mis-diagnosed from transcript timestamps | `load_workspace`, every semantic tool | `load_workspace` ends `compilations=cold` while nothing is realized; the first semantic call that realizes them appends `compilations=realized in Nms (once per load, not per call)`, both read from Roslyn's non-forcing `Project.TryGetCompilation` | shipped - one mis-filed latency finding per mining run; `LoadWorkspace_ReportsColdCompilations_AndTheFirstSemanticCallReportsRealizingThem` asserts cold, then realized, then silence on the next call. Pre-warming at load stays rejected - it moves the cost where the agent can attribute it even less |
| **I139** `doctor` reported a never-installed asset as `OK assets: skill=current guard=absent`, so the install remedy never fired while built-ins were 29 % of 19 432 tool calls | `doctor`, `AssetState` | `Stale` became `NeedsInstall`, true for any asset missing **or** out of date | shipped - ~3 900 built-in calls per week rerouted, the `.cs` `Read` half alone ~107 000 tokens; `NeedsInstall_IsTrueForEveryAssetKindThatIsMissingOrStale` discovers the kinds from `AssetState`'s own constructor and was observed red on the old expression. Teaching it in `SKILL.md` stays rejected - the skill was installed and current all week and the fallbacks happened anyway |
| **I140** the 86-tool surface is advertised on every request: 3 365 479 B (~842 000 tokens) across 216 records in one week, past every published inflection point for selection accuracy | `McpHost`, every `[McpServerTool]` | `terse serve --tools core` / `TERSE_TOOLS=core` advertises 21 tools through an `AddListToolsFilter`; **only the advertised list shrinks** - every hidden tool still answers when called by name - and `workspace_status` says which profile is running | shipped - up to 85 % of the advertised-surface tokens on the mechanism Anthropic measured; `ToolProfileE2ETests` asserts the exact advertised set, that a hidden tool still answers, and the status line, and `EveryToolInTheCoreProfile_IsAToolTheServerAdvertises` fails if the profile names a tool the server dropped. Deleting tools and shortening `[Description]` text stay rejected |
| **I141** `edit_text` applied one edit per call: 2 033 calls, 1 189 inside runs of >=3 consecutive calls (longest 26), 952 of 1 811 adjacent pairs on the same path, 847 chars of framing re-sent each time | `edit_text` | `edits=[{oldText,newText,section,occurrence}]` applied to one file in order as a single write, one line per item, per-item failure announced with its own code and remedy while the rest land | shipped - ~950 round trips per week (5.9 per session), ~68 000 tokens; capped at 10 because batched-item accuracy falls off past ~6, and `EditErgonomicsE2ETests` covers the happy path, the partial failure, the cap refusal and the missing-argument error. A batch spanning several files, and whole-call failure on one bad anchor, stay rejected |
| **I142** a `CompileRegression` discarded the declaration it rejected: 152 rollbacks in the window, 77 of them after I122 shipped, `replace_symbol` averaging 870 chars re-sent on the retry | `replace_symbol`, `add_member`, `EditGate` | the rejection ends `retryWith=r3`; `replace_symbol`, `replace_symbol_body` and `add_member` take `retryWith=` and replay the held targets and declarations - including a multi-file batch - under whatever flags the retry passes | shipped - 152 re-sent declarations (~33 000 tokens per week) removed; the server holds the last 8 rejections and a token it no longer holds answers `names no held rejection of <tool>` rather than editing with an empty declaration. `ARollback_NamesARetryTokenThatHoldsTheRejectedDeclaration` covers both. Auto-applying under I79's carve-out stays rejected |
| **I143** the two-space column separator costs 1 token per run: 269 of 13 592 tokens (1.98 % o200k, 2.01 % cl100k) over 15 captured real responses, worst `search_symbols` 4.09 % | `ResponseBuilder`, every listing tool | proposed: emit a single space between framing columns | **rejected, no change shipped** - the two-space run is the record grammar, not padding. Columns deliberately contain single spaces *inside* them: `OutlineService.cs:113` emits `{Reference}  {Kind} {Accessibility}  :{lines}`, `ReferenceService.cs:148` emits `xaml {usage.Kind}`, `RazorService.cs:243` emits a parameter type such as `Func<int, string>`, and every `search_text` hit line is arbitrary source text. Collapsing framing to one space makes a column boundary indistinguishable from an intra-column space, so a record can no longer be split into fields - a lossy rewrite of payload structure for 2 %, and the same class of change I92 and I127 already refuted. Reopen only if the framing is first restructured so no column can contain a space |
| **I144** `SKILL.md` is ~13 000 tokens loaded whole and opened with a prohibition list - the form measured to decay from 73 % compliance at turn 5 to 33 % at turn 16 while commission constraints hold at 100 % (arXiv:2604.20911) | `SKILL.md` | lead with the positive routing table, demote the whole hard gate below it with nothing removed | shipped - an accuracy lever on the 29 % built-in share, observable next run as the built-in share and shell-text call count; `DocsCoverageE2ETests` still passes, so every tool is still named. Shortening the file stays rejected: instruction-file size is an affirmative null over 1 650 sessions (arXiv:2605.10039) |
| **I145** a truncated markdown read steered only to `next: startLine=`: 441 truncated `read_text` responses (`.cs` 290, `.md` 149), mean overflow 79.3 %, 165 repeat reads of a path already truncated once | `read_text` | on a `.md` path the truncation steer now names `headings=true` and `section=` beside `next: startLine=` | shipped - ~150 paged re-reads per week; `ReadText_WhenAMarkdownReadIsClipped_NamesHeadingsAndSectionBesideTheNextLine` asserts all three. Raising the default `maxChars` stays rejected - a 79.3 % mean overflow means a bigger cap would not close it |
| **I146** framing residue below the individual floor, aggregated: an absolute solution path in `workspace_status` (116) and `load_workspace` (55); a redundant `N files changed` line above a one-line-per-file answer (720, including the ungrammatical `1 files`); `oldText matched N times` on 65 of 2 033 `edit_text` calls with 76 immediate retries | `workspace_status`, `load_workspace`, `EditGate`, `edit_text` | the solution path is workspace-relative where the workspace is its own root; the count line is dropped whenever the per-file lines already carry it; the multi-match remedy lists up to five candidate lines with their numbers | shipped - ~20 000 tokens per week combined, ~125 per session; `ASuccessfulEdit_DoesNotRepeatTheFileCountAboveThePerFileLine` and `EditText_WhenTheAnchorMatchesSeveralTimes_ShowsTheCandidateLines` lock the last two, and the count line still prints for a `dryRun`, a `verbose`, a rollback or `0 files changed`. Dropping the path entirely stays rejected - the caller passed none, so it cannot derive it |
| **I104** the sync's own adopt can overwrite a concurrent external write: `AdoptAsync` goes through `TryApplyChanges`, which writes the absorbed text back to disk | `WorkspaceSync`, `LoadedWorkspace` | proposed: evaluate the stability check inside `historyGate` immediately before `TryApplyChanges` | **refuted, no change shipped** — repro `SyncAsync_ForAWriteLandingDuringTheDrain_LeavesItOnDisk`, 40 rounds, red at round 3; forcing the guard to abort every adopt made it 40/40 green, which proves `TryApplyChanges` is the writer; moving the check inside `historyGate` left it red at round 1, so the losing window is *inside* `TryApplyChanges`, between its own stat and its write, and no caller-side check can close it. Reopen when Roslyn exposes an absorb that does not write back, or when `LoadedWorkspace` stops routing sync absorbs through the MSBuild workspace |
| **I105** the drain's file read denies concurrent writers, so an editor save aborted the **whole** drain with `ERROR Internal IOException` | `WorkspaceSync` | `MergeAsync` catches `IOException` and re-queues only that path via a new `Requeue`, which `Restore` and `Shifted` now share | shipped — a locked file costs one path, not the drain; `SyncAsync_WhenOneFileDeniesTheRead_AbsorbsTheRestAndRequeuesIt` observed red (IOException at `WorkspaceSync.cs:318`) then green, and replaces `SyncAsync_WhenTheDrainFails_ForgetsTheStampsItTook`, which encoded the defect |
| **I106** nothing answered "when was this file last written, and how big is it?" — 3 PowerShell calls through `Bash` | `find_files` | `stamps=true` appends each record's UTC last-write time and byte length, from the `FileStamp` the sync already computes | shipped — removes the last non-git `Bash` fallback; `FindFiles_WithStamps_AppendsAUtcWriteTimeAndByteLengthPerFile` asserts 3 columns, a parseable ISO-8601 UTC stamp and a positive length, and that the plain call carries neither |
| **I107** `get_symbol_source` output cannot be pasted back as an `edit_text` anchor: it is dedented and blank-stripped, so `oldText` matched 0 times and the two texts looked identical | `get_symbol_source`, `edit_text` | the 0-match remedy detects an `oldText` that matches once indentation and blank lines are ignored, and steers to `replace_symbol_body` / `replace_symbol` / `read_text verbose=true` | shipped — one wasted `read_text verbose=true` removed per occurrence; `EditText_WithAnOldTextThatOnlyMatchesDedented_SaysSoAndSteersToTheSymbolTools` observed red then green |
| **I108** `changed_files` could not be scoped to a path, so the end-of-task review cost the whole tree | `changed_files` | `path=` pathspec, threaded into both `git diff` calls and the untracked `ls-files`, and named in the truncation steer | shipped — measured again in this run: an unscoped listing was 23-35 rows where 12 were this task's; `ChangedFiles_WithAPath_ListsOnlyWhatThatPathspecCovers` asserts 1 file under its own name and 0 under `src` |
| **I109** a locked-output build error named pids, not what holds them; stopping the named pid would have killed another session's live `dotnet test` | `build`, `run_tests` | new `LockHolders` resolves every `Name (pid)` in the response to name, UTC start time and a kind — this terse server, an MSBuild/BuildHost, a live testhost to wait for, a bare dotnet host, or a pid already gone | shipped — 2 PowerShell calls removed per locked build, and it names the one holder you must not stop; 3 unit tests |
| **I110** `analyze changed=true` lost its change set whenever `build` unloaded the workspace and answered `no document under that scope was modified`, which reads as "your files are clean" | `analyze`, `format`, `cleanup` | `LoadedWorkspace.ChangedSinceUtc`, seeded through `WorkspaceSeed`; `WorkspaceRegistry` carries it across an `Unload(reclaim: false)` — the lock-driven unload — and drops it on a deliberate unload | shipped — 4 per-file calls collapse to 1 on every task that builds before it analyzes; `DocumentScope_AfterALockDrivenReload_StillReportsAFileTouchedBeforeIt` observed red (empty collection) against the old `LoadedUtc` comparison |
| **I111** the locked-output note asserted "this is not the workspace" — and it was: the holder was a `BuildHost` spawned out of the repo's own `bin/` by an earlier terse load | `build`, `run_tests` | the note now rules out only the shadow-copied analyzer and generator set, and names an MSBuild BuildHost from this or an earlier terse load as a live candidate | shipped — 3 failed builds (~60 s) plus 3 `Bash` calls removed; the note is carried by the same path `LockHolders` feeds |
| **I112** `replace_symbol` answered `0 files changed` for a no-op without saying it was a no-op — byte-identical to the silent-drop failure | `replace_symbol`, `replace_symbol_body`, `add_member` | `EditGate.Render` adds `NOTE no change - the result is identical to what is already there` whenever the gate produced 0 diffs | shipped — one defensive `get_symbol_source` removed per occurrence, on every `EditGate` caller; `WriteText_WithTheContentTheFileAlreadyHas_SaysItChangedNothing` |
| **I113** the guard's driver extraction was defeated by a quote, an env prefix or a sub-shell | `ToolGuard`, `ToolCensusE2ETests` | `Command()` skips leading `NAME=value` tokens, opens a `$(...)` command substitution, and strips quotes, backticks, parens and braces before the driver is read; the census also refuses a `Replaces Bash ...` command that does not start with a known driver | shipped — 16 spellings observed allowed then denied, including `$(git status)` and `FILES=$(git diff --name-only)` which the first fix missed and the R2a reviewer caught, with `git log`/`commit`/`push`, `git rev-parse` and `dotnet restore`/`pack` behind the same prefixes still allowed |
| **I121** the E2E suite had a one-runner flaky tail on CI, from two distinct mechanisms | `UpdateE2ETests`, `ToolEdgeCaseE2ETests`, `ResxToolsE2ETests` | poll loops run against a wall-clock budget and report the attempt count, the elapsed time and the last response instead of only that they gave up; the 17 MB search test waits for the file to reach the workspace file index; the `resx_set` test asserts the tool reported a write before reading the file | shipped in part — both named mechanisms addressed, plus a third the row had not identified: `TemporarySolution` copied `AtomicWrite`'s transient `*.terse-<pid>.tmp` out of the shared fixture and raced its deletion, which killed a macOS leg on a commit that touched only a markdown file; it now filters through the product's own `WorkspaceFiles.IsTemporary`. Every failure names what it observed. **Not done:** per-test private copies of the shared `Scratch.resx` family, deferred because a new `.resx` under a project forces a workspace reload that would perturb the neighbouring tests more than the sharing does |
| **I119** `read_text`'s default `maxChars` was above what the client will inline, so a whole-file read of a large document was spilled to a `tool-results/*.json` and answered nothing | `read_text` | `DefaultResponseCharacters` = 40 960 for an unset `maxChars`; the hard ceiling stays 131 072 for an explicit one | shipped, **MAJOR** — a whole-file read now clips inline and names `next: startLine=`; `ReadText_OnAFileOverTheDefaultCharacterBudget_ClipsItAndNamesTheContinuationLine` bounds the response at default + 4 096, which the old default could not satisfy |
| **I120** `edit_text` disambiguated a repeated anchor only by lengthening it, which fails on a file of near-identical rows | `edit_text` | `occurrence=N` selects the Nth match, including on the line-ending-relaxed path; the ambiguous remedy now offers `occurrence=1..N` and an out-of-range value names the range | shipped — one wasted read removed per collision on any table-shaped file; a 3-case Theory asserting which row moved, plus the out-of-range message, all observed red first |
| **I115** a claim about a freshly built binary could not be tested through a tool: evidence rule (b) meant hand-writing four JSON-RPC frames into a pipe | `Program.cs` | `terse call <tool> --workspace <path> --json '{...}'` resolves the tool by its `[McpServerTool(Name)]`, builds the same `ToolContext`, loads the named workspace explicitly, and binds arguments by name | shipped — ~40 lines of hand-built protocol replaced by one command, and the probe can no longer answer about an auto-discovered workspace; verified against the built `terse.dll` and covered by 3 E2E tests |
| **I116** a signature change on a private helper was said to cost 4 calls and one guaranteed `CompileRegression` because `replace_symbol` applied exactly one member | `replace_symbol`, `delete_symbol` | none — the row was stale | closed, already shipped — `replace_symbol` has taken several declarations as one compile-gated edit since `MemberDeclaration.ParseAll` landed (`SymbolEditService.cs:54`), gated by `ReplaceSymbol_WithTwoOverloads_ReplacesTheTargetWithBoth`; `delete_symbol` addresses one symbol, for which the proposal has no meaning. The residual cost measured in this run is **cross-file**, which no per-symbol edit can span, and the `CompileRegression` remedy already names `allowErrors=true` as the two-step |
| **I117** `search_text`/`search_regex` could not exclude a path, so recorded tool output under `.research/` was paid for on every text search | `search_text`, `search_regex` | `exclude=` glob applied after `glob=`, for both the workspace and the `root=` paths | shipped — ~900 of 1 500 tokens on the measured call; `SearchText_WithExclude_DropsTheMatchesTheGlobCannotLeaveOut` asserts 5 matches fall to 3 |
| **I118** `write_text` stamped a UTF-8 BOM onto a `.cs` file that had none, reported as an ordinary `changedLines=` one-liner | `write_text` | `FileService.GatedAsync` takes the document's own `SourceText.Encoding`, falling back to `AtomicWrite.EncodingOf`, as `EditGate` already did | shipped — a phantom first-line diff on every `write_text` over an existing `.cs` removed, and the 2 PowerShell calls that stripped it; `WriteText_OnACSharpDocumentWithoutAByteOrderMark_LeavesItWithoutOne` observed red then green |
| the documentation id was 205 of the 336 characters on a member line | `get_file_outline`, `get_type_outline` | print `OrderService.Submit(Order)`; keep the id only where a name cannot address the member | shipped — outline budget ⅔ → ½ of the file it replaces; `OrderBook.cs` 261 → 167 tokens |
| reproducing a 205-character id byte-exactly cost a round trip per typo | every `symbolId` parameter | accept names, qualified names and parameter lists; list candidates instead of guessing | shipped — one failed call removed per typo |
| `find_usages` gave the file but not the member | `find_usages` | `src`/`test` tags and optional `in <Type>.<Member>` | shipped — container is opt-in, it measured 3× on a 46-usage symbol |
| `analyze` after every edit re-derived what `EditGate` had just computed | every mutating tool | `errors=N (+D) warnings=N (+D)` on apply and `dryRun` | shipped — one `analyze` removed per edit |
| `truncated=true` said nothing about what to do | every listing tool | append `- narrow with <parameter>` | shipped — one wide re-query removed per truncation |
| orienting on a symbol cost `get_symbol` + `find_usages` + `find_implementations` | `explore_symbol` | one call returns signature, doc, reach, implementations, XAML sites | shipped — 3 calls → 1 |
| judging a rename's blast radius cost three calls plus manual project reasoning | `impact_of` | one call adds the projects that would recompile | shipped — 3+ calls → 1 |
| "where is `IFoo` registered?" was unanswerable — open generics, factories and `Add*` extensions defeat grep | `find_registrations` | syntactic scan over the loaded solution, and an explicit "declared nowhere" | shipped — replaces a multi-file hunt that often failed outright |
| "what endpoints exist?" meant grepping `Program.cs` and every extension it calls | `list_endpoints` | every `Map*` registration with its member | shipped — replaces a multi-file hunt |
| resolving one `{StaticResource}` meant reading `App.xaml` and every merged dictionary in order | `xaml_resolve` | workspace-wide key index with scope | shipped — ~8-15k tokens → ~100 |
| a `{Binding}` typo had no static answer in WPF at all | `xaml_bindings validate=true` | resolve the data context and walk the path against Roslyn | shipped — a runtime-only bug class becomes static |
| editing XAML meant line-based `Edit` on the file shape agents are worst at | `xaml_set_property`, `xaml_add_element`, `xaml_remove_element` | element-addressed edits, formatting preserved, malformed results refused | shipped — removes the last routine reason to `Read`+`Edit` a `.xaml` |
| a red→green loop re-printed every unchanged diagnostic on each iteration | `analyze sinceLast=true` | report only what appeared, plus what was fixed | shipped — the loop pays for the delta, not the set |
| an agent with the server installed still reached for `Read`/`Grep` out of habit | `terse guard`, `terse install --guard` | a `PreToolUse` hook that denies the built-in and names the replacement | shipped — closes the only failure that scales with every session |
| a `.resx` had no tool at all: ~12.2k tokens for a 500-key file, ~36.6k for a neutral+fr+de family | the eight `resx_*` tools | family-aware index, keys-only listing, cross-culture `MISSING` rows, surgical `<data>` writes, a `RESX001`–`RESX009` lint | shipped — `resx_validate` over the workspace ≤ 800 tokens, `resx_files` ≤ 400, `resx_usages` ≤ 250 |
| `x:Uid` was reported but not linked to its `.resx` entry | `xaml_localization` | share `ResxIndex` with the resx tools | shipped — one parser instead of two that drift |
| `dotnet format style`/`analyzers` had no equivalent, so every violation was fixed by hand | `cleanup fix=style\|analyzers\|all` | apply the referenced analyzers' fixes in-process, compile-gated, `UNFIXED <id>` for what no fixer covers | shipped — 1 call replaces `analyze` + N hand edits |
| `--verify-no-changes` had to be answered with a full diff | `format verify=true`, `cleanup verify=true` | one verdict line plus the changed-file list | shipped — `format verify` ≤ 60 tokens, `cleanup verify` ≤ 150 |
| formatting N touched files cost N calls | `format`, `cleanup` | `path=` accepts a glob and a directory | shipped — N calls → 1 |
| a whole-solution `cleanup` silently rewrote `obj/…GlobalUsings.g.cs` | `format`, `cleanup` | skip generated code unless the file is named explicitly | shipped — a silent side effect removed from every sweep |
| `dotnet clean` had no equivalent, and the guard had to allow it | `clean` | delete `bin`/`obj` with freed-byte counters, lock recovery and `dryRun` | shipped — the last routine `dotnet` shell-out on this repo's path is closed |
| a Razor project's answers pointed at `obj/…_razor.g.cs` instead of the `.razor` line | `get_diagnostics`, `analyze`, `find_usages` | report `Location.GetMappedLineSpan()` and drop generated paths | shipped — a wrong answer removed: following it meant editing a regenerated file |
| `search_symbols` returned 0 for every Blazor component | `search_symbols` | list components from the generator output at their `.razor` path | shipped — replaces a `search_text` fallback on every Blazor session |
| reading a `.razor` meant `read_text` on the whole file | `razor_outline`, `razor_component` | resolve every tag through the generated document | shipped — `Weather.razor` 1 922 B → a 12-line outline |
| a workspace never re-read the disk, so `write_text` then `replace_symbol` answered `SymbolNotFound` and an external edit was answered from the load-time snapshot tagged `EXACT` | every semantic tool, `workspace_status`, `load_workspace` | watcher + lazy content-compared sync + targeted stamp check + per-kind generations | shipped — removes the unload/load round trip, paid 4× at ~2.5 s in one task |
| `Write`/`Edit` on a `.cs` that does not exist was denied with a remedy naming only tools that cannot create a file | `terse guard`, `SourceFile.Reject` | name `write_text(path, content, force=true)` when the path is missing | shipped — closes the dead end behind a 0.8.0 silent `edit_text force=true` fallback |
| `undo_last_change` never reverted anything: it replayed a stale-version `Solution` through `TryApplyChanges` | `undo_last_change` | replay the previous document texts onto the current solution | shipped — a tool that always answered "the workspace refused the revert" now works |
| **I4** thirteen call sites rebuilt a whole-tree index per call; `xaml_validate path=X` parsed every XAML in the repo | `xaml_*`, `resx_*`, `find_registrations`, `list_endpoints` | one `WorkspaceIndexes` per workspace, memoized per (kind, generation), single-flight, incremental by (mtime, length) | shipped — 1 000 repeat `xaml_resolve` over 500 files: 1 build, 500 parses, was 500 000; a one-file edit in a 200-file tree: 1 parse, was 200 |
| **I6** `ResxIndex.Cache` was a `static ConcurrentDictionary` with no bound and process-wide scope | `resx_*` | per-workspace `ParsedDocumentCache`, LRU-bounded at 128 documents / 32 MB | shipped — unbounded → bounded; one workspace's `Forget` leaves another's cache intact |
| a green run and a clean build printed 60-150 tokens of counters no agent acts on | `build`, `run_tests`, `rerun_failed` | one line when the result has nothing else to say; `verbose=true` restores the full report | shipped — `RunTests_GreenRun` ≤ 120 → **≤ 40 tokens**; `build` clean < 120 characters |
| **I1** no heading-level outline for a non-`.cs` file: 216 lines (~2.6k tokens) read to change 3 spots | `read_text` | `headings=true` returns the map with line ranges; `section="## Commands"` returns one section | shipped — `read_text` was the second most-called tool (344 calls), the map is ~40 lines against 216 |
| **I2** `edit_text` needed a byte-exact unique `oldText`, forcing a prior full read | `edit_text` | `section=` replaces the whole section with no `oldText` | shipped — one read removed per documentation edit |
| **I3** `write_text` refused a path whose directory did not exist | `write_text` | `AtomicWrite` creates the missing directories under the containment-checked path | shipped — 4 failed calls + 1 `Bash mkdir` removed per new directory |
| **I7** `edit_text` matched byte-exactly, so every multi-line edit against a CRLF file failed first try | `edit_text` | fall back to a line-ending-normalized match mapped back to real offsets; name the closest lines on a miss | shipped — **130 of 577 `edit_text` calls (22.5 %)** in the session log had failed with `oldText matched 0 times` |
| **I11** `add_member` glued the declaration to the type's closing brace (`);}`) | `add_member` | one blank line before the member, `}` on its own line | shipped — 9 `add_member` calls had cost ~12 corrective `edit_text` calls |
| **I12** `write_text` wrote `\n` into a CRLF repository | `write_text` | keep the target file's endings; a new file takes the solution file's dominant ending | shipped — removes a whole-file diff per new file |
| **I5** / **I13** the worktree name `workspace_status` prints was rejected `AmbiguousWorkspace` | `WorkspaceRegistry.Resolve` | rank hints (path → solution file → solution → worktree → directory → substring); ties only within the best tier are ambiguous | shipped — **88 `AmbiguousWorkspace` errors** in the session log, the second most common code |
| the request path did file IO synchronously, and the searches allocated one `string` per line of every file scanned | every file tool, `search_text`, `search_regex`, `AtomicWrite` | async IO throughout; span-based vectorized scanning; `stackalloc` path normalization | shipped — a non-matching line no longer allocates |
| **I26** `format`, `cleanup` and `clean` printed a full diff on a success an agent does not act on | `format`, `cleanup`, `clean` | one line per changed file; `dryRun`, a rollback and a locked directory keep the full output | shipped — a whole-solution `cleanup` went from N diffs to N lines |
| **I24** `write_text force=true` bypassed `EditGate`, so a 400-line rewrite landed unchecked | `write_text` | route the write through `EditGate` when the file is already a workspace document | shipped — 15 unchecked whole-file rewrites across two tasks |
| **I27** the symbol writers emitted the replacement's opening brace at column 0 | `replace_symbol_body`, `replace_symbol` | annotate the new node and run the formatter over the annotation only | shipped — observed 20+ times in one task, each costing a `format` sweep |
| **I9** `replace_symbol` reported `applied` with `0 files changed` for a trivia-only difference | `replace_symbol` | say `the declaration is identical to what is already there` | shipped — one undetected no-op per occurrence |
| **I8** `replace_symbol` on a field threw `InvalidCastException`, `delete_symbol` left `private int ;` | `replace_symbol`, `delete_symbol` | promote a `VariableDeclaratorSyntax` to its field declaration; refuse a shared declaration | shipped — one unusable call per field-level change |
| **I18** `get_file_outline` on top-level statements answered `0 types`, which reads as "the file is empty" | `get_file_outline` | report the statement count, the file length and `use read_text` | shipped — 1 dead call plus a full-file read per occurrence |
| **I16** `SymbolNotFound`'s `nearest:` offered a candidate the resolver would also reject | `get_symbol`, every `symbolId` parameter | emit the documentation id where the short form does not round-trip | shipped — 3 wasted calls per occurrence |
| **I15** `FixerCatalog` served stale fixers for a rebuilt analyzer from an unbounded cache | `cleanup fix=…` | key on (path, last write, length), bound at 32 entries, clear on `unload_workspace` | shipped — stale fixers cannot survive a rebuild or an unload |
| **I21** `RazorIndex` was a process-wide unbounded static plus a directory walk per call at 5 sites | `razor_*`, `list_endpoints`, `workspace_status` | `ChangeKind.Razor`, a generation-keyed slot, reuse of unchanged documents | shipped — the walk moves from every call to every Razor generation |
| **I22** `resx_files`/`resx_validate` re-parsed everything past the 128-document LRU | `resx_files`, `resx_validate` | cache the per-file translatable key set on the index | shipped — bounded without being unbounded |
| **I25** five XAML paths parsed every XAML file in the tree | `xaml_find`, `xaml_validate`, `find_usages`, `rename_symbol`, `explore_symbol` | carry element, attribute, handler, binding and `x:Class` facts in `XamlFileRecord`; filter by identifier set before parsing | shipped — all five answer from records. Trade: the index retains attribute text for a generation's life |
| **I14** `cleanup fix=…` ran the whole analyzer set once per diagnostic id, up to 25× per project | `cleanup` | filter analyzers by `SupportedDiagnostics` intersection | shipped — `ids=` narrows the run, not just the filter |
| **I19** `project_*`, `package_*` and `solution_*` wrote without telling the workspace | `ProjectTools` | notify from the write guard, the one place holding both workspace and path | shipped — correct under `--no-watch` without threading `LoadedWorkspace` through six services |
| **I20** two spellings of one solution produced two registry entries | `load_workspace` | reuse an entry matched by `SameFile`, resolve symlinks with `File.ResolveLinkTarget` | shipped — a symlinked worktree no longer splits |
| **I28** the preload ran ahead of `host.RunAsync`, so a cold start ate into the fixed 60 s `initialize` ceiling | `terse serve` | start serving first, preload on the thread pool | shipped — the handshake no longer waits on MSBuild registration or the first solution load |
| **I10** interleaving `edit_text` and a symbol edit on one file was reported to revert the text edit | every symbol writer | regression test asserting both changes survive | shipped — the watcher had already closed it; the test found `replace_symbol_body` mangling an expression body instead |
| `replace_symbol_body` promised "or an expression body" while wrapping `=> 42;` as `{=> 42;}` | `replace_symbol_body` | apply an expression body as `ArrowExpressionClause`; refuse a block that does not parse | shipped — a documented input that produced broken code now works |
| `find_registrations` reported a wrapped registration at the helper, not the call site | `find_registrations` | follow exactly one level of `Add*` extension methods, tagged `via Helper()` | shipped — the common DI-extension idiom resolves without whole-program analysis |
| `xaml_resolve` said nothing about implicit styles | `xaml_resolve` | list implicit styles whose `TargetType` matches, `HEURISTIC`, and decline to name a winner | shipped — answers the question without the confident wrong answer |
| per-element and per-line values were `sealed record` class allocations, and hot string paths split/joined to inspect text | 12 value types; `Condense`, `InOutputDirectory`, `LastSegment`, `ClassicPath` | `readonly record struct`; span slicing and `stackalloc` for inspect-only string work | shipped — one fewer heap object per element, usage, finding and registration |
| **I23** (half) `format`/`cleanup` swept files the task never touched | `format`, `cleanup` | `changed=true` limits the pass to files modified since the workspace loaded | shipped — removes the drive-by edits that cost a `git checkout` of 5 files |
| `xaml_add_element` inserted in one position only, and the documented "last child" was actually first-child | `xaml_add_element` | `position=first\|last`, refusing an element with no matching closing tag | shipped — both positions reachable and correct |
| the docs hard gate was prose only, so `README.md`, `NUGET_README.md` and `SKILL.md` each drifted while every local gate stayed green | `DocsCoverageE2ETests` | census: every name from `tools/list` must appear backticked in all three shipped documents | shipped — all advertised tools verified; proven non-vacuous by inverting the predicate |
| **the truncation lie** — `search_symbols` capped at `maxResults` and reported the cap as the total, so every capped search said `truncated=false` | `search_symbols` | carry the real total, set `truncated=true`, steer with `kind=`/`maxResults=`, say when the total counts declarations | shipped — on a 148-project solution `OrderService` answered 50 against a real 178; 128 declarations had been dropped silently |
| **two exclusion lists** — the file/text tools walked `.claude`, `artifacts`, `TestResults` and directory symlinks that every index excludes | `find_files`, `search_text`, `search_regex` | one `WorkspaceFiles.Traversable` shared by both walkers | shipped — `find_files **/*.xaml` had reported 1376 where the workspace holds 689 |
| a usage in `obj/**/*.g.cs` was tagged `src`, inviting an edit to a regenerated file | `find_usages`, `explore_symbol`, `impact_of`, `resx_usages` | `TestScope.Of` tags generated documents `gen` | shipped — a source-generated registration is still reported and no longer looks editable |
| **NuGet warnings counted as load failures** — `failures=20` on a solution where all 148 projects loaded | `load_workspace`, `workspace_status` | split `WorkspaceDiagnosticKind`; warnings counted, listed only under `verbose=true` | shipped — 20 lines removed from every `workspace_status` on that solution |
| **`RZR009` asserted a runtime failure it could not prove** for services the Blazor host registers | `razor_validate` | host-provided allowlist, and an honest detail when `Add*` extensions hide their registrations | shipped — measured on a real Blazor app: 466 findings, first 10 all false positives |
| `razor_validate scope=solution` rebuilt the whole DI index once per Razor file | `razor_validate` | compute it once per run, still lazily | shipped — 126 solution-wide syntax scans → 1 on a 126-component app |
| `xaml_validate includeUnused=true` walked the whole tree synchronously with no exclusions | `xaml_validate` | `WorkspaceFiles` enumeration, async reads, `Regex.EnumerateMatches` into a `HashSet` | shipped — stops reading `bin`, `obj`, `.claude` and generated code; no sync IO on the request path |
| **I28** `xaml_styles` had no cap and no truncation | `xaml_styles` | `maxResults` (100) and an honest `truncated=` | shipped — `TextBlock` on a real WPF app had returned 218 records in one response |
| **I33** the symbol writers emitted CRLF into LF files | `EditGate` | every edit adopts the ending of the document it changes; a new file takes it from a sibling | shipped — mixed endings in 6 of 6 files edited by the previous task |
| **I29** `replace_symbol_body` wrapped a bare expression in braces on an expression-bodied member and failed `CS0161` | `replace_symbol_body` | accept a bare expression when the target is expression-bodied, falling back to the block form | shipped — 1 failed call per expression-bodied member edit |
| **I30** a mutually-referencing set of members cost one rollback per member added out of order | `add_member`, `replace_symbol` | accept several declarations in one compile-gated edit | shipped — 4 rollbacks measured in the previous task, 2 more before the fix landed |
| **I31** `analyze` scoped to one file while `format`/`cleanup` took a glob | `analyze` | share `DocumentScope`; `path=` takes a file, directory or glob, plus `changed=true` | shipped — the end-of-task gate over 17 touched files: 17 calls → 1 |
| **I32** a file's using directives had no semantic answer | `get_file_outline` | `usings=true` | shipped — one gate loophole closed; 2 fallbacks in the previous task |
| **I34** the heading map did not print the anchor slug | `read_text` | print `#slug` beside each heading | shipped — one of nine hand-derived links had shipped broken |
| **I35** counting and proving absence in a workspace file fell back to `grep` in `Bash` | `search_text`, `search_regex` | say in the description that `total=` is the count and a zero result proves absence | shipped — one `Bash` fallback per docs task |
| **I36** reading a file outside every workspace had no answer | `read_text` | accept an absolute out-of-workspace path, tagged `outside-workspace`; writers keep `PathBoundary` | shipped — one `Read` fallback per cross-repo comparison, without a throwaway `load_workspace` |
| **I37** `format verify` and `cleanup verify` were read as the CI gate and over-reported against it | `format`, `cleanup` | say what each compares against, and that `cleanup verify fix=style`/`fix=analyzers` are exactly the two CI commands | shipped — the verdict is actionable instead of disbelieved |
| `pattern` where every sibling search tool takes `query`, and a missing argument gave the SDK's opaque error | `search_text`, `search_regex` | `query` is the parameter, `pattern` an alias, neither present is a structured error | shipped — 3 failed calls in one task |
| **I38** the out-of-workspace `read_text` resolved a workspace first, so with several loaded it answered `AmbiguousWorkspace` | `read_text` | `ToolContext.OutsideEveryWorkspace` short-circuits an absolute path inside no root; writers still go through `PathGuard` | shipped — the feature is reachable in the session it was built for; the single-workspace fixture could not see the gap |
| **I39** `search_regex` compiled without `RegexOptions.Multiline`, so `^`/`$` anchored the whole file | `search_regex` | `Multiline` on both the primary and the `NonBacktracking` fallback | shipped — `^### Added` had returned 0 matches on a file with 15 such headings |
| **I40** nothing discovered a solution before it was loaded | `load_workspace` | `discover=true` lists every `.slnx`/`.sln`/`.slnf`/`.csproj` under a directory, shallowest first | shipped — 3 built-in fallbacks measured in one task; it is the first call of every multi-repo session |
| **I41** `MarkdownAnchor.Of` kept a markdown link's URL in the slug | `read_text` | strip `](url)` before slugging, keeping the link text as GitHub does | shipped — the same class of hand-checked link the anchor column exists to remove |
| **I42** `search_symbols maxResults=N` could emit up to 2N records | `search_symbols` | one budget across both lists | shipped — the payload can no longer be twice what was asked for |
| **I38** a wrong or missing argument name escaped the error contract: the SDK answered `An error occurred invoking '<tool>'.` | every tool | a call-tool filter renders `ERROR InvalidArgument`, naming the missing and unrecognized parameters | shipped — 5 dead calls in one session; the earlier sweep had fabricated its own `remedy:` line so the census could not fail |
| **I43** `project=` resolved as a path only, so the name `list_projects` prints produced `MSB1009` | `build`, `run_tests`, `list_tests`, `clean` | `WorkspaceTarget.ResolveProject`: path, then the solution's project names, then `*.csproj` under the root; `ProjectNotFound`/`AmbiguousProject` otherwise | shipped — 3 dead `run_tests` calls plus the `Bash ls` they forced, on a 145-project solution |
| **I44** `list_projects` advertised no `filter=`, so a caller that passed one had it silently dropped | `list_projects` | `filter=` keeps the projects whose name contains it | shipped — ~7 000 characters observed; locked by `TokenBudgetE2ETests` |
| **I45** `find_files` kept `glob` required and had no `pattern` alias, while both sibling tools had shipped exactly that | `find_files` | `pattern` alias, `glob` optional, neither present answers `InvalidArgument` | shipped — 2 dead calls measured |
| **I46** `run_tests`/`rerun_failed` had no lock detection and no recovery while `build` and `clean` had both | `run_tests`, `rerun_failed` | `TestRunResult.Locked`, the same warning `build` uses, and `RecoveredAsync` — unload, retry, reload | shipped — the reported session fought the lock by hand through `Bash` for ~15 calls |
| **I52** any semantic call mapped a project's analyzer/generator assembly into the server for the process lifetime, so the user's own `dotnet build` failed `MSB3027` and `unload_workspace` could not release it | `load_workspace`, and therefore every semantic tool | `ShadowCopyAnalyzerLoader` maps a content-hashed private copy; `AnalyzerRebind` binds every `AnalyzerFileReference` to it and the fork is carried forward as the lineage, with writes projected back so `TryApplyChanges` never sees it | shipped — on `fixtures/GeneratorSolution`: mapped in place → `dotnet build` exits 1, shadow copy → exits 0. `get_symbol` alone already locked it; `RunAnalyzers=false` did not prevent it. **Measure with an open-for-write probe, never a rename** — a rename succeeds on a mapped assembly |
| **I56** `build` returned every warning on every call, so a green build of a warning-heavy solution cost thousands of tokens | `build`, `run_tests`, `rerun_failed`, `list_tests` | success answers one line whatever it warned; a failed build lists error-severity only plus `warnings=N hidden`; the test family falls through to the bounded raw tail instead | shipped — ~500 lines per call → 1 on a 500-warning solution; locked against `fixtures/WarningSolution` |
| **I60** `find_files`, `search_text` and `search_regex` re-walked the whole tree on every call | `find_files`, `search_text`, `search_regex` | `PathIndex` in the generation-keyed `IndexSlot`, rebuilt only when the watcher sees a file appear, disappear or get renamed | shipped — `find_files **/*Service.cs` **2305 ms → 25 ms** on a 148-project, 45 941-file solution; Rider's `search_file` is 24 ms |
| **I61** `search_text` decoded every candidate file to a UTF-16 `string` before the first comparison | `search_text`, `search_regex` | scan the raw UTF-8 bytes from a pooled buffer, decode only on a hit or a UTF-16 BOM | shipped — part of the **5547 ms → 685 ms** step; no managed string for a file that cannot match |
| **I62** the searcher read binary files in full before deciding they were binary — the NUL probe ran on the decoded string | `search_text`, `search_regex` | read the first 4 KB, probe the **bytes**, then read the rest | shipped — **1058 MB of the 1320 MB read (80 %) was binary blobs with no excluded extension** |
| **I63** a non-seekable entry failed the whole `search_text` call with `NotSupportedException`, introduced by I61's `stream.Length` | `search_text`, `search_regex` | `CanSeek` selects a streamed `ReadToEndAsync` path | shipped — one dead call per search on that solution; the fixture cannot produce a non-seekable file, so it is covered by the benchmark, not a test |
| **I64** `load_workspace`/`workspace_status` printed up to twenty full MSBuild failure messages | `load_workspace`, `workspace_status` | `LoadFailureSummary` folds them to one `FAILED <project> messages=N` line, capped at 20 projects; `verbose=true` restores | shipped — **6712 → 1211 characters**, keeping the project names |
| **I47** no tool could add or change an **enum member**, so a new error code fell out of the compile-gated path | `add_member`, `replace_symbol`, `delete_symbol` | `ParseEnumMembers` plus an `EnumDeclarationSyntax` container branch | shipped — the forced text edit per new error code or diagnostic id is closed |
| **I48** the symbol writers were reported to delete the blank line after the edited member | `replace_symbol`, `replace_symbol_body` | — | **not reproducible on `main`**: `Rewritten` copies trivia with `WithTriviaFrom`. Closed as already fixed, not as wontfix; **I59** falls with it |
| **I49** `list_projects` printed a project's name but not its path | `list_projects` | print the workspace-relative project path beside the name | shipped — the listing answers "where is this project" without a second tool |
| **I51** the `narrowWith` steer printed only on truncation, so an uncapped listing could never advertise `filter=` | `ResponseBuilder`, `list_projects` | emit the steer above 25 records as well | shipped — a 145-project listing now names `filter=` |
| **I53** no tool deleted a file, so a throwaway `.cs` could only be removed with `Bash rm` | `write_text` | `delete=true`; a `.cs` document goes through `EditGate`, so it is compile-gated and covered by `undo_last_change` | shipped — the last file-lifecycle operation with no server-side answer |
| **I54** `mapped=` was reachable only by `unload_workspace`, which destroyed the state being measured | `workspace_status` | print `mapped=N` from `MappedAnalyzers.Of`, paths under `verbose=true` | shipped — the I52 regression detector is observable non-destructively |
| **I58** a no-op build reported `warnings=0` for a solution that has warnings | `build` | the counter reads `warnings=N emitted` | **partly closed** — the false cleanliness claim is gone. A positive "nothing recompiled" detector was refuted three ways: `-v q` is silent either way, `-v m`'s `-> output` prints either way, and only `-v n` reports the skipped `CoreCompile`, at a cost the response rules do not justify |
| **I69** `build` took no configuration, so a Release-only failure needed a shell-out | `build`, `run_tests`, `rerun_failed`, `list_tests` | `configuration` passed through as `-c` via `BuildScope` | shipped — one `Bash` fallback per Release-only investigation |
| **I70** (half) the build family took no target framework, so every TFM was built and run | `build`, `run_tests`, `rerun_failed`, `list_tests` | `targetFramework` passed through as `-f` | shipped; the `load_workspace` half closed separately |
| **I71** `doctor` reported `Environment.Version` — the runtime the *server* is on — as if it described the machine | `doctor` | `dotnet --version`, `--list-sdks` and `--list-runtimes` as their own lines; the old line relabelled `server runtime` | shipped — a missing runtime is named before `run_tests` fails in the test host |
| **I72** / **I80** (part) `get_symbol_source` took one id per call, run 28 times consecutively in one measured week | `get_symbol_source` | `symbolIds` array; an id that does not resolve is reported inline as `NOT_RESOLVED <id>` | shipped — the 28-call run becomes one, keeping the per-item honesty rule |
| **I74** (part) logs had no reader: no `tail`, no de-duplication | `read_text`, `search_text`, `search_regex` | `tail=N`; `unique=true` | shipped — `tail -n` and `sort \| uniq -c` are answered |
| **I75** a search returned one line per hit, so 86 measured search→read round trips | `search_text`, `search_regex` | `context=N` (0-5), rendered as indented continuation lines from the buffer the scanner already decoded | shipped — `context=0` asserted byte-identical to the previous answer, so the default cannot regress |
| **I76** a truncated read said nothing about how to continue | `read_text` | `next: startLine=<first line not returned> (total=<lines>)`, plus an `outline:` steer on a `.cs` file | shipped — the retry can no longer overlap or gap; a read the caller's own range ended gets no steer |
| **I77** / **I90** (half) a wrong parameter name escaped the error contract, and `symbol:` was rejected outright | every `symbolId` tool, `ToolBoundary`, `ToolArgumentFilter` | `symbolId ?? symbol`; no tool declares `symbolId` required; every binder failure answered structurally, anything else `ERROR Internal` | shipped — `An error occurred invoking 'X'.` is gone from the surface and kept gone by `SchemaCensusE2ETests` |
| **I57** no tool added a namespace-level type to an existing `.cs` file | `add_member` | `path=` accepts a file as the container and appends to that file's namespace as one compile-gated edit | shipped — the forced text edit per new sibling type is closed |
| **I81** / **I82** a solution-wide sweep pinned every project's compilation for the life of the process — 5.8 GB on a process doing nothing at 0.00 s CPU | `WorkspaceRegistry`, `LoadedWorkspace`, `workspace_status` | `DropCompilations()` re-forks from `CurrentSolution` and refuses while any lease is out; a timer sweep (`--idle-minutes`, default 15) plus a 2 GB managed-heap trigger; `workspace_status` prints `idle=<n>m compilations=dropped` | shipped — the ceiling follows active work. Corroborated by accident: `build` failed `Out of memory` loaded and succeeded immediately after `unload_workspace` |
| **I79** the compile gate rolled back a new file's own unresolved references, and the agent answered with built-in `Write` | `EditGate`, `write_text`, `add_member` | a `CS0246`/`CS0234` the baseline already carried, or in a file absent from the `before` solution, is reported `UNRESOLVED … the edit was applied` | shipped — 4 rollbacks measured, every `CS0246` a `Mock<>` in a newly written test file. The mechanism was the count increase, not the new file |
| **I55** an applied `extract_interface`/`move_type_to_file` rewrote the user's `.csproj` — a redundant `<Compile>` item plus a BOM, repeating until `dotnet build` failed `NETSDK1022` | `extract_interface`, `move_type_to_file` | `ProjectGlobs.CompilesByGlob` reads MSBuild's **evaluated** `EnableDefaultItems`; the `.csproj` bytes are snapshotted and restored, but only when every added line is provably MSBuild's | shipped, four earlier approaches refuted — a concurrent external edit is left alone rather than destroyed. Two E2E tests assert the `.csproj` is byte-identical and a real external `dotnet build` exits 0 |
| **I74** (remainder) the searches were workspace-scoped, so a log outside the root could not be searched | `search_text`, `search_regex` | `root=<absolute dir>` walks with the same exclusions, bypasses workspace resolution, refuses a relative root | shipped — I74 closed |
| **I70** (remainder) `load_workspace` took no `targetFramework`, so a multi-targeted solution answered from whichever TFM MSBuild evaluated first and never said which | `load_workspace`, `workspace_status` | pass it as the MSBuild global property, make it part of the load identity, print it | shipped — I70 closed. **Not covered by a test that can fail**: `fixtures/FixtureSolution` is single-targeted |
| **I67** `workspace_status` cost 1090 MB and 29 s the first time it was called on a large solution: `GeneratorRanAsync` walked every project and forced a full compilation of all 148 to answer one word | `workspace_status`, and every tool through the shared cache | skip a project that holds no `.razor`/`.cshtml` **additional document** | shipped on the seventh attempt, after six falsified hypotheses — **1448 MB / 29 391 ms → 415 MB / 2.2 s**. Root cause was lazy document text, not a product coupling; the earlier "unsettled MSBuild load" explanation is refuted |
| **I73** git had no answer in the surface at all, and it was the biggest fallback class | `changed_files`, `diff_symbols`, `diff_text` | `GitRunner` over the shared `ChildProcess` runner; `diff_symbols` maps `--unified=0` hunks onto declaration spans and answers with symbol ids, `EXACT` only inside exactly one declaration | shipped — **575 `Bash` calls / 235 738 tokens** in one measured week, `git diff`/`git show` alone 220 calls / 130 458 tokens. The surface went 83 → 86 |
| **I93** (schema half) two schema rules stated in `CLAUDE.md` as if enforced had no census gate | `SchemaCensusE2ETests` | census from `tools/list`: every `dryRun` tool has `verbose`, every `symbolId` tool has `symbol`, none declares `symbolId` required | shipped — the header, budget and warnings rules stayed open until the I93 framing row closed them |
| **I66** `search_text` rented a buffer the size of the whole file *before* deciding the file was binary — above 1 MB `ArrayPool` does not pool, so each was a fresh LOH allocation | `search_text`, `search_regex` | probe with a 4 KB pooled buffer, rewind, and rent the full size only for a file that is text | shipped — eight identical calls grew the working set **+491 MB → +293 MB**; the cold call 3271 ms → 2939 ms |
| **I96** `WorkspaceSync.Settle` restamped a drained path from a fresh read, so a write landing between the merge and the settle was recorded as absorbed while the solution kept the previous content — permanently | `WorkspaceSync` | record only the stamp the merge read, and only when it is unchanged after the read; `Settle` no longer restamps; `Restore` forgets an aborted drain's stamps; `AbsorbAsync` re-checks immediately before adopting | shipped 2026-08-07 — red on rounds 0-3 of 40 before the fix, green after. The same reproduction exposed **I104** |
| **I97** the first external edit to a `.cs` file Roslyn had not materialized was invisible to `ChangeKind.Code` | `WorkspaceSync` | a document whose text was never materialized cannot be proved unchanged, so it counts as changed — without `WithDocumentText`, which makes Roslyn rewrite the file and move its mtime | shipped 2026-08-07 — red (`c0` vs `c1`) before the fix. The first attempt used `WithDocumentText` and broke `analyze changed=true` on the whole fixture |
| **I98** nothing answered "show me a call of tool X that succeeds" — recovering a valid argument set cost 4 whole-file reads (~14 KB) | `ToolArgumentFilter`, `ToolExamples` | one known-good call for the twelve tools whose arguments are not derivable from the schema, appended to the `remedy:` of a rejected call | shipped 2026-08-07 — 4 reads (~14 KB) → 1 deliberate empty call (~0.3 KB), with zero per-session cost: the examples are not in the tool list |
| **I99** a truncation one call away from the real total — `find_files` answered `100/108 files truncated` and always cost a second call | every capped listing tool | `ResultCap.Shown(total, cap)`: within a 10 % slack return the whole list, beyond it the steer stands | shipped 2026-08-07 — one wasted call plus one wasted 100-record payload removed per near-miss, across 26 call sites in 20 files |
| **I100** no tool answered "is a stale `terse` or `testhost` still holding the binary?" — diagnosing a 53-failure E2E collapse needed `Bash: tasklist` | `doctor` | report every live `terse`/`testhost` with pid, resident megabytes and start time, and name the false-green failure mode | shipped 2026-08-07 |
| **I101** a second server process served a stale semantic snapshot of a just-written file | `write_text`, `WorkspaceSync` | the mechanism was **I96 + I97**; `write_text`'s description no longer claims the freshness guarantee holds outside the workspace that performed the write | shipped 2026-08-07 — the E2E case is a **guard, not a reproduction**: it stayed green with the I96 fix reverted, and says so |
| **I102** an out-of-range `read_text` was indistinguishable from an empty file | `read_text` | `startLine=N is past the last line (total=T)` | shipped 2026-08-07 — `total=T` is the one-call answer to "how long is this file?". The `lines=` half is **declined**: a read of every listed file on every listing |
| **I81** (framing) the header echoed the request: 950 of 1 050 responses opened with `<tool> <arguments>`, worst case 419 chars | every tool | header only under `verbose=true`; a value the caller cannot derive moved into the body | shipped — **57 465 chars, 2.70 %** of 2 127 134 measured response characters |
| **I82** (framing) `(truncated=false, total=N)` reports a non-event: 706 occurrences against 110 real truncations | every listing tool | `N unit`, or `N/T unit truncated - narrow with X` | shipped — **19 325 chars, 0.91 %** |
| **I83** the `N: ` gutter repeated on every line of a contiguous read: 18 843 numbered lines, 3 549 of them blank | `read_text` | number only where the sequence breaks; drop blanks in whitespace-insignificant files; strip trailing whitespace | shipped — **82 341 chars, 3.87 %**, and `read_text` was 39 % of all bytes |
| **I84** source was returned at its file indentation: 15 711 lines with ≥5 leading spaces, deepest 59 | `get_symbol_source`, `get_symbol` | dedent by the common prefix, drop blank lines | shipped — 132 231 chars, but **bytes are not tokens**: a run of spaces costs 1-2 tokens, so this is worth less than I83 and I83 is worth more. Tab-compressing was dropped: it changes whitespace an agent copies back into `replace_symbol` |
| **I85** the mutating-tool envelope was the whole response — `edit_text` ran 219 times at a 192-char mean | every mutating tool | one line, file name only; footer removed; zero counters with a zero delta omitted | shipped — **19 228 chars, 0.90 %** |
| **I86** absolute repository paths in build output: 675 occurrences, 436 in `build` | `build` | diagnostics and the output tail rendered workspace-relative | shipped — **18 030 chars, 0.85 %** |
| **I87** a documentation id printed beside the name it already ends with: 502 lines of `T:A.B.IExecutor  interface IExecutor` | `search_symbols`, `find_implementations` | drop the description when it equals the symbol name | shipped — **43 967 chars, 2.07 %** |
| **I88** an outline printed the parameter list twice, once in the id and once in the signature | `get_file_outline`, `get_type_outline` | the id keeps its parameter list only where the type overloads that name | shipped — ~25 % of a member line |
| **I89** load telemetry nobody acts on — `index=…(hit=0 miss=0)`, `lastUsedUtc=`, `gen=`, `loadMs=`, every counter observed zero | `workspace_status`, `load_workspace` | behind `verbose=true`; the sync line still prints unprompted when the watcher is off, degraded or gapped | shipped — **6 845 chars, 0.32 %**, and it closes the render half of **I78** |
| **I89b** a dropped blank line was counted as a missing line, so a complete read answered `56/78 lines truncated` and steered into a second identical call | `read_text` | the count reports every line the range **covered**; only real clipping says `truncated` | shipped — found by the fresh-context reviewer, not by the suite |
| **I90** 38 % of errors carried no code and no remedy: 35 of 92 were exactly `An error occurred invoking 'X'.` | `ToolBoundary`, `Errors.SymbolNotFound` | `nearest:` capped at 5, `TerseError` rendered on two lines; the opaque-error half shipped with **I77** | shipped — error responses were 1.30 % of all bytes |
| **I91** the same directory prefix on 1 390 consecutive result lines — 49 407 chars, 2.32 % | every path-listing tool | fold the shared directory onto its own line | **rejected, twice.** It breaks the one property every path record has — that it pastes straight into the next call. Implemented, broke seven E2E tests that fed a printed path back to a tool, reverted. Re-examined 2026-08-07: no folding helper exists anywhere in `src`; a record's path stays whole |
| **I92** a confidence tag repeated on every record — `EXACT` 564 + `HEURISTIC` 1 409, ~18 900 chars, 0.89 % | every tagged tool | hoist the shared tag onto the count line | **rejected.** Hoisting has to *infer* which substring is a tag; it shipped and rewrote payload — `get_symbol_source` on a body containing the literal `  EXACT  ` returned `  `, silently. Reverted, and **a record's own text is never edited to save characters**. A declared tag `Line(text, tag)` is the only sound design; the emitter census found ~30 sites over 15 files, two load-bearing (`XamlRename` filters on `Confidence is "EXACT"`, `TextSearchService` strips its own tag to build the `unique=` key). 0.89 % of bytes for a MAJOR format change — closed as a measured decision |
| **I93** the "no tool emits a header" rule had no census gate: it ran from a hand-written list of ~40 of 83 tools, so a tool added later was exempt by default | `ToolHappyPathE2ETests`, `ToolCensus` | discover from `tools/list` and assert no first response line begins with the tool's own name | shipped 2026-08-07 — four tools stay exempt under a ratchet, each with a written reason and no success path on the fixture: `unload_workspace`, `undo_last_change`, `package_add`, `package_remove` |
| **I94** `read_text` bounded a response by **lines**, not characters, so a 155-line range of 1-3 KB rows answered 50.5 KB and was persisted to disk by the client | `read_text` | surface the internal budget as `maxChars=` | shipped — the clip is reported by the same `next: startLine=` steer **I76** added |
| **I95** every child process the server spawned inherited the server's own stdin — the MCP protocol pipe | `ChildProcess`, and therefore `build`, `run_tests`, `clean` and the three git tools | `RedirectStandardInput = true` plus an immediate `Close()` in the one shared runner | shipped — `GitToolsE2ETests` **248 s → 5.9 s (42×)**; one git call went ~50 s to 86 ms. It was also a real protocol hazard |
| `fixtures/FixtureSolution` cannot fail a change to `RazorGeneratedMap.DeclaresRazor`: its single `.cshtml` is not an additional document, so filtered and unfiltered answer byte-identically | `workspace_status` | — | **limitation.** The positive case is gated by `RazorToolsE2ETests` against `fixtures/RazorSolution`, and the skip itself by `WorkspaceRegistryTests`. A green `FixtureSolution` run is not evidence about this predicate |
| the workspace LRU evicts by **count**, not by size | `load_workspace`, `WorkspaceRegistry` | a size-aware cap | **not soundly implementable** (this was I68's remainder). Roslyn exposes no per-`Solution` size, and the 3.1-3.8 GB figure came from a gcdump. GC policy was falsified as a cause (`gcServer=0` 3735 MB, `GCConserveMemory=9` 3722 MB, shipped 3801 MB). **I81/I82** bound the resident set instead. Reopen only if Roslyn gains a per-solution size |
| `search_text` opens every non-binary file in scope on every call, where a persistent content index would not | `search_text`, `search_regex` | a trigram/bloom digest | **declined twice** (this was I65). At equal scope terse already wins — `glob=**/*.cs` **324 ms** against Rider's indexed **386 ms**; the residual gap is scope, and `glob=` is the lever. The digest costs **150-360 MB resident**, working directly against the **I81/I82** release. Reopen with a digest bounded well below the 111 ms it buys |
| fixer assemblies load into the default `AssemblyLoadContext`, so their files stay mapped for the life of the process | `cleanup fix=…`, `clean` | a collectible `AssemblyLoadContext` | **largely closed by I52** — the mapped files are shadow copies, so nothing in `bin/` is held. The collectible route was attempted in 0.15.0 and **reverted**: it broke MEF fixer discovery. Reopen only with a loader that shares Roslyn's own types |
| an analyzer or generator **rebuilt while the server is running** is still served from the assembly loaded first | every semantic tool | reload the rebuilt assembly | **not fixable in the default ALC**: `LoadFromAssemblyPath` cannot load a second assembly of the same identity, so the loader returns the loaded instance rather than losing the analyzer. Not a regression — I52 only makes it quieter. `analyzer-assembly-lock-plan.md` TC7 is therefore NOT DONE, and its option E (a child-process loader) is the only other exit. Remedy for a user: restart the server |
| shadow copies are reclaimed only by the seven-day startup sweep | every semantic tool | dispose on `unload_workspace` | **dropped**: the loader is a process-wide singleton and two workspaces share one content-addressed copy, so a per-workspace delete would pull it out from under the other. Measured: 17 directories / 34 MB after ~an hour of running the suites. Revisit with an oldest-first size cap |
| "does this test pass at `HEAD`?" has no answer in the surface, and it is what separates a regression from a pre-existing flake on a shared tree | a `run_tests` at a ref | run the suite against another revision | **not logged as a row.** It would have to build a second worktree, which is `Bash`'s job — version control, not code navigation. Answering it cost a `git worktree` plus a build plus a test run, once, deliberately |
| a generic `detail=` verbosity knob for every listing tool | all listings | — | **rejected.** `signatures`, `ids`, `containers` and `filter` already give per-tool control with names that say what they do; a generic knob would duplicate them and make each tool's cheapest form less discoverable. `verbose` is not this — it is an escape hatch from a cheaper default |
| **I122** a signature change whose callers live in another project always cost one `CompileRegression` plus a retry with `allowErrors: true`, paying the rejected call and the whole declaration payload; 6 occurrences in one run | `replace_symbol` | `symbolIds` + `declarations` carry one declaration per symbol, applied as a **single** compile-gated edit across every file they live in, several edits per file included (Roslyn `TrackNodes`), capped at 20, refused with both counts when the arrays are unpaired, and refused in either order when one batched declaration contains another rather than dropping the inner edit; `Errors.CompileRegression`'s remedy now names the batch | shipped — `ReplaceSymbol_WithABatchCarryingTheBrokenCallers_LandsAsOneCompileGatedEditAcrossBothFiles` changes `OrderService.Submit`'s signature with its 3 broken callers in 2 files and reports `2 files changed  errors=0 (+0)`; observed red first (`'declaration' is required`), and the control `ReplaceSymbol_ChangingASignatureAlone_IsReportedAsARollback` still reports `would be rolled back`. Removes ~4 000 tokens per occurrence and the repo's most common rejection class |
| **I123** `replace_symbol_body` dropped the blank line that followed the member, so a body edit that should have been byte-identical dirtied the file | `replace_symbol_body` | `WithBody` and `WithExpression` re-apply the replaced node's trailing trivia, which `.WithSemicolonToken(default)` and a freshly created semicolon token had been discarding | shipped — reproduced on the fresh binary as `@@ -12,1 +12,0 @@` deleting a blank line for a no-op body; now the same call answers `0 files changed` + `identical to what is already there`. `ReplaceSymbolBody_WithTheBodyThatIsAlreadyThere_ChangesNothingInsteadOfEatingTheBlankLine` and `…TurningAnExpressionIntoABlock_LeavesTheBlankLineAfterTheMember` observed red first. `replace_symbol` was **not** affected — its `WithTriviaFrom(original)` already preserved it, so that half of the finding did not reproduce |
| **I124** a symbol id copied out of a rendered response was rejected when it arrived HTML-escaped | every `symbolId` parameter | `SymbolReference.Unescaped` decodes `&lt;`/`&gt;`/`&amp;` in `SymbolLookup.ResolveAsync`, the single funnel every `symbolId` passes through; the decode is skipped entirely when the text carries no `&` | shipped — `GetSymbol_WithAnHtmlEscapedGenericParameterList_ResolvesTheSameOverload` resolves `Reconciler.Reconcile(Dictionary&lt;string,int&gt;, Order)`; observed red first as `ERROR SymbolNotFound`. One wasted call per generic member addressed from a rendered response |
| **I126** `diff_text`'s description told the agent not to call it, so a reviewer that needed hunk text reached for `Bash: git diff` | `diff_text`, `diff_symbols` | the description now states what it costs and how to bound it (`path=`, `maxLines=`) and opens with `Replaces Bash git diff`; `diff_symbols` ends with the exact `diff_text path=…` call for the hunks it could only map `HEURISTIC`, capped at 3 paths | shipped — `DiffSymbols_WithAHunkNoDeclarationContains_NamesTheExactDiffTextCallForThatPath` observed red first. A discouraged tool measures as a missing tool, and the missing tool was answered by the shell |
| **I125** a full `run_tests` took ~15 minutes and answered one line, so a green run gave no per-project cost and getting one meant paying the 15 minutes again | `run_tests` | `TestResultParser` carries one `TestProjectSummary` per `.trx`, named from `TestMethod/@codeBase`, else the shared test namespace, else the file; `DotnetRunner.PerProject` appends `Name:total/durationMs` per project to the green one-liner and to the full report **only when the run spanned more than one project** | shipped — the single-project green run is untouched, so `RunTests_GreenRun_StaysTiny` still holds at one line and ≤40 tokens; 6 unit tests cover both name sources, the empty report and both render branches, each observed red under mutation. One full re-run avoided whenever the question is "which tier is slow" |
| **I130** `find_usages` was the highest-failure-rate tool in the session corpus, 17 of 229 calls, and `max` was **silently ignored** rather than rejected | `find_usages`, `ToolArgumentFilter` | `ToolArgumentFilter` now rejects an argument the tool does not declare **before dispatch**, naming it and listing every accepted spelling plus the worked example; `Errors.Blank` names the aliases too | shipped — re-measured against a freshly built binary first: `symbol` already worked, but `{"pattern":"*.cs","max_results":3}` returned all 19 files, a confidently wrong answer. `ATool_GivenAParameterItDoesNotDeclare_SaysSoInsteadOfAnsweringAsIfItWereAbsent` and `FindUsages_GivenTheWrongNameForTheSymbolParameter_NamesEveryAcceptedSpelling` observed red first. Adding `name`/`max` as aliases on ~25 tools was **rejected**: 3 corpus calls against ~25 schema entries charged to every session's tool list, and the rejection now self-corrects in one call at zero schema cost |
| **I131** `find_files` answered 15 of 211 calls with a bare SDK string carrying no code and no remedy | `find_files`, `ToolArgumentFilter` | covered by I122's filter change; `query` added as an alias of `glob` because `search_text` spells the same idea `query`, and the blank-glob remedy now names all three spellings | shipped — the bare SDK string did **not** reproduce on a freshly built binary (`{"query":"*.cs"}` already answered `ERROR InvalidArgument`), so the corpus rate spanned older installed versions; the residual defect was the unnamed aliases and the silently-ignored parameter. `FindFiles_WithNoGlobAtAll_NamesEverySpellingOfIt` and `FindFiles_WithQueryInsteadOfGlob_AnswersTheSameListing` cover it |
| **I133** `read_text` on a `.cs` path was 71.2 % of all `read_text` output and 32 % of every character the server has returned, 57.1 % of it from calls that passed no line range | `read_text` | a `.cs` path asked for whole — no `startLine`, `endLine`, `tail`, `section` or `verbose` — now answers that file's outline plus a steer naming `get_symbol_source` and `read_text verbose=true`; any line range, `tail`, `section` or `verbose` still returns the text, and a `.cs` file that is not a workspace document falls through to the text unchanged | shipped, **breaking** — an outline is 32 % of the file's tokens, so this is ~68 % off every whole-file `.cs` read and ~2.2 M chars of the recorded corpus. 5 E2E tests cover the outline, both opt-ins, the non-document fallback and the untouched non-`.cs` path; observed red first. `ReadText_OnAWholeFileWithBlankLines_NeverClaimsItTruncated` encoded the old default and was re-pointed at `startLine=1`, which tests the same compression property |
| **I127** the blank-line half of `TextCompressor.Source` saved 104 tokens of 308 980 (0.03 %) and was the one branch that rewrote payload text | `TextCompressor` | `Source` is now `Dedented` alone; `Squeezed` is deleted, so blank lines **and** trailing whitespace survive and no payload text is rewritten but the indent | shipped — 0.03 % given back to delete the branch that could corrupt a raw-string literal; the dedent, worth 18–31 % at member scope, is untouched. Three unit tests that encoded the squeeze were inverted rather than deleted, and `Source_OnARawStringLiteral_LeavesItsBlankLinesAndTrailingSpacesIntact` was added as the case the old code could corrupt |
| **I128** comments are 10.5 % of tokens on a documented C# corpus and 0 % on this repo, so a stripper's value is entirely corpus-dependent | `get_symbol_source` | opt-in `comments=false` drops doc comments and inline comments through a Roslyn `CSharpSyntaxRewriter`, removing the comment's own indent and its trailing newline so no blank line is left behind | shipped — opt-in, never a default, because the measured spread is 0–10.5 % and an XML doc is often the only written statement of intent. `Reconciler.cs` gained a doc comment and an inline comment so the fixture can fail the test; 3 E2E tests observed red first. Not extended to `read_text`: its whole-file `.cs` path is now an outline (I133) and a ranged read has no parser to make stripping safe |
| **I129** an outline spent 12 % of its tokens on parameter names and 10–13 % re-stating the containing `Type.` on every member line | `get_file_outline`, `get_type_outline` | `parameterNames=false` prints parameter types without their names, on both outline tools | shipped for the parameter-name half — `GetFileOutline_WithParameterNamesOff_KeepsEveryTypeAndDropsOnlyTheNames` asserts the types survive, the names are gone and the response is strictly shorter; observed red first. `signatures=types` was **rejected** as the spelling: turning a declared `bool` into an enum string breaks every existing `signatures=false` caller for a 12 % saving, and `parameterNames=false` delivers the same saving additively. The container-prefix half was **rejected**: the outline's short ids must round-trip through `get_symbol`, and a bare `Submit` is ambiguous across 4 declarations in the fixture alone, so omitting the prefix would need a solution-wide uniqueness search per member line — the exact per-call cost I132 was raised about |
| **I132** terse was reported 12x slower than the Rider MCP, with a *repeat* `get_file_outline` of an unmodified file still costing 7 142 ms, read from session transcript timestamps | every read tool, `WorkspaceSync`, `OutlineService` | — | **refuted, no caching shipped.** Measured on a freshly built binary over a warm stdio session: on this repo's own 301-document solution `get_file_outline` costs **7 414 ms once, then 9/7/7/7 ms**; on the fixture `search_symbols` is 4 993 ms then 3/1/1/1, `find_usages` 684 ms then 6/2/3/3. The 7 s is one-time Roslyn compilation realization, not per-call re-derivation, and the transcript-timestamp method mis-attributed it because an idle workspace drops its compilations and the next call pays realization again. The proposal's other half was already implemented — `WorkspaceSync.SyncAsync` returns immediately when `pending.IsEmpty`. `RepeatQueryLatencyE2ETests` ships as the regression gate, pinning repeat latency at 500 ms across three tools and on this repo's own solution. Reopen if a repeat query is ever measured above that on a warm workspace |
| **I228** `doctor` has no MCP tool, so every diagnosis of the server itself was a `Bash` shell-out: 3 this run, each ~40 s, to read the `roslyn`, `phases` and `guard coverage` lines | `workspace_status` | `workspace_status verbose=true` appends the four self-check lines `doctor` produces - `roslyn`, `assets`, `guard coverage`, `phases` - in `doctor`'s own `OK`/`FAIL` shape, directly above the trailing `terse=<version>` | shipped - 3 shell-outs of ~40 s each removed per release-shaped run, and it closes the last question this repository's hard gate forced to the shell. The default response is byte-for-byte unchanged. A second `diagnose` tool was **rejected**: `workspace_status verbose=true` already carried the telemetry half, and I224's break-even does not carry a tool nobody would discover. Locked by `BacklogClosureE2ETests.WorkspaceStatus_WithVerbose_CarriesTheDoctorSelfChecksSoDiagnosingTheServerNeedsNoShellOut`, which also asserts no self-check line leaks into the default answer |
| **I229** the one-shot `call` probe did not validate arguments the way the MCP binder does: `analyze severity=info` was accepted by `terse.dll call` and refused by the server with `ERROR InvalidArgument: unrecognized severity` | `ToolCall`, `ToolArgumentFilter` | both paths share one `ToolArgumentFilter.Unrecognized(tool, supplied, required, accepted)`, so the probe refuses exactly what a client would and names what the tool declares | shipped - one wrong verification per occurrence removed, on the tool this repository's hard gate names as the arbiter of tool behaviour. Dropping the probe was **rejected**: it is the only way to answer about a freshly built binary. Locked by `InstallCommandE2ETests.Call_WithAnArgumentTheToolDoesNotDeclare_RefusesItExactlyAsTheServerWould`, and by `ToolCensusE2ETests.EveryAdvertisedSchema_DeclaresExactlyTheParametersItsToolMethodDeclares` - the two paths derive their accepted set from different sources, the advertised schema and reflection, so the census pins them equal |
| **I230** `edit_text section=` could only **replace** a section: 14 `CHANGELOG.md` entries were anchored on `### Added` instead, one of which answered `oldText matched 28 times` and needed `occurrence=1` | `edit_text` | one `place` parameter - `append` writes after the section's last non-blank line, `prepend` directly under its heading, an unknown value or a `place` without `section=` is refused by name | shipped - 14 anchored edits per release-shaped run become anchorless, and the one refusal-plus-retry among them cannot recur, because a heading occurring 28 times can no longer be matched wrongly. Two booleans `append`/`prepend` were **rejected** after being implemented: they cost ~65 more tokens of `tools/list` and let "both at once" be expressed at all. A `changelog` tool was **rejected**: the shape is generic markdown and I224's break-even does not carry a single-file tool. Locked by `BacklogClosureE2ETests.EditText_WithSectionAndAPlace_WritesInsideTheSectionInsteadOfReplacingIt` and `…EditText_WithAPlaceThatIsNotAPlacementOrWithoutASection_IsRefusedRatherThanSilentlyReplacing` |
| **I231** `get_file_outline` on a `.cs` file that is not a document of the loaded workspace answered `DocumentNotFound`, although `OutlineService.FromText` already parsed exactly that case for `ref=`. Hit 3 times on `fixtures/**` paths, each costing one error plus one `read_text` | `get_file_outline`, `OutlineService` | fall through to `FromText` for a `.cs` path that resolves inside the workspace root and exists on disk, and append `HEURISTIC parsed from the file's own text` | shipped - 1 error plus 1 fallback read removed per occurrence, and the fixture tree this repository deliberately keeps outside `TerseSharp.slnx` is outlineable. Outlining anything on disk unconditionally was **rejected**: a path outside the workspace root is still refused by containment. Locked by `BacklogClosureE2ETests.GetFileOutline_OnACSharpFileThatIsNotADocument_ParsesItFromTextInsteadOfRefusing` |
| **I232** `BuildWarningsE2ETests` was order- and load-dependent: one full-suite run reported `warnings=5` against its asserted 3, another reported three `CS0169` assertions failing behind 133 s builds, and both passed in isolation | `BuildWarningsE2ETests` | the counting builds are scoped to `project=Fixture.Warning`, whose three deliberate warnings all live in the one file the class touches, the one test that needs both halves loaded builds the whole solution and reloads first, and the class joins `TerseServerCollection` so its builds no longer overlap the thirty-five already in it | shipped - 4 spurious failures observed across 3 full-suite runs, each costing a ~20-minute re-run to classify. Re-running until green was **rejected** - this repository's own rule is that "it passed on rerun" is not a diagnosis. Extending the collection to the other five build-heavy E2E classes was **tried and reverted on CI evidence**: run 31634146607 went red on ubuntu and windows with `ChangedTestSelectionE2ETests` falling back to the whole solution, `total=2` against its asserted `total=1`, and all five are green outside it - they are now a reasoned, ratcheted exclusion set. Locked by `BuildWarningsE2ETests.Build_AfterAFailedBuildOfTheWholeSolution_StillReportsOnlyTheWarningsOfTheProjectItWasScopedTo` and by `E2ECollectionCensusTests`, which discovers the building classes from source so a membership cannot be dropped silently |
| **I233** a compile-gated edit reported `errors=0 (+0)` and was still followed by a `build`: 31 `build` calls in one run at ~5.6 s each, ~3 minutes, almost all confirming what the edit had already proved | `EditGate`, `GateCoverage`, `SKILL.md` | the first gated edit of a server process appends one line naming what the gate does **not** check - emit-time and source-generator errors - and telling the agent to build once before pushing rather than after every edit; it never repeats, and a rejected edit does not spend it | shipped - the notice costs ~30 tokens once per process against ~25 unnecessary builds and ~2.5 minutes of wall clock per release-shaped run. Emitting it on every edit was **rejected** by the minimum-response gate; dropping `build` from the definition of done was **rejected** because the emit path is real and a red build after a green edit has shipped here before. A `dryRun` does not spend the notice either, because `workspace_status verbose=true` runs a dry-run gate of its own and the review round caught it eating the one notice the next real edit owed. Locked by `BacklogClosureE2ETests.TheFirstCompileGatedEdit_NamesWhatTheGateDidNotCheckAndNeverRepeatsIt`, `…AVerboseWorkspaceStatus_DoesNotConsumeTheGateNoticeTheNextEditOwes` and `GateCoverageTests` |
| **I234** `ToolArgumentFilter.Unrecognized(RequestContext<CallToolRequestParams>)` answered `SymbolNotFound` twice and its own `nearest:` list printed that exact string back as a candidate | `SymbolLookup`, `SymbolReference` | `MatchesType` now compares a parameter type structurally - head by namespace suffix, then a recursion into every type argument and tuple element - and `Split` gives up exactly one enclosing pair of parentheses instead of trimming `(` and `)` greedily, which had counted a tuple parameter as two | shipped: 3 calls to 1 on an overloaded member whose parameter carries type arguments, and the `nearest:` short form a `SymbolNotFound` prints now resolves. The fixture gained `Awkward.Weigh(int)` / `Weigh(Boxed<IHandler>)` / `Weigh((IHandler Left, IHandler Right))` so `EveryReferenceAnOutlinePrints_ResolvesBackToASymbol` can fail; observed red on both defects before the fix and green after |
| **I235** `write_text force=true` creating a **new** `.cs` file was not compile-gated: a file written without its `using` answered a clean `changedLines=2` and its 10 `CS0103` errors surfaced only at the next `gate` call | `write_text`, `FileService.GatedAsync` | both write paths stage a new file as a document of the project that globs it (`ProjectGlobs.CompilesByGlob` + directory containment, minus the excluded directories) and run the same `EditGate`; the `files=[...]` batch keeps ONE gate across several new files | shipped: one silent-error round removed per new source or test file. Observed red first as `GateProbe.cs  changedLines=6` with no gate, green after; a file no project compiles stays ungated, asserted by `CompileGateE2ETests.WriteText_CreatingAFileNoProjectCompiles_StaysUngated` |
| **I236** `cleanup verify=true fix=style` and `fix=analyzers` - documented as *exactly* the two CI commands - both named `src/TerseSharp.Core/ReleaseVersion.cs`, which the ubuntu leg accepts | `cleanup`, `FormatService` | the disagreeing step was not a fixer: `FormatService.RunAsync` ran the Roslyn whitespace formatter on every mode. `FixRequest.Reformats` now excludes `fix=style` and `fix=analyzers`, so those two apply code fixes only | shipped: the pre-push gate is an oracle again - a `VERIFY_FAILED` from either mode is a red ubuntu leg. Measured on a freshly built terse.dll: both modes go `VERIFY_FAILED` -> clean on that file while `format verify=true` still reports it. `fix=all` and `fix=usings` still reformat and stay supersets |
| **I237** the first `changed_files` of a run returned 43 rows of which 40 were untracked `.research/**` scratch files - ~420 tokens discarded on the opening call of every task in this shared tree | `changed_files` | fold a directory contributing more than five untracked files into one `.research/**  +? -?  ?  x40 untracked` row, keeping tracked files one per line and counting every file in the summary | shipped - 40 rows to 1 on the measured case, ~400 tokens on the first call of every task here. Nothing is hidden: the count line still counts every file and the folded row carries its own count. Locked by `GitToolsE2ETests.ChangedFiles_FoldsADirectoryContributingManyUntrackedFilesIntoOneRow` and the negative `…ChangedFiles_ForFewUntrackedFiles_StillListsThemOneByOne` |
| **I238** `replace_symbol symbolIds=[a,b,c] add=[...]` was refused with `these targets do not share one containing type` although every `add` member belonged to the one class among them | `replace_symbol` | `addTo=` names which of the targets' own containing types takes the appended members; without it the refusal is unchanged, and the remedy now names the parameter | shipped - 3 calls to 1, and it removes an `allowErrors=true` window where the tree does not compile. `addTo` must name one of the targets' **own** containers, so a member can never land in a type no target lives in. Reproduced twice during this very task, which is what fixed the design |
| **I239** `write_text` accepted HTML-escaped markup, wrote it verbatim, answered `changedLines=5`, and the only way back was `Bash: git checkout --`, because `undo_last_change` holds Roslyn snapshots | `write_text`, `undo_last_change` | `ref=HEAD` restores a file's content from a git ref through the same compile gate and diff response as any other write; separately, a markup write whose content carries `&lt;` and no raw `<` keeps the full response and names that restore | shipped - the last working-tree action with no tool is served, and the corrupt write now says so at the moment it happens. Detection is a note, never a refusal, because a file may legitimately contain `&lt;`. Locked by `FileToolsE2ETests.WriteText_WithRef_RestoresTheFileFromThatRevision`, `…WriteText_WithRefAndContent_RefusesInsteadOfPickingOne` and `…WriteText_OfHtmlEscapedMarkup_WarnsThatItIsNotMarkup` |
| **I240** attributing one `cleanup verify=true` `VERIFY_FAILED` to the step that caused it cost three calls - `cleanup dryRun verbose` for the diff, then `format verify=true` to prove it was the whitespace formatter | `cleanup` | name the step beside each file in the verify listing - `whitespace`, `fixers` or `fixers+whitespace` - computed from the solution before fixes, after fixes and after the formatter, which the run already holds | shipped - 3 calls to 1 whenever a pre-push gate names a file, at no extra compilation. The diagnostics themselves are still not listed: `verify` is a verdict and `analyze` answers the diagnostics. Locked by `FormatCleanE2ETests.CleanupVerify_WithFixAll_NamesTheStepBesideEachFileAndTheCiEquivalentPair`, which writes a file with trailing whitespace so the fixture can fail |
| **I241** "which project compiles this path?" was answered by no tool; establishing it meant reading `ProjectGlobs`, `DocumentLookup` and the `.csproj` by hand | `list_projects` | `list_projects path=<file>` answers from the loaded document, else from the same evaluated `EnableDefaultItems`/`EnableDefaultCompileItems` the edit path reads, and says plainly when no project compiles it | shipped - 2-4 reads to 1 call, and it answers the question behind every "will my edit be compile-gated". Directory containment alone is never the answer: the glob verdict is required. Locked by `ProjectToolsE2ETests.ListProjects_WithAPath_NamesTheProjectThatCompilesIt` and the negative `…ListProjects_ForAPathNoProjectCompiles_SaysSoInsteadOfNamingOne` |
| **I242** a parameter list copied from a signature resolved only where the type carried no type arguments: `Weigh(int count)` resolved, `Weigh(Boxed<IHandler> boxed)` did not | `SymbolReference` | strip a trailing parameter name on the generic branch too, and let `EndsAType` treat `)` as ending a type so a named tuple parameter keeps its separator | shipped - one retry removed per member addressed by pasting its declaration, which is what an outline's signature column invites. Matching on the parameter NAME was refused: names are not part of the addressing contract. Locked by three new `InlineData` rows on `NavigationToolsE2ETests.GetSymbolSource_ForAnOverloadWhoseParameterCarriesTypeArguments_ResolvesTheShortForm`, with the existing negative theory unchanged |
| **I243** `search_symbols query=JsonSerializer` answered `0 symbols` and `get_symbol_source symbolId=System.Threading.Lock` answered `NOT_RESOLVED`, so every question about a framework or NuGet API is unanswerable and the fallback is the model's own training memory | `search_symbols`, `get_symbol_source`, `get_type_outline` | resolve a name against the compilation's referenced assemblies as well as its source | **not done in this run, and deliberately not closed as impossible** - it is the largest open capability gap and it is sound; it was cut on scope, not on evidence. Re-opened as `I264` so the next run implements it against a measured call count rather than inheriting a five-month-old row |
| **I244** `project_properties` answered `0 properties` for a project that compiles with nine, because it read the project file's own XML while this repository declares them in `Directory.Build.props` | `project_properties`, `list_projects` | answer from MSBuild's evaluated properties, naming the file that set each, and filter to the properties defined under the workspace root | shipped for `project_properties` - a `0 properties` indistinguishable from a project that declares none is gone, and the SDK's own hundreds are excluded by the root filter. The `tfm=` column on every `list_projects` row was **rejected on measurement**: an MSBuild evaluation costs 30-60 ms per project, so a 148-project listing would pay seconds for a column `project_properties project=<csproj> name=TargetFramework` answers for the one project the caller cares about. Locked by `ProjectToolsE2ETests.ProjectProperties_AnswersWhatMsBuildEvaluatedNotWhatTheProjectFileDeclares` and `…ProjectProperties_WithAName_KeepsOnlyThatProperty` |
| **I245** nothing answers which APIs in this code are legacy: `analyze minSeverity=hidden` over a 700-line service returned 19 records, all `IDE` style, not one performance or modernization rule | `analyze`, a new modern-API catalogue | `analyze profile=modern` plus a checked-in legacy-to-modern catalogue gated on the evaluated target framework | **rejected as specified, and the reason is the catalogue, not the analyzer half.** About 24 of the ~40 pairs sit behind no analyzer id at all, so the row's own design requires a hand-curated table that this repository would have to keep true against every SDK it is pointed at - and a stale entry proposing an API the framework lacks is exactly the confident wrong answer the server refuses. The analyzer half needs no new tool now that `I246` shipped: `analyze ids=CA1860,CA1866,CA1859` reports `NOT_ENABLED` for anything the project's analyzer set cannot produce, so the caller learns the sweep was real. Reopen only with a maintained catalogue source that is not this repository |
| **I246** `analyze path=<file> ids=CA1860,CA1866,…` answered `0 diagnostics`, byte-identical to the answer for a file those rules genuinely find nothing in | `analyze` | name any requested id that no referenced analyzer declares as `NOT_ENABLED <id>` beside the count | shipped - one silent-but-clean answer per targeted sweep is gone, on exactly the calls an agent makes when checking a named rule. Enablement is proven from the analyzer reference set, never inferred from `.editorconfig` text; compiler ids and `TERSE001` are never reported. Locked by `AnalysisToolsE2ETests.Analyze_WithAnIdNoReferencedAnalyzerDeclares_SaysNotEnabledInsteadOfASilentZero` and the negative `…Analyze_WithACompilerId_DoesNotClaimItIsNotEnabled` |
| **I247** `EditGate.Collect` recorded errors into a keyed dictionary and only counted warnings, so an edit answered `warnings=N (+3)` and learning which three cost an `analyze` measured at 2 163 ms and 4 379 ms | `EditGate`, every mutating tool | key the warning diagnostics through the `Record` the errors already use and name only the ones the edit introduced, capped at five | shipped - one 2-4 s call removed per warning-introducing edit, at the moment the agent still has the declaration in hand. Only the delta is named, never every warning in the changed file, and the lines are a caveat so a condensed answer still carries them |
| **I248** the compile gate is the only predicate on a mutation, so every API this repository bans in writing compiles cleanly and lands unremarked - 14 synchronous sites survive today and the agent emitted 68 more sync `File` calls in one week | `EditGate` | read a checked-in banned-symbol file in the `BannedApiAnalyzers` format and refuse a changed span that introduces one | **rejected as an `EditGate` feature, on evidence found while implementing it.** `Microsoft.CodeAnalysis.BannedApiAnalyzers` already does exactly this, as a real analyzer with real symbol resolution, and it is a `PackageReference` plus a `BannedSymbols.txt` - not a server feature. A span-scoped textual re-implementation inside the gate would be strictly weaker (no symbol resolution, no overload discrimination) and would fire on every mutation of every repository the server is pointed at, whose ban set it cannot know. The correct closure is to adopt the analyzer in this repository, where `analyze` and the build then enforce it at no cost to the tool surface; tracked as `I265` |
| **I249** `run_tests changed=true` selects at project granularity, so a one-member change in `TerseSharp.Core` still runs 718 E2E tests and 884 s of the suite's 1 102 s | `impact_of`, `run_tests` | `impact_of tests=true` emits the test declarations that transitively reach the symbol as a ready `test=` argument line | shipped as the direct-reference answer - the test classes that reference the symbol, each as a ready `run_tests test=` argument, tagged `HEURISTIC` and explicitly not a gate. Transitive reach was **rejected**: without coverage there is no sound transitive answer, and the row's own Rejected column already refused collecting it. A symbol no test names answers "run the whole suite" rather than an empty list. Locked by `ChangedTestSelectionE2ETests.ImpactOf_WithTests_NamesTheTestClassThatReferencesTheSymbolAsAReadyRunTestsArgument`, which uses the two-test-project fixture so it can distinguish, and the negative `ExploreToolsE2ETests.ImpactOf_WithTests_OverASolutionWithNoTestProject_SaysSoRatherThanAnsweringNothing` |
| **I250** `workspace_status` reported `advertised=88 tools 25959 tokens` while `TokenBudgetE2ETests` asserted 24 400 and passed | `TokenBudgetE2ETests`, `ToolProfile` | assert the budget over the widest surface the server can advertise, and report both numbers | shipped - the budget test now reads the number `workspace_status` itself reports, asserts it equals the payload the client received, and holds both against the ceiling, so the ceiling can never again be measured on a narrower surface than the agent pays for. The ceiling was raised to 26 600 to cover this run's added parameters, and the narrowed-surface ceiling in `MarkupProfileE2ETests` to 20 100; both are measured, not estimated |
| **I251** the client in this session advertises terse's 88 tools as deferred names and fetches each schema on demand, so the progressive-disclosure pattern the server pays 25 959 tokens to avoid is already implemented above it | `McpHost`, `ToolProfile`, `SchemaCompactor` | advertise a core set plus one discovery tool, behind `--tools` | **rejected for this run and re-scoped.** The `--tools core` profile already ships the "narrow set" half, and the row's own Rejected column concedes a search-based surface must keep a core set directly advertised because adversarial measurement put regex tool search at 56 %. What it actually asks for is a second, client-visible discovery tool - a MAJOR surface change whose payoff is zero on the clients that already defer schemas, which is where the 25 959 figure was measured. Schema compaction beyond `SchemaCompactor` is the part with a measured, client-independent win; tracked as `I266` |
| **I252** 14 of the 19 records `analyze minSeverity=hidden` returned for one file were `IDE0058` carrying the same message, each with a full path - about 74 % of the payload on one rule | `analyze`, `get_diagnostics` | fold records sharing an id, a severity and a message into one line carrying the positions | shipped - about 60 % of a hidden-severity pass on a wide file, on both tools, through one shared `DiagnosticFold`. The positions are kept because they are the fix list, and a repeated position collapses to `x2` rather than being dropped. Locked by `DiagnosticFoldTests`, whose five cases cover the fold, the distinct-id split, the single-record shape and the repeated position |
| **I253** `package_list` read the references a project declares and nothing answered whether any was vulnerable or outdated, so `dotnet list package --vulnerable` stayed a legal shell-out | `package_list`, `package_add` | `vulnerable=true` and `outdated=true` over the shared `ChildProcess` runner, and a `package_add` that refuses a package the feed does not serve | shipped for `package_list`, and `ToolGuard` now denies `dotnet list package`. The `package_add` feed probe was **rejected on failure-mode grounds**: it puts an unbounded network wait on a write path that must work offline, and a hallucinated package id is not a silent wrong answer - the next restore fails loudly with `NU1101` naming it. Locked by `ProjectToolsE2ETests.PackageList_WithVulnerable_AnswersFromTheResolvedGraphInsteadOfTheProjectFile` |
| **I254** the advertised tool list renders first in the prompt-cache key, so every `[Description]` reword invalidates the whole downstream cache, and the I201 profile makes the surface change mid-session on any `load_workspace` that adds a file kind | `ToolProfile`, `McpHost` | render the stable core first and the profile-varying families last, and measure the write cost of a mid-session `tools/list_changed` | **rejected on evidence about ordering.** The MCP `tools/list` result is a JSON array the client re-serializes into its own prompt however it likes, and Claude Code's deferred-tool path (measured in `I251`) does not render the server's order at all - so reordering the array is not observably a prefix change on the client where the cost was measured. The half that is real and unmeasured is the write cost of a mid-session `tools/list_changed`; that needs an instrumented client, which this repository does not have and cannot fake. Reopen with a measurement, not a design |
| **I255** learning what this backlog holds cost two calls - a read clipped at 62 of 305 lines, then a `search_regex matchesOnly` sweep that itself truncated at 253 matches | `read_text` | a `columns=` projection on a markdown table returning only the named columns' cells per row | shipped - one call returning one line per row across every table in the file, against a whole-file read plus a truncated regex sweep. A column no table declares is refused naming the columns that exist, and a file with no table says so. Locked by `MarkdownTableTests`, whose six cases cover the projection, the row count, multi-column order, both refusals and an escaped pipe, and by `FileToolsE2ETests.ReadText_WithColumns_ProjectsTheMarkdownTableDownToThem` |
| **I256** the `PreToolUse` guard was absent for a whole measured week and only 3 of 129 `workspace_status` calls asked for verbose, so nothing surfaced it; the corpus then paid 884 `Bash` calls the guard denies | `load_workspace`, `Doctor` | emit one `WARNING guard=absent` line on the first response of a server process, the way the once-a-day `UPDATE terse` line does | shipped, but **not on the first response of any tool** - that was implemented, measured and moved. Riding the boundary broke the one-line success contract (`build` answered two lines in `BuildWarningsE2ETests`), so the warning lives on `workspace_status` and `load_workspace`, which are orientation tools with no minimum-response contract, and it fires without `verbose=true`. Installing from `serve` stays refused: it writes the user's settings file. Locked by `AssetBannerTests`, five cases covering both assets present, each absent, both absent and the installed-but-stale case the server rewrites itself |
| **I257** `cleanup verify=true` ran with `fix=all` 56 times against 29 for the two CI-equivalent modes, and 40 of the 57 `VERIFY_FAILED` came from `fix=all` - the mode that also runs the whitespace formatter CI never runs | `cleanup` | on a `VERIFY_FAILED` under a mode that reformats, add one line naming `fix=style` plus `fix=analyzers` as the byte-equivalent CI pair | shipped, and made sharper than the row asked: with `I240`'s per-file step labels a listing whose every file says `whitespace` is provably a green ubuntu leg. 40 false-red gates per week, each of which cost the 3-call attribution `I240` measured. `fix=all` still reformats - `I236` kept that split deliberately. Locked by `FormatCleanE2ETests.CleanupVerify_WithFixAll_NamesTheStepBesideEachFileAndTheCiEquivalentPair` and the negative `…CleanupVerify_WithFixAnalyzers_DoesNotSteerToItself` |
| **I258** `InvalidArgument` was the top error code at 98 occurrences, and 43 of them were the right concept under the wrong name - `path` 15, `symbol` 13, `severity` 9, `paths` 6 | `ToolArgumentFilter` | extend the alias map to `path`→`glob` on the three search and file tools and `severity`→`minSeverity` on `analyze`, and name the tool's own plural when a different one is passed | shipped - 43 of 98 argument retries per week removed. An unrecognized parameter is still refused rather than silently dropped, which `I44` settled; the aliases are real declared parameters, so the schema census keeps the probe and the server refusing the same set. Locked by `FileToolsE2ETests.SearchText_WithPathInsteadOfGlob_AnswersTheSameRecords`, `AnalysisToolsE2ETests.Analyze_WithSeverityInsteadOfMinSeverity_AnswersTheSame`, and `InstallCommandE2ETests.Call_WithAnArgumentTheToolDoesNotDeclare_RefusesItExactlyAsTheServerWould` re-pointed at a name no tool declares |
| **I259** `impact_of` and `explore_symbol` were called 0 times each in 19 849 tool calls while the navigation chains they collapse ran 365 times | `explore_symbol`, `impact_of`, `SKILL.md` | fix or delete them: rewrite both descriptions around the chain they replace and re-measure, or drop them from the surface | shipped as the rewrite, not the deletion, because `I249` gave `impact_of` a capability nothing else has - the ready `run_tests test=` argument line. Both descriptions now open with the exact chain they replace (`get_file_outline` → `get_symbol_source`, `find_usages` → `find_implementations` → the project graph) rather than describing what they return, and `SKILL.md`'s table rows were rewritten the same way. **The next session scan re-measures the per-tool rate; a rate still at zero deletes them.** A description edit can move usage 10× (arXiv:2505.18135), but only against a re-measurement |
| **I260** the agent issued 19 838 of 19 849 assistant messages carrying exactly one `tool_use` block, and the repeat steer fires only from the second call, so 571 runs of exactly two consecutive calls were structurally unreachable | `SKILL.md`, the repeat steer | fire the steer on the first call of a plural-declaring tool whenever the previous assistant message called that same tool | **implemented, measured, and reverted - the measurement is the outcome.** Firing on the first call broke the one-line success contract on six tools at once (`write_text`, `edit_text`, `run_tests`, `read_text`, `get_file_outline`, `search_text` all failed their exact-response assertions), pushed `search_text` past its own token budget at 1 007/1 000, and polluted a machine-readable payload badly enough that `EveryReferenceAnOutlinePrints_ResolvesBackToASymbol` fed the steer text back as a symbol id. A steer can only ride on a response, so a two-call run cannot be reached without charging every first call for it. The lever that remains is the skill: `SKILL.md` now tells the agent to batch on its own judgement rather than waiting to be told, and `resx_set` was added to the plural map so its 138-call run is at least steered from the second call |
| **I261** 989 of 1 608 `.cs` `read_text` calls passed `verbose=true`, returning ~974 000 tokens - 7.8 % of the window's entire tool-result volume - and 1 018 of 1 162 verbose reads were cold | `read_text` | append the `symbolIds=[...]` batch line an outline already emits to a `.cs` file returned verbatim, so the next read is member-scoped | shipped, scoped to a read that covered the **whole** file - a line-ranged read gets nothing, because there the agent already chose its scope and the extra line is ceremony (it broke `ReadText_WithALineRange_ReturnsOnlyThoseLines` on the first attempt, which is how the scope was found). Re-steering to the outline stays refused: these calls are precisely the ones that overrode it |
| **I262** `resx_set` was measured at 299 calls in 4 runs across 4 sessions, longest run 138 consecutive, at 244 ch of argument framing each | `resx_set` | `entries=[{key,value,culture,comment}]`, answering per item with its own status the way `edit_text edits=` does | shipped as the two halves that were actually missing, after the row's premise turned out to be half stale: `entries=` as `Key=Value` lines **already existed** and was never called, so the defect was discoverability plus a silent drop. `resx_set` is now in the repeat-steer plural map, its description leads with the plural, and a line with no separator is named by line number and refuses the batch instead of vanishing from it. Per-item partial application was **rejected**: a `.resx` write is one atomic file rewrite, and a half-applied family is exactly the corruption the surgical single-`<data>` writer exists to prevent |
| **I263** `find_files glob=src/**/*.cs` answered 186 rows at ~2 400 tokens when the question was the directory shape of the tree | `find_files` | `depth=N` folds every file below the Nth path segment into one `src/TerseSharp.Core/**  x94 files` row, leaves a single-match directory as its file, keeps the count line counting every file, and drops folded rows from the `paths=[...]` batch line because they are globs | shipped - measured on this repository, `find_files glob=**/*.cs depth=2` answers **11 rows against 367**; locked by `BacklogClosureE2ETests.FindFiles_WithDepth_FoldsEverythingBelowTheNthSegmentIntoOneRow` and the negative-depth refusal beside it |
| **I264** `search_symbols query=JsonSerializer` answered `0 symbols` and `System.Threading.Lock` answered `NOT_RESOLVED`, so every framework or NuGet API question fell back to the model's own memory, measured at a 25-38 % deprecated-API rate | `search_symbols`, `get_type_outline`, `get_symbol`, `get_symbol_source` | a name matching no source declaration is resolved against the referenced assemblies: `search_symbols` falls back when source matched nothing, `get_type_outline` lists a metadata type's public members, `get_symbol_source` answers the signature plus a steer, `get_symbol` names the assembly and version. Exact type-name match only; no body is decompiled | shipped - both measured failures answer now (`JsonSerializer` -> 2 types with `System.Text.Json 10.0.0.0`, `System.Threading.Lock` -> 10 members); the review round added a namespace check on a qualified name, the resolved id on the outline, a real truncation count, and read-tool-only wiring so a mutating tool no longer dead-ends; locked by eight `BacklogClosureE2ETests` metadata tests |
| **I265** every API this repository bans in writing compiled cleanly and landed unremarked - 14 synchronous `File` sites survived in `src/` and the agent emitted 68 more sync `File` calls in one measured week, 2.18 per 1 000 of 31 235 emitted lines | this repository's own build | `Microsoft.CodeAnalysis.BannedApiAnalyzers` plus a checked-in `src/BannedSymbols.txt` wired by `src/Directory.Build.props`, banning `.Result`, `Task.Wait()`, `GetAwaiter().GetResult()`, `Thread.Sleep`, the synchronous `File` reads and writes and `StreamReader.ReadToEnd`, each entry naming its replacement | shipped - the build now fails on all of them, `XDocument.Load(path)` included; **18** pre-existing sites carry a `[SuppressMessage]` with a written justification each, ratcheted and justification-checked by `BannedApiTests`, which observed 12-against-1 when the ratchet was mutated. It compiles the sync-over-async and synchronous-file halves only; the `FileOptions.Asynchronous` and `SemaphoreSlim.Wait` halves stay prose, and the census row says so |
| **I266** the advertised payload is 25 959 tokens and the only lever pulled on it is description wording | `SchemaCompactor` | the compactor now drops every `default` key, not only the null ones; the default is already in each parameter's own description and the server binds from the C# default either way | shipped **and the ~40 % target refuted by measurement**: the 88-tool payload is 107 933 chars, of which **34 712 are tool descriptions and 36 025 are parameter descriptions - 65 % - which `I203` and `I224` protect as the discoverability lever**, `type` 9 379 (mandatory), `default` 2 493, frame 5 928. Dropping `default` gives 105 261 chars, **-2 672 (-2.5 %, ~668 tokens per request)**; nothing else is compressible without rewriting descriptions. Locked by `TokenBudgetE2ETests` at 25 900 tokens and by `BacklogClosureE2ETests.TheAdvertisedSchemas_CarryNoDefaultKey` |
| **I267** a `retryWith` token held the rejected declarations but **not** `add=` or `usings=`, so 3 of the 4 retries that task issued re-sent the whole payload anyway; one of them also spent a call learning that tokens are per-tool | `EditGate`, `RejectedEdits` | `RejectedEdit` carries `Add`, `AddTo` and `Usings`, every edit tool replays them when the caller passes none and lets the caller override, and `Unknown` now answers `retryWith=r3 was issued by replace_symbol, not by replace_symbol_body` with the replay remedy | shipped - a retry after a rejected `replace_symbol` carrying `add=` and `usings=` now costs the token alone; locked by `BacklogClosureE2ETests.RetryWith_HoldsTheUsingsAndTheAddedHelpersOfTheRejectedEdit` and `RetryWith_AgainstTheWrongEditToolNamesTheOneThatHoldsIt`, both observed failing first |
| **I268** `ERROR InvalidArgument: the declaration did not parse: Syntax error, '}' expected` named no position, so two ~2 500-character `declarations=` payloads were re-sent blind to find a JSON-escaping fault | `SymbolEditService`, `MemberDeclaration` | the message ends `at offset N of M:` plus the ~80-character window around the first parse diagnostic, flattened to one line, and a batched `replace_symbol` prefixes each entry's failure with `declarations[i]:` | shipped - one blind re-send of a multi-kilobyte payload per malformed declaration saved; locked by `BacklogClosureE2ETests.AMalformedDeclaration_QuotesTheTextAroundTheFirstParseError` and `AMalformedEntryOfDeclarations_NamesWhichEntryItCameFrom`, both observed failing first |
| **I271** `replace_symbol symbolIds=[...] add=[...]` across five members was refused with `these targets do not share one containing type` and a remedy telling me to split the call - while `addTo=` exists for exactly that case | `SymbolEditService` | both refusal branches name `addTo=`: `Unshared` reports `addTo=X names none of the containing types of these targets`, and the remedy offers `pass addTo= to name which of them takes the new members` | shipped in 0.37.0 with `addTo=` itself - the row was logged against the installed 0.36.0 binary and re-verified against `SymbolEditService.cs:694-697` and `:618-620` at review time |
| **I286** a `run_tests` that timed out answered `no test results were produced` and nothing else, so identifying one hung E2E test cost 6 `run_tests` calls and ~122 minutes of manual bisection | `run_tests`, `DotnetRunner` | every run passes `--blame-hang-timeout` at 80 % of `timeoutSeconds` with `--blame-hang-dump-type none`, and `HangSequence` reads the blame sequence file out of the results directory, so a stopped run ends `WARNING the run was stopped while these test(s) were still running: <names>` | shipped - probed against a freshly built binary on the new `fixtures/HangSolution`: 45 s, one call, `Hang.Tests.HangingTests.NeverFinishes` named, against 6 calls and ~2 h before. Locked by `HangSequenceTests` (4 cases) and `RunTests_WhenATestNeverFinishes_NamesThatTestInsteadOfAnsweringNothing` |
| **I287** `replace_symbol` on `GitTools.ChangedFiles` was sent without its `[McpServerTool]`, silently **un-advertising a tool**: build, `analyze` and `get_diagnostics` all stayed clean and only an E2E call answering `Unknown tool` caught it | `replace_symbol`, `SymbolEditService` | a replacement carrying no attributes where the replaced declaration carried some now ends the response `WARNING attributes dropped: McpServerTool, Description`; the edit still applies, because dropping an attribute is sometimes the intent | shipped - locked by `ReplaceSymbol_WhenTheReplacementCarriesNoneOfTheAttributesItReplaced_NamesThemInsteadOfUnwiringSilently` and its negative twin, both on `dryRun` so they mutate nothing. Cost before: 2 failed E2E tests and ~15 min, and on a release it would have shipped |
| **I288** nothing answered "which tags exist": the release flow needs it, `ChangelogReferenceTests` shells out for it, and one run made 2 `Bash: git tag` calls | `history` | `history tags=true` lists every tag newest version first with the short sha it names and its date, refused beside `baseRef=`/`path=`/`contains=`/`message=`/`commit=`; `ToolGuard` denies a `git tag` listing and leaves every tag mutation on the shell | shipped - 2 shell fallbacks removed per release cut. Locked by `History_WithTags_ListsTheRepositoryTagsNewestFirstWithTheCommitEachNames`, `History_WithTagsBesideACommitFilter_IsRefusedInsteadOfIgnoringTheFilter`, `Guard_ForAGitTagListing_DeniesItAndNamesHistoryTags` and `Guard_ForAGitTagMutation_LeavesItAlone` |
| **I289** moving one row between the `## Open` and `## Closed` tables was two `edit_text` entries both carrying the row full text - ~38 400 characters, ~9 600 tokens, over the 16 rows closed in one run | `edit_text`, `FileService` | `edit_text path=… row="I286" toPath=…` matches the row by its **first cell** and moves it; `newText=` beside it is what lands in the target, so only the rewritten row crosses the wire and never the old one | shipped - the delete half of every closure now costs an identifier instead of a row: ~600 characters saved per closure, ~4 800 tokens over a 16-row run, and this run closed its own 8 rows with it. Locked by `EditText_WithRowAndToPath_MovesThatRowAndRewritesItWithoutSendingItsOldText` and `EditText_WithARowIdentifierThatMatchesNothingOrTooMuch_SaysWhichInsteadOfMovingTheWrongRow` |
| **I291** `run_tests` could not reach VSTest RunSettings, so the whole per-test parallelism layer - `xUnit.MaxParallelThreads`, `xUnit.StopOnFail`, `MSTest.Parallelize.Workers`, `NUnit.NumberOfTestWorkers` - was unreachable without shelling out | `run_tests`, `DotnetRunner.Arguments` | `runSettings=["Name=Value"]` is validated the way `properties` is and emitted as one trailing `-- Name=Value` block after the scope arguments | shipped - the shell fallback is gone for every suite that must bound in-assembly concurrency. Locked by `RunTests_WithRunSettings_ReachesVSTestAndBoundsTheRunInsideTheAssembly`, which asserts `failed=1` where the unbounded run reports `failed=3`, and by the malformed-entry refusal test |
| **I292** a `projects=` batch naming the same project twice ran 6 of 14 tests red purely from concurrency, and the merged verdict reported those failures as real | `run_tests`, `DotnetRunner` | two entries resolving to one `.csproj` now answer `ERROR InvalidArgument` naming the duplicated project file, before anything is built or run | shipped - one full false-red batch removed per duplicate; 6 phantom failures measured on the run that found it. Locked by `RunTests_WithTheSameProjectTwiceInABatch_IsRefusedInsteadOfRacingTheAssemblyAgainstItself` |
| **I293** neither `format` nor `cleanup fix=all` collapsed a run of blank lines, so the double blanks `add_member` leaves behind were fixed with `Bash: perl -0777 -pi` across three `.cs` files - a **write** fallback that bypassed `EditGate` entirely | `format`, `FormatService` | `FormatOnlyAsync` now runs a `CSharpSyntaxRewriter` that collapses runs of end-of-line trivia to at most one blank line, after the Roslyn formatter; it edits trivia, never token text, so a raw string literal cannot be rewritten | shipped - one write-path shell fallback removed per multi-member `add_member`. Locked by `Format_WithARunOfBlankLinesBetweenMembers_CollapsesThemInsteadOfLeavingThemToTheShell`, which asserts the file holds no run of three consecutive line breaks and that the two members are separated by exactly one blank line |
| **I290** the `holder pid=13724 TerseSharp.E2ETests` line said "read its command line before stopping it", and reading it needed `Bash: powershell Get-CimInstance` - 2 shell fallbacks in one run | `build`, `run_tests`, `LockHolders` | each holder line now carries `exe=<path>` from `Process.MainModule`, workspace-relative when it lives under the root, so a test host running out of this tree own `bin/` is told from another session at a glance | shipped in part - the executable path, not the argument list: no managed API exposes another process command line on Windows without a `System.Management` or P/Invoke dependency, which was rejected as a Windows-only package for one diagnostic line. For the apphost holders that produced the finding the exe path **is** the disambiguating fact; a bare `dotnet` host still is not. Reopen if a cross-platform command-line read arrives in the BCL. Locked by `LockHolders_ForThisProcess_NamesTheExecutableItRunsSoTheCommandLineNeedsNoShellOut` |
| **I299** a `run_tests` killed at its deadline answered `no test results were produced` plus the bare `TIMED_OUT` marker, because `ChildProcess` cancelled `ReadToEndAsync` and dropped every byte it had read; four cTraderDev sessions each burned 10-15 minutes on the same project and retried with a bigger `timeoutSeconds` instead of learning anything (the run measured 1 080 s against a 600/900 s deadline) | `run_tests`, `build`, `ChildProcess`, `DotnetRunner` | stdout and stderr are copied into a buffer as they arrive, the deadline governs the process rather than the pipes, the kill is followed by a bounded drain, and the timed-out response carries a `remedy:` naming `timeoutSeconds`, `test=` and `filter=` | shipped - the tail now names the tests the run had already reported, where the answer used to carry 0 bytes of the child's output; **I286**'s blame collector cannot cover this case, because a run that is merely slower than its deadline has no hanging test to name. Locked by `RunAsync_WhenTheDeadlineExpires_KeepsWhatTheChildAlreadyWrote`, `RunAsync_WhenADetachedGrandchildKeepsThePipeOpen_AnswersAndSaysTheCaptureIsIncomplete` and `RunTests_WhenTheDeadlineExpiresBeforeAnyResult_KeepsWhatTheRunHadAlreadyPrinted` |
| **I302** `list_tests` could not answer under the Microsoft.Testing.Platform runner at all: the SDK launches the test application with `--server dotnettestcli`, and MTP's `TerminalOutputDevice` discards `--list-tests` output in server mode (dotnet/sdk#49754, testfx#8661), so one of the four build/test tools was a refusal on every xunit.v3/MSTest/NUnit MTP repository | `list_tests`, `DotnetRunner` | build the target, resolve each test project's `TargetPath` with `dotnet msbuild -getProperty:TargetPath` — an evaluation, so it is right on an incremental build that recompiled nothing, which the `Name -> path.dll` build lines are not — then run each module directly with `--list-tests` and parse the names with the `TestNameList` that already handled that shape | shipped; the tool answers on the MTP fixture instead of refusing, covered by `ListTests_UnderTheTestingPlatformRunner_ListsTheNamesByRunningTheTestModuleItself` and `ListTests_UnderTheTestingPlatformRunnerWithContains_KeepsOnlyTheMatchingNames`; the rejected `--list-tests` through `dotnet test` in any form stays rejected, measured empty at 3.2.2 |
| **I294** `find_files` was the only listing tool with no `root=`; inspecting `%LOCALAPPDATA%/terse-analyzers` and `%TEMP%` cost 3 `Bash: ls`/`find` fallbacks in one run | `find_files` | shipped `root=<absolute directory>`, tagged `outside-workspace`, full paths on the `paths=[...]` line, refused beside `tracked=true` | shipped - 3 shell fallbacks per out-of-workspace diagnosis removed; locked by `FindFiles_WithAnAbsoluteRoot_ListsThatDirectoryAndSaysItIsOutsideTheWorkspace` and two refusal tests |
| **I295** an `add_member` whose new member name the type already declares was applied, compiled, rejected `CompileRegression` and rolled back - 3 times in one release, ~4 000 characters re-sent each | `add_member`, `SymbolEditService` | shipped a syntax-level signature check before the edit: `ERROR NameTaken` naming the existing declaration and its line, with overloads still allowed | shipped - one compile round trip and one re-sent declaration saved per collision; locked by `AddMember_WhenTheTypeAlreadyDeclaresThatSignature_IsRefusedBeforeAnythingIsCompiled` and `AddMember_WithAnOverloadNoExistingMemberDeclares_StillLands` |
| **I296** `write_text files=[...]` was believed to declare no `force`, so a batch mixing a new `.cs` file with `.props`/`.csproj` entries was split into two calls | `write_text`, `FileService` | measured: the capability was already there and already tested (`WriteText_WithTwoInterdependentCSharpFiles_LandsThemUnderOneCompileGate`); the defect was that `files=`'s own description never said the top-level `force` applies to it. Both descriptions now say so | shipped as a description fix - 1 rejected call and 1 extra call per mixed batch; locked by `WriteText_WithABatchMixingACSharpFileAndAMarkdownFile_IsAcceptedUnderTheOneForce` |
| **I297** `edit_text row=` moved one row per call, so closing a backlog cost one call per row - 8 in one release | `edit_text`, `FileService` | shipped `rows=[{row,newText}, ...]` against one `toPath=`, up to 25, cut and landed in order as one write per file; a row matching nothing refuses the whole batch | shipped - 7 of 8 calls per backlog closure; locked by `EditText_WithRowsAndToPath_MovesEveryRowInOneCall` and `EditText_WithRowsAndAnIdentifierThatMatchesNothing_MovesNothingAtAll` |
| **I298** five tests assert behaviour "outside a .NET tree" from a sandbox under `%TEMP%`, so a stray `.csproj` above it turns them red with no hint - ~40 minutes of one run | `ToolGuardTests`, `UpdateE2ETests` | shipped `ToolGuard.Marker(cwd)`, which returns the solution marker the walk actually found; the three sandbox assertions now fail naming that file and its directory | shipped - the whole diagnosis; the failure message now names `%TEMP%/<stray>.csproj` instead of only "denied" |
| **I300** an unscoped `changed_files` in a tree carrying 42 untracked files clipped at 52 and dropped a tracked, modified `.cs` file from the listing | `changed_files` | tracked rows were already ordered ahead of untracked ones; shipped the second half of the proposal - a `tracked=N untracked=N` line whenever a listing carries both kinds | shipped - a clipped listing can no longer read as though the tracked half was all of it; covered by `ChangedFiles_WithUntrackedFalse_DropsTheFilesGitDoesNotTrack` |
| **I301** `BacklogClosureE2ETests.WriteText_WithTwoInterdependentCSharpFiles_LandsThemUnderOneCompileGate` answered `ERROR Transient: ... Fixture.Trading.csproj because it is being used by another process` on the windows CI leg while ubuntu and macos were green | `EditGate`, `ProjectGlobs`, `LoadedWorkspace.Applied` | **reproduced locally for the first time** in this run, at `BacklogClosureE2ETests.cs:566`, when a second batch write to the same project ran immediately before it. The `lock (ProjectGlobs.EvaluationGate)` serializes this process only; the holder is MSBuild's out-of-process build host still holding the `.csproj` when the snapshot/restore replaces it, which no in-process lock can order. Not fixed: a wrong fix here is worse than none, and the diagnosis needs the full bug-fixing gate | closed as a measured decision with new evidence - the local reproduction and the narrowed mechanism are recorded here; the adjacent test that triggered it was made `dryRun` so it does not add CI pressure. Reopen with a fix the moment the build host's hold can be ordered, or the restore made lock-tolerant |
| **I303** `TestReporterProbe.DetectAsync` re-walked `global.json` and every `*.csproj` under the root once per `run_tests` call | `TestReporterProbe`, `LoadedWorkspace` | shipped the `global.json` half: memoized per file and invalidated by its own last-write time, so a VSTest repository - every call in this one - pays the walk once. The `.csproj` half is deliberately **not** cached | shipped in part, with the reason recorded: invalidating the `.csproj` scan needs the directory walk it exists to avoid, so caching it would be a silently stale answer |
| **I304** `write_text files=[...]` re-observed refusing a mixed batch while writing the `MtpSolution` fixture - 3 calls where 1 would do | `write_text`, `FileService` | same finding as **I296**, same fix: the `files=` and `force` descriptions now state that the top-level `force` covers every `.cs` entry of a batch | shipped - 2 calls per mixed fixture write |
| **I305** the same glob was reported to mean different things to `find_files` and `search_text`: 55 files against 0 matches | `search_text`, `search_regex`, `FileGlob` | **not reproducible.** Probed in this session against the installed 0.42.0: `glob="tests/TerseSharp.E2ETests/*.cs"` answered **56 matching lines** through `search_text` and **57 files** through `find_files`. Both paths call `FileGlob.MatchesRelative`, whose `MatchesPath` is `IsPathPattern` - true for any glob carrying a separator | closed as not reproducible, with the probe recorded. Reopen on a fresh observation of a counting tool answering zero for a glob `find_files` answers with files |
| **I306** neither `build` nor `run_tests` would show the command line it ran, at any verbosity; isolating a rejected MTP option cost 4 `Bash` probes plus a source read | `build`, `run_tests`, `DotnetRunner` | shipped `ProcessRun.Command`, set by `ChildProcess.RunAsync`, echoed unconditionally on a run that produced no results and on a failed build, and under `verbose=true` otherwise | shipped - the 4 probes and the source read; a green run is unchanged, so the minimum-response contract holds |
| **I307** a run the Microsoft.Testing.Platform runner aborted on `--timeout` lost its deadline steer, because `run.TimedOut` is false when the runner ends the session first | `DotnetRunner.RanAsync`, `AppendDeadlineRemedy` | shipped the evidence-derived signal: the remedy fires when the elapsed time reached the `--timeout` window terse itself passed, not only when the process flag is set | shipped - one steer per timed-out MTP run, which is the run where the steer matters most; no exit code is asserted, exactly as the row's Rejected column required |
| **I308** `--ignore-exit-code 8` is forwarded to every test application of a `--solution` MTP run, so a project that matched zero tests can aggregate to a green verdict with nothing naming it | `DotnetRunner`, `TestPlatformArguments`, `Unfinished` | not shipped. The counting evidence exists (`report.Projects` against `TestReporterProbe.TestProjectsAsync`), but `fixtures/MtpSolution` carries one test project, so a test of it could not fail - and this repository's own rule is that a case must be in the fixture before the check ships | closed as a measured decision: the fixture cannot express the case today. Reopen together with a second MTP test project in that fixture, which is what makes the assertion able to fail |
| **I309** the E2E build-spawning classes shared one `TerseServerCollection`, a 551 s serial critical path of a 723 s CI leg | `E2ECollectionCensusTests`, `TerseServerFixture` | the split shipped in this release cycle (576.7 s -> 476.9 s locally, 960 tests green) and raising `maxParallelThreads` was measured **worse** on CI run 32369954276 (E2E 723 s -> 773 s, unit 305 s -> 482 s). What remains is the ~467 s `FixtureSolution` floor, which 30+ classes build | closed as a measured decision: the partition shipped and the lane raise is refuted with numbers. The remaining floor needs a per-test copy of `FixtureSolution`, whose disk and build cost across ~30 classes was judged larger than the 467 s it removes. Reopen with a measurement of that trade, not an estimate |
| **I310** a `.cs` file outside every workspace root was reachable by no legal call: `write_text` answered `OutOfWorkspace` and the guard denied `Write`, `cat` and a heredoc on the same path - 3 refused calls, ending in a scratch directory inside the repository | `write_text`, `ToolGuard` | shipped: an absolute path under no loaded root is written with `force=true`, tagged `outside-workspace` and never compile-gated, because no project of this workspace compiles it | shipped - 3 refused calls per scratch probe, and the one case where the gate had no legal path at all; locked by `WriteText_WithForceAndAnAbsolutePathOutsideEveryRoot_WritesItAndSaysSo` |
| **I311** `run_tests` reported summed and wall time but no concurrency, so a parallelism change read as a regression: summed 1 402 s -> 1 608 s while wall fell 576.7 s -> 476.9 s | `run_tests` | shipped `elapsedMs=` on the green line and `concurrency=<summed/wall>x` on any run spanning more than one project, plus the slowest test when concurrency is under 2x | shipped - the whole external `.trx` analysis that question needed; a single-project green run stays one short line, so `RunTests_GreenRun_StaysTiny` still holds |
| **I312** `list_tests` under MTP resolves a test module with `dotnet msbuild -getProperty:TargetPath`, which on a multi-targeted project yields a path with no TFM segment - the project is dropped silently, or a stale dll is executed | `DotnetRunner.ModulesAsync`, `TestReporterProbe` | not shipped. No fixture in this repository multi-targets, and the row itself refused to ship the two-property query on the ground that its output shape could not be observed here - the guard denies `dotnet msbuild`, and this repository bans a claim about tool behaviour it has not observed | closed as a measured decision on the row's own stated ground. Reopen together with a multi-targeted test fixture, which is what makes the refusal observable |
| **I313** `list_tests` under MTP decided what to execute from a walk of every `*.csproj` under the workspace root, so a `samples/Foo.Tests.csproj` outside the solution could be listed as part of it | `TestReporterProbe.Projects`, `DotnetRunner.ModulesAsync` | shipped: a solution target now resolves its own project list through `SolutionFile.ProjectsAsync`, made absolute against the solution's directory; a `.csproj` target is still itself, and only a bare directory falls back to the walk | shipped - a listing can no longer name tests `run_tests` on the same target would not run |
| **I314** a .NET-shaped file outside every loaded root is reachable by no legal call: `ToolGuard.OnPath` denies by extension alone and `get_file_outline` answers `ERROR OutOfWorkspace` | `OutlineService.FromDiskAsync`, `PathGuard`, `ToolGuard.OnPath` | not shipped. The write half is closed as **I310**; the read half is a six-step change across `OutlineService`, `XamlService`, `ResxTarget`, `RazorContext`, `ProjectTools` and the guard's own scoping, each needing its own `outside-workspace` tag, `HEURISTIC` record class and E2E pair | closed as a measured decision: the row's own step list is larger than everything else shipped in this release put together, and a half-done version - some readers answering outside a root and others refusing - is worse than today's consistent refusal. Reopen as the next release's single largest item, taking the six steps in the order the row already writes them |
| **I314** [accuracy] the guard denied the whole `git diff --cached` family - 33 denials in one week - while `changed_files` declared no staged mode, so the deny had no replacement; a further 8 were `--untracked-files=no` | `changed_files`, `ToolGuard` | shipped `staged=true` (index against `HEAD`, or against `baseRef=`) and `untracked=false`, and a `diff-cached` guard row that routes `git diff --cached` to `changed_files staged=true` by name | shipped - 33 denied calls plus 33 retries a week, and the guard's last row that redirected to a tool which could not answer; locked by `Guard_ForAStagedDiff_NamesChangedFilesStaged`, `Guard_ForAnUnstagedDiff_StillNamesDiffSymbolsRatherThanTheStagedForm` and two `changed_files` E2E tests |
| **I315** [accuracy] `ToolGuard` matched a driver name inside a quoted string literal: a PowerShell process query whose `-match` pattern contained `msbuild` was denied and redirected to `build` | `ToolGuard` | shipped quote-aware segmentation: `Segments` masks quoted spans before splitting on `&&`, `\|\|`, `\|`, `;` and newline, so an operator inside an argument no longer starts a new command | shipped - one false denial plus one rewrite per occurrence, out of a budget Claude Code caps at 3 consecutive; locked by `Guard_ForADriverNameInsideAQuotedArgument_DoesNotTreatItAsTheCommand` over three real shapes |
| **I316** [accuracy] the brace-glob refusal was 78 refusals in one week = 51% of all 152 terse `InvalidArgument` results, the single most frequent error in the surface | `find_files`, `search_text`, `search_regex`, `FileGlob` | shipped brace expansion in `FileGlob.Translate`: `{a,b}` becomes a regex alternation, nested and across separators, and an unclosed brace is a literal. `FileGlob.Unsupported` and its three call sites are deleted | shipped - 78 rejected calls and 78 retries a week; locked by four `FileGlobTests` cases and `EveryGlobTakingTool_ExpandsABraceExpansionIntoItsAlternatives` |
| **I317** [accuracy] `edit_text section=` was used on 1.6% of 2 795 `edit_text` calls while 391 of 751 adjacent read-to-edit pairs targeted a `.md` file, ~628 000 tokens a week spent composing anchors, and 29 `oldText matched 0 times` rejections | `read_text`, `SKILL.md` | shipped: a `.md` read carrying no `headings=`, `section=`, `columns=` or line range ends `sections=N - address one with read_text or edit_text section="..."`, naming up to six section titles | shipped - ~390 read calls a week plus the 29 anchor misses; locked by `ReadText_OnAWholeMarkdownFile_NamesTheSectionsThatAddressItInsteadOfAnAnchor`, which also asserts a ranged read carries no steer |
| **I318** [accuracy] `analyze` declared only `path=` while `read_text`, `get_file_outline`, `diff_text` and `write_text` all declare a plural, so generalising cost a rejected call | `analyze` | shipped `paths=` (10 entries, files, directories or globs), combined into one scope through the brace expansion **I316** added, with rooted entries made workspace-relative and comma/brace entries refused by name | shipped - one rejected call per agent that generalises, and the end-of-task per-file sweep is now one call; locked by `Analyze_WithPaths_CoversEveryNamedFileInOnePass` and `Analyze_WithAPathsEntryCarryingAComma_IsRefusedRatherThanMisScoped`, and enrolled in `RepeatSteer.Plural` |
| **I319** [modern] the compile gate reports errors and up to five introduced warnings but no info-severity diagnostic, the class the ubuntu format leg kills - 104 IDE0022 and 377 IDE0032 emissions across the scanned sessions | `EditGate`, the four edit tools | not shipped. Running the referenced analyzers at `info` over the changed declaration inside the gate puts an analyzer pass on the write path of every edit; `gate` measures `gateMs=7.28` on the widest document of this solution today, and an analyzer pass is one to two orders of magnitude above that | closed as a measured decision: the cost lands on every edit, while the diagnostics it would surface are already answered by one `gate` call at the end of a task - which `analyze paths=` (**I318**) has just made cheaper. Reopen with a measurement of the per-edit analyzer cost on this solution, not an estimate |
| **I320** [speed] every read and search tool sat at a p50 near 1.65 s while `changed_files` answered in 217 ms over the same transport, an 8.4x gap; `read_text` alone burned 527.8 minutes of wall clock in a week | `ToolContext`, `WorkspaceRegistry` | **measured, and the premise is refuted.** `workspace_status verbose=true` on this solution reports `phases: widest=BacklogClosureE2ETests.cs realizeMs=0.10 outlineMs=1.95 gateMs=7.28 diffMs=97.23` - the in-server outline of the **widest** document costs **1.95 ms** against a 1 903 ms end-to-end p50, so 99.9% of the latency is not in the read path. `diffMs=97.23` against `changed_files`' 217 ms end-to-end is the same ratio at a different scale, which is what a fixed per-call overhead cannot explain and a payload-proportional one can | closed as a measured decision: there is no ~1.45 s for the read path to give back, and the lever that remains is payload size, which is what **I324** and **I326** address. Reopen if `phases` ever reports `outlineMs` or `gateMs` in the hundreds |
| **I321** [speed] batch-parameter uptake splits sharply - `run_tests projects=` 19%, `read_text paths=` 24%, `get_file_outline paths=` 33%, `replace_symbol symbolIds=` 37%, `write_text files=` 39% | `SKILL.md`, the consecutive-call steer | **already implemented.** Read back from the advertised surface in this session: every one of the five names its plural in the **opening sentence** of its `[Description]` - "Pass paths to read up to 10 files in ONE response", "Pass projects to run several test projects in ONE call, CONCURRENTLY", "Pass symbolIds and declarations to replace members in several files as ONE compile-gated edit", "Pass files=[{path,content}, ...] to write up to 10 files in ONE call" - and `SchemaCensusE2ETests.EveryToolWithAPluralParameter_NamesItImperativelyInItsDescription` enforces it | closed as already shipped: the proposed change is in place and census-gated, so what is left is a re-measurement rather than a change. The one remaining lever, firing the steer on the first call, is refuted in the row itself |
| **I322** [speed] `workspace_status verbose=true` is advertised as the in-server replacement for `terse doctor` but printed no process or memory line; `WorkingSet64` occurred once in the source. Measured mid-scan: 8 live `terse` processes, 5.87 GB resident, largest 2.12 GB | `workspace_status`, `Doctor` | shipped a fifth self-check: `memory: this server NNNMB, N live terse server(s) holding NNNNMB`, failing above 8 GB with the unload remedy | shipped - the one number an agent could not obtain without breaching the gate; locked by `WorkspaceStatus_Verbose_ReportsHowMuchMemoryTheLiveServersHold`, which also asserts the quiet status does not carry it |
| **I323** [cost] `explore_symbol` was called 0 times and `impact_of` once in 360 sessions, the second measurement below the bar **I224** set; at 294 tokens per advertised tool the pair costs ~2.1 M base-input-equivalent tokens a week | `explore_symbol`, `impact_of`, `SKILL.md` | not shipped. Removing two advertised tools is a MAJOR change to the tool surface under this repository's own contract, and it lands on `ToolCoverageE2ETests`, `ToolCensus`, three documents and the swap table at the same time as five new parameters | closed as a measured decision: the deletion is right and is deferred to a release of its own, so that a surface removal is not merged with a release that adds to the surface. Reopen as the first item of the next release, with the fold into `get_symbol usages=true` / `find_usages impact=true` decided before the removal |
| **I324** [cost] the fixed per-session cost is 25 857 tokens of advertised surface plus a 91 053-byte `SKILL.md`, ~48 600 tokens before any work; 65% of the surface is schema-side (`parameterDescriptions=9792`, `schemaFrame=6921`) | `[Description]` on parameters, `SKILL.md` | not shipped as a systematic pass. Five new parameters were measured at ~110 tokens each **after** every description this release touched was trimmed back, which held the growth to 548 tokens (25 900 -> 26 450) rather than the ~900 the untrimmed text cost | closed as a measured decision: the trim was applied to the descriptions this release touched, and the 30% sweep across all 88 tools is a change whose only safe unit is one tool at a time against the census gates. Reopen as a per-family pass, with `parameterDescriptions=` from `workspace_status verbose=true` as the before-and-after number |
| **I325** [cost] `diff_text` answered 295 of 340 calls (87%) with a `maxLines=` continuation while `diff_symbols`, the tool the skill puts ahead of it, was called 30 times - 743 000 tokens a week of mostly incomplete answers | `changed_files`, `diff_text`, `SKILL.md` | shipped: a `changed_files` listing carrying tracked changes ends with the exact `next: diff_symbols ...` call for them, carrying the `baseRef=` and `path=` the listing itself used | shipped - the hand-over the skill described in prose is now a line of the response; locked by `ChangedFiles_WithATrackedModification_EndsWithTheDiffSymbolsCallForIt` |
| **I326** [cost] multi-space column padding costs 1.98% of result tokens (578 942 a week) and residual absolute-path text 7.95% on the sampled responses, with `workspace_status` printing the absolute solution path twice | `ResponseBuilder`, `workspace_status` | not shipped. Collapsing the column separator is a format change to **every** record of every tool at once, and this repository's response-shape assertions are spread across `TokenBudgetE2ETests`, `ToolCensus`, `AnswerQualityE2ETests` and roughly forty per-tool E2E tests | closed as a measured decision: the saving is real and the blast radius is the whole surface, so it belongs in a release whose only change it is - the same reasoning that defers **I323**. Reopen paired with it, and take the `workspace_status` duplicated path first, which is one line and no format contract |
| **I327** [accuracy] a compound `git add ... && git commit ... && git show --stat HEAD` was denied whole because of its last segment, and the denial named only the replacement, never which segment tripped it or that nothing ran | `ToolGuard` | shipped: a denial of one segment of a compound now ends "This segment of the compound command is what was denied, and NO part of the command ran", and the segment itself was already named in the reason | shipped - one lost commit plus one re-issued call per occurrence; locked by `Guard_ForACompoundCommandWhoseLastSegmentIsReplaced_SaysNoPartOfItRan` and `Guard_ForASingleReplacedCommand_DoesNotClaimItWasPartOfACompound`, which is the other direction |
| **I328** [cost] `read_text path=CHANGELOG.md section="## [Unreleased]"` returned **531 lines** - the heading and everything after it to the character cap - instead of the 120-line section, ~13 000 tokens against ~300 | `read_text`, `DocumentOutline` | neither the brackets nor the fence was the cause: `Close` walked the heading list backwards and `break`ed on the first already-**closed** sibling, so any section with two or more subsections was never given an end line. Replaced the backward walk with an explicit stack of open headings, which is also O(n) instead of O(n²) | shipped. ~12 700 tokens per over-read on the one file every release reads. `DocumentOutlineTests.Headings_ForASectionWithMoreThanOneChild_StillEndsItAtTheNextSectionOfItsOwnLevel` (observed failing at `(3, 19)` against the expected `(3, 14)`) and `FileToolsE2ETests.ReadText_ForASectionWithMoreThanOneSubsection_StopsAtTheNextSectionOfItsOwnLevel` lock it; the fixture carries a fenced block holding a heading-shaped line, so the fence path cannot regress silently. The `## [0.41.1]` section, which has exactly one child, closed correctly all along - which is why the defect survived five months of use |
| **I329** [speed] `replace_symbol symbolIds=[...] add=[...]` refuses when the targets do not share one containing type and names `addTo=`, but `addTo=` took exactly ONE container - so a batch whose members live in two types could not carry its helpers at all. It cost **2 extra `add_member` calls** in one run | `replace_symbol`, `SymbolEditService` | `addTo=` now accepts a comma-separated list paired positionally with `add=`, so each helper is routed to its own container in the one compile-gated edit; entries sharing a container are merged, a single name still takes every entry, and any other count is refused naming both counts. The parameter type is unchanged, so no caller passing one name is affected | shipped. 2 calls saved per multi-container edit, which the callee-first rule makes the common shape. `CompileGateE2ETests.AddTo_WithOneContainerPerAddEntry_LandsEachHelperInItsOwnType` and `...AddTo_WithADifferentNumberOfContainersThanAddEntries_RefusesNamingBothCounts` were both observed failing against the single-container semantics before the fix. The refuted approach in the row - inferring the container from the declaration - was not taken: the container stays an explicit choice |
| **I330** [speed] a `CompileRegression` whose only error is a missing import cost **3 round trips** in one run (`System.Text` twice, `System.Globalization` once) even though the remedy named `usings=` exactly: `usings=` was documented in the parameter and mid-description, which is where a long session no longer reads | `add_member`, `replace_symbol`, `replace_symbol_body` | `usings=` is now named in the **opening sentence** of all three tool descriptions, and `SKILL.md` says the same thing in the same place. The refuted approach - adding the import silently - was not taken: an import the caller did not ask for is a change they cannot see in a one-line success | shipped. Up to 3 rejected edits per session that writes new code, at the full declaration payload each. Locked by `DocsCoverageE2ETests` and `ToolCensusE2ETests.NoTwoAdvertisedTools_DescribeThemselvesNearlyIdentically`, which the reworded descriptions still pass; the saving itself is a discoverability change and is not directly assertable |
| **I331** [cost] `get_symbol_source` on a one-line `readonly record struct` answered `1 types  <name>  struct  :742-742` plus a 110-character `steer: ... verbose=true for the whole type` - it withheld a declaration *shorter than the steer*. **3 wasted calls** in one run (`FileService.FileWrite`, `PendingWrite`, `RowCut`), each answered again with `verbose=true` | `get_symbol_source`, `SourceService` | a type answers its source instead of the outline when it has exactly ONE declaring reference and its **rendered** source - the declaration plus the leading doc comment, which is what `AppendAsync` emits as `ToFullString()` - is at most `MaxInlinedTypeLines` (4) lines AND `MaxInlinedTypeChars` (200) characters. A partial type, a documented one-liner and a 4-line type with long lines all keep the outline and the steer, so the type steer is not dropped and the line gate cannot be gamed on the character axis | shipped. 3 calls per run that reads record structs, which is every run that edits this codebase. Observed pre-fix on the connected 0.42.0 binary: `get_symbol_source SymbolEditService.AddRoute` answered 2 lines plus the steer to withhold a 1-line declaration. `SteersAndRemediesE2ETests.GetSymbolSource_OnATypeShorterThanItsOwnSteer_AnswersTheDeclarationInstead` and the existing `..._OnATypeId_AnswersTheOutlineAndSteersToOneMember` (on `OrderRouter`) lock both directions, and `TokenBudgetE2ETests.GetSymbolSource_OnACompactType_CostsLessThanTheSteerItReplaces` asserts the inlined answer for `Tag` is cheaper than the steered answer for `Wide`, the 4-line type the character gate refuses. `Money`, `Tag` and `Wide` were added to `fixtures/FixtureSolution` for this - a review round found the first version of both the rule and its budget measured `reference.Span`, which excludes the doc comment the response actually carries, so a documented one-liner cost MORE than the steer it replaced |
| **I332** [accuracy] `unload_workspace` warned `2 analyzer or source-generator assembly(ies) ... still mapped` naming `Microsoft.CodeAnalysis.Analyzers.dll` and `...CSharp.Analyzers.dll` **from the NuGet cache**, and `AnalyzerLockE2ETests.UnloadWorkspace_AfterTheAnalyzerRan_ReportsNothingStillMapped` failed on it deterministically on one machine while the same commit's CI passed on all three legs | `unload_workspace`, `workspace_status`, `MappedAnalyzers`, `AnalyzerLockE2ETests` | `MappedAnalyzers.Of` now takes the workspace root and reports only analyzer references **inside** it - the exact set an external build of that solution can rewrite. The `still locked` NOTE no longer rules the analyzer set out, because a shadow copy that cannot be made falls back to mapping in place | shipped, and the row's premise was **partly refuted**: the two files are not a false report of a mapping that never happened - an open-for-write probe measured them **LOCKED**, and the 3.3.4 originals (620 672 / 27 264 bytes) match no directory in the shadow cache, so that directory was never copied. What is wrong is the *warning*: `MSB3027` is raised by a build **writing** the file, and nothing writes into the NuGet global-packages cache, so it told the user to restart a server over a failure that cannot occur. The pre-existing unit test could not fail - it ran the analyzers of `Fixture.Generated` only, and that project references no analyzer package; the new `...MappedAnalyzers_ReportsAReferenceInsideTheRootAndNeverOneOutsideIt` runs every project's and was observed reporting both NuGet paths before the fix, and now also asserts the predicate directly in both directions so it discriminates on a runner where the loader's in-place fallback never fires. **Reopen if** an analyzer that a build really does rewrite is ever referenced from outside the workspace root; the residual - that `ShadowCopyAnalyzerLoader` can silently fall back to mapping in place - is logged separately as I333 |
| **I354** [speed] `run_tests`, `build` and `list_tests` built with an implicit restore on every call. Measured over `fixtures/SelectionSolution`, five runs each: 2 749 ms default against 2 198 ms with `--no-restore`, on a solution whose tests take 421 ms | `run_tests`, `build`, `DotnetRunner.BuiltAsync` | `RestoreNeeded` compares every project's `obj/project.assets.json` against its project file and the root and per-project `Directory.Packages.props`, `Directory.Build.props`/`.targets`, `global.json` and `nuget.config`, and passes `--no-restore` only when the assets are provably newer; a build failing with `NETSDK1004`, `NU1101` or `project.assets.json` is retried once with restore | **2 749 ms -> 2 050 ms, 25 %** warm; touching `Directory.Build.props` restores as before (3 294 ms) and a fixture with every `bin`/`obj` deleted still answers `PASSED` (7 808 ms) |
| **I334** [cost] `read_text headings=true` silently ignored `maxLines=`: `FileService.Outline` never read `request.MaxLines`, so `maxLines: 30` on `CHANGELOG.md` answered all 179 sections (~2 400 tokens) | `read_text`, `FileService.Outline` | the heading map is bounded by `maxLines=` and reports `N/T sections truncated - narrow with maxLines= or maxLevel=`, and a new `maxLevel=` lists the headings down to that level only, keeping the anchors GitHub assigns over the whole document; `maxLevel=` without `headings=true` is refused rather than dropped | shipped 0.48.0. Locked by `FileToolsE2ETests.ReadText_WithHeadingsAndMaxLines_TruncatesTheHeadingMapAndSaysSo`, `...ReadText_WithHeadingsAndMaxLevel_DropsTheDeeperHeadingsAndKeepsGitHubAnchors` and `...ReadText_WithMaxLevelAndNoHeadings_RefusesRatherThanDroppingIt` over the new `fixtures/FixtureSolution/headings.md`, which after review carries **colliding slugs across the `maxLevel` boundary** (`### Detail`, `### Detail`, `## Detail`) so the anchor assertion `## Detail  #detail-2` fails if `Unique` is ever moved inside the level filter. Measured on this repo's own `CHANGELOG.md`: 190 sections at `maxLines=8` before, 8 after - **~2 300 tokens per heading map of a long file** |
| **I337** [accuracy] `get_symbol_source` on a type id concatenated the steer onto the outline with no separator, so the answer read `SymbolEditService.AddRoute  struct private  :835-835steer: get_symbol_source symbolId=...`. Hit twice in one session; the line range and the steer ran together, which is exactly the shape an agent parses positions out of | `get_symbol_source`, `SourceService.OutlinedAsync` | the outline value is `TrimEnd('\n')`-ed and the steer written on its own line, the way every other steer in the surface already is | shipped 0.48.0. Locked by `SteersAndRemediesE2ETests.GetSymbolSource_OnATypeId_PutsTheSteerOnItsOwnLine`, which finds the steer as a whole line and asserts it ends with the sentence. No token change; a position an agent can no longer misread |
| **I338** [accuracy] `AddReplacesItsOwnType`'s guard compared `edit.Target.Node.Span == container.Span` - `TextSpan`s **across documents**. Pre-existing, but multi-container `addTo=` routing (I329) made a coincidental cross-file span match materially more reachable, and the failure mode is a spurious refusal of a valid edit | `replace_symbol`, `SymbolEditService` | `SymbolEditService.RoutePlan` compares the document id alongside the span, as every other cross-document check in that file already does | shipped 0.48.0. `fixtures/FixtureSolution` gained `TwinAlpha.cs`/`TwinBravo.cs` - two files of identical layout whose declarations really do share a `TextSpan` - so the case can fail; locked in BOTH directions by `SteersAndRemediesE2ETests.ReplaceSymbol_AddingToATypeWhoseSpanTwinIsReplacedInAnotherFile_IsNotRefused` and, after review, `...ReplaceSymbol_AddingToTheVeryTypeItReplaces_IsStillRefused`, so deleting the guard no longer leaves the suite green. Saves one wrongly refused batch, which costs the whole declaration payload plus a retry |
| **I339** [cost] `search_text matchesOnly=true` over a query with no variable part returned N identical lines: `McpServerTool(Name = "` across `src/TerseSharp.Server/Tools/*.cs` gave 88 records each reading `McpServerTool(Name = "` - **~1 100 tokens carrying zero information**, because the answer to "which names are declared" is exactly the part `matchesOnly` clips off | `search_text`, `TextSearchService` | `search_text matchesOnly=true` is refused, naming `search_regex` (where the matched span varies) and `countOnly=true` (which answers the presence question it was being used for); `search_regex` keeps the flag unchanged | shipped 0.48.0. Locked by `FileToolsE2ETests.SearchText_WithMatchesOnly_RefusesAndNamesSearchRegex`; the `matchesOnly`+`unique` compose test was **replaced**, not deleted, by `...SearchRegex_WithMatchesOnlyAndUnique_CollapsesTheDistinctMatchedValues`, which covers the same collapse on the tool the flag is for. **~1 100 tokens per enumeration call** |
| **I340** [cost] `search_text` had no per-file count mode, so "is `--tools` documented in the READMEs?" cost a 4-query pass that came back `40/88 matches truncated` - **~2 500 tokens** of CHANGELOG prose - to answer a question whose whole content is "0 in README.md, 0 in NUGET_README.md, 2 in SKILL.md" | `search_text`, `search_regex` | `countOnly=true` answers ONE line per file - path and match count, plus `q1=N` per entry of `queries=` - with no matched text at all, bounded by the same `maxResults`; refused beside `matchesOnly=`, `unique=` and `context=`, which have nothing to act on | shipped 0.48.0 on both search tools. Locked by `FileToolsE2ETests.SearchText_WithCountOnly_AnswersOneLinePerFileAndNoMatchedText`, `...SearchText_WithCountOnlyAndSeveralQueries_TagsEachCount`, `...SearchText_WithCountOnlyBesideUnique_RefusesRatherThanDroppingIt` and, after review, `TokenBudgetE2ETests.SearchTextWithCountOnly_CostsAFractionOfTheMatchingLinesOverTheWidestQuery`, which asserts the saving against the widest fixture query so a format change cannot give it back. Review also fixed two holes: a match spanning a line break now falls back to `counts[match.Query]++` instead of vanishing from the `qN=` tags, and the count path calls `Annotate`, so `HEURISTIC`, `outside-workspace` and the over-16 MB skip note are no longer dropped. **~2 400 tokens per presence check over a large tree** |
| **I342** [unanswerable] nothing reported what the **whole** advertised surface costs. `workspace_status` printed `advertised=N tools T tokens` only for the last `tools/list` the client made, so a server narrowed by a profile, by the markup families or by `.terse.json` could only report the narrowed number - and the README's saving claim had to be taken from a test constant (`TokenBudgetE2ETests.AdvertisedPayloadBudget`) | `workspace_status`, `AdvertisedCost` | `AdvertisedCost.Unnarrowed()` is registered INSIDE `ToolProfile.Filter` and outside `SchemaCompactor.Filter`, so it measures the full surface in the same compacted shape the narrowed one is measured in; `verbose=true` then answers `advertised=20 tools 6000 tokens of 88 tools 25857`, and the clause is omitted when nothing was narrowed | shipped 0.48.0. `SchemaCompactor` moved innermost so both readings are compacted; the two filters commute (dropping tools then compacting equals compacting then dropping). Locked by `ToolProfileE2ETests.WorkspaceStatusVerbose_ReportsTheWholeSurfaceBesideTheNarrowedOne`, which asserts the narrowed count equals `ToolProfile.CoreTools.Count` and that both whole figures exceed it. Saves one test-file read per README claim |
| **I336** [speed] a malformed `edit_text edits=[...]` entry answered `JsonException ... BytePositionInLine: 2796` and the accepted parameter list, but did not name which entry is malformed. One 4-entry batch had a missing `newText` key and the whole ~2 800-character payload had to be re-sent to find it | `edit_text`, `ToolArgumentFilter` | `ToolArgumentFilter.Entry` walks the array parameter's own JSON with a `Utf8JsonReader`, counting depth-1 objects up to the failing byte, and `Quoted` prefixes the index and carries `json.Path`: `edits[1] is the entry that failed to bind (json path $[1].newText); byte 2796 of the 2812 characters of edits falls near: ...` | shipped 0.48.0. Locked by `FileToolsE2ETests.EditText_WithAMalformedEditsEntry_NamesTheEntryThatFailedToBind` and `ToolEdgeCaseE2ETests.ReplaceSymbol_WithAnArrayEntryThatCannotConvert_...`, which after review asserts the whole attributing phrase rather than the parameter name alone; an array of non-objects (`declarations`) now reads `declarations is the array parameter that failed to bind` instead of calling the parameter an entry. Saves one re-sent batch payload per malformed entry - measured at ~2 800 characters |
| **I341** [accuracy] a mixed-outcome `edit_text edits=[...]` batch **opened with `ERROR`** even when most entries landed: three edits, two to `SKILL.md` and one to a `.cs` file, answered `ERROR InvalidArgument: '...' is a C# file` first and `SKILL.md changedLines=9 edits=2/2` after, which reads as a failed call and invites a needless re-send | `edit_text`, `FileService.EditTextGroupedAsync` | the groups are sorted into applied and refused: a partial batch leads with the files that changed and renders each refusal underneath as `REFUSED <path>: <code> - <message>; remedy: <remedy>`, while a batch where NOTHING landed keeps the bare `ERROR` first line unchanged | shipped 0.48.0. Locked by `FileToolsE2ETests.EditText_WithAPartlyRefusedBatch_LeadsWithWhatLandedAndRefusesUnderneath`, which asserts the applied path's index is below the `REFUSED` index and that the answer does not open with `ERROR`. Saves one re-sent batch payload (~2 800 characters, I336) |
| **I366** [accuracy] one parameter accounted for the largest single error class in the corpus: **`glob` was refused `InvalidArgument` 101 times** - `search_text` 41, `find_files` 40, `search_regex` 20 - and one rejection line appears 97 times in the boilerplate census. The three tools already accept `pattern`/`path`/`query` aliases, so the failure is the glob reaching a tool that takes its scope another way | `search_text`, `search_regex`, `find_files`, `ToolArgumentFilter` | `ToolArgumentFilter.GlobHint` names the parameter that takes a glob on the tool that was called and carries a copy-paste value - `this tool takes the glob in paths=, e.g. paths=["src/**/*.cs"]` - and `find_files`, `search_text` and `search_regex` gained `ToolExamples` entries, so any rejected call on them also ends with a complete worked call | shipped 0.48.0. Locked by `FileToolsE2ETests.Analyze_WithAGlobArgument_NamesThePathsParameterAndAWorkedGlob` and `...SearchText_RejectingAnArgument_CarriesAWorkedCopyPasteCall`, and the three new examples are covered by the existing `ToolCensusE2ETests.EveryWorkedExample_NamesAnAdvertisedToolAndOnlyParametersThatToolDeclares`. 101 refusals/week, each a full round trip |
| **I333** [accuracy] `ShadowCopyAnalyzerLoader` fell back to `Map(original)` whenever `Copy` could not produce a shadow directory, and said nothing. Measured while closing I332: the Roslyn analyzer package at `microsoft.codeanalysis.analyzers/3.3.4` had no copy under `%LOCALAPPDATA%/terse-analyzers` while four other versions did, and an open-for-write probe reported both originals LOCKED. Harmless there, because nothing writes into the NuGet cache; the same silent fallback on an analyzer built from source is the I52 defect coming back with every gate green | `ShadowCopyAnalyzerLoader`, `workspace_status` | the fallback is recorded: `Load` adds the directory to a `ConcurrentDictionary` when the shadow copy is absent, and `Doctor` gained a `shadow` self-check that `workspace_status verbose=true` prints - `every analyzer assembly was loaded from the shadow cache`, or `N analyzer directory(ies) loaded IN PLACE, not from the shadow cache: <first three>` with the MSB3027 remedy | shipped 0.48.0. Locked by `SteersAndRemediesE2ETests.WorkspaceStatusVerbose_SaysWhetherEveryAnalyzerCameFromTheShadowCache`, which asserts one of the two verdicts is present and so fails if the check is dropped. The ROOT CAUSE of the one missing copy was NOT found and is not claimed - what shipped makes the fallback observable before the user's build fails MSB3027, which is what I52 exists for; reopen with the directory name from a `shadow` WARNING in hand |
| **I365** [accuracy] the truncation steer was measured firing as a wrong default - `diff_text` 421/498 = 84 %, `razor_outline` 10/11, `history` 98/179, `changed_files` 305/770, `get_file_outline` 134/391, `find_files` 318/1 012, `search_regex` 207/814, `search_text` 445/2 127 | `diff_text`, `history`, `changed_files`, `find_files`, `search_text`, `search_regex` | shipped: `diff_text`'s own `[Description]` claimed `maxLines= caps it at 400 by default` while `MaxDiffLines` is **1000** - a tool description stating the wrong default is the confidently-wrong answer this repo refuses, and it is corrected. The defaults themselves were re-measured and left alone | **the finding is largely mis-measured, and the evidence is here.** (a) `ResponseBuilder` emits `- narrow with X` for BOTH a real truncation (`N/T unit truncated - narrow with X`) and the deliberate `Shown >= 25` offer on a COMPLETE listing (`N unit - narrow with X`); observed live this run, `find_files` answered `58 files - narrow with ...` and `changed_files` answered `45 files - narrow with ...`, both complete, so the `find_files` 31 % and `changed_files` 39 % rates are offers, not truncations - only a line carrying the word `truncated` is one. (b) `diff_text`: five per-commit diffs of this repo measured 1 174, 371, 19, 14 lines and a four-commit range 737, so the 1 000-line cap already returns three of four single-commit diffs whole; the 84 % is dominated by whole-working-tree diffs, which `diff_symbols` is the documented answer for. Raising the cap uniformly is refused by the row's own evidence (over-eager truncation 78.1 %->32.6 % downstream accuracy). Reopen only with a per-tool payload distribution that counts the word `truncated`, not `narrow with` |
| **I343** [accuracy] when a grandchild inherits the pipe, `ChildProcess` answered with an **empty** capture while reporting `Drained=false` - "the capture is incomplete" over nothing at all. `SettledAsync` waited `DrainGrace=2s` on the reader TASKS, then disposed the streams and reported whatever `CopyAsync`'s continuation happened to have appended; a continuation not scheduled inside that window lost text the OS pipe already held. `ChildProcessTests.RunAsync_WhenADetachedGrandchildKeepsThePipeOpen_...` failed on the windows leg of two consecutive CI runs with `run.Output` empty, then passed on a re-run of the same commit | `ChildProcess`, `build`, `run_tests` | on the grace timeout `SettledAsync` now calls `QuietAsync` before `Release`: it samples `ProcessText.Length` (a new allocation-free accessor) every 25 ms and returns as soon as the captured total stops growing, bounded by the SAME `DrainGrace`. The drain warning also names what was captured - `captured=N chars` - so an empty partial snapshot can never read as a quiet command | shipped 0.48.0. `DrainGrace` was NOT raised - the row banned that - and **after review the settle was given its own 250 ms budget rather than a second `DrainGrace`**, so the worst case on the already-failed path is 2 s + 0.25 s rather than 2 s + 2 s; the early return no longer requires `captured > 0`, so a genuinely empty capture settles in ~50 ms instead of spinning the whole budget. Locked by the existing `...AnswersAndSaysTheCaptureIsIncomplete` (now a guarantee the product makes) plus the new `ChildProcessTests.RunAsync_WhenADetachedGrandchildKeepsThePipeOpen_StillCapturesWhatTheChildAlreadyWrote`, which asserts `Output` is non-empty and `StandardOutput` carries the child marker. 8/8 green |
| **I344** [accuracy] the probe path this repo's own HARD GATE names as evidence (b) - `dotnet src/TerseSharp.Server/bin/<Configuration>/net10.0/terse.dll call run_tests --workspace <this solution>` - cannot work on this repository: the probe process runs the very file its build target writes, so MSBuild retries `MSB3026` ten times and the call answers `WARNING this run produced no results` naming `holder pid=... - this terse server`. Measured: 42 s burned, zero tests run | `terse call`, `DotnetRunner`, `LoadedWorkspace` | new `TerseSharp.Core.SelfBuild`: `WorkspaceTarget` carries `RunningAssembly`, set by `ToolContext.Described` when a project of the loaded solution has an `OutputFilePath` equal to `Assembly.GetEntryAssembly().Location`, and `ToolContext.WithTargetAsync` - the one path every build and test tool goes through - refuses UP FRONT naming MSB3026 and the copy to make | shipped 0.48.0, and **narrowed after review**: the refusal is NOT on `WithTargetAsync` (which `package_list` also uses) but on the five build and test tools, each passing a `writes` predicate - `noBuild=true`, `dryRun=true`, a `project=`, a `projects=` batch and a `configuration=` all scope the call away from that output and are never refused, because `OutputFilePath` is the loaded configuration's. The match is on the exact output path, so the installed tool serving this solution and the E2E fixture server serving `fixtures/FixtureSolution` are both unaffected. **Stated, not claimed: a probe run from `bin/Release` against a Debug-evaluated workspace is not detected** - under-refusing was chosen over false refusals. Locked by `SelfBuildTests` - `Builds` positive through a separator difference, negative and empty, and `Refusal` in all four `writes`/`RunningAssembly` combinations |
| **I345** [speed] every build under `run_tests`/`build` passes `-nodeReuse:false` (`DotnetRunner.BuiltAsync`), so each invocation pays a cold MSBuild start. Measured by patching it out over `fixtures/SelectionSolution`, five runs each beside `--no-restore`: **2 198 ms -> 1 941 ms**, so node reuse is worth about **257 ms per call** | `build`, `run_tests`, `DotnetRunner.BuiltAsync` | left as it is | **closed as a measured decision.** 257 ms per call does not buy back live MSBuild nodes holding this tree's outputs: the flag exists for exactly the analyzer and output-file lock class that `unload_workspace`, the build retry, the `holder pid=` report and `I333`'s shadow cache all exist to fight, and this run added a fifth (`I344`, MSB3026 on the running assembly). Reopen only if the lock story changes - e.g. if MSBuild gains a way to release node file handles on demand |
| **I346** [accuracy] `run_tests` selects a solution's test projects with `TestScope` (a metadata-reference scan of the loaded compilation) while `list_tests` selects them with `TestReporterProbe.TestProjectsAsync` (a text scan of the `.csproj`). The two can disagree about which projects hold tests in one solution, and neither said which it used | `run_tests`, `list_tests`, `TestScope`, `TestReporterProbe` | the disagreement is now visible at the point it bites: `DotnetRunner.NoTestModule` names the classifier `list_tests` used, how many projects that scan matched and the first five of them, and its remedy states that `run_tests` selects by metadata reference instead, so the two can disagree on a solution whose test framework arrives through `Directory.Build.props` | shipped 0.48.0, **partial and stated as such**. The two classifiers were NOT unified: `TestScope.TestProjectsOf` answers from the loaded Roslyn solution, which `WithTargetAsync` has already released by the time the MTP module path runs, and its `allowDirect` mode returns OUTPUT ASSEMBLIES where `ModulesAsync` needs `.csproj` paths for `msbuild -getProperty:TargetPath`. Unification belongs with `I350`-`I353`, which all sit on the same path. Reopen with `WorkspaceTarget.TestProjects` plumbed through `list_tests` (`tests: true`, `allowDirect: false`) |
| **I347** [accuracy] `TestScope` classifies any project referencing a test-framework assembly as a test project, so a shared test-helper library that references xunit but sets `<IsTestProject>false</IsTestProject>` is run by an expanded solution `run_tests`, produces no results, and `Merge` propagates its non-zero exit as the whole batch's verdict | `run_tests`, `TestScope`, `DotnetRunner.Merge` | not implemented | **closed as a measured decision, with the blocker identified and logged as its own row.** Both halves the row proposed are refused on evidence: (a) "do not let a project that reported zero tests turn a green batch red" would silently green a filter typo, which is exactly what the `total=0 WARNING ... this is not a green run` shape exists to prevent - a confidently-wrong green is the worst outcome in this repo's own ranking; (b) honouring an evaluated `IsTestProject=false` needs MSBuild's evaluated property set, which the loaded Roslyn `Project` does not expose and which `project_properties` can only answer ONE project at a time - N calls for an N-project solution. That gap is now **I375**. Reopen when I375 ships |
| **I348** [speed] `run_tests` rebuilds on every call. Re-measured on the direct-assembly path over `fixtures/SelectionSolution`, five pairs: **2 749 ms with the build against 421 ms with `noBuild=true`** - the up-to-date no-op build is **2 328 ms, 85 % of the call**. At corpus scale: `run_tests` is 54.5 h of the 156.4 h tool-wall week, a `build` immediately followed by a rebuilding `run_tests` ran 282 times for 57.9 m, and 673 of 1 022 `build` calls were bare whole-solution builds at avg 12.7 s | `run_tests`, `build`, `DotnetRunner.BuiltAsync` | not implemented | **closed as a measured decision.** The proposal - pass `--no-build` automatically when no document changed since the last successful build - requires a staleness oracle that is trusted, and this repo's own trap list names "a test result read before its build is the previous binary's" as a defect that has shipped here more than once; the change tracking `analyze changed=true` uses observes only files the SERVER wrote, not an external editor, a `git checkout` or a generator, so it cannot answer "is the binary current". Superseded in practice by the restore-skip already shipped (see the 0.47.0 notes), which removed the restore half of the same cost. Reopen only with an oracle that observes the OUTPUT timestamps, not the input edits |
| **I349** [speed] `run_tests` passes `--blame-hang-timeout` on every VSTest run above 30 s, routing the run through a datacollector process; the xunit.v3 in-process runner offers `-longRunning <seconds>` for the same answer with no extra process | `run_tests`, `DotnetRunner.VsTestArguments` | not implemented | **closed as a measured decision.** The row's own expected-saving cell already says the cost is UNMEASURED and that the hang report is the only thing keeping the datacollector on the VSTest path. The trade cannot be made without a number, and the number cannot be taken here: this solution's own suite is the only VSTest workload available and it runs 1 004 E2E tests in ~1 940 s wall, where a per-process datacollector start is inside the noise. Reopen with a measured A/B of `--blame-hang-timeout` on and off over a suite whose per-project runtime is under 10 s, where the fixed cost would actually show |
| **I350** [accuracy] the direct-assembly branch of an expanded `run_tests` is gated on the `xunit.v3.runner.inproc.console` metadata reference plus `Reporter is VsTestLogger`, but no fixture can exercise the refusal: `fixtures/MtpSolution` holds ONE test project and `MinimumExpandedProjects` is 2, so the gate that keeps an MTP entry point off `-trx/-noLogo/-reporter` is untested in both directions | `run_tests`, `TestScope.DirectlyRunnable`, `DotnetRunner.Expandable` | attempted and REVERTED | **closed as a measured decision, and the attempt produced new evidence.** A second MTP test project (`tests/Mtp.Reporting.Tests`, a byte-for-byte copy of the existing one's `.csproj` shape with two passing tests) was added to `MtpSolution.slnx`. `build` reported `exitCode=0` over **3 projects**, the Roslyn load reported `projects=3 documents=18 failures=0` - and `dotnet test --solution MtpSolution.slnx` under `Microsoft.Testing.Platform` still answered **`total=5`**, the unchanged one-project count, with the second module never run; `run_tests test=Mtp.Trading.Tests.LedgerTests` then regressed from `total=3` to `total=0`. So the SDK's `--solution` mode does not pick the second module up, and the fixture cannot exercise this gate until that is understood. The fixture and the three test expectations were reverted rather than left half-applied. **Reopen with that SDK behaviour diagnosed first** - it blocks `I350`, `I351` and `I352` alike |
| **I351** [accuracy] `TestScope.TestProjectsOf` can only see projects Roslyn loaded, so a test project in a language the MSBuild workspace does not open - an `.fsproj` under `SkipUnrecognizedProjects` - is invisible to the expansion, and it is not known whether that registers a load Failure. If it does not, the `Load.Failures.Count is 0` guard passes and the run covers fewer projects than `dotnet test <solution>` would, silently | `run_tests`, `TestScope`, `ToolContext.TestProjectsOf` | not implemented | **closed as a measured decision, blocked on the same fixture the `I350` attempt refuted.** The proposed fix - compare the loaded project set against the solution file's own list and refuse to expand when they differ - is sound and `solution_projects` already reads a `.slnx`/`.sln`/`.slnf` without loading it, so the comparison is one call. What is missing is a fixture that can FAIL: it needs a multi-language solution whose non-C# test project the workspace skips, and this repository ships no `.fsproj`. The `I350` attempt showed that adding a project to an existing test fixture is not the cheap move it looks like. Reopen with an `.fsproj`-bearing fixture, and assert both directions |
| **I352** [accuracy] the direct-assembly branch hard-codes `-trx <path> -noLogo -reporter quiet` and gates only on the PRESENCE of `xunit.v3.runner.inproc.console`, never its version. Proven against xunit.v3 3.2.2 only; an older v3 that rejects one switch would turn a whole solution red with no way back | `run_tests`, `TestScope.DirectlyRunnable`, `DotnetRunner.AssemblyArguments` | not implemented | **closed as a measured decision.** The proposed backstop - fall back once to `dotnet test <csproj> --no-build` when a direct invocation exits non-zero having produced no trx - is not soundly distinguishable from the case it must NOT swallow: a test host that crashed before writing its trx also exits non-zero with no trx, and retrying it under `dotnet test` would report the second run's result while hiding the crash. Distinguishing them needs the runner to say WHY, which is what the version gate would have told us up front. Reopen as a **version** gate instead: read the `xunit.v3.runner.inproc.console` assembly version off the metadata reference the branch already inspects, and refuse the direct path below the version whose switches are proven |
| **I353** [accuracy] a workspace loaded over a never-restored solution carries NO metadata references, so `TestScope` classifies none of its projects as tests - and a later `build` does not refresh the already-loaded Roslyn solution, so BOTH `changed=true` and the direct-assembly path stay silently disabled for the life of that server. Measured on CI run 32879507657, red on ubuntu and macos and green on windows purely by test ordering, then reproduced locally by deleting the fixture's `bin`/`obj` | `run_tests`, `TestScope`, `ToolContext.Described`, `LoadedWorkspace` | not implemented | **closed as a measured decision.** The row asks for either a re-resolve after a restoring build or a report of the degraded classification. The first is the wrong shape - `MSBuildWorkspace` has no supported way to re-resolve references on a live `Solution`, and the only mechanism that does is `load_workspace reload=true`, which discards the undo history and every edit tracking `analyze changed=true` and `format changed=true` depend on. The second half is now partly served from a different direction: `I346` shipped the classifier disclosure on the path where the disagreement bites. Reopen as the REPORT half only - `workspace_status` saying `references=absent - run a restore and reload` when every project's `MetadataReferences` is empty, which is a one-line, always-true detector |
| **I355** [accuracy] the restore backstop - a build failing for missing assets being retried WITH restore - is the riskiest path of the restore-skip change and has no E2E test. It is covered by a unit test over a fabricated `ProcessRun` string and by one hand-run measurement, not by anything CI runs | `run_tests`, `build`, `DotnetRunner.FailedForMissingAssets` | not implemented | **closed as a measured decision, and this run produced the evidence that the proposed shape is wrong.** The row proposes copying a fixture to a temp directory, deleting its `obj`, and asserting `build` answers ok. The `I350` attempt in this same run showed why that is not the cheap test it looks like: a fixture whose project graph is touched behaves differently under the SDK than the checked-in one does, and the failure is silent (`total=5` for a 3-project solution). A temp copy adds the same class of variable to the ONE path whose whole purpose is recovering a broken tree. Reopen with `TerseTempSolution` (which already exists in the E2E project) as the copier and an assertion on the RETRY, not on the outcome - i.e. that the second invocation's command line carries no `--no-restore` - so the test cannot pass for the wrong reason |
| **I356** [accuracy] `RestoreInputStamp` covers the files MSBuild finds by directory walk, but not one a project reaches by an explicit `<Import Project="...Versions.props" />` outside that walk, nor a user- or machine-level `nuget.config`. A change to one of those leaves the stamp unmoved, so the session keeps skipping restore and the build resolves the old package versions - silently | `run_tests`, `build`, `DotnetRunner.RestoreInputStamp` | not implemented | **closed as a measured decision, with the blocker identified.** The proposed fix - stamp the imports MSBuild actually evaluated rather than a name list - needs the evaluated import list, which is the same capability gap `I347` hit and which this run logged as **I375**: MSBuild's evaluated state is reachable one project at a time through `project_properties`, and nothing exposes it across a solution. A user-level `nuget.config` is worse still: it is outside every workspace root, so no watcher this server owns can see it move. Reopen when I375 ships; the honest interim is that the restore skip is a within-session optimisation and a `load_workspace reload=true` clears it |
| **I357** [accuracy] `run_tests changed=true` was called **0 times in 2 375 calls** across 120 test-running sessions. `run_tests` is **54.5 h of the 156.4 h tool-wall week (34.9 %)**, and **1 057 of those calls (25.8 h)** re-ran an invocation already run earlier in the same session. Measured shape cost: `project+timeoutSeconds` 398x avg **126.1 s** against `project+test+timeoutSeconds` 779x avg **20.3 s** - narrowing is **6.2x**. `impact_of tests=true` was called **1** time | `run_tests`, `SKILL.md` | not implemented | **closed as a measured decision, refused on this run's own evidence.** Making `changed=true` the DEFAULT rests on the selection being trustworthy, and `I353` - open at the time, closed in this same run - measures it silently disabling itself for the life of any server started over an unrestored tree, with CI run 32879507657 red on two legs purely by test ordering. Defaulting to a selector that can silently narrow to nothing is the confidently-wrong answer this repo refuses, and it would be defaulted ON for every consumer, not just here. Reopen in this order: fix `I353`'s detector first, then default `changed=true`, and assert the fallback in both directions |
| **I358** [accuracy] a shipped tool nobody calls: **`explore_symbol` 0 calls and `impact_of` 1 call in 35 739 tool calls**, while the chains they exist to collapse ran adjacent - `get_file_outline`->`get_symbol_source` 19x, `get_symbol_source`->`read_text` 208x, `get_symbol_source`+`replace_symbol` on one target 23x. **65 of 88 advertised tools were called at all; 23 were never called and 24 more fewer than 10 times** | `explore_symbol`, `impact_of`, `SKILL.md` | not implemented | **closed as a measured decision.** The proposal - fold both into `usages=true`/`impact=true` parameters on `get_symbol_source` and `get_file_outline` - is a **MAJOR** change (two tools removed from the surface) and it is coupled to `I367`, which proposes narrowing the default profile from the same measurement and would reach the same two tools by a mechanism that breaks nothing. Shipping both independently would remove a capability twice. Reopen as ONE change with `I367`, and re-measure the per-tool selection rate on the next scan before deciding which of the two mechanisms actually moved it - this run's own transcript is the reminder that a shipped instruction does not move a number (see `I363`) |
| **I359** [accuracy] `read_text` returns **verbose=true on 4 087 of 6 154 calls (66 %)**, so the compression the tool exists for is opted out of two times in three - and `read_text` is **37.9 M chars = 10.1 M tokens = 59 % of all terse-sharp result volume** in the week. 2 030 of those calls named a `.cs` path, exactly where `verbose=true` discards the outline and takes ~3x the tokens | `read_text`, `SKILL.md` | not implemented | **closed as a measured decision, with the likeliest cause now named from direct observation.** This run used `read_text` 20 times and passed `verbose=true` on 5, every one of them on a NON-`.cs` file, and in each case the reason was the same: the compressed form prints a line number only where the numbering jumps, so an agent that intends to quote a line back cannot address it. That is a real defect in the compressed form, not caller error - and it means scoping `verbose` to the `.cs` opt-in (the row's own proposal) would REMOVE the only way to get addressable line numbers. Reopen as `lines=true` - number every line without restoring blank lines or trailing whitespace - which splits the two things `verbose=true` currently bundles and lets the `.cs` opt-in be scoped separately |
| **I360** [cost] the `read_text` -> `edit_text` anchor round trip: **823 adjacent same-path pairs**, read line span p50 **21** lines, and the `oldText` the edit then carried was p50 **162** chars. **2 115 342 chars (~528 835 tokens) of read payload existed only to let the caller copy an anchor**, and the copy is error-prone - 34 `oldText matched 0 times` refusals re-paid 19 492 tokens of input | `edit_text`, `read_text` | not implemented | **closed as a measured decision, and this run is the counter-evidence.** The proposal is `startLine=`/`endLine=` on `edit_text` guarded by a mandatory `expect=` first-line check. This run made **21 `edit_text` calls and hit ZERO anchor misses**, because every one of them used an address rather than remembered text: `section=`, `section=`+`place=`, `row=`/`rows=`+`toPath=`, or an `oldText` copied from a read taken in the same turn. Those addressing modes shipped after the measured week, so the 823 pairs and the 34 misses are a corpus that predates them, and a line-range edit would add a THIRD addressing mode - the least safe of the three - to solve a problem the other two already dissolved. Reopen only if the next scan still shows anchor misses AFTER `section=`/`row=` adoption is measured |
| **I361** [perf] the edit path reports errors and introduced **warnings**, but never the **info-severity** analyzer findings the new text introduced, so a performance rule is learned one or two calls later from a `build`. Measured over 7 935 edit calls: only **124 (1.6 %)** were followed by an `analyze`, while the gate output that did run named **CA1859 x93**, **CA2000 x63**, **CA1861 x43**, **CA2012 x37**, **CA1822 x18** | `replace_symbol`, `replace_symbol_body`, `add_member`, `write_text`, `EditGate` | not implemented | **closed as a measured decision, with this run's own counter-measurement.** The premise is that the info-severity rule is learned late. It was not: this run's `TextSearchService.Rows` returned `IReadOnlyList<string>` and **CA1859 was reported by the very next `build`, 40 seconds later, as an ERROR** - because `Directory.Build.props` sets `TreatWarningsAsErrors` and `AnalysisLevel=latest-recommended`, so on THIS repository the analyzer set that matters is already error-severity and already blocking. The 1.6 % figure therefore measures repositories where those rules are advisory, and returning them from every edit would spend tokens on every write to duplicate what the build already refuses here. Reopen scoped to diagnostics that are NOT error-severity in the changed project, which is a different and much smaller set |
| **I362** [modern] a diagnostic that names only its rule id is measurably weaker than one that names the replacement. The agent emitted **543 `.ToString()`/`string.Format(` without an `IFormatProvider`**, **227 hand-written backing fields**, **131 `new List<T>()`/`Array.Empty<T>()` where a collection expression applies** and **49 block bodies that `IDE0022` kills on the ubuntu-only CI leg**, over 277 413 emitted lines | `replace_symbol`, `add_member`, `cleanup`, `[Description]` | not implemented | **closed as a measured decision, superseded by the cheaper mechanism this repo already ships.** `cleanup fix=all` APPLIES the Roslyn code fix rather than naming it, and the row's own strongest citation says an automatic fix scored 85.2-99.6 % against 25.7-97.2 % for injecting an instruction - so the fix path already dominates the naming path it proposes. What is missing is not the text but the CALL: a measured week of this server's own sessions made 356 `analyze` calls and **zero** `gate` calls, and `gate` runs `analyze`->`format`->`cleanup fix=all`->`analyze` in one call. Reopen as adoption of `gate`, measured as calls per session on the next scan, not as new diagnostic prose |
| **I363** [speed] the agent achieved **no parallelism at all**: **35 731 of 35 735 assistant messages carrying a tool call carried exactly ONE `tool_use` block**, 2+ blocks appeared **4** times, and **8 697 calls sat inside runs of 3 or more consecutive calls of the same tool**. The from-the-second-call plural steer shipped and did not move it | `SKILL.md`, `ResponseBuilder`, every plural-parameter tool | not implemented | **closed as a measured decision, and this run reproduced the defect under the fix.** `SKILL.md` working rule 14 already says verbatim that independent calls go in one message and to prioritize calling tools simultaneously; that rule was in context for this entire session, and the session still issued ~120 tool calls with **zero** multi-`tool_use` messages, including runs the response itself flagged (`5 replace_symbol calls in a row`, `3 edit_text calls in a row`, `5 diff_text calls in a row`). So the instruction half is shipped and measured ineffective, and the response-side steer is the only remaining lever - but it fires only from the SECOND call, which is one call too late for a pair. Reopen as: fire the steer on the FIRST response of a tool that declares a plural parameter AND whose call carried no per-entry-only argument, which is the condition the previous attempt lacked |
| **I364** [cost] **collapsing internal multi-space runs to one space saves 2.36 % of all terse-sharp output - 406 472 tokens in the measured week** - verified with `tiktoken` before/after on the 65 widest real responses (664 897 chars): **5.02 % on `o200k_base` and 5.03 % on `cl100k_base`**. Per tool: `get_file_outline` **13.7 %**, `find_usages` **11.9 %**, `razor_outline` 16.7 %, `find_files` 6.7 %, `changed_files` 6.6 %, `get_symbol_source` 6.3 %, `search_text` 4.8 % | `ResponseBuilder`, `TextCompressor` | not implemented | **closed as a measured decision: the saving stands, the change is MAJOR and must be its own change set.** Scope enumerated here rather than estimated - **76 composition sites** under `src/**/*.cs` (regex `\$?"[^"\n]*[A-Za-z}\]]  [A-Za-z{$][^"\n]*"`) and **61 matching lines** under `tests/**/*.cs` (regex `"[^"\n]*[A-Za-z0-9)\]]  [A-Za-z0-9#:{$][^"\n]*"`, re-measured after this change). Every site is a per-record separator inside an interpolation, so there is no central switch: `TextSearchService.RecordSeparator` is the only named constant and it covers one tool. A blanket `ResponseBuilder` collapse is refused for the reason the row already gives - `search_text` records embed the matched source line, so framing and payload share a record. Reopen as a dedicated change whose ONLY content is this rename-and-flip plus its 60 assertions, so a regression in it cannot be confused with anything else |
| **I367** [cost] the 88-tool advertised surface costs **26 475 tokens on every request**, against Anthropic's first-party statement that *"Claude's ability to pick the right tool degrades once you exceed 30-50 available tools"*. Corroborated by LongFuncEval (7-85 % selection drop from 49 to 741 tools, arXiv:2505.10570) and adaptive shortlisting (+6.0 to +15.9 pp on Claude Sonnet 4.6, arXiv:2605.24660). Measured here: **23 of 88 advertised tools were never called in 35 739 calls** | `ToolProfile`, `ToolGroups`, `.terse.json`, `workspace_status` | not implemented, but the number it argues from is now self-reported | **closed as a measured decision.** `I342` shipped in this same run: `workspace_status verbose=true` now answers `advertised=<narrowed> tools <t> tokens of <whole> tools <T>`, so the saving a narrower default would buy is readable off any running server instead of a test constant - which is the precondition for choosing the default on evidence rather than on a corpus average. Narrowing the DEFAULT itself is refused for now on the row's own Rejected cell: the only runtime narrowing mechanism is `notifications/tools/list_changed`, whose client-side support is still UNVERIFIED, so a startup-only default would silently deny a workspace its markup tools until the server restarts. Reopen with (a) the `list_changed` support verified, or (b) a measured per-tool call rate taken with `I342`'s new line, jointly with `I358` |
| **I368** [accuracy] the guard is firing but not converting: **225 `Bash` git denials in the week** - `git log` 90, `git diff` 60, `git status` 60, `git show` 8, `git tag` 4, `git ls-files` 3 - every one already answered by `history`, `changed_files`, `diff_symbols`, `diff_text`, `read_text ref=` or `find_files tracked=true`, and every one costing a turn. Alongside them ~950 `Bash` shell-text calls and **153 built-in `Read` calls averaging 50 561 chars** | `ToolGuard`, `SKILL.md` | not implemented | **closed as a measured decision, and this run measured the guard converting.** Across ~120 tool calls this session there was **exactly one** `Bash` denial (`git log --oneline -1`) and **zero** built-in `Read`/`Grep`/`Glob`/`Edit`/`Write` on repository content - the deny-time message named `history` and the next call used it. So the deny-then-convert loop works; what it cost was one turn, and the reason it cost a turn is that the denied verb sat inside a compound command whose allowed segments were rejected with it. That is the actual defect, it is smaller and sharper than "the message arrives too late", and it is logged as its own row **I373**. Reopen the `additionalContext`-on-allow idea only if a later scan shows denials recurring AFTER a first conversion in the same session |
| **I369** [accuracy] the user had to intervene on **126 of 302 post-first-turn prompts (41 %, 0.29/session)**, and the tool called immediately before an intervention was **`Bash` 50 times (40 %)** - four times the next entry. Cue mix: re-scope 74, **gate reminder 60**, correction 54, unblock 54, redo 26 | `ToolGuard`, `SKILL.md`, `CLAUDE.md` | not implemented | **closed as a measured decision, because the row's own success criterion is a TREND and this run supplies the next point.** It asks to move mechanical invariants into the `PreToolUse` guard and re-measure interventions per session. This session took **one** user intervention, and it was not a gate reminder or a correction - it was a **re-scope** ("finish all open improvements before reviewing"), i.e. the class no guard can enforce because it is a judgement about ordering, not about a tool call. The `Bash`-before-intervention correlation also did not reproduce: the single `Bash` denial converted without an intervention. Reopen at the next corpus scan with interventions/session as the metric; a single session is one point, not a trend, and this row should not be re-implemented on it |
| **I370** [speed] `write_text` is the **third most expensive tool in the week by wall clock - 926.8 m (15.4 h) over 1 582 calls** - and almost all of it is tail: p50 **3 999 ms**, p90 11 262 ms, p99 **56 304 ms**, so p50 x calls accounts for 105 m and the other **88 %** is the tail. **251 of those 1 582 calls (15.9 %) carried a cold-compilation notice.** The same shape appears on every semantic read | `write_text`, `EditGate`, `LoadedWorkspace`, `WorkspaceRegistry` | not implemented | **closed as a measured decision, with the first half found already shipped.** The row's first ask - attribute the cold realization the way `load_workspace` does - is already in place for `write_text` and every other semantic call: `ToolContext.AttributedAsync` appends `compilations=realized in Nms (once per load, not per call)`, and `LoadedWorkspace.DropCompilations` resets `realizedNoticeTaken`, so a RE-realization after an idle drop is attributed too. The second ask cannot be measured here: this solution is 4 projects and 370 documents, and realization measured **0.1-3.3 s** across this session, nowhere near the corpus p99 of 56 s. The row's own headline lever is refuted in its own Rejected cell (`LoadMetadataForReferencedProjects` is paid at LOAD, the cost is on later calls). Reopen with a drop->re-realize counter on a 148-project tree |
| **I371** [cost] harness-injected context is **35 % of the corpus's entire tool-result volume and nobody calls for it**: 9 730 attachment records / 29.5 MB, of which `skill_listing` **468 records = 15.4 MB (~3.86 M tokens)** and `deferred_tools_delta` **584 records = 8.3 MB (~2.08 M tokens)**. Against that, terse-sharp's own advertised surface is 26 475 tokens per request | `.terse.json`, `ToolProfile`, `SKILL.md` | not implemented | **closed as a measured decision.** Two of the three numbers are outside this product entirely: `skill_listing` and `deferred_tools_delta` are harness attachments, and nothing in `terse` emits, sizes or can shrink them - they belong in a Claude Code issue, not in this backlog. The part that IS ours is `SKILL.md`, and this run measured its ceiling working as designed: adding the rows that teach `countOnly`, `maxLevel` and the whole-surface reading pushed it to 24 516 tokens against a 24 100 budget, the census failed the build, ~1 100 characters were trimmed back out and the budget was raised deliberately to 24 400 with the reason recorded. So the file is already governed by exactly the mechanism the row asks for. Reopen as a measured section-by-section pricing of `SKILL.md` with a target, not as a restructure |
| **I372** [cost] a `replace_symbol symbolIds=[...] declarations=[...]` batch rejected for ONE unresolvable id cost the WHOLE payload: a `CompileRegression` held the declarations behind a `retryWith` token, a RESOLUTION failure held nothing. Measured twice in one run at ~2 400 and ~1 500 characters re-sent verbatim | `replace_symbol`, `replace_symbol_body`, `add_member` | `SymbolNotFound` and `AmbiguousSymbol` now mint a `retryWith` token too, holding the declarations, and an explicitly passed `symbolId`/`symbolIds` OUTRANKS the held one, so the retry is the token plus the corrected ids | Shipped. Locked by `ABatchRejectedForAnUnresolvableId_HoldsItsDeclarationsBehindARetryToken`; the two measured rejections would now cost a token plus one id instead of ~2 400 and ~1 500 characters |
| **I373** [accuracy] a `ToolGuard` denial of ONE segment of a compound `Bash` command rejected the WHOLE command and left the caller to split it - measured at a full turn for `git fetch && git rev-parse && git pull && git log && gh auth status` | `ToolGuard` | every segment is judged, not just the first: `additionalContext` now names the tool call for each denied segment AND the allowed remainder to re-issue in `Bash`; `git tag` listings also route to `history tags=true` instead of a bare `history`, which answers commits rather than tags | Shipped. Locked by `Routing_ForACompoundCommandWithOneDeniedSegment_NamesTheAllowedRemainderToReIssue`; one turn per compound command carrying a single denied segment, on the `cd X && <tool>` shape that is the most common `Bash` form in the corpus |
| **I374** [accuracy] `replace_symbol_body` answered `the body did not parse` for two causes it did not distinguish - an unbalanced brace in a block that DID start with `{`, and a target (a field) with no body at all. Two misdirected retries in one run, each re-sending the whole body (~1 900 characters for one of them) | `replace_symbol_body`, `MemberDeclaration` | a target with no body is refused by KIND naming `replace_symbol` and `edit_text force=true`; a genuine parse failure carries the parser's own message plus `at offset N of M: ...`, the same shape `I336` gave array arguments | Shipped. Locked by `ReplaceSymbolBody_OnAMemberWithNoBody_RefusesByKindInsteadOfBlamingTheBody` and `ReplaceSymbolBody_WithAnUnbalancedBrace_NamesTheOffsetTheParserStoppedAt` |
| **I375** [unanswerable] no tool answered one MSBuild property across every project of a solution: `project_properties` answers one project, so `IsTestProject` over an N-project solution was N calls, which is why `I347` could not honour the evaluated value | `list_projects` | `list_projects properties="IsTestProject,TargetFramework"` prints each project's EVALUATED value beside its name, `(unset)` where the project defines none and `UNEVALUATED` where MSBuild cannot load it; refused beside `path=`, which answers about one file | Shipped. N calls become 1 - 4 on this solution, 148 on a 148-project tree. Locked by `ListProjects_WithProperties_PrintsTheEvaluatedValueBesideEachProject` and, over three projects with two different `IsPackable` values, `ListProjects_WithProperties_AnswersEachProjectsOwnEvaluatedValue`; no `TokenBudgetE2ETests` row, because the default response is byte-identical and the saving is call count, not tokens |
| **I376** [speed] the harness's parallel `tool_use` capability was 99.997% unspent - 36 070 of 36 071 tool-bearing assistant messages in one week issued exactly ONE call, each paying a p50 6 097 ms model gap (102.9 h in the window), and the existing nudge fired only AFTER the serial calls were paid and only for the SAME tool | `SKILL.md` + `RepeatSteer` | the parallel rule is now a first-class clause of `SKILL.md`'s hard gate with its measured cost, with its measured cost, so the rule is stated where the agent reads it rather than inferred from a nudge | Shipped as the hard-gate rule. The cross-tool `RepeatSteer` half was implemented and **reverted in the same run, on measured evidence**: even capped at two emissions per process its landing site is chosen by call ORDER, and it broke three unrelated assertions - `SearchText_WithPathInsteadOfGlob_AnswersTheSameRecords`, `EditText_OnSuccess_NamesTheFileWithoutItsDirectory` and `FindUsages_OnTheWidestSymbol_StaysUnderItsBudget` - by appending framing to responses those tests measure exactly. A steer whose landing site cannot be predicted is the same defect class as editing a record's own text. Reopen only with a channel that is not the answering tool's own payload |
| **I377** [speed] verification was 41% of all tool wall time and half of it provably redundant: `run_tests` alone 2 454 calls / 58.0 h, mean 85 s, with 1 183 (48%) byte-identical repeats inside one session, and 787 of 1 053 `build` calls (75%) the same | `run_tests` / `build` | proposed: fingerprint the inputs a run covered and answer an identical call from the previous result as `CACHED age=4m inputs unchanged` | **Closed as a measured decision - not soundly implementable with the fingerprint this server can prove.** The only input signal available is the watcher's generation counters, and the watcher deliberately EXCLUDES `bin`, `obj`, `.git`, `artifacts`, `TestResults` and `node_modules` - the trees holding the very assemblies `run_tests` reports on. An external `git clean -xfd`, another session's `clean`, or a sibling terse process deleting `bin/` moves no counter, so a cached `PASSED` would outlive the binary it describes: the undetectable false positive this repo's prime rule bans. A test's inputs also reach outside the workspace entirely - environment, the SDK on PATH, the NuGet cache, clock, network - which no index covers. `build` is not where the hours are: MSBuild's own up-to-date check already makes a duplicate build cheap, measured in this run at 6.7 s for a rebuild that emitted nothing. The behavioural half of the row shipped instead as `I378`. **Reopen when the key can be derived from the resolved test assemblies' own bytes** - each project's evaluated `TargetPath`, hashed - which makes the claim provable for `noBuild=true` runs; `I375` shipped the first half of that mechanism, since `TargetPath` is an evaluated MSBuild property `list_projects properties=` can now read |
| **I378** [speed] the agent ran the full suite per EDIT, not per task: `edit_text -> run_tests` 708x and `run_tests -> edit_text` 569x among the most frequent adjacent pairs, giving 3.7 full-suite runs per task cycle at mean 85 s, with the interleaved loop accounting for 85% of post-first-edit wall time | `SKILL.md` / `CLAUDE.md` definition-of-done | the verification ladder is now written with its measured cost per rung - `analyze` 7.0 s after every edit, scoped `build` 13.0 s when the edit crosses a signature or a consumer, the affected project's tests once the slice compiles, the whole-solution suite ONCE at the end (85 s, p99 16 min), `rerun_failed` after a red run - plus the four banned shapes | Shipped in `SKILL.md` (`## Behaviour the table cannot carry`) and in `CLAUDE.md`'s definition-of-done. Target: 2 of the 3.7 suite runs per cycle, ~31 h/week. No census gate is possible - the subject is the agent's call sequence, not the server's output - so it is measured on the next `/mine-sessions` scan, and a per-tool selection rate that has not moved is the signal to replace the prose with a mechanism |
| **I379** [speed] the written `sleep` gate was breached ~156 times a week: 156 BARE sleeps - no `while`/`until`/`for` guard - declaring 25 307 s and costing 7.0 h of real wall clock, largest single 580 s, while the guarded shape worked because it exits when the process dies | `ToolGuard` | the guard denies a `Bash` command whose `sleep` is not inside a loop carrying `while`/`until`/`for`, with a remedy naming the two alternatives that already exist: background work re-invokes you on completion, and ending the turn is free | Shipped. Locked by `Bash_WithABareSleep_IsDeniedBecauseWaitingIsNotWork` (bare, compound, and PowerShell `Start-Sleep`) and `Bash_WithASleepInsideALoop_IsAllowedBecauseThatIsTheGuardedShape`. The first cut denied any command whose TEXT contained `sleep`, including `git commit -m "...bare sleep..."` - caught by self-review before the commit and fixed by scanning the quote-masked command, locked by `Bash_MentioningSleepOnlyInsideQuotes_IsAllowed`. Denies exactly the class the measurement defined - 7.0 h/week - and no tool is named in the reason, so `.terse.json` cannot switch it off |
| **I380** [speed] the two remaining wall-clock sinks: shell text tools at 2 369 `Bash` calls / 18.1 h = 51.5% of all `Bash` wall time at a 13.2% error rate, and the subagent at 184 calls / 6.4 h, p99 2 303 s, max 6 589 s - 110 minutes inside one call - with no scope stated in the spawn | `ToolGuard` / `SKILL.md` | the shell-text denial reason now carries what that class costs, so the substitution is a cost decision rather than only a prohibition; and `SKILL.md` working rule 15 requires every spawn to carry the `changed_files` output and the `diff_symbols` ids as its scope plus a stated call ceiling, one review round then a re-review of the fixes only | Shipped. Locked by `Reason_ForABashTextRead_PricesTheShellTextClassItReplaces`. The user-wide `grep`/`cat` denial the row already refuted stays refuted - outside a .NET tree there is no replacement - so only the priced reason and the spawn brief changed |
